---
title: 'JDBC드라이버 연결 확인'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

# **JDBC드라이버 연결을 확인한다**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381146/notion-blog/_/_/_/_/JDBC_/Untitled.png)

```java
테스트를 통해서 제대로 연결이 되었는지 확인한다
```

```java
package org.zerock.persistence;

import java.sql.Connection;
import java.sql.DriverManager;

import org.junit.Test;

import lombok.extern.log4j.Log4j;

@Log4j
public class JDBCTests {

	@Test
	public void testConnection() throws Exception {

		Class clz = Class.forName("oracle.jdbc.driver.OracleDriver");

		log.info(clz); // 드라이버 로딩에 문제가 없음

		Connection con = DriverManager.getConnection(
				"jdbc:oracle:thin:@localhost:1521:orcl",
				"DONGK",
				"1234"
				);

		log.info(con); // 계정 연결
		con.close(); //bad code final 로 처리해야한다
	}
}
```
