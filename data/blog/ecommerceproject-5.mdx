---
title: 'EcommerceProject -5'
date: '2026-09-02'
category: '풀스택/프로젝트 정리'
tags: ['풀스택', '프로젝트 정리']
draft: false
summary: ''
---

# Payment.jsx 추가

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381463/notion-blog/_/_/EcommerceProject/EcommerceProject_-5/Untitled.png)

```jsx
Payment 페이지 추가를 예전에 깜빡 했는거 같다 이것도 조금 정리해주자

기본적으로 로컬스토리지 라스트오더 데이터에서 가져와서 데이터를 사용한다
```

```jsx
// React와 필요한 훅, 컴포넌트를 가져옵니다.
import React, { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { useSelector } from 'react-redux'

// 외부 스타일시트에서 스타일을 가져옵니다.
import styles from '../../styles/styles'

// 동일한 디렉토리에서 KakaoPayment 컴포넌트를 가져옵니다.
import KakaoPayment from './KakaoPayment'

// Payment 함수형 컴포넌트를 정의합니다.
const Payment = () => {
  // 상태 변수를 정의합니다.
  const [orderData, setOrderData] = useState([])
  const [shippingAddress, setShippingAddress] = useState('')
  const [open, setOpen] = useState(false)

  // Redux 스토어에서 'user' 객체를 추출합니다.
  const { user } = useSelector((state) => state.user)

  // react-router-dom 라이브러리에서 'navigate' 함수를 가져옵니다.
  const navigate = useNavigate()

  // useEffect 훅을 사용하여 컴포넌트가 마운트될 때 실행될 코드를 정의합니다.
  useEffect(() => {
    // 로컬 스토리지에서 'latestOrder' 키의 값을 가져와 파싱합니다.
    const orderData = JSON.parse(localStorage.getItem('latestOrder'))

    // 상태 변수를 업데이트합니다.
    setOrderData(orderData)
    setShippingAddress(shippingAddress)
  }, [])

  // JSX를 반환합니다.
  return (
    <div className="flex w-full flex-col items-center py-8">
      <div className="1000px:w-[70%] 800px:flex block w-[90%]">
        <div className="800px:w-[65%] w-full">
          {/* PaymentInfo 컴포넌트를 렌더링합니다. */}
          <PaymentInfo />
        </div>
        <div className="800px:w-[35%] 800px:mt-0 mt-8 w-full">
          {/* CartData 컴포넌트를 렌더링하고 orderData를 props로 전달합니다. */}
          <CartData orderData={orderData} />
        </div>
      </div>
    </div>
  )
}

// PaymentInfo 함수형 컴포넌트를 정의합니다.
const PaymentInfo = () => {
  // useState 훅을 사용하여 선택 상태를 관리합니다.
  const [select, setSelect] = useState(1)
  // react-router-dom 라이브러리에서 'navigate' 함수를 가져옵니다.
  const navigate = useNavigate()

  // 결제 핸들러 함수를 정의합니다.
  const paymentHandler = (e) => {
    e.preventDefault()
    // '/order/success/fdbxf9848' 경로로 이동합니다.
    navigate('/order/success/fdbxf9848')
  }

  // JSX를 반환합니다.
  return (
    <div className="800px:w-[95%] w-full rounded-md bg-[#fff] p-5 pb-8">
      {/* 선택 버튼 */}
      <div>
        <div className="mb-2 flex w-full border-b pb-5">
          {/* 선택 버튼을 렌더링하고 클릭 시 선택 상태를 변경합니다. */}
          <div
            className="relative flex h-[25px] w-[25px] items-center justify-center rounded-full border-[3px] border-[#1d1a1ab4] bg-transparent"
            onClick={() => setSelect(1)}
          >
            {/* 선택 상태에 따라 표시되는 원을 렌더링합니다. */}
            {select === 1 ? (
              <div className="h-[13px] w-[13px] rounded-full bg-[#1d1a1acb]" />
            ) : null}
          </div>
          <h4 className="pl-2 text-[18px] font-[600] text-[#000000b1]">신용/체크카드로 결제</h4>
        </div>

        {/* 카드 결제 폼 */}
        {select === 1 ? (
          <div className="flex w-full border-b">
            {/* 결제 폼을 렌더링하고 폼 제출 시 paymentHandler 함수를 실행합니다. */}
            <form className="w-full" onSubmit={paymentHandler}>
              <div className="flex w-full pb-3">
                <div className="w-[50%]">
                  <label className="block pb-2">카드 번호</label>
                  <input required className={`${styles.input} !w-[95%]`} />
                </div>
                <div className="w-[50%]">
                  <label className="block pb-2">유효기간</label>
                  <input type="number" required className={`${styles.input}`} />
                </div>
              </div>

              <div className="flex w-full pb-3">
                <div className="w-[50%]">
                  <label className="block pb-2">카드 소지자 이름</label>
                  <input required className={`${styles.input} !w-[95%]`} />
                </div>
                <div className="w-[50%]">
                  <label className="block pb-2">청구지 주소</label>
                  <input type="text" required className={`${styles.input}`} />
                </div>
              </div>
              {/* 제출 버튼 */}
              <input
                type="submit"
                value="Submit"
                className={`${styles.button} h-[45px] cursor-pointer rounded-[5px] !bg-[#f63b60] text-[18px] font-[600] text-[#fff]`}
              />
            </form>
          </div>
        ) : null}
      </div>

      <br />
      {/* 페이팔 결제 */}
      <div>
        <div className="mb-2 flex w-full border-b pb-5">
          {/* 선택 버튼을 렌더링하고 클릭 시 선택 상태를 변경합니다. */}
          <div
            className="relative flex h-[25px] w-[25px] items-center justify-center rounded-full border-[3px] border-[#1d1a1ab4] bg-transparent"
            onClick={() => setSelect(2)}
          >
            {/* 선택 상태에 따라 표시되는 원을 렌더링합니다. */}
            {select === 2 ? (
              <div className="h-[13px] w-[13px] rounded-full bg-[#1d1a1acb]" />
            ) : null}
          </div>
          <h4 className="pl-2 text-[18px] font-[600] text-[#000000b1]">페이팔로 결제</h4>
        </div>

        {/* 카드 결제 */}
        {select === 2 ? (
          <div className="flex w-full border-b">
            {/* 결제 폼을 렌더링하고 폼 제출 시 paymentHandler 함수를 실행합니다. */}
            <form className="w-full" onSubmit={paymentHandler}>
              <div className="flex w-full pb-3">
                <div className="w-full">
                  <label className="block pb-2">페이팔 이메일</label>
                  <input required className={`${styles.input}`} />
                </div>
              </div>
              <input
                type="submit"
                value="Submit"
                className={`${styles.button} h-[45px] cursor-pointer rounded-[5px] !bg-[#f63b60] text-[18px] font-[600] text-[#fff]`}
              />
            </form>
          </div>
        ) : null}
      </div>

      <br />
      {/* 착불 결제 */}
      <div>
        <div className="mb-2 flex w-full border-b pb-5">
          {/* 선택 버튼을 렌더링하고 클릭 시 선택 상태를 변경합니다. */}
          <div
            className="relative flex h-[25px] w-[25px] items-center justify-center rounded-full border-[3px] border-[#1d1a1ab4] bg-transparent"
            onClick={() => setSelect(3)}
          >
            {/* 선택 상태에 따라 표시되는 원을 렌더링합니다. */}
            {select === 3 ? (
              <div className="h-[13px] w-[13px] rounded-full bg-[#1d1a1acb]" />
            ) : null}
          </div>
          <h4 className="pl-2 text-[18px] font-[600] text-[#000000b1]">착불 결제</h4>
        </div>

        {/* 카드 결제 */}
        {select === 3 ? (
          <div className="flex w-full">
            {/* 결제 폼을 렌더링하고 폼 제출 시 paymentHandler 함수를 실행합니다. */}
            <form className="w-full" onSubmit={paymentHandler}>
              <input
                type="submit"
                value="Confirm"
                className={`${styles.button} h-[45px] cursor-pointer rounded-[5px] !bg-[#f63b60] text-[18px] font-[600] text-[#fff]`}
              />
            </form>
          </div>
        ) : null}
      </div>
    </div>
  )
}

// CartData 함수형 컴포넌트를 정의합니다.
const CartData = ({ orderData }) => {
  // orderData에서 배송비(shipping)를 추출하고 소수점 2자리로 고정합니다.
  const shipping = orderData?.shipping?.toFixed(2)

  // 배송비를 콘솔에 출력합니다.
  console.log(shipping)

  // JSX를 반환합니다.
  return (
    <div className="w-full rounded-md bg-[#fff] p-5 pb-8">
      <div className="flex justify-between">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">소계:</h3>
        <h5 className="text-[18px] font-[600]">${orderData?.subTotalPrice}</h5>
      </div>
      <br />
      <div className="flex justify-between">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">배송비:</h3>
        <h5 className="text-[18px] font-[600]">${shipping}</h5>
      </div>
      <br />
      <div className="flex justify-between border-b pb-3">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">할인:</h3>
        <h5 className="text-[18px] font-[600]">
          {orderData?.discountPrice ? '원' + orderData.discountPrice : '-'}
        </h5>
      </div>
      <h5 className="pt-3 text-end text-[18px] font-[600]">${orderData?.totalPrice}</h5>
      <br />
      {/* KakaoPayment 컴포넌트를 렌더링합니다. */}
      <KakaoPayment />
    </div>
  )
}

// Payment 컴포넌트를 내보냅니다.
export default Payment
```

```jsx

주문 데이터 가져오기:

useEffect 훅 내에서 localStorage.getItem("latestOrder")를 통해 최근 주문 데이터를 가져와 상태를 업데이트한다.

----------------------------------------------------------------------------

컴포넌트 구조:

Payment 컴포넌트는 두 개의 자식 컴포넌트를 렌더링한다: PaymentInfo와 CartData.

----------------------------------------------------------------------------

결제 정보 (PaymentInfo): ( 현재 이 기능은 사용하지 않는다 )

PaymentInfo 컴포넌트는 결제 수단을 선택할 수 있는 버튼과 해당 정보를 입력받는 폼을 제공합니다.

각 결제 수단에 대한 선택 상태는 select 상태로 관리됩니다.

선택한 결제 수단에 대한 정보를 입력하고 제출할 수 있는 폼이 제공되며, 제출 시에는 navigate("/order/success/fdbxf9848")를 통해 주문 성공 페이지로 이동합니다.

----------------------------------------------------------------------------

카트 데이터 (CartData):

CartData 컴포넌트는 주문 데이터를 표시하며, 서브토탈, 배송비, 할인 등의 정보를 출력한다.

KakaoPayment 컴포넌트를 렌더링하여 카카오페이 결제 기능을 추가한다.

```

# KakaoPayment.jsx

```jsx
카카오 결제를 위해서 만들어 준다

이걸로도 결제가 가능하지만 검증을 위해서 추후 서버쪽 코드를 작성해줘야 한다

현재 여기서는 결제 완료 후 결제 정보를 저장하는 로직을 가지고 있다 ( const callback = async (response) => 이부분 )

(여기서 몽고 DB 에 주문 내역을 저장하는 기능을 작성해줘 성공후 DB 에 저장해준다 order 에 )
```

```jsx
import React, { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { useSelector } from 'react-redux'
import { toast } from 'react-toastify'
import axios from 'axios'

const KakaoPayment = (effect, deps) => {
  const [orderData, setOrderData] = useState([])
  const [open, setOpen] = useState(false)
  const [shippingAddress, setShippingAddress] = useState([])

  const { user } = useSelector((state) => state.user)
  const navigate = useNavigate()

  useEffect(() => {
    const jquery = document.createElement('script')
    jquery.src = 'https://code.jquery.com/jquery-1.12.4.min.js'
    const iamport = document.createElement('script')
    iamport.src = 'https://cdn.iamport.kr/js/iamport.payment-1.1.7.js'
    document.head.appendChild(jquery)
    document.head.appendChild(iamport)
    return () => {
      document.head.removeChild(jquery)
      document.head.removeChild(iamport)
    }
  }, [])

  useEffect(() => {
    const orderData = JSON.parse(localStorage.getItem('latestOrder'))
    setOrderData(orderData)
    setShippingAddress(orderData.shippingAddress)
  }, [])

  const onClickPayment = () => {
    const { IMP } = window
    IMP.init('imp38821552') // 결제 데이터 정의
    const data = {
      pg: 'kakaopay', // PG사 (필수항목)
      pay_method: 'card', // 결제수단 (필수항목)
      merchant_uid: `mid_${new Date().getTime()}`, // 결제금액 (필수항목)
      name: '퀵딜 마켓 상품결제', // 주문명 (필수항목)
      amount: orderData?.subTotalPrice, // 금액 (필수항목)
      custom_data: { name: '부가정보', desc: '세부 부가정보' },
      buyer_name: user.name, // 구매자 이름
      buyer_tel: user.phoneNumber, // 구매자 전화번호 (필수항목)
      buyer_email: user.email, // 구매자 이메일
      buyer_addr: shippingAddress?.address1,
      buyer_postalcode: shippingAddress?.zipCode,
    }
    IMP.request_pay(data, callback)
  }

  const callback = async (response) => {
    try {
      const { success, error_msg, imp_uid, merchant_uid, pay_method, paid_amount, status } =
        response

      if (success) {
        toast.success('상품결제를 성공하였습니다!')

        // 주문 정보를 서버에 저장하는 부분
        const config = {
          headers: {
            'Content-Type': 'application/json',
          },
        }

        const orderDataToSend = {
          cart: orderData.cart,
          shippingAddress: shippingAddress,
          user: user,
          totalPrice: orderData.subTotalPrice,
          paymentInfo: {
            id: imp_uid,
            status: status,
            type: pay_method,
          },
        }

        await axios.post(`http://localhost:5000/api/v2/order/create-order`, orderDataToSend, config)

        // 기존 코드는 유지
        navigate('/order/success')
      } else {
        alert(`결제 실패 : ${error_msg}`)
      }
    } catch (error) {
      console.error(error)
      toast.error('주문 정보 저장 중 오류가 발생했습니다.')
    }
  }

  return (
    <>
      <button onClick={onClickPayment}>결제하기</button>
    </>
  )
}

export default KakaoPayment
```

# order.js 모델

```jsx
order.js 모델을 작성해준다 주문 내역에 사용하는 모델이다
```

```jsx
const mongoose = require('mongoose')

const orderSchema = new mongoose.Schema({
  cart: {
    type: Array,
    required: true,
  },
  shippingAddress: {
    type: Object,
    required: true,
  },
  user: {
    type: Object,
    required: true,
  },
  totalPrice: {
    type: Number,
    required: true,
  },
  status: {
    type: String,
    default: '처리 중',
  },
  paymentInfo: {
    id: {
      type: String,
    },
    status: {
      type: String,
    },
    type: {
      type: String,
    },
  },
  paidAt: {
    type: Date,
    default: Date.now(),
  },
  deliveredAt: {
    type: Date,
  },
  createdAt: {
    type: Date,
    default: Date.now(),
  },
})

module.exports = mongoose.model('Order', orderSchema)
```

# order.js 컨트롤러를 작성

```jsx
const express = require('express')
const router = express.Router()
const ErrorHandler = require('../util/ErrorHandler')
const catchAsyncErrors = require('../middleware/catchAsyncErrors')
const { isAuthenticated, isSeller } = require('../middleware/auth')
const Order = require('../model/order')
const Product = require('../model/product')

// 새 주문 생성
router.post(
  '/create-order',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const { cart, shippingAddress, user, totalPrice, paymentInfo } = req.body

      // shopId로 카트 아이템 그룹화
      const shopItemsMap = new Map()

      for (const item of cart) {
        const shopId = item.shopId
        if (!shopItemsMap.has(shopId)) {
          shopItemsMap.set(shopId, [])
        }
        shopItemsMap.get(shopId).push(item)
      }

      // 각 상점에 대한 주문 생성
      const orders = []

      for (const [shopId, items] of shopItemsMap) {
        const order = await Order.create({
          cart: items,
          shippingAddress,
          user,
          totalPrice,
          paymentInfo,
        })
        orders.push(order)
      }

      res.status(201).json({
        success: true,
        orders,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자의 모든 주문 가져오기
router.get(
  '/get-all-orders/:userId',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const orders = await Order.find({ 'user._id': req.params.userId }).sort({
        createdAt: -1,
      })

      res.status(200).json({
        success: true,
        orders,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 판매자의 모든 주문 가져오기
router.get(
  '/get-seller-all-orders/:shopId',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const orders = await Order.find({
        'cart.shopId': req.params.shopId,
      }).sort({
        createdAt: -1,
      })

      res.status(200).json({
        success: true,
        orders,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 판매자를 위한 주문 상태 업데이트
router.put(
  '/update-order-status/:id',
  isSeller,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const order = await Order.findById(req.params.id)

      if (!order) {
        return next(new ErrorHandler('주문을 찾을 수 없습니다', 400))
      }
      if (req.body.status === '배송 파트너로 이관됨') {
        order.cart.forEach(async (o) => {
          await updateOrder(o._id, o.qty)
        })
      }

      order.status = req.body.status

      if (req.body.status === '배송 완료') {
        order.deliveredAt = Date.now()
        order.paymentInfo.status = '배송 완료'
      }

      await order.save({ validateBeforeSave: false })

      res.status(200).json({
        success: true,
        order,
      })

      // 주문 업데이트 시 상품 재고 및 판매량 업데이트
      async function updateOrder(id, qty) {
        const product = await Product.findById(id)

        product.stock -= qty
        product.sold_out += qty

        await product.save({ validateBeforeSave: false })
      }
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 환불 요청 ---- 사용자
router.put(
  '/order-refund/:id',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const order = await Order.findById(req.params.id)

      if (!order) {
        return next(new ErrorHandler('주문을 찾을 수 없습니다.', 400))
      }

      order.status = req.body.status

      await order.save({ validateBeforeSave: false })

      res.status(200).json({
        success: true,
        order,
        message: '주문 환불 요청이 성공적으로 완료되었습니다!',
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 환불 승인 ---- 판매자
router.put(
  '/order-refund-success/:id',
  isSeller,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const order = await Order.findById(req.params.id)

      if (!order) {
        return next(new ErrorHandler('주문을 찾을 수 없습니다.', 400))
      }

      order.status = req.body.status

      await order.save()

      res.status(200).json({
        success: true,
        message: '주문 환불이 성공적으로 완료되었습니다!',
      })

      if (req.body.status === '환불 완료') {
        order.cart.forEach(async (o) => {
          await updateOrder(o._id, o.qty)
        })
      }

      // 환불 완료 시 상품 재고 및 판매량 업데이트
      async function updateOrder(id, qty) {
        const product = await Product.findById(id)

        product.stock += qty
        product.sold_out -= qty

        await product.save({ validateBeforeSave: false })
      }
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

module.exports = router
```

```jsx
현재 카카오 페이 결제 완료뒤면 아래 컨트롤러를 실행한다

최종적으로 얻게 되는 맵은 각 키가 고유한 shopId이고, 해당하는 값은 해당 상점에 속한 모든 아이템을 담은 배열

// 새 주문 생성
router.post(
  "/create-order",
  catchAsyncErrors(async (req, res, next) => {
    try {
      // 요청 바디에서 필요한 정보 추출
      const { cart, shippingAddress, user, totalPrice, paymentInfo } = req.body;

      // shopId로 카트 아이템을 그룹화하기 위한 Map 객체 생성
      const shopItemsMap = new Map();

      // 카트에 있는 각 아이템을 순회하며 shopId를 기준으로 그룹화
      for (const item of cart) { // cart 배열의 각 아이템을 하나씩 가져와 반복합니다.
        const shopId = item.shopId; // 현재 아이템의 shopId 속성 값을 가져와 shopId 변수에 할당
        if (!shopItemsMap.has(shopId)) { // shopItemsMap에 현재 shopId가 존재하지 않으면, 새로운 항목을 추가합니다. 이 항목은 빈 배열로 초기화
          shopItemsMap.set(shopId, []);
        }
        shopItemsMap.get(shopId).push(item);
      }

      // 각 상점에 대한 주문 생성
      const orders = [];

      // shopItemsMap을 순회하며 주문 생성
      for (const [shopId, items] of shopItemsMap) {
        const order = await Order.create({
          cart: items,
          shippingAddress,
          user,
          totalPrice,
          paymentInfo,
        });
        orders.push(order);
      }

      // 성공적으로 주문이 생성되면 201 상태 코드와 생성된 주문 목록을 응답
      res.status(201).json({
        success: true,
        orders,
      });
    } catch (error) {
      // 에러 발생 시 ErrorHandler를 이용하여 500 상태 코드와 에러 메시지를 응답
      return next(new ErrorHandler(error.message, 500));
    }
  })
);

-----------------------------------------------------------------------

Map 객체 생성: const shopItemsMap = new Map();

Map은 JavaScript에 내장된 객체로, 키-값 쌍을 저장할 수 있다.

이 경우 shopId를 기반으로 카트 아이템을 그룹화하기 위해 사용된다. 각 shopId는 키이며 해당하는 값은 해당 상점에 속한 아이템의 배열이다

-----------------------------------------------------------------------

카트 아이템 순회 및 shopId에 따른 그룹화:

for (const item of cart): 이 반복문은 cart 배열의 각 아이템을 순회한다.

const shopId = item.shopId;: 각 아이템에 대해 현재 shopId 속성을 추출한다. 이는 아이템이 속한 상점을 나타낸다.

if (!shopItemsMap.has(shopId)) { shopItemsMap.set(shopId, []); }: 현재 shopId에 대한 shopItemsMap 항목이 이미 있는지 확인한다.

없다면, 빈 배열을 값으로 가지는 새로운 항목을 생성한다.

shopItemsMap.get(shopId).push(item);: 현재 아이템을 해당 shopId에 대응하는 배열에 추가한다.

-----------------------------------------------------------------------

각 상점에 대한 아이템 그룹을 바탕으로 주문을 생성하고, 이를 orders 배열에 추가하는 역할을 한다.

// shopItemsMap을 순회하며 주문 생성
      for (const [shopId, items] of shopItemsMap) {
        const order = await Order.create({
          cart: items,
          shippingAddress,
          user,
          totalPrice,
          paymentInfo,
        });
        orders.push(order);
      }
```

# EventCard.jsx 수정 ( 상품 디테일과 상품 장바구니에 추가하는 기능 )

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381463/notion-blog/_/_/EcommerceProject/EcommerceProject_-5/Untitled_1.png)

```jsx
하드 코딩된 상품의 디테일을 동적으로 수정해주고 장바구니에 추가하는 기능을 추가해주었다 ( 예전에 안해서 지금해줌 )
```

```jsx
// 필요한 라이브러리 및 스타일을 불러옵니다.
import React from 'react'
import styles from '../../styles/styles' // 외부 스타일 파일을 불러옵니다.
import CountDown from './CountDown' // CountDown 컴포넌트를 불러옵니다.
import { Link } from 'react-router-dom' // 네비게이션을 위한 Link 컴포넌트를 불러옵니다.
import { useDispatch, useSelector } from 'react-redux' // Redux의 useDispatch 및 useSelector 훅을 사용합니다.
import { addToCart } from '../../redux/cart/cartAction' // Redux 액션을 불러옵니다.
import { toast } from 'react-toastify' // 토스트 알림을 위한 라이브러리를 불러옵니다.

const EventCard = ({ active, data }) => {
  const { cart } = useSelector((state) => state.cart) // Redux 상태에서 장바구니 정보를 가져옵니다.
  const dispatch = useDispatch() // Redux의 dispatch 함수를 가져옵니다.

  // 장바구니에 상품을 추가하는 핸들러 함수를 정의합니다.
  const addToCartHandler = (data) => {
    // 상품이 이미 장바구니에 있는지 확인합니다.
    const isItemExists = cart && cart.find((i) => i._id === data._id)

    if (isItemExists) {
      toast.error('상품이 이미 장바구니에 존재합니다!')
    } else {
      // 상품 재고가 있는지 확인하고, 장바구니에 추가합니다.
      if (data.stock < 1) {
        toast.error('제품 재고를 초과하였습니다!')
      } else {
        const cartData = { ...data, qty: 1 }
        dispatch(addToCart(cartData))
        toast.success('상품을 장바구니에 추가하였습니다!')
      }
    }
  }

  return (
    <div className={`block w-full rounded-lg bg-white ${active ? 'unset' : 'mb-12'} p-2 lg:flex`}>
      <div className="lg:-w[50%] m-auto w-full">
        {/* 상품 이미지를 표시합니다. */}
        <img src={`http://localhost:5000/${data?.images[0]}`} alt="" />
      </div>
      <div className="lg:[w-50%] flex w-full flex-col justify-center">
        {/* 상품 제목을 표시합니다. */}
        <h2 className={`${styles.productTitle}`}>{data?.name}</h2>
        {/* 상품 설명을 표시합니다. */}
        <p>{data?.description}</p>
        {/* 가격 정보를 표시합니다. */}
        <div className="flex justify-between py-2">
          <div className="flex">
            <h5 className="pr-3 text-[18px] font-[500] text-[#d55b45] line-through">
              {data?.originalPrice}$
            </h5>
            <h5 className="font-Roboto text-[20px] font-bold text-[#333]">
              {data?.discountPrice}$
            </h5>
          </div>
          <span className="pr-3 text-[17px] font-[400] text-[#44a55e]">{data?.sold_out} 판매</span>
        </div>
        {/* 이벤트 종료까지의 카운트다운을 표시합니다. */}
        <CountDown data={data} />
        <br />
        {/* 상세 보기 및 장바구니 추가 버튼을 표시합니다. */}
        <div className="flex items-center">
          <Link to={`/product/${data?._id}?isEvent=true`}>
            <div className={`${styles.button} text-[#fff]`}>상세 보기</div>
          </Link>
          <div
            className={`${styles.button} ml-5 text-[#fff]`}
            onClick={() => addToCartHandler(data)}
          >
            장바구니에 추가
          </div>
        </div>
      </div>
    </div>
  )
}

export default EventCard
```

# ProductDetailsPage.jsx 페이지 수정 (이벤트 디테일 프로덕트 디테일 선택 이동 )

```jsx
위의 이벤트 카드에서 디테일로 넘어갈 수 있게 수정해준다 초기화 할떄 쿼리 스트링에

isEvent 이걸 가져와서 렌더링할 컴포넌트를 선택한다

ex) /product/65eef37c66c40f6974c0bc8e?isEvent=true
```

```jsx
import React, { useEffect, useState } from 'react'
import { useParams, useSearchParams } from 'react-router-dom'
import Footer from '../components/Layout/Footer'
import Header from '../components/Layout/Header'
import ProductDetails from '../components/Products/ProductDetails'
import { productData } from '../static/data'
import { useSelector } from 'react-redux'
import SuggestedProduct from '../components/Products/SuggestedProduct'

const ProductDetailsPage = () => {
  // Redux에서 제품 및 이벤트 상태를 가져옵니다.
  const { allProducts } = useSelector((state) => state.product)
  const { allEvents } = useSelector((state) => state.event)

  // URL에서 파라미터 및 쿼리 스트링을 가져옵니다.
  const { id } = useParams()
  const [data, setData] = useState(null)
  const [searchParams] = useSearchParams()
  const eventData = searchParams.get('isEvent')

  // 제품 또는 이벤트 데이터를 설정합니다.
  useEffect(() => {
    if (eventData !== null) {
      // 이벤트 페이지인 경우 이벤트 데이터를 찾아 설정합니다.
      const data = allEvents && allEvents.find((i) => i._id === id)
      setData(data)
    } else {
      // 제품 페이지인 경우 제품 데이터를 찾아 설정합니다.
      const data = allProducts && allProducts.find((i) => i._id === id)
      setData(data)
    }
  }, [allProducts, allEvents])

  return (
    <div>
      {/* 헤더 컴포넌트를 렌더링합니다. */}
      <Header />

      {/* 제품 상세 정보 컴포넌트를 렌더링하고 제품 또는 이벤트 데이터를 전달합니다. */}
      <ProductDetails data={data} />

      {/* 이벤트 페이지가 아닌 경우에만 관련 제품을 제안하는 컴포넌트를 렌더링합니다. */}
      {!eventData && <>{data && <SuggestedProduct data={data} />}</>}

      {/* 푸터 컴포넌트를 렌더링합니다. */}
      <Footer />
    </div>
  )
}

export default ProductDetailsPage
```

# profileContent 수정 ( 하드코딩 수정 AllOrders 를 수정 )

```jsx
이제 사용자 정보에서 주문 내역을 확인 가능하다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381463/notion-blog/_/_/EcommerceProject/EcommerceProject_-5/Untitled_2.png)

```jsx
import React, { useState } from 'react'
import { AiOutlineArrowRight, AiOutlineCamera, AiOutlineDelete } from 'react-icons/ai'
import { useDispatch, useSelector } from 'react-redux'
import styles from '../../styles/styles'
import { DataGrid } from '@material-ui/data-grid'
import { Button } from '@material-ui/core'
import { Link } from 'react-router-dom'
import { toast } from 'react-toastify'
import { MdOutlineTrackChanges } from 'react-icons/md'
import { MdTrackChanges } from 'react-icons/md'
import { RxCross1 } from 'react-icons/rx'
import { Country, State } from 'country-state-city'
import { useEffect } from 'react'
import axios from 'axios'
import {
  deleteUserAddress,
  loadUser,
  updateUserAddress,
  updateUserInformation,
} from '../../redux/user/userAction'
import { getAllOrdersOfUser } from '../../redux/order/orderAction'

const ProfileContent = ({ active }) => {
  const { user, error, successMessage } = useSelector((state) => state.user)
  const [name, setName] = useState(user && user.name)
  const [email, setEmail] = useState(user && user.email)
  const [phoneNumber, setPhoneNumber] = useState(user && user.phoneNumber)
  const [password, setPassword] = useState('')
  const [avatar, setAvatar] = useState(null)
  const dispatch = useDispatch()

  useEffect(() => {
    if (error) {
      toast.error(error)
      dispatch({ type: 'clearErrors' })
    }
    if (successMessage) {
      toast.success(successMessage)
      dispatch({ type: 'clearMessages' })
    }
  }, [error, successMessage])

  const handleSubmit = async (e) => {
    e.preventDefault()
    try {
      // 사용자 정보 업데이트
      await dispatch(updateUserInformation({ name, email, phoneNumber, password }))

      // 사용자 정보 다시 가져오기
      await dispatch(loadUser())

      // 화면 다시 렌더링을 위한 동작 수행
      // 예: 상태 업데이트, 필요한 동작 수행 등

      toast.success('사용자 정보 업데이트 성공!')
    } catch (error) {
      toast.error('사용자 정보 업데이트 실패!')
    }
  }

  const handleImage = async (e) => {
    const file = e.target.files[0]
    setAvatar(file)

    const formData = new FormData()

    formData.append('image', e.target.files[0])

    await axios
      .put(`http://localhost:5000/api/v2/user/update-avatar`, formData, {
        headers: {
          'Content-Type': 'multipart/form-data',
        },
        withCredentials: true,
      })
      .then((response) => {
        dispatch(loadUser())
        toast.success('아바타 변경 성공!')
      })
      .catch((error) => {
        toast.error(error)
      })
  }

  return (
    <div className="w-full">
      {/* 프로필 */}
      {active === 1 && (
        <>
          <div className="flex w-full justify-center">
            <div className="relative">
              <img
                src={`http://localhost:5000/${user?.avatar}`}
                className="h-[150px] w-[150px] rounded-full border-[3px] border-[#3ad132] object-cover"
                alt=""
              />
              <div className="absolute right-[5px] bottom-[5px] flex h-[30px] w-[30px] cursor-pointer items-center justify-center rounded-full bg-[#E3E9EE]">
                <input type="file" id="image" className="hidden" onChange={handleImage} />
                <label htmlFor="image">
                  <AiOutlineCamera />
                </label>
              </div>
            </div>
          </div>
          <br />
          <br />
          <div className="w-full px-5">
            <form onSubmit={handleSubmit} aria-required={true}>
              <div className="800px:flex block w-full pb-3">
                <div className="800px:w-[50%] w-[100%]">
                  <label className="block pb-2">이름</label>
                  <input
                    type="text"
                    className={`${styles.input} 800px:mb-0 mb-4 !w-[95%]`}
                    required
                    value={name}
                    onChange={(e) => setName(e.target.value)}
                  />
                </div>
                <div className="800px:w-[50%] w-[100%]">
                  <label className="block pb-2">이메일 주소</label>
                  <input
                    type="text"
                    className={`${styles.input} 800px:mb-0 mb-1 !w-[95%]`}
                    required
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                  />
                </div>
              </div>

              <div className="800px:flex block w-full pb-3">
                <div className="800px:w-[50%] w-[100%]">
                  <label className="block pb-2">전화번호</label>
                  <input
                    type="number"
                    className={`${styles.input} 800px:mb-0 mb-4 !w-[95%]`}
                    required
                    value={phoneNumber}
                    onChange={(e) => setPhoneNumber(e.target.value)}
                  />
                </div>

                <div className="800px:w-[50%] w-[100%]">
                  <label className="block pb-2">비밀번호</label>
                  <input
                    type="password"
                    className={`${styles.input} 800px:mb-0 mb-4 !w-[95%]`}
                    required
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                  />
                </div>
              </div>
              <input
                className={`mt-8 h-[40px] w-[250px] cursor-pointer rounded-[3px] border border-[#3a24db] text-center text-[#3a24db]`}
                required
                value="업데이트"
                type="submit"
              />
            </form>
          </div>
        </>
      )}

      {/* 주문 */}
      {active === 2 && (
        <div>
          <AllOrders />
        </div>
      )}

      {/* 환불 */}
      {active === 3 && (
        <div>
          <AllRefundOrders />
        </div>
      )}

      {/* 주문 추적 */}
      {active === 5 && (
        <div>
          <TrackOrder />
        </div>
      )}

      {/* 비밀번호 변경 */}
      {active === 6 && (
        <div>
          <ChangePassword />
        </div>
      )}

      {/* 사용자 주소 */}
      {active === 7 && (
        <div>
          <Address />
        </div>
      )}
    </div>
  )
}

const AllOrders = () => {
  const { user } = useSelector((state) => state.user)
  const { orders } = useSelector((state) => state.orders)
  const dispatch = useDispatch()

  useEffect(() => {
    dispatch(getAllOrdersOfUser(user._id))
  }, [])

  const columns = [
    { field: 'id', headerName: '주문 ID', minWidth: 150, flex: 0.7 },

    {
      field: 'status',
      headerName: '상태',
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, 'status') === '배송 완료' ? 'greenColor' : 'redColor'
      },
    },
    {
      field: 'itemsQty',
      headerName: '상품 수량',
      type: 'number',
      minWidth: 130,
      flex: 0.7,
    },

    {
      field: 'total',
      headerName: '총액',
      type: 'number',
      minWidth: 130,
      flex: 0.8,
    },

    {
      field: ' ',
      flex: 1,
      minWidth: 150,
      headerName: '',
      type: 'number',
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/user/order/${params.id}`}>
              <Button>
                <AiOutlineArrowRight size={20} />
              </Button>
            </Link>
          </>
        )
      },
    },
  ]

  const row = []

  orders &&
    orders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.cart.length,
        total: item.totalPrice + '원 ',
        status: item.status,
      })
    })

  return (
    <div className="pt-1 pl-8">
      <DataGrid rows={row} columns={columns} pageSize={10} disableSelectionOnClick autoHeight />
    </div>
  )
}

const AllRefundOrders = () => {
  const { user } = useSelector((state) => state.user)
  const { orders } = useSelector((state) => state.orders)
  const dispatch = useDispatch()

  useEffect(() => {
    dispatch(getAllOrdersOfUser(user._id))
  }, [])

  const eligibleOrders = orders && orders.filter((item) => item.status === '환불 처리 중')

  const columns = [
    { field: 'id', headerName: 'Order ID', minWidth: 150, flex: 0.7 },

    {
      field: 'status',
      headerName: 'Status',
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, 'status') === '배송 완료' ? 'greenColor' : 'redColor'
      },
    },
    {
      field: 'itemsQty',
      headerName: 'Items Qty',
      type: 'number',
      minWidth: 130,
      flex: 0.7,
    },

    {
      field: 'total',
      headerName: 'Total',
      type: 'number',
      minWidth: 130,
      flex: 0.8,
    },

    {
      field: ' ',
      flex: 1,
      minWidth: 150,
      headerName: '',
      type: 'number',
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/user/order/${params.id}`}>
              <Button>
                <AiOutlineArrowRight size={20} />
              </Button>
            </Link>
          </>
        )
      },
    },
  ]

  const row = []

  eligibleOrders &&
    eligibleOrders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.cart.length,
        total: item.totalPrice + ' 원',
        status: item.status,
      })
    })

  return (
    <div className="pt-1 pl-8">
      <DataGrid rows={row} columns={columns} pageSize={10} autoHeight disableSelectionOnClick />
    </div>
  )
}
```

```jsx
const TrackOrder = () => {
  const { user } = useSelector((state) => state.user);
  const { orders } = useSelector((state) => state.orders);
  const dispatch = useDispatch();

  useEffect(() => {
    dispatch(getAllOrdersOfUser(user._id));
  }, []);

  const columns = [
    { field: "id", headerName: "Order ID", minWidth: 150, flex: 0.7 },
    {
      field: "status",
      headerName: "Status",
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, "status") === "배송 완료"
          ? "greenColor"
          : "redColor";
      },
    },
    {
      field: "itemsQty",
      headerName: "Items Qty",
      type: "number",
      minWidth: 130,
      flex: 0.7,
    },

    {
      field: "total",
      headerName: "Total",
      type: "number",
      minWidth: 130,
      flex: 0.8,
    },
    {
      field: " ",
      flex: 1,
      minWidth: 150,
      headerName: "",
      type: "number",
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/user/track/order/${params.id}`}>
              <Button>
                <MdTrackChanges size={20} />
              </Button>
            </Link>
          </>
        );
      },
    },
  ];

  const row = [];

  orders &&
    orders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.cart.length,
        total: item.totalPrice + " 원",
        status: item.status,
      });
    });

  return (
    <div className="pl-8 pt-1">
      <DataGrid
        rows={row}
        columns={columns}
        pageSize={10}
        disableSelectionOnClick
        autoHeight
      />
    </div>
  );
};

const ChangePassword = () => {
  const [oldPassword, setOldPassword] = useState("");
  const [newPassword, setNewPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");

  const passwordChangeHandler = async (e) => {
    e.preventDefault();

    await axios
      .put(
        `http://localhost:5000/api/v2/user/update-user-password`,
        { oldPassword, newPassword, confirmPassword },
        { withCredentials: true }
      )
      .then((res) => {
        toast.success(res.data.message);
        setOldPassword("");
        setNewPassword("");
        setConfirmPassword("");
      })
      .catch((error) => {
        toast.error(error.response.data.message);
      });
  };
  return (
    <div className="w-full px-5">
      <h1 className="block text-[25px] text-center font-[600] text-[#000000ba] pb-2">
        비밀번호 변경
      </h1>
      <div className="w-full">
        <form
          aria-required
          onSubmit={passwordChangeHandler}
          className="flex flex-col items-center"
        >
          <div className=" w-[100%] 800px:w-[50%] mt-5">
            <label className="block pb-2">기존 비밀번호 입력</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={oldPassword}
              onChange={(e) => setOldPassword(e.target.value)}
            />
          </div>
          <div className=" w-[100%] 800px:w-[50%] mt-2">
            <label className="block pb-2">새 비밀번호 입력</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={newPassword}
              onChange={(e) => setNewPassword(e.target.value)}
            />
          </div>
          <div className=" w-[100%] 800px:w-[50%] mt-2">
            <label className="block pb-2">새 비밀번호 확인</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={confirmPassword}
              onChange={(e) => setConfirmPassword(e.target.value)}
            />
            <input
              className={`w-[95%] h-[40px] border border-[#3a24db] text-center text-[#3a24db] rounded-[3px] mt-8 cursor-pointer`}
              required
              value="수정"
              type="submit"
            />
          </div>
        </form>
      </div>
    </div>
  );
};

const Address = () => {
  const [open, setOpen] = useState(false);
  const [country, setCountry] = useState("");
  const [city, setCity] = useState("");
  const [zipCode, setZipCode] = useState();
  const [address1, setAddress1] = useState("");
  const [address2, setAddress2] = useState("");
  const [addressType, setAddressType] = useState("");
  const { user } = useSelector((state) => state.user);
  const dispatch = useDispatch();

  const addressTypeData = [
    {
      name: "기본",
    },
    {
      name: "집",
    },
    {
      name: "회사",
    },
  ];

  const handleSubmit = async (e) => {
    e.preventDefault();

    if (addressType === "" || country === "" || city === "") {
      toast.error("모든 필드를 입력하세요!");
    } else {
      console.log(country, city, address1, address2, zipCode, addressType);
      dispatch(
        updateUserAddress({
          country,
          city,
          address1,
          address2,
          zipCode,
          addressType,
        })
      );
      setOpen(false);
      setCountry("");
      setCity("");
      setAddress1("");
      setAddress2("");
      setZipCode(null);
      setAddressType("");
    }
  };
```

```jsx
  const handleDelete = (item) => {
    const id = item._id;
    dispatch(deleteUserAddress(id));
  };
  return (
    <div className="w-full px-5">
      {open && (
        <div className="fixed w-full h-screen bg-[#0000004b] top-0 left-0 flex items-center justify-center ">
          <div className="w-[35%] h-[80vh] bg-white rounded shadow relative overflow-y-scroll">
            <div className="w-full flex justify-end p-3">
              <RxCross1
                size={30}
                className="cursor-pointer"
                onClick={() => setOpen(false)}
              />
            </div>
            <h1 className="text-center text-[25px] font-Poppins">
              새 주소 추가
            </h1>
            <div className="w-full">
              <form aria-required onSubmit={handleSubmit} className="w-full">
                <div className="w-full block p-4">
                  <div className="w-full pb-2">
                    <label className="block pb-2">국가</label>
                    <select
                      name=""
                      id=""
                      value={country}
                      onChange={(e) => setCountry(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        국가를 선택하세요
                      </option>
                      {Country &&
                        Country.getAllCountries().map((item) => (
                          <option
                            className="block pb-2"
                            key={item.isoCode}
                            value={item.isoCode}
                          >
                            {item.name}
                          </option>
                        ))}
                    </select>
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">도시 선택</label>
                    <select
                      name=""
                      id=""
                      value={city}
                      onChange={(e) => setCity(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        도시를 선택하세요
                      </option>
                      {State &&
                        State.getStatesOfCountry(country).map((item) => (
                          <option
                            className="block pb-2"
                            key={item.isoCode}
                            value={item.isoCode}
                          >
                            {item.name}
                          </option>
                        ))}
                    </select>
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">주소</label>
                    <input
                      type="address"
                      className={`${styles.input}`}
                      required
                      value={address1}
                      onChange={(e) => setAddress1(e.target.value)}
                    />
                  </div>
                  <div className="w-full pb-2">
                    <label className="block pb-2">상세주소</label>
                    <input
                      type="address"
                      className={`${styles.input}`}
                      required
                      value={address2}
                      onChange={(e) => setAddress2(e.target.value)}
                    />
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">우편 번호</label>
                    <input
                      type="number"
                      className={`${styles.input}`}
                      required
                      value={zipCode}
                      onChange={(e) => setZipCode(e.target.value)}
                    />
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">주소 유형</label>
                    <select
                      name=""
                      id=""
                      value={addressType}
                      onChange={(e) => setAddressType(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        주소 유형을 선택하세요
                      </option>
                      {addressTypeData &&
                        addressTypeData.map((item) => (
                          <option
                            className="block pb-2"
                            key={item.name}
                            value={item.name}
                          >
                            {item.name}
                          </option>
                        ))}
                    </select>
                  </div>

                  <div className=" w-full pb-2">
                    <input
                      type="submit"
                      className={`${styles.input} mt-5 cursor-pointer`}
                      required
                      readOnly
                    />
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      )}
      <div className="flex w-full items-center justify-between">
        <h1 className="text-[25px] font-[600] text-[#000000ba] pb-2">
          내 주소
        </h1>
        {/* <div className={`${styles.button} !rounded-md`}> */}
        <div
          className={`${styles.button} !rounded-md`}
          onClick={() => setOpen(true)}
        >
          <span className="text-[#fff]">새 주소 추가</span>
        </div>
      </div>
      <br />
      {user &&
        user.addresses.map((item, index) => (
          <div
            className="w-full bg-white h-min 800px:h-[70px] rounded-[4px] flex items-center px-3 shadow justify-between pr-10 mb-5"
            key={index}
          >
            <div className="flex items-center">
              <h5 className="pl-5 font-[600]">{item.addressType}</h5>
            </div>
            <div className="pl-8 flex items-center">
              <h6 className="text-[12px] 800px:text-[unset]">
                {item.address1} {item.address2}
              </h6>
            </div>
            <div className="pl-8 flex items-center">
              <h6 className="text-[12px] 800px:text-[unset]">
                {user && user.phoneNumber}
              </h6>
            </div>
            <div className="min-w-[10%] flex items-center justify-between pl-8">
              <AiOutlineDelete
                size={25}
                className="cursor-pointer"
                onClick={() => handleDelete(item)}
              />
            </div>
          </div>
        ))}

      {user && user.addresses.length === 0 && (
        <h5 className="text-center pt-8 text-[18px]">
          저장된 주소가 없습니다!
        </h5>
      )}
    </div>
  );
};
export default ProfileContent;

```

```jsx
주문 내역을 확인하는 핵심 부분은 AllOrders 컴포넌트이다

상태 및 Redux:

ProfileContent 컴포넌트는 Redux 스토어에서 user 및 orders 데이터를 가져오기 위해 useSelector 훅을 사용한다.

-----------------------------------------------------------------------

주문 가져오기:

useEffect 훅은 컴포넌트가 마운트될 때 getAllOrdersOfUser 액션을 디스패치한다.

이 액션은 현재 로그인된 사용자와 관련된 모든 주문을 가져오는 데 사용된다.

const AllOrders = () => {
  const { user } = useSelector((state) => state.user);
  const { orders } = useSelector((state) => state.orders);
  const dispatch = useDispatch();

  useEffect(() => {
    dispatch(getAllOrdersOfUser(user._id));
  }, []);

-----------------------------------------------------------------------

주문 표시:

컴포넌트는 주문을 표 형식으로 표시하기 위해 Material-UI의 DataGrid 컴포넌트를 사용한다.

columns 배열은 테이블의 구조를 정의하며 표시할 필드, 헤더 및 일부 스타일을 지정한다.

row 배열은 테이블에 표시할 주문 데이터로 채워진다. 각 주문은 id, itemsQty, total, status 등의 속성을 가진 객체로 표시된다.

-----------------------------------------------------------------------

주문 상세정보로 연결:

테이블의 각 행에는 해당 주문의 상세 보기로 이동하는 링크가 있다.

오른쪽 화살표(<AiOutlineArrowRight />) 버튼을 클릭하면 사용자가 해당 주문의 상세 보기로 이동한다.
```

# order redux 작성

```jsx
위의 "/get-all-orders/:userId" 를 사용하기 위해서 order redux 를 작성해준다
```

## orderAction.js 작성

```jsx
import { createAsyncThunk } from '@reduxjs/toolkit'
import axios from 'axios'

// 사용자의 모든 주문을 가져오는 비동기 Thunk 액션 생성자
export const getAllOrdersOfUser = createAsyncThunk('order/getAllOrdersOfUser', async (userId) => {
  try {
    // 서버로부터 사용자의 주문 목록을 비동기적으로 요청
    const { data } = await axios.get(`http://localhost:5000/api/v2/order/get-all-orders/${userId}`)
    // 요청 성공 시 주문 목록 반환
    return data.orders
  } catch (error) {
    // 요청 실패 시 에러 메시지 반환
    return error.response.data.message
  }
})

// 판매자의 모든 주문을 가져오는 비동기 Thunk 액션 생성자
export const getAllOrdersOfShop = createAsyncThunk('order/getAllOrdersOfShop', async (shopId) => {
  try {
    // 서버로부터 판매자의 주문 목록을 비동기적으로 요청
    const { data } = await axios.get(
      `http://localhost:5000/api/v2/order/get-seller-all-orders/${shopId}`
    )
    // 요청 성공 시 주문 목록 반환
    return data.orders
  } catch (error) {
    // 요청 실패 시 에러 메시지 반환
    return error.response.data.message
  }
})

// 관리자의 모든 주문을 가져오는 비동기 Thunk 액션 생성자
export const getAllOrdersOfAdmin = createAsyncThunk('order/getAllOrdersOfAdmin', async () => {
  try {
    // 서버로부터 관리자의 주문 목록을 비동기적으로 요청
    const { data } = await axios.get(`http://localhost:5000/api/v2/order/admin-all-orders`, {
      withCredentials: true,
    })
    // 요청 성공 시 주문 목록 반환
    return data.orders
  } catch (error) {
    // 요청 실패 시 에러 메시지 반환
    return error.response.data.message
  }
})
```

## orderReducer.js 작성

```jsx
import { createSlice } from '@reduxjs/toolkit'
import { getAllOrdersOfUser, getAllOrdersOfShop, getAllOrdersOfAdmin } from './orderAction'

const orderSlice = createSlice({
  name: 'order',
  initialState: {
    isLoading: true,
    orders: [],
    adminOrderLoading: true,
    adminOrders: [],
    error: null,
  },
  reducers: {
    clearErrors: (state) => {
      state.error = null
    },
  },
  extraReducers: (builder) => {
    // get all orders of user
    builder
      .addCase(getAllOrdersOfUser.pending, (state) => {
        state.isLoading = true
      })
      .addCase(getAllOrdersOfUser.fulfilled, (state, action) => {
        state.isLoading = false
        state.orders = action.payload
      })
      .addCase(getAllOrdersOfUser.rejected, (state, action) => {
        state.isLoading = false
        state.error = action.payload
      })

      // get all orders of shop
      .addCase(getAllOrdersOfShop.pending, (state) => {
        state.isLoading = true
      })
      .addCase(getAllOrdersOfShop.fulfilled, (state, action) => {
        state.isLoading = false
        state.orders = action.payload
      })
      .addCase(getAllOrdersOfShop.rejected, (state, action) => {
        state.isLoading = false
        state.error = action.payload
      })

      // get all orders for admin
      .addCase(getAllOrdersOfAdmin.pending, (state) => {
        state.adminOrderLoading = true
      })
      .addCase(getAllOrdersOfAdmin.fulfilled, (state, action) => {
        state.adminOrderLoading = false
        state.adminOrders = action.payload
      })
      .addCase(getAllOrdersOfAdmin.rejected, (state, action) => {
        state.adminOrderLoading = false
        state.error = action.payload
      })
  },
})

export const { clearErrors } = orderSlice.actions

export default orderSlice.reducer
```
