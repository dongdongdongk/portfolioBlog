---
title: '31. 자바 메소드(1)'
date: '2026-09-02'
category: '백엔드/JAVA'
tags: ['백엔드', 'JAVA']
draft: false
summary: ''
---

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled.png>)

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled%201.png>)

```java
**문법 : 리턴타입 메소드이름(매개변수 선언) { 실행코드 }**

ex)void powerOn() { 실행코드 }

ex)double divide(int x, int y ) { 실행코드}

호출 방법
ex)powerOn();

ex)double result = divide(10, 20);
```

```java
void power On () { ... } //전월을 켜고 끄는 실행 리턴값이 없기 때문에 리턴 타입이 void 다.

double divide( int x, int y ) { ... } // 계산기의 나누기 리턴 타입은 double 이다.

powerOn(); // powerOn 은 리턴값이 없는 void 기 때문에 단순하게 호출하면 된다.

double result = divide(10, 20); //divide 는 결과 값을 받을 변수를 선언하고 메소드 호출

int result = divide( 10, 20 ); //컴파일 에러 (결과 값을 받을 타입이 잘못 되었다.)
```

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled%202.png>)

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled%203.png>)

```java
// 메소드 실행에 필요한 데이터를 외부에서 받아 저장할 목적
double divide( int x, int y ) { 내용 } //매개변수 int x, int y

double result = divide( 10, 20 ); //데이터 10 과 20 x,y 에 대입

byte b1 = 10; // byte 타입은 int 보다 작은 타입이기 때문에 자동 타입 변환이 일어난다.
byte b2 = 20;
double result = divide( b1, b2 ); //문제 x

double result = divide( 10.5, 20.0); // int 타입에 대입할 값이 10.5 등 실수가 나오면
                                     //오류가 발생한다
```

**실습**

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled%204.png>)

**실행 클래스**

![Untitled](<31%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C(1)/Untitled%205.png>)

### 우리는 왜 메소드를 사용할까?

1.변수를 사용하는 이유는 반복되는 값을 쉽게 관리하기 위해

2.입출력 함수를 사용하는 이유는 개발자가 한 작업에 대해 확인하기 위해서

3.조건문을 사용하는 이유는 결과가 2가지 이상일 경우 선택하기 위해

4.반복문을 사용하는 이유는 반복되는 것들을 쉽게 사용하기 위해서

5.배열을 사용하는 이유는 반복되는 같은 타입의 변수들을 쉽게 관리하기 위해서

메소드를 사용하는 이유는 반복되는 기능들을 쉽게 관리하기 위해서,코드를 줄이기 위해! 그리고 궁극적인 목적은 재사용에 있다.단 상황에 따라 특정성을 부여함에 있어 주의해야 한다.롯데리아, 맥도날드, 버거킹에게 order라는 메소드를 동일하게 사용한다 가정했을 때,"안녕하세요 롯데리아입니다" 라는 특정성을 넣으면 안된다는 것이다.
