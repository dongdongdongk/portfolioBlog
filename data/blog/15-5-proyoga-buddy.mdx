---
title: '섹션 15: 프로젝트 5 : 애니메이션과 마이크로 인터랙션이 포함된 ProYoga Buddy 웹 앱 디자인'
date: '2026-09-02'
category: '프론트/디자인'
tags: ['프론트', '디자인']
draft: false
summary: ''
---

## 1. 새 프로젝트 파일 생성

```tsx
파일 생성 및 이름 변경: 새 파일을 만들고 이름을 "ProYoga Body Classes"로 변경합니다.

프레임 설정: 데스크톱 프레임을 선택하고, 높이를 900으로 줄이며 X와 Y 위치를 0으로 설정합니다.

						 프레임의 이름을 "ProYoga Body Homepage"로 변경합니다.
```

## 2. 헤더와 네비게이션 바 디자인

```tsx
이미지 삽입: Unsplash 플러그인을 사용하여 "요가" 이미지를 검색해 프레임에 배치합니다.

텍스트 추가: 텍스트 도구를 사용해 "ProYoga Body", "Home", "Course", "About" 등의 텍스트를 추가합니다.

	"ProYoga Body"는 글꼴을 'Raleway'로 설정하고, Bold 스타일에 글자 크기를 24로 설정합니다.

	"Home", "Course", "About"의 글자 크기는 18로 설정합니다.

글로벌 스타일 설정: 글꼴, 크기, 두께 등을 글로벌 스타일로 추가해 일관성 있게 관리합니다.

레이아웃 그리드 설정: 프레임의 레이아웃 그리드를 설정하고, 6개의 열과 100의 마진을 사용해 텍스트를 정렬합니다.

자동 레이아웃 추가: 세 개의 네비게이션 텍스트에 자동 레이아웃을 적용해 간격을 줄이고, 프레임의 오른쪽 모서리에 배치합니다.
```

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381591/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image.png)

## 3. 메인 콘텐츠 디자인

```tsx
제목 추가: 텍스트 도구를 사용해 "ProYoga Buddy와 함께 자신을 찾으세요"라는 제목을 추가합니다.

	글꼴을 'Lora'로 변경하고, 크기를 64로 설정하며, 중앙에 배치하고 흰색으로 색상을 변경합니다.

부제목 추가: 부제목 텍스트를 "Raleway" 글꼴로 설정하고, Medium 스타일에 크기를 24로 설정하며, 중앙에 배치합니다.

					   부제목 텍스트의 색상도 흰색으로 변경합니다.

이미지 오버레이 추가: 프레임에 흰색 오버레이를 추가하고 불투명도를 60%로 설정합니다.
```

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381589/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_1.png)

## 4. 슬라이드 효과 만들기

```tsx
슬라이드 점 표시: 슬라이드 효과를 나타내기 위해 24x24 크기의 원 세 개를 만들어 배치합니다. 두 개의 원은 50% 불투명도로 설정합니다.

Variant를 사용한 슬라이더 프레임 생성:

	세 개의 프레임을 만들고 각각 다른 요가 이미지를 삽입합니다.

	이 세 개의 프레임을 연결하여 슬라이드 효과를 구현합니다.

	트리거를 'On click'에서 'After delay'로 변경하고, 각 슬라이드의 지연 시간을 3초로 설정합니다.

슬라이드 점 반응 추가:

	각 프레임에 맞춰 슬라이드 점의 불투명도를 조정해, 슬라이드가 전환될 때 점의 색상도 변하도록 설정합니다.
```

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381590/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_2.png)

# { 홈페이지 - 요가 수업에 참여해야 하는 이유 페이지 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381590/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_3.png)

## 1. 기존 디자인 정리

```tsx
프레임 이름 변경: 기존 프레임의 이름을 변경하고 정리합니다.

새 프레임 생성: 이전 프레임 아래에 새 프레임을 생성하고, 높이를 900으로 설정한 후 프레임의 이름을 "홈페이지"로 변경합니다.

네이밍 규칙: "Why to join ProYogaBuddy"라는 제목을 사용하여 페이지의 목적을 나타냅니다.
```

## 2. 텍스트 디자인

```tsx
텍스트 추가: "Why ProYogaBuddy"라는 텍스트를 추가합니다.

텍스트 스타일 설정: 글꼴을 'Lora', 크기를 48픽셀, 세미볼드로 설정합니다.

디자인 시스템에 추가: 이 텍스트 스타일을 "Lora, 48픽셀, 세미볼드"라는 이름으로 디자인 시스템에 저장합니다.

텍스트 색상: 텍스트 색상을 #4A4C5C (짙은 회색)으로 변경하고, 이 색상도 디자인 시스템에 추가합니다.

텍스트 배치: 텍스트를 중앙에 정렬하고 Y 값을 40으로 설정하여 위치를 조정합니다.
```

## 3. 카드 디자인

```tsx
프레임 생성: 크기 610x500의 프레임을 생성합니다.

이미지 삽입: Unsplash에서 'peace'라는 키워드로 이미지를 검색하여 프레임에 삽입합니다.

이미지 반경 설정: 이미지의 반경을 10으로 설정합니다.

텍스트 추가: "Experience the thrill"이라는 텍스트를 추가합니다.

텍스트 스타일 설정: 글꼴을 'Raleway', 굵기를 Bold, 크기를 36픽셀, 대문자로 설정합니다.

디자인 시스템에 추가: 이 텍스트 스타일을 "Raleway, 36픽셀, Bold, Uppercase"라는 이름으로 디자인 시스템에 저장합니다.
```

## 이미지 오버레이 추가

```tsx
오버레이 설정: 카드에 오버레이를 추가하여 텍스트 가독성을 높입니다. 불투명도를 40%로 설정합니다.

색상 변경: 프레임 색상을 흰색(#FFFFFF)에서 연한 파란색(#f7f8fe)으로 변경하고, 이 색상도 디자인 시스템에 추가합니다.
```

# { 홈페이지 - 강의 페이지로 이동하는 버튼 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381590/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_4.png)

## 이미지 추가

```tsx
플러그인을 사용하여 이미지를 선택하고 프레임에 삽입합니다.

이미지를 프레임 내에서 조정하고, 그 위에 오버레이를 추가합니다.

오버레이의 불투명도를 50%로 설정합니다.
```

## 버튼 생성

```tsx
텍스트 도구를 사용하여 버튼에 "강좌 탐색하기"라는 텍스트를 입력합니다.

텍스트 스타일을 다음과 같이 설정합니다:

	글자 크기: 36px에서 16px로 변경

	글꼴: 'Raleway', 'SemiBold'

	문자 케이스: 'As typed'

이 텍스트 스타일을 디자인 시스템에 추가합니다: "Raleway, 16px, SemiBold".

버튼의 배경 채우기를 추가합니다:

	색상을 단색에서 선형 그라데이션으로 변경합니다.

	색상: 흰색에서 파란색(6C72D7)으로 변경

	두 번째 색상: 8E93E3, 불투명도 100%

	그라데이션 방향을 조정합니다.

테두리 반경을 10으로 설정하고 프레임 이름을 "Button"으로 변경합니다.
```

## 호버 효과 추가

```tsx
버튼을 컴포넌트로 변환하고, 한 개의 변형(variant)을 추가합니다.

두 번째 변형의 속성을 "Hover"로 변경합니다.

오버레이를 더 어둡게 하기 위해 불투명도를 10%로 줄입니다.

프로토타입에서 트리거를 "on click"에서 "while hovering"로 변경하고 애니메이션을 "smart animate"로 설정합니다.
```

## 버튼 테스트 및 배치

```tsx
만든 버튼을 Assets에서 끌어와 중앙에 배치합니다.

프레임을 선택하고 프로토타입을 실행하여 호버 효과가 제대로 작동하는지 확인합니다.

커서를 버튼 위에 올리면, 버튼이 약간 어두워지는 것을 확인할 수 있습니다.
```

# { 홈페이지 - 애니메이션이 포함된 멤버십 플랜 디테일 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381590/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_5.png)

```tsx
카드에 마우스를 올리면 카드가 불이 들어오는 ( 반전 ) 디자인을 만들어 본다
```

## 멤버십 플랜 카드 생성

```tsx
새로운 프레임을 생성하여 멤버십 카드로 설정하고, 크기를 500으로 조정합니다. 모서리 반경을 10으로 설정합니다.

텍스트 도구를 사용하여 카드에 "$5 per month" 등의 텍스트를 추가하고, 글꼴과 스타일을 설정합니다.

선 도구를 사용하여 카드에 선을 추가하고, 텍스트와 선 사이의 간격을 조정합니다.

카드에 버튼을 추가하고, 버튼을 '시작하기'라는 텍스트로 설정한 후, 색상과 스타일을 지정합니다. 버튼을 컴포넌트로 변환하고, 호버(hover) 효과를 적용합니다.
```

## 애니메이션 효과 추가

```tsx
멤버십 카드에 변형(variant)을 추가하여 다양한 상태를 설정합니다. 각 상태에 애니메이션 효과를 적용합니다.

프로토타입 모드에서 카드의 애니메이션이 제대로 작동하는지 확인합니다.
```

## 멤버십 카드 복제 및 수정

```tsx
멤버십 카드의 복사본을 생성하여 다양한 가격과 기능을 표시하는 추가 카드를 만듭니다.

각 카드의 텍스트와 레이아웃을 조정하여 중앙에 정렬합니다. 마스터 컴포넌트를 수정하여 모든 카드에 동일한 스타일을 적용합니다.
```

## 최종 조정 및 마무리

```tsx
생성된 모든 컴포넌트를 별도의 페이지에 정렬하여 배치합니다.

강의에서 멤버십 페이지의 모든 요소가 제대로 배치되고 애니메이션이 완벽하게 작동하는지 확인합니다.
```

# { 홈페이지 - 다이나믹 이메일 인풋이 있는 푸터 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381591/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_6.png)

## 1. Footer 프레임 생성

```tsx
Figma에서 데스크톱 크기의 새로운 프레임을 생성하고, 이름을 "Homepage/Footer"로 변경합니다.

프레임의 크기를 300으로 조정하고, 배경을 흰색에서 그라데이션으로 변경합니다.

텍스트 도구를 사용하여 가운데 정렬된 텍스트를 추가하고, 크기를 16에서 36픽셀로 변경합니다.
```

## 2. 동적 이메일 입력란 디자인

```tsx
700x60 크기의 프레임을 생성하여 동적 이메일 입력란을 만듭니다. 모서리 반경을 10으로 설정합니다.

이 프레임의 복사본을 만들어 크기를 700x90으로 변경하고 색상을 진한 회색으로 설정합니다.

이메일 아이콘을 추가하고, 색상을 검정에서 흰색으로 변경한 후 중앙에 정렬합니다.

텍스트 도구로 "Your email address"라는 텍스트를 추가하고, 크기를 36픽셀에서 18픽셀로 줄여서 중앙에 정렬합니다.
```

## 3. Variant 활용하여 상태 생성

```tsx
기본 상태 외에 세 개의 추가 상태를 생성합니다:

	두 번째 상태: "hit" 유형으로, 사용자가 타이핑 중임을 나타냅니다.

	세 번째 상태: 추가적인 효과를 부여하여 상태를 변경합니다.

	네 번째 상태: "이메일 타입"으로 설정하고, 텍스트를 "구독해 주셔서 감사합니다"로 변경합니다.

							 배경색을 연한 파란색으로 설정하고, 텍스트 색상을 파란색으로 변경합니다.
```

## 4. 프로토타입 연결

```tsx
각각의 상태를 Smart animate 애니메이션으로 연결합니다:

	기본 상태에서 두 번째 상태로, 두 번째에서 세 번째 상태로, 세 번째에서 네 번째 상태로 연결합니다.

	네 번째 상태에서 "Thanks" 메시지가 표시되도록 설정합니다.
```

## 5. 트리거 및 애니메이션 설정

```tsx
트리거를 클릭에서 "after delay"로 변경하고, 지속 시간을 2초로 설정하여 "Thanks" 메시지가 2초 후 사라지도록 합니다.
```

## 6. Footer에 입력란 배치 및 마무리

```tsx
이메일 입력란을 Footer에 배치하고, 모든 상태와 변형을 변형 페이지로 옮깁니다
```

# { CourseDetails 헤딩 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381591/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_7.png)

```tsx
헤더 만들기 설명할게 별로 없다
```

# { 호버 효과가 있는 CourseDetails 카드 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381591/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_8.png)

```tsx
컴포넌트를 여러개 만들어서 호버 기능에 사진에 불투명도 30 을 줘서 마우스를 올리면 어두워 지게 해줬다
```

# { 페이지 헤딩 및 요가 수업의 미션 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381591/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_9.png)

## 1. Heading 섹션 만들기

```tsx
기존 프레임의 복사본을 만들어 프레임 이름을 **"About/Heading"**으로 변경.

텍스트를 **"About ProYoga Body"**로 변경하고, 섹션을 중앙에 배치.
```

## 2. Mission 섹션 만들기

```tsx
데스크톱 크기의 프레임을 가져와 이름을 **"About/Mission"**으로 변경하고, 높이를 900으로 설정.

이 프레임은 ProYoga Body의 비전과 미션을 설명하는 페이지로, 헤딩과 텍스트로 구성.
```

## 3. 헤딩 및 디자인 스타일 설정

```tsx
텍스트 도구를 사용하여 **"Our Mission"**이라는 텍스트를 작성하고, 스타일을 타이틀 케이스로 변경.

글꼴 스타일: Lora, 48px, Semi-bold.

색상을 검정에서 진한 회색으로 변경하고, 배경 색상을 연한 파란색으로 변경.
```

## 4. 아이콘 추가

```tsx
Iconify에서 로터스 아이콘을 검색하여 가져온 후, 크기를 600으로 조절하고 중앙에 배치.

불투명도를 30%로 줄이고, 색상을 하늘색으로 변경.
```

## 5. 텍스트 추가

```tsx
아이콘 위에 텍스트 영역을 만들고, Lorem Ipsum을 사용해 한 문단을 생성.

텍스트 스타일: 24px Medium, 중앙 정렬.

색상을 검정에서 기본 회색으로 변경하고, 중앙에 맞춤.
```

## 6. 이정표 섹션 만들기

```tsx
"1875+ Students", "20+ Classes", "500+ Events"라는 세 가지 이정표를 작성.

각각의 텍스트를 선택하여, 크기를 18px Medium으로 설정하고, 주요 텍스트는 36px Semi-bold로 조정.

자동 레이아웃 기능을 사용해 요소들을 쉽게 배열하고, 간격 모드를 "packed"에서 "space between"으로 변경.
```

# { 모든 페이지 연결 및 ProYoga Buddy 웹 앱 최종 손질 }

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381590/notion-blog/_/_/_/_UI_UX_/_15_5_ProYoga_Buddy_/image_10.png)

## 1. 프로토타입 페이지 생성

```tsx
새 페이지를 만들어 이름을 **"ProYoga Buddy Prototype"**으로 변경.

모든 디자인 프레임을 선택하여 새 페이지에 복사하고 적절히 배치.
```

## 2. 프레임 정리 및 이름 변경

```tsx
데스크톱 프레임을 가져와 메인 프레임으로 사용.

홈페이지, CourseDetails, About 페이지, 그리고 Footer 프레임을 메인 프레임 안에 배치하고 각 프레임의 크기와 이름을 적절히 조정.
```

## 3. 프로토타입 연결

```tsx
프로토타입 모드로 전환하여 각 프레임을 연결.

애니메이션을 Smart Animate에서 Dissolve로 변경.

About 텍스트를 About 페이지와 연결하고, CourseDetails를 Course 상세 페이지와 연결.

홈 텍스트를 홈페이지와, 로고를 메인 프레임과 연결하여 네비게이션 설정.
```

## 4. 프로토타입 테스트

```tsx
프로토타입을 시작하고 페이지 간의 전환과 애니메이션을 테스트.

네비게이션을 통해 페이지 간 쉽게 이동할 수 있으며, 카드 애니메이션과 이메일 입력 바도 정상 작동함을 확인.
```
