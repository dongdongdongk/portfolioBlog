---
title: 'Docker 시험'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

1.다음이 설명하는 것은 무엇인가?(3)

```jsx
비스 운영에 필요한 서버 프로그램, 소스코드 및 라이브러리, 컴파일된 실행 파일을 묶은 형태
```

```xml
1.	Container
2.	Docker
3.	Image
4.	Files
```

2.다음 중 데비안 계열 리눅스에서 사용하는 패키지 관리 도구로 가장 알맞은 것은?(1)

```jsx
이미지(Image)를 실행한 상태로,
응용프로그램의 종속성과 함께 응용프로그램 자체를 패키징 or 캡슐화하여 격리된 공간에서 프로세스를 동작시키는 기술
```

```xml
1.	Container
2.	Process
3.	Pakcage
4.	Linux
```

3.다음 중 mariadb 이미지를 검색하는 명령어는?(3)

```xml
1.	docker find mariadb
2.	docker pull mariadb
3.	docker search mariadb
4.	docker get mariadb
```

4. 다음 중 Local에 다운 받은 이미지 목록을 보여주는 명령어는?(4)

```xml
1.	docker list
2.	docker containers
3.	docker dir
4.	docker images
```

5. Docker Image를 삭제 하는 명령어는?(4)

```xml
1.	docker rm -rf 이미지명
2.	docker delete 이미지명
3.	docker remove 이미지명
4.	docker rmi 이미지명
```

6.  Container를 생성 하는 명령어는?(2)

```xml
1.	docker create
2.	docker run
3.	docker start
4.	docker make
```

7.  Container 생성시 Port Fowarding 옵션은 무엇인가?(1)

```xml
1.	-p
2.	-d
3.	-port
4.	-f
```

8. 생성한 Container의 /bin/bash shell로 접속하는 방법으로 맞는 것은?(1)

```xml
1.	docker exec -it 컨테이너명 /bin/bash
2.	docker run -it 컨테이너명 /bin/bash
3.	docker connect -it 컨테이너명 /bin/bash
4.	docker conn -it 컨테이너명 /bin/bash
```

9.  Host 와 Container간의 폴더 공유 옵션은 무엇인가?(2)

```xml
1.	-s
2.	-v
3.	-h
4.	-f
```

10. 현재 실행중인 Container목록만 보여주는 명령어는?(3)

```xml
1.	docker view
2.	docker ps -a
3.	docker ps
4.	docker list
```
