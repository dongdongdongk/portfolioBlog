---
title: 'EcommerceProject -4-2'
date: '2026-09-02'
category: '풀스택/프로젝트 정리'
tags: ['풀스택', '프로젝트 정리']
draft: false
summary: ''
---

# 유저 업데이트 기능 구현

## ProfileContent.jsx 수정 ( 업데이트 인풋 만들어주기 폰넘버, 패스워드 )

```jsx
사용자 정보를 업데이트 해주는 부분을 작성해준다

이름과 주소 휴대폰 번호를 수정 가능하다 ( 자신의 비밀번호를 입력해야지 수정 가능하다 )
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381463/notion-blog/_/_/EcommerceProject/EcommerceProject_-4-2/Untitled.png)

```jsx
import React, { useState } from "react";
import {
  AiOutlineArrowRight,
  AiOutlineCamera,
  AiOutlineDelete,
} from "react-icons/ai";
import { useDispatch, useSelector } from "react-redux";
import styles from "../../styles/styles";
import { DataGrid } from "@material-ui/data-grid";
import { Button } from "@material-ui/core";
import { Link } from "react-router-dom";
import { toast } from "react-toastify";
import { MdOutlineTrackChanges } from "react-icons/md";
import { MdTrackChanges } from "react-icons/md";
import { RxCross1 } from "react-icons/rx";
import { Country, State } from "country-state-city";
import { useEffect } from "react";
import axios from "axios";
import {
  deleteUserAddress,
  loadUser,
  updateUserAddress,
  updateUserInformation,
} from "../../redux/user/userAction";

const ProfileContent = ({ active }) => {
  // Redux에서 상태와 액션을 가져오기
  const { user, error, successMessage } = useSelector((state) => state.user);
  const [name, setName] = useState(user && user.name);
  const [email, setEmail] = useState(user && user.email);
  const [phoneNumber, setPhoneNumber] = useState(user && user.phoneNumber);
  const [password, setPassword] = useState("");
  const [avatar, setAvatar] = useState(null);
  const dispatch = useDispatch();

  // 에러와 성공 메시지 처리를 위한 useEffect
  useEffect(() => {
    if (error) {
      toast.error(error);
      dispatch({ type: "clearErrors" });
    }
    if (successMessage) {
      toast.success(successMessage);
      dispatch({ type: "clearMessages" });
    }
  }, [error, successMessage]);

  // 사용자 정보 업데이트 핸들러
  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(name, email, phoneNumber, password);
    dispatch(updateUserInformation({ name, email, phoneNumber, password }));
  };

  // 이미지 업로드 핸들러
  const handleImage = async (e) => {
    const file = e.target.files[0];
    setAvatar(file);

    const formData = new FormData();

    formData.append("image", e.target.files[0]);

    await axios
      .put(`http://localhost:5000/api/v2/user/update-avatar`, formData, {
        headers: {
          "Content-Type": "multipart/form-data",
        },
        withCredentials: true,
      })
      .then((response) => {
        dispatch(loadUser());
        toast.success("avatar updated successfully!");
      })
      .catch((error) => {
        toast.error(error);
      });
  };

  return (
    <div className="w-full">
      {/* 프로필 */}
      {active === 1 && (
        <>
          <div className="flex justify-center w-full">
            <div className="relative">
              <img
                src={`http://localhost:5000/${user?.avatar}`}
                className="w-[150px] h-[150px] rounded-full object-cover border-[3px] border-[#3ad132]"
                alt=""
              />
              <div className="w-[30px] h-[30px] bg-[#E3E9EE] rounded-full flex items-center justify-center cursor-pointer absolute bottom-[5px] right-[5px]">
                <input
                  type="file"
                  id="image"
                  className="hidden"
                  onChange={handleImage}
                />
                <label htmlFor="image">
                  <AiOutlineCamera />
                </label>
              </div>
            </div>
          </div>
          <br />
          <br />
          <div className="w-full px-5">
            <form onSubmit={handleSubmit} aria-required={true}>
              {/* 이름, 이메일, 전화번호, 비밀번호 입력란 */}
              <div className="w-full 800px:flex block pb-3">
                <div className=" w-[100%] 800px:w-[50%]">
                  <label className="block pb-2">Full Name</label>
                  <input
                    type="text"
                    className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
                    required
                    value={name}
                    onChange={(e) => setName(e.target.value)}
                  />
                </div>
                <div className=" w-[100%] 800px:w-[50%]">
                  <label className="block pb-2">Email Address</label>
                  <input
                    type="text"
                    className={`${styles.input} !w-[95%] mb-1 800px:mb-0`}
                    required
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                  />
                </div>
              </div>

              <div className="w-full 800px:flex block pb-3">
                <div className=" w-[100%] 800px:w-[50%]">
                  <label className="block pb-2">Phone Number</label>
                  <input
                    type="number"
                    className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
                    required
                    value={phoneNumber}
                    onChange={(e) => setPhoneNumber(e.target.value)}
                  />
                </div>

                <div className=" w-[100%] 800px:w-[50%]">
                  <label className="block pb-2">Enter your password</label>
                  <input
                    type="password"
                    className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
                    required
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                  />
                </div>
              </div>
              {/* 업데이트 버튼 */}
              <input
                className={`w-[250px] h-[40px] border border-[#3a24db] text-center text-[#3a24db] rounded-[3px] mt-8 cursor-pointer`}
                required
                value="Update"
                type="submit"
              />
            </form>
          </div>
        </>
      )}

      {/* 주문 */}
      {active === 2 && (
        <div>
          <AllOrders />
        </div>
      )}

      {/* 환불 주문 */}
      {active === 3 && (
        <div>
          <AllRefundOrders />
        </div>
      )}

      {/* 주문 추적 */}
      {active === 5 && (
        <div>
          <TrackOrder />
        </div>
      )}

      {/* 비밀번호 변경 */}
      {active === 6 && (
        <div>
          <ChangePassword />
        </div>
      )}

      {/* 사용자 주소 */}
      {active === 7 && (
        <div>
          <Address />
        </div>
      )}
    </div>
  );
};

// 모든 주문 목록 컴포넌트
const AllOrders = () => {
  const orders = [
    {
      _id: "7463hvbfbhfbrtr28820221",
      orderItems: [
        {
          name: "Iphone 14 pro max",
        },
      ],
      totalPrice: 120,
      orderStatus: "Processing",
    },
  ];

  const columns = [
    { field: "id", headerName: "Order ID", minWidth: 150, flex: 0.7 },
    {
      field: "status",
      headerName: "Status",
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, "status") === "Delivered"
          ? "greenColor"
          : "redColor";
      },
    },
    {
      field: "itemsQty",
      headerName: "Items Qty",
      type: "number",
      minWidth: 130,
      flex: 0.7,
    },
    {
      field: "total",
      headerName: "Total",
      type: "number",
      minWidth: 130,
      flex: 0.8,
    },
    {
      field: " ",
      flex: 1,
      minWidth: 150,
      headerName: "",
      type: "number",
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/order/${params.id}`}>
              <Button>
                <AiOutlineArrowRight size={20} />
              </Button>
            </Link>
          </>
        );
      },
    },
  ];

  const row = [];

  orders &&
    orders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.orderItems.length,
        total: "US$ " + item.totalPrice,
        status: item.orderStatus,
      });
    });

  return (
    <div className="pl-8 pt-1">
      <DataGrid
        rows={row}
        columns={columns}
        pageSize={10}
        disableSelectionOnClick
        autoHeight
      />
    </div>
  );
};

// 모든 환불 주문 목록 컴포넌트
const AllRefundOrders = () => {
  const orders = [
    {
      _id: "7463hvbfbhfbrtr28820221",
      orderItems: [
        {
          name: "Iphone 14 pro max",
        },
      ],
      totalPrice: 120,
      orderStatus: "Processing",
    },
  ];

  const columns = [
    { field: "id", headerName: "Order ID", minWidth: 150, flex: 0.7 },
    {
      field: "status",
      headerName: "Status",
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, "status") === "Delivered"
          ? "greenColor"
          : "redColor";
      },
    },
    {
      field: "itemsQty",
      headerName: "Items Qty",
      type: "number",
      minWidth: 130,
      flex: 0.7,
    },
    {
      field: "total",
      headerName: "Total",
      type: "number",
      minWidth: 130,
      flex: 0.8,
    },
    {
      field: " ",
      flex: 1,
      minWidth: 150,
      headerName: "",
      type: "number",
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/order/${params.id}`}>
              <Button>
                <AiOutlineArrowRight size={20} />
              </Button>
            </Link>
          </>
        );
      },
    },
  ];

  const row = [];

  orders &&
    orders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.orderItems.length,
        total: "US$ " + item.totalPrice,
        status: item.orderStatus,
      });
    });

  return (
    <div className="pl-8 pt-1">
      <DataGrid
        rows={row}
        columns={columns}
        pageSize={10}
        autoHeight
        disableSelectionOnClick
      />
    </div>
  );
};

// 주문 추적 컴포넌트
const TrackOrder = () => {
  const orders = [
    {
      _id: "7463hvbfbhfbrtr28820221",
      orderItems: [
        {
          name: "Iphone 14 pro max",
        },
      ],
      totalPrice: 120,
      orderStatus: "Processing",
    },
  ];

```

```jsx
  const columns = [
    { field: "id", headerName: "Order ID", minWidth: 150, flex: 0.7 },
    {
      field: "status",
      headerName: "Status",
      minWidth: 130,
      flex: 0.7,
      cellClassName: (params) => {
        return params.getValue(params.id, "status") === "Delivered"
          ? "greenColor"
          : "redColor";
      },
    },
    {
      field: "itemsQty",
      headerName: "Items Qty",
      type: "number",
      minWidth: 130,
      flex: 0.7,
    },
    {
      field: "total",
      headerName: "Total",
      type: "number",
      minWidth: 130,
      flex: 0.8,
    },
    {
      field: " ",
      flex: 1,
      minWidth: 130,
      headerName: "",
      type: "number",
      sortable: false,
      renderCell: (params) => {
        return (
          <>
            <Link to={`/order/${params.id}`}>
              <Button>
                <MdOutlineTrackChanges size={20} />
              </Button>
            </Link>
          </>
        );
      },
    },
  ];

  const row = [];

  orders &&
    orders.forEach((item) => {
      row.push({
        id: item._id,
        itemsQty: item.orderItems.length,
        total: "US$ " + item.totalPrice,
        status: item.orderStatus,
      });
    });

  return (
    <div className="pl-8 pt-1">
      <DataGrid
        rows={row}
        columns={columns}
        pageSize={10}
        disableSelectionOnClick
        autoHeight
      />
    </div>
  );
};

// 비밀번호 변경 컴포넌트
const ChangePassword = () => {
  const [oldPassword, setOldPassword] = useState("");
  const [newPassword, setNewPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");

  // 비밀번호 변경 핸들러
  const passwordChangeHandler = async (e) => {
    e.preventDefault();

    await axios
      .put(
        `http://localhost:5000/api/v2/user/update-user-password`,
        { oldPassword, newPassword, confirmPassword },
        { withCredentials: true }
      )
      .then((res) => {
        toast.success(res.data.message);
        setOldPassword("");
        setNewPassword("");
        setConfirmPassword("");
      })
      .catch((error) => {
        toast.error(error.response.data.message);
      });
  };
  return (
    <div className="w-full px-5">
      <h1 className="block text-[25px] text-center font-[600] text-[#000000ba] pb-2">
        비밀번호 변경
      </h1>
      <div className="w-full">
        <form
          aria-required
          onSubmit={passwordChangeHandler}
          className="flex flex-col items-center"
        >
          <div className=" w-[100%] 800px:w-[50%] mt-5">
            <label className="block pb-2">기존 비밀번호 입력</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={oldPassword}
              onChange={(e) => setOldPassword(e.target.value)}
            />
          </div>
          <div className=" w-[100%] 800px:w-[50%] mt-2">
            <label className="block pb-2">새로운 비밀번호 입력</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={newPassword}
              onChange={(e) => setNewPassword(e.target.value)}
            />
          </div>
          <div className=" w-[100%] 800px:w-[50%] mt-2">
            <label className="block pb-2">새로운 비밀번호 확인</label>
            <input
              type="password"
              className={`${styles.input} !w-[95%] mb-4 800px:mb-0`}
              required
              value={confirmPassword}
              onChange={(e) => setConfirmPassword(e.target.value)}
            />
            <input
              className={`w-[95%] h-[40px] border border-[#3a24db] text-center text-[#3a24db] rounded-[3px] mt-8 cursor-pointer`}
              required
              value="변경"
              type="submit"
            />
          </div>
        </form>
      </div>
    </div>
  );
};

// 사용자 주소 관리 컴포넌트
const Address = () => {
  const [open, setOpen] = useState(false);
  const [country, setCountry] = useState("");
  const [city, setCity] = useState("");
  const [zipCode, setZipCode] = useState();
  const [address1, setAddress1] = useState("");
  const [address2, setAddress2] = useState("");
  const [addressType, setAddressType] = useState("");
  const { user } = useSelector((state) => state.user);
  const dispatch = useDispatch();

  // 주소 유형 데이터
  const addressTypeData = [
    {
      name: "기본",
    },
    {
      name: "집",
    },
    {
      name: "회사",
    },
  ];

  // 주소 제출 핸들러
  const handleSubmit = async (e) => {
    e.preventDefault();

    if (addressType === "" || country === "" || city === "") {
      toast.error("모든 필드를 입력하세요!");
    } else {
      console.log(country, city, address1, address2, zipCode, addressType);
      dispatch(
        updateUserAddress({
          country,
          city,
          address1,
          address2,
          zipCode,
          addressType,
        })
      );
      setOpen(false);
      setCountry("");
      setCity("");
      setAddress1("");
      setAddress2("");
      setZipCode(null);
      setAddressType("");
    }
  };
```

```jsx
  // 주소 삭제 핸들러
  const handleDelete = (item) => {
    const id = item._id;
    dispatch(deleteUserAddress(id));
  };
  return (
    <div className="w-full px-5">
      {open && (
        <div className="fixed w-full h-screen bg-[#0000004b] top-0 left-0 flex items-center justify-center ">
          <div className="w-[35%] h-[80vh] bg-white rounded shadow relative overflow-y-scroll">
            <div className="w-full flex justify-end p-3">
              <RxCross1
                size={30}
                className="cursor-pointer"
                onClick={() => setOpen(false)}
              />
            </div>
            <h1 className="text-center text-[25px] font-Poppins">
              새로운 주소 추가
            </h1>
            <div className="w-full">
              <form aria-required onSubmit={handleSubmit} className="w-full">
                <div className="w-full block p-4">
                  <div className="w-full pb-2">
                    <label className="block pb-2">국가</label>
                    <select
                      name=""
                      id=""
                      value={country}
                      onChange={(e) => setCountry(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        국가를 선택하세요
                      </option>
                      {Country &&
                        Country.getAllCountries().map((item) => (
                          <option
                            className="block pb-2"
                            key={item.isoCode}
                            value={item.isoCode}
                          >
                            {item.name}
                          </option>
                        ))}
                    </select>
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">도시 선택</label>
                    <select
                      name=""
                      id=""
                      value={city}
                      onChange={(e) => setCity(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        도시를 선택하세요
                      </option>
                      {State &&
                        State.getStatesOfCountry(country).map((item) => (
                          <option
                            className="block pb-2"
                            key={item.isoCode}
                            value={item.isoCode}
                          >
                            {item.name}
                          </option>
                        ))}
                    </select>
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">주소 1</label>
                    <input
                      type="address"
                      className={`${styles.input}`}
                      required
                      value={address1}
                      onChange={(e) => setAddress1(e.target.value)}
                    />
                  </div>
                  <div className="w-full pb-2">
                    <label className="block pb-2">주소 2</label>
                    <input
                      type="address"
                      className={`${styles.input}`}
                      required
                      value={address2}
                      onChange={(e) => setAddress2(e.target.value)}
                    />
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">우편 번호</label>
                    <input
                      type="number"
                      className={`${styles.input}`}
                      required
                      value={zipCode}
                      onChange={(e) => setZipCode(e.target.value)}
                    />
                  </div>

                  <div className="w-full pb-2">
                    <label className="block pb-2">주소 유형</label>
                    <select
                      name=""
                      id=""
                      value={addressType}
                      onChange={(e) => setAddressType(e.target.value)}
                      className="w-[95%] border h-[40px] rounded-[5px]"
                    >
                      <option value="" className="block border pb-2">
                        주소 유형을 선택하세요
                      </option>
                      {addressTypeData.map((item, index) => (
                        <option
                          className="block pb-2"
                          key={index}
                          value={item.name}
                        >
                          {item.name}
                        </option>
                      ))}
                    </select>
                  </div>

                  <input
                    className={`w-[95%] h-[40px] border border-[#3a24db] text-center text-[#3a24db] rounded-[3px] mt-8 cursor-pointer`}
                    required
                    value="등록"
                    type="submit"
                  />
                </div>
              </form>
            </div>
          </div>
        </div>
      )}
      <h1 className="block text-[25px] text-center font-[600] text-[#000000ba] pb-2">
        주소 관리
      </h1>
      <div className="w-full">
        <div className="flex justify-end">
          <button
            className="w-[140px] h-[35px] bg-[#3a24db] rounded text-white text-[12px] mt-4 mr-5 mb-3"
            onClick={() => setOpen(true)}
          >
            새로운 주소 추가
          </button>
        </div>
        {user &&
          user.addresses.map((item, index) => (
            <div key={index} className="flex items-center justify-between mt-2">
              <div className="flex-1">
                <div className="font-semibold text-[#000000ba]">
                  {item.country}, {item.city}
                </div>
                <div>{item.address1}, {item.address2}</div>
                <div>우편 번호: {item.zipCode}</div>
                <div className="text-[#3a24db]">주소 유형: {item.addressType}</div>
              </div>
              <div className="flex items-center">
                <AiOutlineDelete
                  size={20}
                  className="text-[#ff6161] cursor-pointer"
                  onClick={() => handleDelete(item)}
                />
              </div>
            </div>
          ))}
      </div>
    </div>
  );
};

export default ProfileContent;
```

```jsx

어떻게 사이드에 항목을 클릭해서 본문을 출력하는지 원리를 간단하게 설명하자면

사이드바의 항목을 클릭하면 상위 컴포넌트로 active 값을 보내줘 Page.jsx 에서 active 값을 profile.jsx 컴포넌트로 보내줘

원하는 본문을 출력한다

--------------------------------------------------------------------------------------------------------------

사용자 정보 업데이트 (handleSubmit 함수):

폼 제출 시 updateUserInformation 액션을 호출하여 사용자 정보를 업데이트한다.

서버로 전송할 데이터로 사용되는 name, email, phoneNumber, password 등을 출력한다.

  // 사용자 정보 업데이트 핸들러
  const handleSubmit = (e) => {
    e.preventDefault();
    console.log(name, email, phoneNumber, password);
    dispatch(updateUserInformation({ name, email, phoneNumber, password }));
  };

action 에서 async ( {name, email, phoneNumber, password} ) => {  이렇게 객체로 받고 여기서는 {} 객체에 넣어서 보낸다

Redux 액션은 주로 객체 형태로 구성된다. Redux에서는 액션을 객체로 정의하여 상태 변화를 일으킨다. 따라서 Redux의 규칙을 따르려면 액션 생성자에도 객체를 사용하는 것이 일반적이다.

--------------------------------------------------------------------------------------------------------------

프로필 사진 업데이트 (handleImage 함수):

파일 업로드를 위해 FormData를 사용하여 이미지를 서버에 전송한다.

update-avatar 엔드포인트로 이미지를 업데이트하고, 성공 시 loadUser 액션을 호출하여 사용자 정보를 다시 불러온다.

react-toastify를 사용하여 업데이트 성공 또는 실패 메시지를 사용자에게 알린다.

  const handleImage = async (e) => {
    const file = e.target.files[0];
    setAvatar(file);

    const formData = new FormData();

    formData.append("image", e.target.files[0]);

    await axios
      .put(`http://localhost:5000/api/v2/user/update-avatar`, formData, {
        headers: {
          "Content-Type": "multipart/form-data",
        },
        withCredentials: true,
      })
      .then((response) => {
        dispatch(loadUser());
        toast.success("avatar updated successfully!");
      })
      .catch((error) => {
        toast.error(error);
      });
  };

e.target.files[0]를 통해 사용자가 선택한 파일을 가져오고, 이를 file 상태에 저장한다.

FormData 객체를 생성하고, append 메소드를 사용하여 파일을 FormData에 추가한다. 이때 키 값은 "image"이다.

--------------------------------------------------------------------------------------------------------------

<img
  src={`http://localhost:5000/${user?.avatar}`}
  className="w-[150px] h-[150px] rounded-full object-cover border-[3px] border-[#3ad132]"
  alt=""
/>

현재 사용자의 프로필 이미지를 서버에서 가져와서 표시한다.

user?.avatar를 통해 사용자 객체가 존재하고 그 안에 avatar 속성이 있다면 해당 이미지의 URL을 생성한다.

undefined 또는 null일 가능성이 있는 속성에 접근할 때 발생할 수 있는 오류를 방지하기 위해 사용

user 객체가 존재하면 user.avatar를 반환

만약 user 객체가 null 또는 undefined이면 undefined를 반환하며, 여기에 옵션 체이닝이 사용되어 다음에 연결된 속성이나 메서드를 평가하지 않음

<div className="w-[30px] h-[30px] bg-[#E3E9EE] rounded-full flex items-center justify-center cursor-pointer absolute bottom-[5px] right-[5px]">
  <input
    type="file"
    id="image"
    className="hidden"
    onChange={handleImage}
  />
</div>

이미지를 업데이트할 수 있는 버튼 영역이다

input 엘리먼트는 숨겨져 있으며, 파일을 선택하면 onChange 이벤트가 발생하도록 설정되어 있다

사용자가 파일을 선택하면 handleImage 함수가 호출되어 이미지 업로드 프로세스를 처리한다
```

## user Redux ( 추가 업데이트 관련 ProfileContent 에서 사용할 내용 )

### userAction.js

```jsx
import { createAsyncThunk } from '@reduxjs/toolkit'
import axios from 'axios'

// 에러 클리어 액션
export const clearErrors = createAsyncThunk('user/clearErrors', async () => {
  return null
})

// 사용자 정보 불러오기 액션
export const loadUser = createAsyncThunk('user/loadUser', async () => {
  try {
    // 서버에 사용자 정보 요청
    const { data } = await axios.get(`http://localhost:5000/api/v2/user/getuser`, {
      withCredentials: true,
    })
    // 불러온 사용자 정보 반환
    return data.user
  } catch (error) {
    // 에러 발생 시 해당 메시지를 에러로 반환
    throw error.response.data.message
  }
})

// 판매자 정보 불러오기 액션
export const loadSeller = createAsyncThunk('user/loadSeller', async () => {
  try {
    // 서버에 판매자 정보 요청
    const { data } = await axios.get(`http://localhost:5000/api/v2/shop/getSeller`, {
      withCredentials: true,
    })
    // 불러온 판매자 정보 반환
    return data.seller
  } catch (error) {
    // 에러 발생 시 해당 메시지를 에러로 반환
    throw error.response.data.message
  }
})

// 사용자 정보 업데이트 액션
export const updateUserInformation = createAsyncThunk(
  'user/updateUserInformation',
  async ({ name, email, phoneNumber, password }) => {
    // 이 부분에 {} 에 넣어서 보내는거 주의
    try {
      // 서버에 사용자 정보 업데이트 요청
      const { data } = await axios.put(
        `http://localhost:5000/api/v2/user/update-user-info`,
        { name, email, phoneNumber, password },
        {
          withCredentials: true,
        }
      )
      // 업데이트된 사용자 정보 반환
      return data.user
    } catch (error) {
      // 에러 발생 시 해당 메시지를 에러로 반환
      throw error.response.data.message
    }
  }
)

// 사용자 주소 업데이트 액션
export const updateUserAddress = createAsyncThunk(
  'user/updateUserAddress',
  async ({ country, city, address1, address2, zipCode, addressType }) => {
    try {
      // 서버에 사용자 주소 업데이트 요청
      const { data } = await axios.put(
        `http://localhost:5000/api/v2/user/update-user-addresses`,
        { country, city, address1, address2, zipCode, addressType },
        {
          withCredentials: true,
        }
      )
      // 업데이트 성공 시 메시지와 사용자 정보 반환
      return {
        successMessage: '주소 등록이 완료되었습니다!', // 두개의 값을 리턴해서 user 정보 업데이트와 성공 메세지 전달
        user: data.user,
      }
    } catch (error) {
      // 업데이트 실패 시 해당 메시지를 에러로 반환
      throw error.response.data.message
    }
  }
)

// 사용자 주소 삭제 액션
export const deleteUserAddress = createAsyncThunk('user/deleteUserAddress', async (id) => {
  try {
    // 서버에 사용자 주소 삭제 요청
    const { data } = await axios.delete(
      `http://localhost:5000/api/v2/user/delete-user-address/${id}`,
      {
        withCredentials: true,
      }
    )
    // 삭제 성공 시 메시지와 사용자 정보 반환
    return {
      successMessage: '주소 삭제가 완료되었습니다!',
      user: data.user,
    }
  } catch (error) {
    // 삭제 실패 시 해당 메시지를 에러로 반환
    throw error.response.data.message
  }
})
```

```jsx
updateUserInformation 액션:

사용자의 이름, 이메일, 전화번호, 비밀번호를 받아와서 서버에 업데이트 요청한다.

성공 시 서버에서 반환한 업데이트된 사용자 정보를 반환한다.

실패 시 서버에서 반환한 에러 메시지를 에러로 반환한다.

updateUserAddress 액션:

사용자의 국가, 도시, 주소 등 주소 정보를 받아와서 서버에 업데이트 요청한다.

성공 시 메시지와 서버에서 반환한 업데이트된 사용자 정보를 반환한다.

실패 시 서버에서 반환한 에러 메시지를 에러로 반환한다.

deleteUserAddress 액션:

사용자의 특정 주소 ID를 받아와서 해당 주소를 서버에서 삭제 요청한다.

성공 시 메시지와 서버에서 반환한 업데이트된 사용자 정보를 반환한다.

실패 시 서버에서 반환한 에러 메시지를 에러로 반환한다.
```

### userReducer.js

```jsx
import { createSlice } from '@reduxjs/toolkit'
import {
  loadUser,
  clearErrors,
  updateUserInformation,
  updateUserAddress,
  deleteUserAddress,
} from './userAction'

const userSlice = createSlice({
  name: 'user',
  initialState: {
    isAuthenticated: false,
    loading: false,
    user: null,
    error: null,
  },
  extraReducers: (builder) => {
    builder
      .addCase(loadUser.pending, (state) => {
        state.loading = true
      })
      .addCase(loadUser.fulfilled, (state, action) => {
        state.isAuthenticated = true
        state.loading = false
        state.user = action.payload
      })
      .addCase(loadUser.rejected, (state, action) => {
        state.loading = false
        state.error = action.error.message
        state.isAuthenticated = false
      })
      .addCase(clearErrors.fulfilled, (state) => {
        state.error = null
      })

      //유저 정보 업데이트
      .addCase(updateUserInformation.pending, (state) => {
        state.loading = true
      })
      .addCase(updateUserInformation.fulfilled, (state, action) => {
        state.loading = false
        state.user = action.payload
      })
      .addCase(updateUserInformation.rejected, (state, action) => {
        state.loading = false
        state.error = action.error.message
      })

      //유저 주소 업데이트
      .addCase(updateUserAddress.pending, (state) => {
        state.addressLoading = true
      })
      .addCase(updateUserAddress.fulfilled, (state, action) => {
        state.addressLoading = false
        state.successMessage = action.payload.successMessage
        state.user = action.payload.user
      })
      .addCase(updateUserAddress.rejected, (state, action) => {
        state.addressLoading = false
        state.error = action.payload
      })

      // 유저 주소 삭제
      .addCase(deleteUserAddress.pending, (state) => {
        state.addressLoading = true
      })
      .addCase(deleteUserAddress.fulfilled, (state, action) => {
        state.addressLoading = false
        state.successMessage = action.payload.successMessage
        state.user = action.payload.user
      })
      .addCase(deleteUserAddress.rejected, (state, action) => {
        state.addressLoading = false
        state.error = action.payload
      })
  },
})

export default userSlice.reducer
```

## user 컨트롤러 추가 ( 리덕스에서 추가한 주소 추가 삭제 등등 추가해주기 )

### user.js 컨트롤러

```jsx
const express = require('express')
const path = require('path')
const router = express.Router()
const { upload } = require('../multer')
const User = require('../model/user')
const fs = require('fs')
const ErrorHandler = require('../util/ErrorHandler')
const jwt = require('jsonwebtoken')
const sendMail = require('../util/sendMail')
const sendToken = require('../util/jwtToken')
const catchAsyncErrors = require('../middleware/catchAsyncErrors')
const { isAuthenticated } = require('../middleware/auth')

// 새로운 사용자 생성
router.post('/create-user', upload.single('file'), async (req, res, next) => {
  try {
    const { name, email, password } = req.body
    const userEmail = await User.findOne({ email })

    // 이미 존재하는 이메일인 경우 에러 처리
    if (userEmail) {
      const filename = req.file.filename
      const filePath = `uploads/${filename}`
      fs.unlink(filePath, (err) => {
        if (err) {
          console.log(err)
          res.status(500).json({ message: 'Error deleting file' })
        }
      })
      return next(new ErrorHandler('User already exists', 400))
    }

    // 새로운 사용자 정보 및 파일 업로드 경로 생성
    const filename = req.file.filename
    const fileUrl = path.join(filename)

    const user = {
      name: name,
      email: email,
      password: password,
      avatar: fileUrl,
    }

    // 계정 활성화를 위한 토큰 생성 및 이메일 발송
    const activationToken = createActivationToken(user)
    const activationUrl = `http://localhost:3000/activation/${activationToken}`

    try {
      await sendMail({
        email: user.email,
        subject: '계정 활성화',
        message: `안녕하세요 ${user.name}님, 계정을 활성화하려면 다음 링크를 클릭해주세요: ${activationUrl}`,
      })

      res.status(201).json({
        success: true,
        message: `이메일(${user.email})을 확인하여 계정을 활성화해주세요!`,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  } catch (error) {
    return next(new ErrorHandler(error.message, 400))
  }
})

// 계정 활성화 토큰 생성
const createActivationToken = (user) => {
  return jwt.sign(user, process.env.ACTIVATION_SECRET, {
    expiresIn: '5m',
  })
}

// 사용자 계정 활성화
router.post(
  '/activation',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const { activation_token } = req.body
      const newUser = jwt.verify(activation_token, process.env.ACTIVATION_SECRET)

      // 만료된 토큰일 경우 에러 처리
      if (!newUser) {
        return next(new ErrorHandler('만료된 토큰입니다!', 400))
      }
      const { name, email, password, avatar } = newUser

      let user = await User.findOne({ email })

      // 이미 존재하는 유저일 경우 에러 처리
      if (user) {
        return next(new ErrorHandler('유저가 이미 존재합니다!', 400))
      }
      // 새로운 사용자 생성 및 토큰 발급
      user = await User.create({
        name,
        email,
        avatar,
        password,
      })

      sendToken(user, 201, res)
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 로그인
router.post(
  '/login-user',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const { email, password } = req.body

      if (!email || !password) {
        return next(new ErrorHandler('모든 필드를 입력하세요!', 400))
      }

      const user = await User.findOne({ email }).select('+password')

      // 사용자가 존재하지 않을 경우 에러 처리
      if (!user) {
        return next(new ErrorHandler('사용자가 존재하지 않습니다!', 400))
      }

      const isPasswordValid = await user.comparePassword(password)

      // 비밀번호가 일치하지 않을 경우 에러 처리
      if (!isPasswordValid) {
        return next(new ErrorHandler('올바른 정보를 입력해 주세요.', 400))
      }

      // 로그인 성공 시 토큰 발급
      sendToken(user, 201, res)
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 로그아웃
router.get(
  '/logout',
  catchAsyncErrors(async (req, res, next) => {
    try {
      res.cookie('token', null, {
        expires: new Date(Date.now()),
        httpOnly: true,
      })
      res.status(201).json({
        success: true,
        message: '로그아웃 성공!',
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 로그인된 사용자 정보 불러오기
router.get(
  '/getuser',
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const user = await User.findById(req.user.id)

      // 사용자가 존재하지 않을 경우 에러 처리
      if (!user) {
        return next(new ErrorHandler('유저가 존재하지 않습니다!', 400))
      }

      // 사용자 정보 반환
      res.status(200).json({
        success: true,
        user,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자 정보 업데이트
router.put(
  '/update-user-info',
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const { email, password, phoneNumber, name } = req.body
      const user = await User.findOne({ email }).select('+password')

      // 사용자가 존재하지 않을 경우 에러 처리
      if (!user) {
        return next(new ErrorHandler('User not found', 400))
      }

      const isPasswordValid = await user.comparePassword(password)

      // 비밀번호가 일치하지 않을 경우 에러 처리
      if (!isPasswordValid) {
        return next(new ErrorHandler('Please provide the correct information', 400))
      }

      // 사용자 정보 업데이트 및 저장
      user.name = name
      user.email = email
      user.phoneNumber = phoneNumber
      await user.save()

      // 업데이트된 사용자 정보 반환
      res.status(201).json({
        success: true,
        user,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자 아바타 업데이트
router.put(
  '/update-avatar',
  isAuthenticated,
  upload.single('image'),
  catchAsyncErrors(async (req, res, next) => {
    try {
      const existsUser = await User.findById(req.user.id)
      const existAvatarPath = `uploads/${existsUser.avatar}`

      // 기존 아바타 파일 삭제
      fs.unlinkSync(existAvatarPath)

      // 새로운 아바타 파일 경로 생성
      const fileUrl = path.join(req.file.filename)

      // 사용자 업데이트 및 업데이트된 사용자 정보 반환
      const user = await User.findByIdAndUpdate(req.user.id, {
        avatar: fileUrl,
      })

      res.status(200).json({
        success: true,
        user,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자 주소 업데이트
router.put(
  '/update-user-addresses',
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const user = await User.findById(req.user.id)

      // 동일한 타입의 주소가 이미 존재할 경우 에러 처리
      const sameTypeAddress = user.addresses.find(
        (address) => address.addressType === req.body.addressType
      )
      if (sameTypeAddress) {
        return next(new ErrorHandler(`${req.body.addressType} address already exists`))
      }

      // 주소 ID에 해당하는 주소가 이미 존재할 경우 업데이트, 그렇지 않을 경우 신규 추가
      const existsAddress = user.addresses.find((address) => address._id === req.body._id)
      if (existsAddress) {
        Object.assign(existsAddress, req.body) // 객체를 병합하거나 복사할 때 사용되는 메서드 ( 첫번째 인자가 타겟 거기에 뒤에 인자를 더한다 )
      } else {
        user.addresses.push(req.body)
      }

      // 사용자 정보 저장 및 업데이트된 사용자 정보 반환
      await user.save()
      res.status(200).json({
        success: true,
        user,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자 주소 삭제
router.delete(
  '/delete-user-address/:id',
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const userId = req.user._id
      const addressId = req.params.id

      // 주소 삭제 요청
      await User.updateOne(
        {
          _id: userId,
        },
        { $pull: { addresses: { _id: addressId } } }
      )

      // 삭제된 주소를 제외한 업데이트된 사용자 정보 반환
      const user = await User.findById(userId)
      res.status(200).json({ success: true, user })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 사용자 비밀번호 업데이트
router.put(
  '/update-user-password',
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const user = await User.findById(req.user.id).select('+password')

      // 기존 비밀번호 확인
      const isPasswordMatched = await user.comparePassword(req.body.oldPassword)

      // 기존 비밀번호가 일치하지 않을 경우 에러 처리
      if (!isPasswordMatched) {
        return next(new ErrorHandler('기존 비밀번호가 정확하지 않습니다!', 400))
      }

      // 새로운 비밀번호 확인
      if (req.body.newPassword !== req.body.confirmPassword) {
        return next(new ErrorHandler('비밀번호가 서로 일치하지 않습니다!', 400))
      }

      // 새로운 비밀번호로 업데이트 및 성공 메시지 반환
      user.password = req.body.newPassword
      await user.save()

      res.status(200).json({
        success: true,
        message: '비밀번호가 성공적으로 업데이트되었습니다!',
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

// 특정 사용자 정보 불러오기
router.get(
  '/user-info/:id',
  catchAsyncErrors(async (req, res, next) => {
    try {
      const user = await User.findById(req.params.id)

      // 사용자 정보 반환
      res.status(201).json({
        success: true,
        user,
      })
    } catch (error) {
      return next(new ErrorHandler(error.message, 500))
    }
  })
)

module.exports = router
```

```jsx
// 사용자 아바타 업데이트
router.put(
  "/update-avatar",
  isAuthenticated,
  upload.single("image"),
  catchAsyncErrors(async (req, res, next) => {
    try {
      const existsUser = await User.findById(req.user.id);
      const existAvatarPath = `uploads/${existsUser.avatar}`;

      // 기존 아바타 파일 삭제
      fs.unlinkSync(existAvatarPath);

      // 새로운 아바타 파일 경로 생성
      const fileUrl = path.join(req.file.filename);

      // 사용자 업데이트 및 업데이트된 사용자 정보 반환
      const user = await User.findByIdAndUpdate(req.user.id, {
        avatar: fileUrl,
      });

      res.status(200).json({
        success: true,
        user,
      });
    } catch (error) {
      return next(new ErrorHandler(error.message, 500));
    }
  })
);

const fileUrl = path.join(req.file.filename);은 업로드된 파일의 이름을 기반으로 한 상대적인 파일 경로를 생성한다.

이렇게 생성된 파일 경로는 사용자의 아바타를 나타내는 파일의 위치를 나타낸다.

이 파일 경로는 사용자의 정보를 업데이트할 때 데이터베이스에 저장되어, 프로필 사진의 경로를 관리하는 데 사용된다

( example.jpg라는 파일이 업로드되었다면, req.file.filename은 예를 들어 example.jpg가 된다. 그리고 const fileUrl = path.join(req.file.filename);

	코드를 통해 fileUrl에는 현재 서버에서의 전체 경로가 저장된다. 결과적으로 fileUrl에는 예를 들어 /uploads/example.jpg와 같은 경로가 저장된다

--------------------------------------------------------------------

// 사용자 주소 삭제
router.delete(
  "/delete-user-address/:id",
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const userId = req.user._id;
      const addressId = req.params.id;

      // 주소 삭제 요청
      await User.updateOne(
        {
          _id: userId,
        },
        { $pull: { addresses: { _id: addressId } } }
      );

      // 삭제된 주소를 제외한 업데이트된 사용자 정보 반환
      const user = await User.findById(userId);
      res.status(200).json({ success: true, user });
    } catch (error) {
      return next(new ErrorHandler(error.message, 500));
    }
  })
);

$pull 연산자를 사용하여 사용자의 addresses 배열에서 _id가 주어진 addressId와 일치하는 주소를 삭제 ( pull 배열 필드 내에 특정한 값을 제거할 때 사용 )

--------------------------------------------------------------------

// 사용자 주소 업데이트를 처리하는 라우터
router.put(
  "/update-user-addresses", // 엔드포인트 경로
  isAuthenticated, // 사용자 인증 미들웨어
  catchAsyncErrors(async (req, res, next) => { // 비동기 함수를 catch하는 미들웨어
    try {
      // 요청된 사용자의 ID를 사용하여 사용자 정보를 데이터베이스에서 찾음
      const user = await User.findById(req.user.id);
      console.log(user); // 사용자 정보를 콘솔에 출력

      // 동일한 유형의 주소가 이미 있는지 확인
      const sameTypeAddress = user.addresses.find(
        (address) => address.addressType === req.body.addressType
      );
      if (sameTypeAddress) {
        // 동일한 유형의 주소가 이미 있으면 오류를 발생시킴
        return next(
          new ErrorHandler(`${req.body.addressType} 주소가 이미 존재합니다!`)
        );
      }

      // 주어진 ID를 가진 주소가 이미 존재하는지 확인
      const existsAddress = user.addresses.find(
        (address) => address._id === req.body._id
      );

      if (existsAddress) {
        // 존재하는 경우, 주어진 주소 객체의 속성을 요청된 데이터로 업데이트
        Object.assign(existsAddress, req.body);
      } else {
        // 존재하지 않는 경우, 주소를 배열에 추가
        user.addresses.push(req.body);
      }

      // 사용자 정보를 저장
      await user.save();

      // 성공 응답 반환
      res.status(200).json({
        success: true,
        user,
      });
    } catch (error) {
      // 오류가 발생한 경우 오류 처리 미들웨어로 전달
      return next(new ErrorHandler(error.message, 500));
    }
  })
);

주소 객체에는 여러 속성이 있을 수 있으며, 모든 속성을 일일이 업데이트하는 대신 Object.assign() 메서드를 사용하여 한 번에 여러 속성을 업데이트할 수 있다.

예를 들어, Object.assign(target, source1, source2)는 source1과 source2의 속성들을 target 객체로 복사 (인자가 두개면 앞이 타겟 뒤가 새로운 값)

-------------------------------------------------------------

// 사용자 비밀번호 업데이트
router.put(
  "/update-user-password",
  isAuthenticated,
  catchAsyncErrors(async (req, res, next) => {
    try {
      const user = await User.findById(req.user.id).select("+password");

      // 기존 비밀번호 확인
      const isPasswordMatched = await user.comparePassword(
        req.body.oldPassword
      );

      // 기존 비밀번호가 일치하지 않을 경우 에러 처리
      if (!isPasswordMatched) {
        return next(new ErrorHandler("기존 비밀번호가 정확하지 않습니다!", 400));
      }

      // 새로운 비밀번호 확인
      if (req.body.newPassword !== req.body.confirmPassword) {
        return next(
          new ErrorHandler("비밀번호가 서로 일치하지 않습니다!", 400)
        );
      }

      // 새로운 비밀번호로 업데이트 및 성공 메시지 반환
      user.password = req.body.newPassword;
      await user.save();

      res.status(200).json({
        success: true,
        message: "비밀번호가 성공적으로 업데이트되었습니다!",
      });
    } catch (error) {
      return next(new ErrorHandler(error.message, 500));
    }
  })
);

select를 사용하면 특정 필드를 선택하여 가져오거나, 반대로 특정 필드를 제외하고 가져올 수 있다

기본적으로 Mongoose는 스키마 정의에서 select: false로 설정된 필드를 쿼리 결과에서 제외하게 된다.

따라서 명시적으로 select를 사용하여 비밀번호와 같은 필요한 정보를 가져올 수 있다
```

## 유저 모델 업데이트

```jsx
주소등 기본 정보를 추가해줬다
```

```jsx
const mongoose = require('mongoose')
const bcrypt = require('bcryptjs')
const jwt = require('jsonwebtoken')

const userSchema = new mongoose.Schema({
  name: {
    type: String,
    required: [true, '이름을 입력해주세요'],
  },
  email: {
    type: String,
    required: [true, '이메일을 입력해주세요'],
  },
  password: {
    type: String,
    required: [true, '비밀번호를 입력해주세요'],
    minLength: [4, '비밀번호는 4자리 이상 입력하세요'],
    select: false,
  },
  phoneNumber: {
    type: Number,
  },
  addresses: [
    {
      country: {
        type: String,
      },
      city: {
        type: String,
      },
      address1: {
        type: String,
      },
      address2: {
        type: String,
      },
      zipCode: {
        type: Number,
      },
      addressType: {
        type: String,
      },
    },
  ],
  role: {
    type: String,
    default: 'user',
  },
  avatar: {
    type: String,
    required: true,
  },
  createdAt: {
    type: Date,
    default: Date.now(),
  },
  resetPasswordToken: String,
  resetPasswordTime: Date,
})

//  Hash password
userSchema.pre('save', async function (next) {
  if (!this.isModified('password')) {
    next()
  }

  this.password = await bcrypt.hash(this.password, 10)
})

// jwt token
userSchema.methods.getJwtToken = function () {
  return jwt.sign({ id: this._id }, process.env.JWT_SECRET_KEY, {
    expiresIn: process.env.JWT_EXPIRES,
  })
}

// compare password
userSchema.methods.comparePassword = async function (enteredPassword) {
  return await bcrypt.compare(enteredPassword, this.password)
}

module.exports = mongoose.model('User', userSchema)
```

# 결제 페이지 ( Checkout.jsx ) 작성

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381462/notion-blog/_/_/EcommerceProject/EcommerceProject_-4-2/Untitled_1.png)

```jsx
checkout 부분을 수정하여 Choose From saved address 을 클릭했을떄 저장된 정보가 인풋창에 입력되도록 한다

+ 총 가격과 배달료 등을 오른쪽에 출력되도록 수정해준다

이후 쿠폰코드 입력을 위해서

쿠폰코드 컨트롤러에 새로운 라우터 ( 쿠폰코드 이름으로 값을 가져오는 ) 를 추가해준다
```

```jsx
import React, { useState, useEffect } from 'react'
import styles from '../../styles/styles'
import { Country, State } from 'country-state-city'
import { useNavigate } from 'react-router-dom'
import { useSelector } from 'react-redux'
import { toast } from 'react-toastify'
import axios from 'axios'

// Checkout 컴포넌트 정의
const Checkout = () => {
  // Redux에서 유저 및 카트 정보를 가져옴
  const { user } = useSelector((state) => state.user)
  const { cart } = useSelector((state) => state.cart)
  // 주문 정보 관련 상태 정의
  const [country, setCountry] = useState('')
  const [city, setCity] = useState('')
  const [userInfo, setUserInfo] = useState(false)
  const [address1, setAddress1] = useState('')
  const [address2, setAddress2] = useState('')
  const [zipCode, setZipCode] = useState(null)
  const [couponCode, setCouponCode] = useState('')
  const [couponCodeData, setCouponCodeData] = useState(null)
  const [discountPrice, setDiscountPrice] = useState(null)
  const navigate = useNavigate()

  // 페이지 로드시 화면 상단으로 스크롤
  useEffect(() => {
    window.scrollTo(0, 0)
  }, [])

  // 결제 버튼 클릭 시 실행되는 함수
  const paymentSubmit = () => {
    // 필수 배송 정보 누락 시 에러 메시지 토스트 출력
    if (address1 === '' || address2 === '' || zipCode === null || country === '' || city === '') {
      toast.error('배송지 주소를 선택해주세요!')
    } else {
      // 배송 주소 객체 생성
      const shippingAddress = {
        address1,
        address2,
        zipCode,
        country,
        city,
      }

      // 주문 정보 객체 생성
      const orderData = {
        cart,
        totalPrice,
        subTotalPrice,
        shipping,
        discountPrice,
        shippingAddress,
        user,
      }

      // 업데이트된 주문 정보를 로컬 스토리지에 저장
      localStorage.setItem('latestOrder', JSON.stringify(orderData))
      // 결제 페이지로 이동
      navigate('/payment')
    }
  }

  // 장바구니 총 가격 계산
  const subTotalPrice = cart.reduce((acc, item) => acc + item.qty * item.discountPrice, 0)

  // 배송비 계산 (총 가격의 10%)
  const shipping = subTotalPrice * 0.1

  // 쿠폰 적용 버튼 클릭 시 실행되는 함수
  const handleSubmit = async (e) => {
    e.preventDefault()
    const name = couponCode

    // 쿠폰 정보를 백엔드에서 가져옴
    await axios.get(`http://localhost:5000/api/v2/coupon/get-coupon-value/${name}`).then((res) => {
      const shopId = res.data.couponCode?.shopId
      const couponCodeValue = res.data.couponCode?.value
      // 유효한 쿠폰인지 확인
      if (res.data.couponCode !== null) {
        const isCouponValid = cart && cart.filter((item) => item.shopId === shopId)

        // 쿠폰이 유효하지 않은 상점의 상품이면 에러 메시지 출력
        if (isCouponValid.length === 0) {
          toast.error('유요하지 않은 상점의 쿠폰코드 입니다.')
          setCouponCode('')
        } else {
          // 쿠폰 할인 가격 계산 및 상태 업데이트
          const eligiblePrice = isCouponValid.reduce(
            (acc, item) => acc + item.qty * item.discountPrice,
            0
          )
          const discountPrice = (eligiblePrice * couponCodeValue) / 100
          setDiscountPrice(discountPrice)
          setCouponCodeData(res.data.couponCode)
          setCouponCode('')
        }
      }
      // 존재하지 않는 쿠폰코드인 경우 에러 메시지 출력
      if (res.data.couponCode === null) {
        toast.error('존재하지 않는 쿠폰코드입니다!')
        setCouponCode('')
      }
    })
  }

  // 쿠폰 할인율 표시 변수
  const discountPercentenge = couponCodeData ? discountPrice : ''

  // 총 주문 가격 계산
  const totalPrice = couponCodeData
    ? (subTotalPrice + shipping - discountPercentenge).toFixed(2)
    : (subTotalPrice + shipping).toFixed(2) // toFixed(2)는 소수점 이하를 최대 2자리까지로 고정시킨다는 의미 즉, 최종적인 주문 총액에는 소수점 이하 두 자리까지만 표시되도록 처리

  // JSX 렌더링
  return (
    <div className="flex w-full flex-col items-center py-8">
      <div className="1000px:w-[70%] 800px:flex block w-[90%]">
        <div className="800px:w-[65%] w-full">
          {/* 배송 정보 컴포넌트 렌더링 */}
          <ShippingInfo
            user={user}
            country={country}
            setCountry={setCountry}
            city={city}
            setCity={setCity}
            userInfo={userInfo}
            setUserInfo={setUserInfo}
            address1={address1}
            setAddress1={setAddress1}
            address2={address2}
            setAddress2={setAddress2}
            zipCode={zipCode}
            setZipCode={setZipCode}
          />
        </div>
        <div className="800px:w-[35%] 800px:mt-0 mt-8 w-full">
          {/* 카트 데이터 및 쿠폰 입력 컴포넌트 렌더링 */}
          <CartData
            handleSubmit={handleSubmit}
            totalPrice={totalPrice}
            shipping={shipping}
            subTotalPrice={subTotalPrice}
            couponCode={couponCode}
            setCouponCode={setCouponCode}
            discountPercentenge={discountPercentenge}
          />
        </div>
      </div>
      {/* 결제 버튼 렌더링 */}
      <div className={`${styles.button} 800px:w-[280px] mt-10 w-[150px]`} onClick={paymentSubmit}>
        <h5 className="text-white">결제하기</h5>
      </div>
    </div>
  )
}
```

```jsx
// ShippingInfo 컴포넌트 정의
const ShippingInfo = ({
  user,
  country,
  setCountry,
  city,
  setCity,
  userInfo,
  setUserInfo,
  address1,
  setAddress1,
  address2,
  setAddress2,
  zipCode,
  setZipCode,
}) => {
  return (
    <div className="800px:w-[95%] w-full rounded-md bg-white p-5 pb-8">
      {/* 배송지 정보 제목 */}
      <h5 className="text-[18px] font-[500]">배송지</h5>
      <br />
      <form>
        {/* 이름 및 이메일 입력란 */}
        <div className="flex w-full pb-3">
          <div className="w-[50%]">
            <label className="block pb-2">이름</label>
            <input
              type="text"
              required
              value={user && user.name}
              className={`${styles.input} !w-[95%]`}
            />
          </div>
          <div className="w-[50%]">
            <label className="block pb-2">이메일 주소</label>
            <input type="email" required className={`${styles.input}`} value={user && user.email} />
          </div>
        </div>

        {/* 전화번호 및 우편번호 입력란 */}
        <div className="flex w-full pb-3">
          <div className="w-[50%]">
            <label className="block pb-2">전화번호</label>
            <input
              type="number"
              required
              value={user && user.phoneNumber}
              className={`${styles.input} !w-[95%]`}
            />
          </div>
          <div className="w-[50%]">
            <label className="block pb-2">우편번호</label>
            <input
              type="number"
              required
              className={`${styles.input}`}
              value={zipCode}
              onChange={(e) => setZipCode(e.target.value)}
            />
          </div>
        </div>

        {/* 국가 및 도시 선택 드롭다운 */}
        <div className="flex w-full pb-3">
          <div className="w-[50%]">
            <label className="block pb-2">국가</label>
            <select
              className="h-[40px] w-[95%] rounded-[5px] border"
              value={country}
              onChange={(e) => setCountry(e.target.value)}
            >
              <option className="block pb-2" value="">
                국가를 선택하세요
              </option>
              {Country &&
                Country.getAllCountries().map((item) => (
                  <option key={item.isoCode} value={item.isoCode}>
                    {item.name}
                  </option>
                ))}
            </select>
          </div>
          <div className="w-[50%]">
            <label className="block pb-2">도시</label>
            <select
              className="h-[40px] w-[95%] rounded-[5px] border"
              value={city}
              onChange={(e) => setCity(e.target.value)}
            >
              <option className="block pb-2" value="">
                도시를 선택하세요
              </option>
              {State &&
                State.getStatesOfCountry(country).map((item) => (
                  <option key={item.isoCode} value={item.isoCode}>
                    {item.name}
                  </option>
                ))}
            </select>
          </div>
        </div>

        {/* 주소 및 상세주소 입력란 */}
        <div className="flex w-full pb-3">
          <div className="w-[50%]">
            <label className="block pb-2">주소</label>
            <input
              type="address"
              required
              value={address1}
              onChange={(e) => setAddress1(e.target.value)}
              className={`${styles.input} !w-[95%]`}
            />
          </div>
          <div className="w-[50%]">
            <label className="block pb-2">상세주소</label>
            <input
              type="address"
              value={address2}
              onChange={(e) => setAddress2(e.target.value)}
              required
              className={`${styles.input}`}
            />
          </div>
        </div>

        {/* 저장된 주소 선택 기능 */}
        <div></div>
      </form>

      {/* 저장된 주소에서 선택하기 버튼 */}
      <h5
        className="inline-block cursor-pointer text-[18px]"
        onClick={() => setUserInfo(!userInfo)}
      >
        저장된 주소에서 선택하기
      </h5>

      {/* 저장된 주소 목록 */}
      {userInfo && (
        <div>
          {user &&
            user.addresses.map((item, index) => (
              <div className="mt-1 flex w-full">
                <input
                  type="checkbox"
                  className="mr-3"
                  value={item.addressType}
                  onClick={() =>
                    setAddress1(item.address1) ||
                    setAddress2(item.address2) ||
                    setZipCode(item.zipCode) ||
                    setCountry(item.country) ||
                    setCity(item.city)
                  }
                />
                <h2>{item.addressType}</h2>
              </div>
            ))}
        </div>
      )}
    </div>
  )
}

// CartData 컴포넌트 정의
const CartData = ({
  handleSubmit,
  totalPrice,
  shipping,
  subTotalPrice,
  couponCode,
  setCouponCode,
  discountPercentenge,
}) => {
  return (
    <div className="w-full rounded-md bg-[#fff] p-5 pb-8">
      {/* 소계 정보 */}
      <div className="flex justify-between">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">소계:</h3>
        <h5 className="text-[18px] font-[600]">${subTotalPrice}</h5>
      </div>
      <br />

      {/* 배송비 정보 */}
      <div className="flex justify-between">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">배송비:</h3>
        <h5 className="text-[18px] font-[600]">${shipping}</h5>
      </div>
      <br />

      {/* 할인 정보 */}
      <div className="flex justify-between border-b pb-3">
        <h3 className="text-[16px] font-[400] text-[#000000a4]">할인:</h3>
        <h5 className="text-[18px] font-[600]">
          - {discountPercentenge ? '원' + discountPercentenge.toString() : null}
        </h5>
      </div>

      {/* 총 가격 정보 */}
      <h5 className="pt-3 text-end text-[18px] font-[600]">${totalPrice}</h5>
      <br />

      {/* 쿠폰 입력 및 적용 폼 */}
      <form onSubmit={handleSubmit}>
        <input
          type="text"
          className={`${styles.input} h-[40px] pl-2`}
          placeholder="쿠폰 코드"
          value={couponCode}
          onChange={(e) => setCouponCode(e.target.value)}
          required
        />
        <input
          className={`mt-8 h-[40px] w-full cursor-pointer rounded-[3px] border border-[#f63b60] text-center text-[#f63b60]`}
          required
          value="쿠폰 적용"
          type="submit"
        />
      </form>
    </div>
  )
}

export default Checkout
```

## 상품의 총 결제 금액 출력

```jsx

  // 장바구니 상품의 총 가격 계산
  const subTotalPrice = cart.reduce(
    (acc, item) => acc + item.qty * item.discountPrice,
    0
  );

cart.reduce: cart 배열에 대해 reduce 메서드를 호출한다. reduce는 배열의 각 요소에 대해 지정된 콜백 함수를 실행하고 그 결과를 누적하는 메서드이다.

(acc, item) => acc + item.qty * item.discountPrice: 이 부분은 reduce 메서드의 콜백 함수이다. 이 함수는 두 개의 매개변수를 받는다.

acc: accumulator(누적기)로서, 현재까지의 누적된 값이다.

item: 배열의 각 요소, 여기서는 장바구니에 있는 각 상품이다.

콜백 함수의 목적은 각 상품의 수량(item.qty)과 할인 가격(item.discountPrice)을 곱한 값을 현재까지의 누적된 값에 더하는 것이다. 이 값은 다음 반복에서 acc로 사용된다.

0: reduce 메서드에 전달된 두 번째 인수는 초기 누적값이다. 여기서는 0으로 설정되어 있으므로 첫 번째 상품부터 시작한다.

결과적으로 subTotalPrice에는 장바구니에 있는 모든 상품의 수량과 할인 가격을 곱한 총합이 저장되게 된다.
```

## 저장된 주소 불러오기

```jsx
Choose From saved address 를 클릭하면 저장된 주소들이 출력되고 그걸 클릭하면 입력 필드에 자동으로 저장된 주소가 입력된다
```

```jsx
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

이걸 사용하여 페이지로 이동했을 떄 바로 최 상단으로 올려 사용자 경험을 향상 시킨다

----------------------------------------------------------------------------

사용자가 저장한 주소를 체크하면 해당 주소의 정보로 배송 정보를 업데이트 기능

1. 링크 클릭 시 상태 변경:

링크에는 onClick 이벤트가 있어 클릭 시 setUserInfo(!userInfo)가 호출된다.

userInfo는 상태 변수로, 사용자가 저장된 주소를 사용할지 여부를 나타낸다.

따라서 링크를 클릭할 때마다 userInfo 상태가 토글되어 저장된 주소를 사용할지 여부가 변경된다.

<h5
  className="text-[18px] cursor-pointer inline-block"
  onClick={() => setUserInfo(!userInfo)}
>
  Choose From saved address
</h5>

2. 저장된 주소 목록 표시:

만약 userInfo가 true인 경우, 저장된 주소 목록이 화면에 표시된다.

저장된 주소 목록은 user 상태에서 addresses 배열을 통해 가져온다. (Redux)

이 목록은 map 함수를 사용하여 각 주소를 순회하며 화면에 표시된다.

{userInfo && (
  <div>
    {user &&
      user.addresses.map((item, index) => (
        <div className="w-full flex mt-1" key={index}>
          {/* 주소 선택을 위한 체크박스 */}
          <input
            type="checkbox"
            className="mr-3"
            value={item.addressType}
            onClick={() =>
              setAddress1(item.address1) ||
              setAddress2(item.address2) ||
              setZipCode(item.zipCode) ||
              setCountry(item.country) ||
              setCity(item.city)
            }
          />
          {/* 주소 목록에 표시되는 주소 정보 */}
          <h2>{item.addressType}</h2>
        </div>
      ))}
  </div>
)}

3. 주소 선택 및 업데이트:

사용자가 주소 목록에서 체크박스를 선택하면 해당 주소의 정보가 배송 정보로 업데이트된다.

각 주소에 대한 체크박스 클릭 시, 선택된 주소의 정보가 개별적으로 setAddress1, setAddress2, setZipCode, setCountry, setCity 함수를 통해 업데이트된다.

<input
  type="checkbox"
  className="mr-3"
  value={item.addressType}
  onClick={() =>
    setAddress1(item.address1) ||
    setAddress2(item.address2) ||
    setZipCode(item.zipCode) ||
    setCountry(item.country) ||
    setCity(item.city)
  }
/>

```

## 쿠폰 코드 구현

```jsx
쿠폰 코드를 입력하고 할인 반게 하는 로직을 구현한다
```

```jsx
1. 쿠폰 코드 입력 폼 및 적용 버튼:

사용자는 Coupon code 입력란에 쿠폰 코드를 입력하고, Apply code 버튼을 클릭할 수 있다.

handleSubmit 함수가 form의 onSubmit 이벤트로 호출되어 쿠폰 코드를 처리한다.

<form onSubmit={handleSubmit}>
  <input
    type="text"
    className={`${styles.input} h-[40px] pl-2`}
    placeholder="Coupon code"
    value={couponCode}
    onChange={(e) => setCouponCode(e.target.value)}
    required
  />
  <input
    className={`w-full h-[40px] border border-[#f63b60] text-center text-[#f63b60] rounded-[3px] mt-8 cursor-pointer`}
    required
    value="Apply code"
    type="submit"
  />
</form>

----------------------------------------------------------------------------

2. 쿠폰 코드 처리 (handleSubmit 함수):

handleSubmit 함수는 쿠폰 코드를 서버로 전송하고, 서버에서 쿠폰 코드의 유효성을 확인한다.

Axios 라이브러리를 사용하여 서버에 GET 요청을 보내고, 쿠폰 코드의 정보를 가져온다.

const handleSubmit = async (e) => {
  e.preventDefault();
  const name = couponCode;

  await axios
    .get(`http://localhost:5000/api/v2/coupon/get-coupon-value/${name}`)
    .then((res) => {
      // 쿠폰 코드 정보 확인 및 처리 로직
    });
};

----------------------------------------------------------------------------

3. 쿠폰 코드 정보 확인 및 처리:

서버에서 반환된 데이터를 기반으로 쿠폰 코드의 유효성을 확인한다.

유효한 경우, 해당 쿠폰이 적용 가능한지 확인한 후, 할인 금액을 계산한다.

if (res.data.couponCode !== null) {
  const isCouponValid =
    cart && cart.filter((item) => item.shopId === shopId);

  if (isCouponValid.length === 0) {
    toast.error("유요하지 않은 상점의 쿠폰코드 입니다.");
    setCouponCode("");
  } else {
    const eligiblePrice = isCouponValid.reduce(
      (acc, item) => acc + item.qty * item.discountPrice,
      0
    );
    const discountPrice = (eligiblePrice * couponCodeValue) / 100;
    setDiscountPrice(discountPrice);
    setCouponCodeData(res.data.couponCode);
    setCouponCode("");
  }
}

----------------------------------------------------------------------------

4. 할인 정보 업데이트:

유효한 쿠폰 코드가 적용되면, 상태 변수인 discountPrice와 couponCodeData를 업데이트한다.

const discountPercentenge = couponCodeData
  ? discountPrice
  : "";

const totalPrice = couponCodeData
  ? (subTotalPrice + shipping - discountPercentenge).toFixed(2)
  : (subTotalPrice + shipping).toFixed(2);

이로써 사용자가 입력한 쿠폰 코드가 유효하면, 해당 쿠폰이 적용되어 할인된 가격이 화면에 표시된다.
```

## couponCode.js 작성

```jsx
// Express 프레임워크 및 관련 미들웨어, 모델, 유틸리티를 불러옵니다.
const express = require('express')
const catchAsyncErrors = require('../middleware/catchAsyncErrors')
const Shop = require('../model/shop')
const ErrorHandler = require('../util/ErrorHandler')
const { isSeller } = require('../middleware/auth')
const CouponCode = require('../model/couponCode')

// Express 라우터를 생성합니다.
const router = express.Router()

// 쿠폰코드 생성 엔드포인트
router.post(
  '/create-coupon-code',
  isSeller,
  catchAsyncErrors(async (req, res, next) => {
    try {
      // 요청된 쿠폰 코드의 중복 여부를 확인합니다.
      const isCouponCodeExists = await CouponCode.find({
        name: req.body.name,
      })

      // 중복된 쿠폰 코드가 존재하면 에러를 발생시킵니다.
      if (isCouponCodeExists.length !== 0) {
        return next(new ErrorHandler('쿠폰 코드가 이미 있습니다!', 400))
      }

      // 새로운 쿠폰 코드를 생성하고 성공 응답을 클라이언트에게 전송합니다.
      const couponCode = await CouponCode.create(req.body)
      res.status(201).json({
        success: true,
        couponCode,
      })
    } catch (error) {
      // 에러가 발생하면 ErrorHandler를 통해 클라이언트에게 에러 응답을 전송합니다.
      return next(new ErrorHandler(error, 400))
    }
  })
)

// 모든 쿠폰코드 가져오기 엔드포인트
router.get(
  '/get-coupon/:id',
  isSeller,
  catchAsyncErrors(async (req, res, next) => {
    try {
      // 셀러 아이디를 기반으로 해당 상점의 모든 쿠폰 코드를 가져옵니다.
      const couponCodes = await CouponCode.find({ shopId: req.seller.id })
      res.status(201).json({
        success: true,
        couponCodes,
      })
    } catch (error) {
      // 에러가 발생하면 ErrorHandler를 통해 클라이언트에게 에러 응답을 전송합니다.
      return next(new ErrorHandler(error, 400))
    }
  })
)

// 쿠폰코드 삭제 엔드포인트
router.delete(
  '/delete-coupon/:id',
  isSeller,
  catchAsyncErrors(async (req, res, next) => {
    try {
      // 쿠폰 코드 ID를 기반으로 쿠폰 코드를 삭제합니다.
      const couponCode = await couponCode.findByIdAndDelete(req.params.id)

      // 삭제할 쿠폰 코드가 존재하지 않으면 에러를 발생시킵니다.
      if (!couponCode) {
        return next(new ErrorHandler('쿠폰 코드가 존재하지 않습니다!', 400))
      }

      // 성공 응답을 클라이언트에게 전송합니다.
      res.status(201).json({
        success: true,
        message: '쿠폰 코드 삭제 성공!',
      })
    } catch (error) {
      // 에러가 발생하면 ErrorHandler를 통해 클라이언트에게 에러 응답을 전송합니다.
      return next(new ErrorHandler(error, 400))
    }
  })
)

// 쿠폰코드 값 name 으로 가져오기 엔드포인트
router.get(
  '/get-coupon-value/:name',
  catchAsyncErrors(async (req, res, next) => {
    try {
      // 쿠폰 코드의 이름으로 해당 쿠폰 코드를 가져옵니다.
      const couponCode = await CouponCode.findOne({ name: req.params.name })

      // 쿠폰 코드 정보를 클라이언트에게 전송합니다.
      res.status(200).json({
        success: true,
        couponCode,
      })
    } catch (error) {
      // 에러가 발생하면 ErrorHandler를 통해 클라이언트에게 에러 응답을 전송합니다.
      return next(new ErrorHandler(error, 400))
    }
  })
)

// 라우터를 내보냅니다.
module.exports = router
```
