---
title: '파일 업로드 추가하기'
date: '2026-09-02'
category: '풀스택/React, NodeJS, Express 및 MongoDB - MERN 풀스택 가이드'
tags: ['풀스택', 'React, NodeJS, Express 및 MongoDB - MERN 풀스택 가이드']
draft: false
summary: ''
---

# ImageUpload 컴포넌트 구축하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381323/notion-blog/_/React_NodeJS_Express_MongoDB_-_MERN_/_/Untitled.png)

```jsx
import React, { useRef } from 'react'

// useRef를 사용하여 파일 선택기 엘리먼트를 참조할 수 있게 선언
import './ImageUpload.css'

// 이미지 업로드 컴포넌트에서 사용할 외부 스타일 파일 및 Button 컴포넌트를 import
import Button from './Button'

// ImageUpload 컴포넌트 선언
const ImageUpload = (props) => {
  // useRef 훅을 사용하여 파일 선택기 엘리먼트를 참조
  const filePickerRef = useRef()

  // 파일 선택기 열기를 처리하는 함수
  const pickImageHandler = () => {
    // useRef로 참조한 파일 선택기 엘리먼트의 click 메서드 호출
    filePickerRef.current.click()
  }

  // 파일 선택 시 실행되는 핸들러
  const pickHandler = (event) => {
    // 선택된 파일 정보를 콘솔에 출력
    console.log(event.target)
  }

  // 컴포넌트 렌더링
  return (
    <div className="form-control">
      {/* 파일 선택기 엘리먼트 */}
      <input
        id={props.id}
        ref={filePickerRef}
        style={{ display: 'none' }}
        type="file"
        accept=".jpg,.png,.jpeg"
        onChange={pickHandler}
      />

      {/* 이미지 업로드 영역 */}
      <div className={`image-upload ${props.center && 'center'} `}>
        {/* 이미지 미리보기 영역 */}
        <div className="image-upload__preview">
          <img src="" alt="Preview" />
        </div>

        {/* "PICK IMAGE" 버튼 */}
        <Button type="button" onClick={pickImageHandler}>
          PICK IMAGE
        </Button>
      </div>
    </div>
  )
}

// ImageUpload 컴포넌트를 외부에서 사용할 수 있도록 export
export default ImageUpload
```

```jsx
이 코드는 React를 사용하여 이미지 업로드를 담당하는 컴포넌트이다.

기본적으로 ImageUpload 컴포넌트는 파일 선택(input type="file")을 트리거하는 버튼과

이미지 미리보기를 표시하는 부분으로 구성되어 있다.

useRef를 이용한 파일 선택기 제어:

useRef를 사용하여 파일 선택(input type="file") 엘리먼트를 참조한다.

pickImageHandler 함수에서 이 참조된 엘리먼트의 click 메서드를 호출하여 파일 선택기를 간접적으로 열 수 있다.

* 설명: useRef 훅을 이용하여 생성한 filePickerRef는 파일 선택기(input 태그)를 가리키는 참조이다.

파일 선택기는 style={{ display: 'none' }}로 숨겨져 있다.

이때, 파일 선택기를 클릭하는 것은 사용자에게 보이지 않는 숨겨진 버튼을 클릭하는 것과 같은 역할을 한다.

따라서 pickImageHandler 함수에서 filePickerRef.current.click()을 호출하면, 실제로는 파일 선택기가 열리는 것과 동일한 효과를 얻을 수 있다.

이것이 클릭 이벤트로만 파일 선택기를 열 수 있는 원리이다.

이미지 선택 핸들러:

"PICK IMAGE" 버튼을 클릭하면 pickImageHandler 함수가 호출되고, 이 함수에서는 파일 선택기를 열도록 한다.

파일 선택(input type="file") 엘리먼트 및 미리보기 설정:

파일 선택기 엘리먼트는 화면에 표시되지 않도록 display: 'none' 스타일을 가지고 있다.

이 엘리먼트의 onChange 이벤트는 pickHandler 함수를 호출하도록 설정되어 있다. ( 클릭으로 스타일이 변하니까 onChnge )

파일 선택 핸들러 구현:

pickHandler 함수는 파일 선택기에서 파일을 선택할 때 호출되며, 현재 선택된 파일 정보에 대한 이벤트 객체가 콘솔에 출력된다.

컴포넌트 렌더링:

마지막으로, ImageUpload 컴포넌트는 form-control 및 image-upload 클래스를 사용하여 스타일링되었으며,

Button 컴포넌트를 통해 "PICK IMAGE" 버튼이 표시되고, 이미지 미리보기 영역이 생성되었다.
```

```jsx
원래 아래 파일선택이 있지만 숨겨놨다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381322/notion-blog/_/React_NodeJS_Express_MongoDB_-_MERN_/_/Untitled_1.png)

```jsx
**ImageUpload 컴포넌트 생성**

shared/components/FormElements 폴더에 ImageUpload.js 파일 추가.

ImageUpload.css 파일 복사하여 스타일링 적용.

**ImageUpload 컴포넌트 내용 작성**

React 및 ImageUpload.css 불러오기.

외부 props 수용 후 JSX 반환.

ImageUpload 컴포넌트 내보내기.

**이미지 업로드 양식 구현**

JSX에 이미지 업로더를 위한 마크업 작성.

form-control 클래스를 이용하여 스타일링.

input 태그 추가 및 type 속성을 file로 설정.

accept 속성으로 이미지 확장자 제한 설정.

파일 선택기는 숨겨져 있고, 브라우저가 기본 파일 선택 기능을 제공.

**ImageUpload 컴포넌트 상태 및 이벤트 처리**

useRef를 사용하여 filePickerRef 생성.

pickImageHandler 함수 생성: 파일 선택기 열기.

filePickerRef를 이용하여 파일 선택기 클릭 이벤트 시뮬레이션.

**파일 선택 시 처리**

onChange 이벤트로 pickedHandler 함수 호출.

pickedHandler 함수 내에서 파일 정보 확인 및 console.log로 출력.

**미리보기 및 선택 버튼 구현**

JSX에 이미지 미리보기를 위한 div 구현.

이미지 선택 버튼을 템플릿 리터럴로 유연하게 처리.

onClick 이벤트에 pickImageHandler 함수 연결.

**ImageUpload 컴포넌트 적용**

Auth 페이지에서 ImageUpload 컴포넌트 불러오기.

로그인 모드에 따라 ImageUpload 렌더링 여부 결정.

ImageUpload에 고유한 id 전달.

```

# 양식에서) ImageUpload 컴포넌트 마무리 및 사용하기

```jsx
import React, { useRef, useState, useEffect } from 'react'

// useRef를 사용하여 파일 선택기 엘리먼트를 참조할 수 있게 선언
import './ImageUpload.css'

// 이미지 업로드 컴포넌트에서 사용할 외부 스타일 파일 및 Button 컴포넌트를 import
import Button from './Button'

// ImageUpload 컴포넌트 선언
const ImageUpload = (props) => {
  // useState를 사용하여 파일 상태 및 미리보기 상태, 유효성 상태를 관리
  const [file, setFile] = useState() // 선택된 파일
  const [previewUrl, setPreviewUrl] = useState() // 미리보기 이미지 URL
  const [isValid, setIsValid] = useState(false) // 파일 유효성 상태

  // useRef 훅을 사용하여 파일 선택기 엘리먼트를 참조
  const filePickerRef = useRef()

  // useEffect를 사용하여 파일이 변경될 때마다 미리보기 업데이트
  useEffect(() => {
    if (!file) {
      return
    }

    // 브라우저 내장 API 파일 미리보기 기능, 파일을 읽는 것과 파일의 구문 분석을 돕는다
    const fileReader = new FileReader()

    fileReader.onload = () => {
      // 여기서 onload 실행 이게 뒤에
      setPreviewUrl(fileReader.result)
    }

    fileReader.readAsDataURL(file) // 여기서 읽고 이게먼저고
  }, [file])

  // 파일 선택기 열기를 처리하는 함수
  const pickImageHandler = () => {
    // useRef로 참조한 파일 선택기 엘리먼트의 click 메서드 호출
    filePickerRef.current.click()
  }

  // 파일 선택 시 실행되는 핸들러
  const pickHandler = (event) => {
    let pickedFile
    let fileIsValid = isValid

    // 파일이 있는지 없는지 확인 (이 앱은 파일을 하나만 허용)
    if (event.target.files && event.target.files.length === 1) {
      pickedFile = event.target.files[0]
      setFile(pickedFile)
      setIsValid(true)
      fileIsValid = true
    } else {
      setIsValid(false)
      fileIsValid = false
    }

    // 외부로 전달되는 onInput 프로퍼티에 선택된 파일과 유효성 정보를 전달
    props.onInput(props.id, pickedFile, fileIsValid)
  }

  // 컴포넌트 렌더링
  return (
    <div className="form-control">
      {/* 파일 선택기 엘리먼트 */}
      <input
        id={props.id}
        ref={filePickerRef}
        style={{ display: 'none' }}
        type="file"
        accept=".jpg,.png,.jpeg"
        onChange={pickHandler}
      />

      {/* 이미지 업로드 영역 */}
      <div className={`image-upload ${props.center && 'center'} `}>
        {/* 이미지 미리보기 영역 */}
        <div className="image-upload__preview">
          {previewUrl && <img src={previewUrl} alt="Preview" />}
          {!previewUrl && <p>이미지를 선택하세요</p>}
        </div>

        {/* "PICK IMAGE" 버튼 */}
        <Button type="button" onClick={pickImageHandler}>
          PICK IMAGE
        </Button>
      </div>
    </div>
  )
}

// ImageUpload 컴포넌트를 외부에서 사용할 수 있도록 export
export default ImageUpload
```

```jsx
1. useState로 상태 정의하기

useState를 사용하여 세 가지 상태를 정의한다.

file: 현재 선택된 파일을 저장하는 상태이다.

previewURL: 선택된 파일의 미리보기 이미지 URL을 저장하는 상태이다.

isValid: 선택된 파일의 유효성을 나타내는 상태로, 초기값은 false이다.

2. 파일 선택 시 미리보기 및 유효성 처리 추가 ( const pickHandler = (event) => {} 부분 )

파일 선택(input 변경) 이벤트 핸들러이다.

event.target.files를 통해 선택된 파일을 추출하고, 파일이 없거나 여러 개인 경우 유효성을 false로 설정한다.

선택된 파일이 유효하면 setFile로 파일 상태를 업데이트하고, setIsValid로 유효성을 true로 설정한다.

3. useEffect를 활용하여 파일 변경 시 미리보기 업데이트

useEffect를 사용하여 파일이 변경될 때 미리보기 업데이트 로직을 처리한다.

파일이 없으면 미리보기를 업데이트하지 않고 종료한다.

FileReader를 사용하여 파일을 읽고, onload 이벤트로 미리보기 URL을 설정한다

* FileReader 객체:

FileReader는 웹 브라우저에서 제공하는 API로, 파일과 관련된 작업을 수행할 수 있게 도와주는 객체이다.

파일을 읽어들이고 다양한 형식으로 변환하는 기능을 제공한다.

fileReader.onload:

onload는 FileReader 객체에서 발생하는 이벤트 중 하나이다.

onload 이벤트는 파일을 성공적으로 읽었을 때 호출되는 콜백 함수를 정의하는 데 사용된다.

파일 읽기 작업이 완료되면 이 콜백 함수가 실행되어 추가적인 작업을 수행할 수 있다.

fileReader.readAsDataURL(file):

readAsDataURL은 FileReader 객체의 메서드 중 하나이다.

이 메서드를 사용하여 지정된 파일을 읽어들일 수 있다.

readAsDataURL 메서드를 호출하면 파일의 내용을 데이터 URL로 읽어인다.

데이터 URL은 파일의 내용을 Base64로 인코딩한 문자열을 포함하며, 이미지 등의 미디어 자원을 포함하는 데 사용된다.

파일 읽기가 완료되면 onload 이벤트가 트리거되어 onload에 등록된 콜백 함수가 호출된다.

이러한 과정을 통해 FileReader 객체는 파일을 읽어들이고,

onload 이벤트가 발생하면 그 시점에 지정된 작업(여기서는 이미지 미리보기 업데이트)을 수행하게 된다.

코드에서는 onload 이벤트에 setPreviewUrl을 호출하여 파일의 데이터 URL을 상태로 업데이트하고, 이를 통해 이미지 미리보기가 갱신된다.

4. 컴포넌트 렌더링에서 미리보기 및 오류 텍스트 출력 ( return 부분 )

파일 선택을 위한 input 엘리먼트와 미리보기를 출력하는 영역을 렌더링한다.

previewURL이 존재하면 이미지를 출력하고, 없으면 "이미지를 선택하세요" 텍스트를 출력한다.

파일이 유효하지 않은 경우 props.errorText를 출력하여 오류 메시지를 표시한다.

5. 외부로부터 onInput 프로퍼티 전달 및 호출

props.onInput(props.id, file, isValid);

외부로부터 전달받은 onInput 프로퍼티를 호출하여 선택된 파일과 유효성 정보를 전달한다.

6. Auth 컴포넌트에서 onInput 설정 및 inputHandler 사용

Auth 컴포넌트에서 onInput 프로퍼티를 설정하고, inputHandler 함수를 정의한다.

inputHandler 함수는 ImageUpload 컴포넌트로부터 전달된 데이터를 처리하는 로직을 포함한다.
```

### **FileReader 에 대한 추가 설명**

```jsx
파일 URL은 데이터 URL 형식으로, 파일이나 이미지와 같은 바이너리 데이터를 텍스트로 인코딩한 문자열을 나타낸다.

이 문자열은 데이터를 포함하고 있어서 해당 데이터를 브라우저에서 직접 사용할 수 있게 한다.

데이터 URL은 "data:" 프로토콜을 사용하며, 기본적인 형식은 다음과 같다:

data:[<mediatype>][;base64],<data>

여기서 <mediatype>은 데이터의 MIME 타입을 의미하고, ;base64는 데이터가 Base64로 인코딩되었음을 나타낸다.

<data> 부분에는 인코딩된 데이터가 들어간다.

파일 URL을 생성하는 과정은 다음과 같다:

FileReader 인스턴스 생성: FileReader는 브라우저에서 제공하는 API로, 파일을 읽고 비동기적으로 데이터를 다양한 형식으로 제공할 수 있다.

* 여기서 분제는 콜백을 사용할 수 없으며 프로미스도 제공되지 않는다는 게 문제 이는 onLaod 로 해결한다

const fileReader = new FileReader();

onload 이벤트 핸들러 등록: onload 이벤트 핸들러는 파일의 읽기가 완료되었을 때 실행된다.

이때 파일 데이터를 가지고 원하는 동작을 수행할 수 있다.

fileReader.onload = () => {
    // 여기에서 파일의 내용을 가지고 작업 수행
};

파일 읽기 시작: readAsDataURL() 메서드를 사용하여 파일을 읽기 시작한다

fileReader.readAsDataURL(file);

onload 이벤트 핸들러 내부에서 작업 수행: onload 이벤트 핸들러 내부에서 파일의 내용에 접근하여 Base64로 인코딩된 데이터를 얻을 수 있다.

const dataUrl = fileReader.result;

따라서 dataUrl에는 Base64로 인코딩된 파일 데이터가 포함된 데이터 URL이 생성되게 된다.

이후에는 이 데이터 URL을 이미지의 src 속성이나 다른 용도로 사용할 수 있다.

이렇게 생성된 데이터 URL은 브라우저에서 바로 사용 가능하며,

서버로의 전송이나 로컬 저장소에 저장하는 등의 용도로 활용될 수 있다.

데이터 URL은 작은 이미지나 아이콘과 같은 리소스를 인라인으로 포함할 때 유용하게 사용된다.
```

```jsx
// Auth.js

import React, { useState, useContext } from 'react'

import Card from '../../shared/components/UIElements/Card'
import Input from '../../shared/components/FormElements/Input'
import Button from '../../shared/components/FormElements/Button'
import ErrorModal from '../../shared/components/UIElements/ErrorModal'
import LoadingSpinner from '../../shared/components/UIElements/LoadingSpinner'
import ImageUpload from '../../shared/components/FormElements/ImageUpload'

import { useForm } from '../../shared/hooks/form-hook'
import { useHttpClient } from '../../shared/hooks/http-hook'
import {
  VALIDATOR_EMAIL,
  VALIDATOR_MINLENGTH,
  VALIDATOR_REQUIRE,
} from '../../shared/util/validators'
import { AuthContext } from '../../shared/context/auth-context'

import './Auth.css'

const Auth = () => {
  const auth = useContext(AuthContext)

  // 로그인 및 회원 가입을 위한 상태 및 커스텀 훅 초기화
  const [isLoginMode, setIsLoginMode] = useState(true)
  const { isLoading, error, sendRequest, clearError } = useHttpClient()
  const [formState, inputHandler, setFormData] = useForm(
    {
      email: {
        value: '',
        isValid: false,
      },
      password: {
        value: '',
        isValid: false,
      },
    },
    false
  )

  // 로그인 및 회원 가입 모드 전환
  const switchModeHandler = () => {
    if (!isLoginMode) {
      // 회원 가입 모드에서 로그인 모드로 전환 시, 이름 입력 필드 추가
      setFormData(
        {
          ...formState.inputs,
          name: undefined,
          image: undefined,
        },
        formState.inputs.email.isValid && formState.inputs.password.isValid
      )
    } else {
      // 로그인 모드에서 회원 가입 모드로 전환 시, 이름 입력 필드 제거
      setFormData(
        {
          ...formState.inputs,
          name: {
            value: '',
            isValid: false,
          },
          image: {
            value: null,
            isValid: false,
          },
        },
        false
      )
    }
    setIsLoginMode((prevMode) => !prevMode)
  }

  // 로그인 또는 회원 가입 시도
  const authSubmitHandler = async (event) => {
    event.preventDefault()

    if (isLoginMode) {
      // 로그인 모드일 때
      try {
        const responseData = await sendRequest(
          'http://localhost:5000/api/users/login',
          'POST',
          JSON.stringify({
            email: formState.inputs.email.value,
            password: formState.inputs.password.value,
          }),
          {
            'Content-Type': 'application/json',
          }
        )
        auth.login(responseData.user._id)
      } catch (error) {
        // 오류 발생 시
      }
    } else {
      // 회원 가입 모드일 때
      try {
        const responseData = await sendRequest(
          'http://localhost:5000/api/users/signup',
          'POST',
          JSON.stringify({
            name: formState.inputs.name.value,
            email: formState.inputs.email.value,
            password: formState.inputs.password.value,
          }),
          {
            'Content-Type': 'application/json',
          }
        )
        auth.login(responseData.user._id)
      } catch (error) {
        // 오류 발생 시
      }
    }
  }

  return (
    <>
      {/* HTTP 요청 오류를 표시하는 모달 및 로딩 스피너 */}
      <ErrorModal error={error} onClear={clearError} />
      {isLoading && <LoadingSpinner asOverlay />}

      {/* 로그인/회원 가입 양식 */}
      <Card className="authentication">
        <h1>Login Required</h1>
        <hr />

        {/* 로그인/회원 가입 양식 */}
        <form onSubmit={authSubmitHandler}>
          {/* 회원 가입 모드에서만 보이는 이름 입력 필드 */}
          {!isLoginMode && (
            <Input
              element="input"
              id="name"
              type="text"
              label="Name"
              validators={[VALIDATOR_REQUIRE()]}
              errorText="Please enter a name"
              onInput={inputHandler}
            />
          )}
          {!isLoginMode && (
            <ImageUpload
              center
              id="image"
              onInput={inputHandler}
              errorText="이미지를 업로드 하세요"
            />
          )}

          {/* 이메일 및 비밀번호 입력 필드 */}
          <Input
            element="input"
            id="email"
            type="email"
            label="E-Mail"
            validators={[VALIDATOR_EMAIL()]}
            errorText="Please enter a valid email address."
            onInput={inputHandler}
          />
          <Input
            element="input"
            id="password"
            type="password"
            label="Password"
            validators={[VALIDATOR_MINLENGTH(6)]}
            errorText="Please enter a valid password at least 5 Ch."
            onInput={inputHandler}
          />

          {/* 로그인/회원 가입 버튼 */}
          <Button type="submit" disabled={!formState.isValid}>
            {isLoginMode ? 'LOGIN' : 'SIGNUP'}
          </Button>
        </form>

        {/* 로그인/회원 가입 모드 전환 버튼 */}
        <Button inverse onClick={switchModeHandler}>
          SWITCH TO {isLoginMode ? 'SIGNUP' : 'LOGIN'}
        </Button>
      </Card>
    </>
  )
}

export default Auth
```

```jsx
                    <Input
                        element='input'
                        id='password'
                        type='password'
                        label="Password"
                        validators={[VALIDATOR_MINLENGTH(6)]}
                        errorText="Please enter a valid password at least 5 Ch."
                        onInput={inputHandler}
                    />

 {!isLoginMode && <ImageUpload center id="image" onInput={inputHandler} errorText="이미지를 업로드 하세요"/>}

이 부분의 onInput 으로

props.onInput(props.id, pickedFile, fileIsValid); 의 id 와 파일 URL ( 값 ) 그리고 유효성 검사 여부 ( true , false ) 가 전달된다

그리고 여기서 inputHandler 로 값을 보내는데

inputHandler 는 form-hook.js 에 정의되어 있다

        } else {
            // 로그인 모드에서 회원 가입 모드로 전환 시, 이름 입력 필드 제거
            setFormData(
                {
                    ...formState.inputs,
                    name: {
                        value: '',
                        isValid: false
                    },
                    image : {
                        value : null,
                        isValid : false
                    }
                },
                false
            );
        }

그리고 회원 가입 모드에서 formData 를 초기화 시켜줘야 한다

image 부분을 추가시켜 주자
```

```jsx
// form-hook.js
import { useCallback, useReducer } from 'react'

// formReducer: useReducer에 사용될 리듀서 함수
const formReducer = (state, action) => {
  switch (action.type) {
    // INPUT_CHANGE 액션: 입력 요소의 변경을 처리
    case 'INPUT_CHANGE':
      // 기본적으로 폼은 유효하다고 가정하고 시작
      let formIsValid = true

      // 현재 상태의 모든 입력 요소에 대해 반복
      for (const inputId in state.inputs) {
        // 만약 현재의 inputId가 undefined이면(즉, 입력 요소가 없으면) continue로 건너뛴다.
        if (!state.inputs[inputId]) {
          continue
        }

        // 현재의 inputId가 액션에서 전달된 inputId와 일치하면
        if (inputId === action.inputId) {
          // 현재 입력 요소의 유효성을 action에서 전달된 값으로 업데이트
          formIsValid = formIsValid && action.isValid
        } else {
          // 현재 입력 요소의 유효성을 유지
          formIsValid = formIsValid && state.inputs[inputId].isValid
        }
      }

      // 업데이트된 입력 값 및 유효성을 포함하여 상태 업데이트
      return {
        ...state,
        inputs: {
          ...state.inputs,
          [action.inputId]: { value: action.value, isValid: action.isValid },
        },
        isValid: formIsValid,
      }

    // SET_DATA 액션: 초기 데이터로 상태를 설정
    case 'SET_DATA':
      // 액션에서 전달된 입력 데이터 및 폼 유효성으로 상태 업데이트
      return {
        inputs: action.inputs,
        isValid: action.formIsValid,
      }

    // 기본 케이스: 상태 변경 없음
    default:
      return state
  }
}

// useForm 커스텀 훅 정의
export const useForm = (initialInputs, initialFormValidity) => {
  // useReducer를 사용하여 formReducer와 초기 상태 설정
  const [formState, dispatch] = useReducer(formReducer, {
    inputs: initialInputs,
    isValid: initialFormValidity,
  })

  // 입력 요소 변경 이벤트 핸들러 정의
  const inputHandler = useCallback((id, value, isValid) => {
    // INPUT_CHANGE 액션 디스패치
    dispatch({
      type: 'INPUT_CHANGE',
      value: value,
      isValid: isValid,
      inputId: id,
    })
  }, [])

  // 초기 데이터로 상태를 설정하는 함수 정의
  const setFormData = useCallback((inputData, formValidity) => {
    // SET_DATA 액션 디스패치
    dispatch({
      type: 'SET_DATA',
      inputs: inputData,
      formIsValid: formValidity,
    })
  }, [])

  // 커스텀 훅 반환
  return [formState, inputHandler, setFormData]
}
```

```jsx
Auth.js 에서 전달받은 onInput 의 값들을

  const inputHandler = useCallback((id, value, isValid) => {
    dispatch({
      type: 'INPUT_CHANGE',
      value: value,
      isValid: isValid,
      inputId: id
    });
  }, []);

이 헨들러에서 사용해서

formReducer 에서 처리하게 된다  ( 유효성 검사 와 값 업데이트 )
```

# 백엔드에서 Multer를 사용하여 파일 저장하기

```jsx
먼저, 백엔드에서 파일 업로드를 위해 Multer 패키지를 사용하기 위해서는 해당 패키지를 설치해야 한다.

이를 위해 백엔드 프로젝트의 루트 디렉토리에서 다음 명령어를 실행한다:

그리고 middleware 폴더를 생성하고 file-upload.js 파일을 생성한다
```

```jsx
npm i multer
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381325/notion-blog/_/React_NodeJS_Express_MongoDB_-_MERN_/_/Untitled_2.png)

```jsx
// file-upload.js

// multer 패키지와 UUID 패키지를 불러옴
const multer = require('multer')
const { v4: uuid } = require('uuid')

// 업로드될 이미지의 MIME 타입과 확장자를 매핑하는 객체
const MIME_TYPE_MAP = {
  'image/png': 'png',
  'image/jpeg': 'jpeg',
  'image/jpg': 'jpg',
}

// multer를 사용하여 파일 업로드를 처리하는 미들웨어 생성
const fileUpload = multer({
  // 파일 크기 제한 설정 (500KB)
  limits: { fileSize: 500000 },

  // 파일 저장 경로 및 이름 설정
  storage: multer.diskStorage({
    destination: (req, file, callback) => {
      // 업로드된 파일의 저장 경로를 설정 ('uploads/images' 폴더)
      callback(null, 'uploads/images') // 첫번째 인자는 오류 전달 두번째 인자는 실제경로
    },
    filename: (req, file, callback) => {
      // 파일 이름을 설정하기 위해 UUID와 확장자 결합
      const ext = MIME_TYPE_MAP[file.mimetype]
      callback(null, uuid() + '.' + ext) // 첫번째 인자 오류 전달 두번째 인자 파일이름 완성본
    },
  }),
})

// 생성한 미들웨어를 외부에서 사용할 수 있도록 내보냄
module.exports = fileUpload
```

```jsx
multer 패키지와 uuid 패키지를 불러온다.

multer 패키지는 파일 업로드를 처리하기 위한 Node.js 미들웨어이며, uuid 패키지는 고유한 식별자를 생성하기 위한 패키지이다.

이미지의 MIME 타입과 확장자를 매핑하기 위한 객체인 MIME_TYPE_MAP을 정의한다.

MIME_TYPE_MAP 객체는 업로드된 이미지의 MIME 타입과 확장자를 매핑하는데 사용된다.

이는 나중에 파일의 확장자를 결정하기 위해 활용된다.

fileUpload 미들웨어를 생성합니다.

multer 함수를 사용하여 파일 업로드를 처리하는 미들웨어를 생성한다.

limits: 파일 크기 제한을 설정한다 (500KB).

limits 옵션을 사용하여 업로드되는 파일의 최대 크기를 500KB로 제한한다.

storage: 업로드된 파일의 저장 경로 및 이름을 설정한다.

storage 옵션을 사용하여 업로드된 파일이 저장될 경로와 이름을 설정한다.

destination: 업로드된 파일의 저장 경로를 설정한다 ('uploads/images' 폴더).

destination 함수는 업로드된 파일의 저장 경로를 설정한다. 여기서는 'uploads/images' 폴더에 저장한다.

filename: 파일 이름을 설정하기 위해 UUID와 확장자를 결합한다.

filename 함수는 업로드된 파일의 이름을 설정한다.

UUID를 사용하여 파일 이름을 고유하게 생성하고, 파일의 확장자는 MIME 타입을 통해 결정한다.

생성한 미들웨어를 외부에서 사용할 수 있도록 내보낸다.

fileUpload 미들웨어를 외부에서 사용할 수 있도록 모듈로 내보낸다. 이후 이 미들웨어를 라우터에서 사용하여 파일 업로드를 처리할 수 있다.
```

```jsx
이제 이 커스텀 미들웨어를 다른 라우터에서 사용할 수 있다.

예를 들어, 회원가입 라우터(users-routes.js)에서 이미지를 업로드하고자 하는 경우 다음과 같이 사용할 수 있다.

// 파일 업로드 미들웨어를 실행합니다. 하나의 이미지 파일만 처리하며, 'image'라는 필드에서 파일을 받습니다.

fileUpload.single('image'), // 이 부분을 나중에 Auth 에서 image 라는 필드로 값을 받는다

// single('image') 부분은 하나의 파일만 업로드하며, 해당 파일은 클라이언트가 요청한 본문(body) 내의 'image' 에서 찾아진다는 의미
```

```jsx
// users-routes.js

// Express 프레임워크를 가져옵니다.
const express = require('express')
const { check } = require('express-validator')

// 사용자 컨트롤러 로직을 가져옵니다.
const usersController = require('../controllers/users-controllers')

// Express의 라우터를 생성합니다.
const router = express.Router()

// 파일 업로드 미들웨어를 가져옵니다.
const fileUpload = require('../middleware/file-upload')

// 사용자 목록을 반환하는 라우트
router.get('/', usersController.getUsers)

// 사용자 가입을 처리하는 라우트
router.post(
  '/signup',
  // 파일 업로드 미들웨어를 실행합니다. 하나의 이미지 파일만 처리하며, 'image'라는 필드에서 파일을 받습니다.
  fileUpload.single('image'),

  // 사용자 입력 유효성을 검사하는 미들웨어를 설정합니다.
  [
    // 'name' 필드가 비어있는지 확인합니다.
    check('name').not().isEmpty(),

    // 'email' 필드를 정규화하고 이메일 형식인지 확인합니다.
    check('email')
      .normalizeEmail() // Test@test.com => test@test.com 변환
      .isEmail(), // 이메일 형식인지 확인

    // 'password' 필드가 최소 6자 이상인지 확인합니다.
    check('password').isLength({ min: 6 }),
  ],

  // 가입 로직을 처리하는 컨트롤러 함수
  usersController.signup
)

// 사용자 로그인을 처리하는 라우트
router.post('/login', usersController.login)

// 라우터를 모듈로 내보냅니다.
module.exports = router
```

# 백엔드에서 파일 필터링(이미지만)하기

```jsx
const multer = require('multer')
const { v4: uuid } = require('uuid')

// 파일의 MIME 타입과 확장자를 매핑하는 객체
const MIME_TYPE_MAP = {
  'image/png': 'png',
  'image/jpeg': 'jpeg',
  'image/jpg': 'jpg',
}

// multer를 사용하여 파일 업로드를 처리하는 미들웨어 생성
const fileUpload = multer({
  // 파일 크기 제한 (500KB)
  limits: 500000,

  // 파일 저장 설정 (디스크 스토리지)
  storage: multer.diskStorage({
    destination: (req, file, callback) => {
      // 파일 저장 경로 설정 ('uploads/images' 폴더)
      callback(null, 'uploads/images')
    },
    filename: (req, file, callback) => {
      // 파일 이름 설정 (고유한 UUID + 확장자)
      const ext = MIME_TYPE_MAP[file.mimetype] // 파일의 .mimetype 을 찾아내서 MIME_TYPE_MAP 중 일치하는 값으로 매핑 ex) const ext = MIME_TYPE_MAP['image/png'];
      callback(null, uuid() + '.' + ext)
    },

    // 파일 필터링 (유효한 MIME 타입 여부 확인)
    fileFilter: (req, file, callback) => {
      // 유효한 MIME 타입인지 여부를 확인하고 isValid 변수에 저장
      const isValid = !!MIME_TYPE_MAP[file.mimetype] // 이는

      // 유효하지 않은 MIME 타입이면 오류 생성, 그렇지 않으면 오류는 null
      let error = isValid ? null : new Error('파일 유형이 유효하지 않습니다.')

      // 오류와 함께 isValid 값을 콜백으로 전달
      callback(error, isValid) // isValid가 true면 파일 수용, 아니면 수용하지 않음
    },
  }),
})

module.exports = fileUpload
```

```jsx
디스크 스토리지와 파일 검증의 중요성

디스크 스토리지는 파일을 안전하게 저장하는 데 중요하다.

그러나 유효하지 않은 파일을 받을 수 있으므로 파일의 검증도 필요하다.

Frontend의 검증만으로는 보안에 취약하며, 사용자가 개발자 도구를 사용해 변경할 수 있기 때문이다.

백엔드에 fileFilter 추가

백엔드에서는 fileFilter를 추가하여 파일 검증을 수행한다.

fileFilter 함수는 req, file, callback (콜백)을 받아 처리하며, 검증 실패 시 오류를 첫 번째 인수로 전달한다.

MIME_TYPE_MAP을 활용한 파일 검증

MIME_TYPE_MAP 객체를 활용하여 파일의 MIME 타입을 검증한다.

MIME 타입이 png, .jpeg, .jpg 중 하나인 경우, 해당 파일은 유효하다고 판단한다.

isValid 변수 설정

isValid 변수를 도입하여 MIME 타입이 유효한지 여부를 저장한다.

MIME 타입이 유효하면 true, 그렇지 않으면 false가 저장된다.

두 번의 !! 연산자: 두 번의 ! 연산자는 값을 두 번 부정하므로 원래 값과 동일한 부울 값을 얻게 된다.

이렇게 함으로써 값의 부울 표현을 명시적으로 얻을 수 있다.

undefined는 불리언으로 부정하면 true가 된다.

문자열은 불리언으로 부정하면 false가 된다.

!!를 사용하면 이를 다시 불리언 값으로 변환하여 undefined일 때 false가 되고, 문자열이 있을 때 true가 된다.

error 처리 및 파일 수용 여부 반환

isValid를 기반으로 오류 처리를 진행한다.

isValid가 true이면 오류가 없으므로 첫 번째 인수를 null로 설정한다.

isValid가 false인 경우,

유효하지 않은 MIME 타입이므로 오류를 생성하고 두 번째 인수에는 false를 전달한다.

Multer 구성 완료

위의 작업으로 Multer의 구성이 완료되었다.

파일을 수용하고 저장할 준비가 되었다.

적용 및 테스트

구성한 미들웨어를 signup 라우트에 적용한다.

서버를 재시작한 뒤 Frontend로 돌아가서 올바른 타입의 데이터와 파일을 포함하여 Backed,

signup 라우트로 HTTP 요청을 전송하여 파일이 올바르게 수용되는지 확인한다.
```

# 프론트엔드와 백엔드 연결하기

```jsx
import React, { useState, useContext } from 'react'

import Card from '../../shared/components/UIElements/Card'
import Input from '../../shared/components/FormElements/Input'
import Button from '../../shared/components/FormElements/Button'
import ErrorModal from '../../shared/components/UIElements/ErrorModal'
import LoadingSpinner from '../../shared/components/UIElements/LoadingSpinner'
import ImageUpload from '../../shared/components/FormElements/ImageUpload'

import { useForm } from '../../shared/hooks/form-hook'
import { useHttpClient } from '../../shared/hooks/http-hook'
import {
  VALIDATOR_EMAIL,
  VALIDATOR_MINLENGTH,
  VALIDATOR_REQUIRE,
} from '../../shared/util/validators'
import { AuthContext } from '../../shared/context/auth-context'

import './Auth.css'

const Auth = () => {
  const auth = useContext(AuthContext)

  // 로그인 및 회원 가입을 위한 상태 및 커스텀 훅 초기화
  const [isLoginMode, setIsLoginMode] = useState(true)
  const { isLoading, error, sendRequest, clearError } = useHttpClient()
  const [formState, inputHandler, setFormData] = useForm(
    {
      email: {
        value: '',
        isValid: false,
      },
      password: {
        value: '',
        isValid: false,
      },
    },
    false
  )

  // 로그인 및 회원 가입 모드 전환
  const switchModeHandler = () => {
    if (!isLoginMode) {
      // 회원 가입 모드에서 로그인 모드로 전환 시, 이름 입력 필드 추가
      setFormData(
        {
          ...formState.inputs,
          name: undefined,
          image: undefined,
        },
        formState.inputs.email.isValid && formState.inputs.password.isValid
      )
    } else {
      // 로그인 모드에서 회원 가입 모드로 전환 시, 이름 입력 필드 제거
      setFormData(
        {
          ...formState.inputs,
          name: {
            value: '',
            isValid: false,
          },
          image: {
            value: null,
            isValid: false,
          },
        },
        false
      )
    }
    setIsLoginMode((prevMode) => !prevMode)
  }

  // 로그인 또는 회원 가입 시도
  const authSubmitHandler = async (event) => {
    event.preventDefault()

    if (isLoginMode) {
      // 로그인 모드일 때
      try {
        const responseData = await sendRequest(
          'http://localhost:5000/api/users/login',
          'POST',
          JSON.stringify({
            email: formState.inputs.email.value,
            password: formState.inputs.password.value,
          }),
          {
            'Content-Type': 'application/json',
          }
        )
        auth.login(responseData.user._id)
      } catch (error) {
        // 오류 발생 시
      }
    } else {
      // 회원 가입 모드일 때
      try {
        const formData = new FormData()
        formData.append('name', formState.inputs.name.value)
        formData.append('email', formState.inputs.email.value)
        formData.append('password', formState.inputs.password.value)
        formData.append('image', formState.inputs.image.value)

        const responseData = await sendRequest(
          'http://localhost:5000/api/users/signup',
          'POST',
          formData
        )
        auth.login(responseData.user._id)
      } catch (error) {
        // 오류 발생 시
      }
    }
  }

  return (
    <>
      {/* HTTP 요청 오류를 표시하는 모달 및 로딩 스피너 */}
      <ErrorModal error={error} onClear={clearError} />
      {isLoading && <LoadingSpinner asOverlay />}

      {/* 로그인/회원 가입 양식 */}
      <Card className="authentication">
        <h1>Login Required</h1>
        <hr />

        {/* 로그인/회원 가입 양식 */}
        <form onSubmit={authSubmitHandler}>
          {/* 회원 가입 모드에서만 보이는 이름 입력 필드 */}
          {!isLoginMode && (
            <Input
              element="input"
              id="name"
              type="text"
              label="Name"
              validators={[VALIDATOR_REQUIRE()]}
              errorText="Please enter a name"
              onInput={inputHandler}
            />
          )}
          {!isLoginMode && (
            <ImageUpload
              center
              id="image"
              onInput={inputHandler}
              errorText="이미지를 업로드 하세요"
            />
          )}

          {/* 이메일 및 비밀번호 입력 필드 */}
          <Input
            element="input"
            id="email"
            type="email"
            label="E-Mail"
            validators={[VALIDATOR_EMAIL()]}
            errorText="Please enter a valid email address."
            onInput={inputHandler}
          />
          <Input
            element="input"
            id="password"
            type="password"
            label="Password"
            validators={[VALIDATOR_MINLENGTH(6)]}
            errorText="Please enter a valid password at least 5 Ch."
            onInput={inputHandler}
          />

          {/* 로그인/회원 가입 버튼 */}
          <Button type="submit" disabled={!formState.isValid}>
            {isLoginMode ? 'LOGIN' : 'SIGNUP'}
          </Button>
        </form>

        {/* 로그인/회원 가입 모드 전환 버튼 */}
        <Button inverse onClick={switchModeHandler}>
          SWITCH TO {isLoginMode ? 'SIGNUP' : 'LOGIN'}
        </Button>
      </Card>
    </>
  )
}

export default Auth
```

```jsx
FormData 객체 사용

FormData 객체를 사용하여 파일 및 텍스트 데이터를 전송할 수 있음.

이미지 파일은 이진 데이터로 JSON 데이터 형식이 아닌 FormData를 사용해야 함.

( 이미지는 010101010101 같은 이진수 형식이기 때문에 )

FormData에 데이터 추가

FormData에는 이미지 파일 외에도 텍스트 데이터를 추가 가능.

예를 들어, 이메일, 이름, 비밀번호 등의 텍스트 데이터를 FormData에 추가.

const formData = new FormData();
formData.append('email', formState.inputs.email.value);
formData.append('name', formState.inputs.name.value);
formData.append('password', formState.inputs.password.value);
formData.append('image', formState.inputs.image.value); // 이미지 파일 추가

Fetch API를 통한 전송

JSON.stringify 대신 FormData를 사용하여 데이터 전송.

Fetch API가 FormData를 감지하고 적절한 헤더를 자동으로 추가.

fetch('http://example.com/signup', {
  method: 'POST',
  body: formData, // JSON.stringify 대신 FormData 사용
})

백엔드 처리

백엔드에서는 FormData의 이미지 데이터를 저장하는 코드 추가 필요.

이미지 저장 후 데이터베이스에는 이미지에 대한 포인터 또는 경로를 저장해야함 뒤에서 계속
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381326/notion-blog/_/React_NodeJS_Express_MongoDB_-_MERN_/_/Untitled_3.png)

```jsx
현재 파일 저장까지는 된다
```

# 사용자를 이미지에 연결하기

```jsx
// index.js

const express = require('express')
const fs = require('fs')
const placesRoutes = require('./routes/places-route')
const usersRoutes = require('./routes/user-route')
const HttpError = require('./models/http-error')

const app = express()
// mongoose 라이브러리 불러오기
const mongoose = require('mongoose')

app.use(express.json()) // 본문 라우터 위에 있어야 한다 순서대로 읽기 때문
app.use(express.urlencoded({ extended: true }))

// CORS 설정을 위한 미들웨어
app.use((req, res, next) => {
  res.setHeader('Access-Control-Allow-Origin', '*')
  res.setHeader(
    'Access-Control-Allow-Headers',
    'Origin, X-Requested-With, Content-Type, Accept, Authorization'
  )
  res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PATCH, DELETE')

  next()
})

// 라우터 설정
app.use('/api/places', placesRoutes) // => /api/places/ 로 시작하는 요청 라우팅
app.use('/api/users', usersRoutes)

// 에러 핸들링 미들웨어: 없는 페이지 에러 생성
app.use((req, res, next) => {
  const error = new HttpError('없는 페이지 입니다', 404)
  throw error
})

// 에러 핸들링 미들웨어: 이전에 던져진 에러를 처리하는 미들웨어입니다.
app.use((error, req, res, next) => {
  // 파일이 존재하면 삭제
  if (req.file) {
    fs.unlink(req.file.path, (err) => {
      console.log(err)
    })
  }

  // 이미 응답이 전송되었는지 확인
  if (res.headersSent) {
    // 이미 응답이 전송된 경우 다음 에러 핸들러로 이동
    return next(error)
  }

  // 에러 응답을 전송
  res.status(error.code || 500).json({ message: error.message || '알 수 없는 에러' })
})

// MongoDB 서버에 연결
mongoose
  .connect(
    'mongodb+srv://dhk9309:kim1458@cluster0.ckluwao.mongodb.net/mern?retryWrites=true&w=majority'
  )
  .then(() => {
    console.log('데이터 베이스 연결 성공')
  })
  .catch(() => {
    console.log('데이터 베이스 연결 실패')
  })

// 서버 리스닝
app.listen(5000)
```

```jsx
롤백 기능 구현하기

유효성 오류 발생 시 파일 롤백이 필요하다 ( 지금은 실패해도 파일이 들어감 )

파일 삭제를 통해 롤백을 해주도록 한다

오류 핸들러 내에 파일 삭제 로직 추가해주도록 한다

오류 핸들링은 index.js 파일에 구현되어 있음

오류 발생 시 파일 삭제를 위해 fs 모듈을 사용한다 ( fs 는 내 컴퓨터 내 파일을 다루는 모듈 )

Multer가 req 로 추가한 file 프로퍼티로 파일 경로 획득한다 ( req.file.path )

unlink 함수를 사용하여 파일 삭제  ( fs.unlink(req.file.path )

// 에러 핸들링 미들웨어: 이전에 던져진 에러를 처리하는 미들웨어입니다.
app.use((error, req, res, next) => {
    if(req.file) {
        fs.unlink(req.file.path, (err) => {
            console.log(err);
        })
    }

```

```jsx
// users-controllers.js

const { v4: uuid } = require('uuid')
const { validationResult } = require('express-validator')

const HttpError = require('../models/http-error')
const User = require('../models/user')

const getUsers = async (req, res, next) => {
  let users
  try {
    users = await User.find({}, '-password') // - 는 제외하고 password 제외하고 불러오기
  } catch (err) {
    const error = new HttpError('유저 정보를 불러오지 못했습니다', 500)
    return next(error)
  }
  res.json(users)
}

const signup = async (req, res, next) => {
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    const error = new HttpError(`${errors.errors[0].path}는 비어있을 수 없습니다`, 422) // 에러가 배열로 나와서 처번째 인덱스만 출력했다 나중에 수정이 필요할듯
    return next(error)
  }

  const { name, email, password } = req.body

  let existingUser
  try {
    existingUser = await User.findOne({ email: email })
  } catch (err) {
    const error = new HttpError('회원가입 실패', 500)
    return next(error)
  }

  if (existingUser) {
    const error = new HttpError('이미 유저가 있습니다.', 422)
    return next(error)
  }

  const createdUser = new User({
    name,
    email,
    image: req.file.path,
    password,
    places: [],
  })

  try {
    await createdUser.save()
  } catch (err) {
    const error = new HttpError('회원 저장에 오류가 발생했습니다', 500)
    return next(error)
  }

  res.status(201).json({ user: createdUser })
}

const login = async (req, res, next) => {
  // 1. 요청 바디에서 이메일과 비밀번호 추출
  const { email, password } = req.body

  let existingUser
  try {
    // 2. 데이터베이스에서 해당 이메일을 가진 사용자 찾기
    existingUser = await User.findOne({ email: email })
  } catch (err) {
    // 3. 데이터베이스 조회 중 에러가 발생한 경우, 500 상태코드로 에러 응답
    const error = new HttpError('로그인 실패', 500)
    return next(error)
  }

  // 4. 찾은 사용자가 없거나 비밀번호가 일치하지 않는 경우, 401 상태코드로 에러 응답
  if (!existingUser || existingUser.password !== password) {
    const error = new HttpError('유저 정보가 틀립니다', 401)
    return next(error)
  }

  // 5. 로그인이 성공한 경우, 성공 메시지를 JSON 형태로 응답
  res.json({ message: '로그인 성공', user: existingUser })
}

exports.getUsers = getUsers
exports.signup = signup
exports.login = login
```

```jsx
파일 경로를 사용자 문서에 추가하기

생성된 사용자 문서에서 이미지 경로 저장

Multer가 제공하는 요청 파일로부터 이미지 경로 추출

사용자 문서에 이미지 경로를 저장하여 연결

    const createdUser = new User({
        name,
        email,
        image: req.file.path,
        password,
        places : []
    })

image 경로를 req.file.path 로 추가해준다 ( 파일의 경로 http:local... 은 프론트에서 추가해주도록 한다 )
```

```jsx
import React from 'react'
import { Link } from 'react-router-dom'

import Avatar from '../../shared/components/UIElements/Avatar'
import Card from '../../shared/components/UIElements/Card'

import './UserItem.css'

const UserItem = (props) => {
  return (
    <li className="user-item">
      <Card className="user-item__content">
        <Link to={`/${props.id}/places`}>
          <div className="user-item__image">
            <Avatar image={`http://localhost:5000/${props.image}`} alt={props.name} />
          </div>
          <div className="user-item__info">
            <h2>{props.name}</h2>
            <h3>
              {props.placeCount} {props.placeCount === 1 ? 'Place' : 'Places'}
            </h3>
          </div>
        </Link>
      </Card>
    </li>
  )
}

export default UserItem
```

```jsx
<Avatar image={`http://localhost:5000/${props.image}`} alt={props.name} />

이런식으로 아까 저장된 image ( req.file.path ) 앞에 http://localhost:5000/ 를 붙여서 파일을 출력해준다

이러면 회원의 이미지가 출력되야 하지만 출력되지 않는다 이는 백엔드에서 추가적으로 수정이 필요하기 때문이다
```

# 이미지를 정적으로 제공하기 ( 알맞은 URL 을 추가했음에도 출력 x 문제 해결)

```jsx
const express = require('express')
const fs = require('fs')
const path = require('path')
const placesRoutes = require('./routes/places-route')
const usersRoutes = require('./routes/user-route')
const HttpError = require('./models/http-error')

const app = express()
// mongoose 라이브러리 불러오기
const mongoose = require('mongoose')

app.use(express.json()) // 본문 라우터 위에 있어야 한다 순서대로 읽기 때문
app.use(express.urlencoded({ extended: true }))

//  express.static 미들웨어를 추가하여 정적 파일 서빙 설정
app.use('/uploads/images', express.static(path.join('uploads', 'images')))

// CORS 설정을 위한 미들웨어
app.use((req, res, next) => {
  res.setHeader('Access-Control-Allow-Origin', '*')
  res.setHeader(
    'Access-Control-Allow-Headers',
    'Origin, X-Requested-With, Content-Type, Accept, Authorization'
  )
  res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PATCH, DELETE')

  next()
})

// 라우터 설정
app.use('/api/places', placesRoutes) // => /api/places/ 로 시작하는 요청 라우팅
app.use('/api/users', usersRoutes)

// 에러 핸들링 미들웨어: 없는 페이지 에러 생성
app.use((req, res, next) => {
  const error = new HttpError('없는 페이지 입니다', 404)
  throw error
})

// 에러 핸들링 미들웨어: 이전에 던져진 에러를 처리하는 미들웨어입니다.
app.use((error, req, res, next) => {
  // 파일이 존재하면 삭제
  if (req.file) {
    fs.unlink(req.file.path, (err) => {
      console.log(err)
    })
  }

  // 이미 응답이 전송되었는지 확인
  if (res.headersSent) {
    // 이미 응답이 전송된 경우 다음 에러 핸들러로 이동
    return next(error)
  }

  // 에러 응답을 전송
  res.status(error.code || 500).json({ message: error.message || '알 수 없는 에러' })
})

// MongoDB 서버에 연결
mongoose
  .connect(
    'mongodb+srv://dhk9309:kim1458@cluster0.ckluwao.mongodb.net/mern?retryWrites=true&w=majority'
  )
  .then(() => {
    console.log('데이터 베이스 연결 성공')
  })
  .catch(() => {
    console.log('데이터 베이스 연결 실패')
  })

// 서버 리스닝
app.listen(5000)
```

```jsx
이미지 미표시 문제 해결: 백엔드에서 정적 파일 서빙 설정

기존에 올바른 URL을 사용했음에도 불구하고 이미지가 표시되지 않는 문제 발생

서버 사이드에서 정적 파일 서빙이 필요한 상황

**정적 파일 서빙을 위한 express.static 미들웨어 추가**

const fs = require('fs');
const path = require('path');

app.use('/uploads/images', express.static(path.join('uploads','images')));

백엔드 코드에 express.static 미들웨어를 추가하여 정적 파일 서빙 설정

/uploads/images로 시작하는 URL에 대한 요청을 해당 폴더의 파일로 서빙하도록 설정

path 모듈을 사용하여 폴더 경로를 생성하여 express.static에 전달

path.join() 은 자동으로 / 를 붙여서 생성 uploads/images

	* express.static 미들웨어의 역할:

	express.static은 Express에서 정적 파일을 제공하기 위한 빌트인 미들웨어이다.

	클라이언트에게 서버의 특정 디렉토리 안에 있는 파일을 직접 제공할 때 사용된다.

	파일 서빙을 위해 사용자의 브라우저가 서버에 파일을 직접 요청할 수 있도록 허용한다.

	* path 모듈을 사용한 폴더 경로 생성:

	path 모듈은 파일 및 디렉토리 경로를 조작하기 위한 유틸리티를 제공한다.

	path.join() 메서드를 사용하여 현재 디렉토리(__dirname)에 uploads/images를 합쳐서 절대 경로를 생성한다.

	( 다른 파일들은 모두 잠겨 있지만 이 폴더에 있는 파일들은 요청과 함께 반환 )

	( app.use('/uploads/images', express.static(path.join('uploads','images'))); )

**서버 사이드 동작 원리 이해**

Node/Express에서 파일은 미들웨어를 통해 제어됨

서버 외부에서 직접 파일 액세스 불가능

미들웨어를 통해 처리된 요청만이 서버에서 처리됨

	* 파일은 미들웨어를 통해 제어:

	클라이언트가 서버에 파일을 요청할 때, 미들웨어를 통해 해당 파일에 대한 처리를 수행할 수 있다.

	서버에 저장된 파일을 직접 노출하지 않고, 미들웨어를 사용하여 파일을 읽거나 변형하여 클라이언트에게 전달할 수 있다.

	이는 보안 및 접근 제어를 강화하고, 파일에 대한 직접적인 액세스를 방지하는데 도움이 된다. ( 주소를 입력해서 파일에 접근하는걸 방지 )


	* 서버 외부에서 직접 파일 액세스 불가능:

	서버에서 파일에 대한 처리는 미들웨어를 통해서만 이루어진다.

	서버의 파일 시스템에 직접적인 액세스를 할 수 없도록 보안을 강화한다.

	미들웨어를 통해 요청된 파일에 대한 적절한 권한 및 검증 절차를 수행할 수 있다.


	* 미들웨어를 통해 처리된 요청만이 서버에서 처리됨:

	클라이언트가 보낸 모든 요청은 Express 애플리케이션 내의 미들웨어를 통과한다.

	특정 미들웨어에서 요청에 대한 처리가 이루어지면, 그 이후의 미들웨어나 라우팅 핸들러로 전달된다.

	따라서 미들웨어를 통과하지 않은 요청은 서버에서 처리되지 않고, 특정 미들웨어에서 처리된 요청만이 서버의 로직에 의해 처리된다.

	( 현재 파일에 접근 불가능 한것도 uploads/images 이런 라우터가 없다 )
```

# 새로운 장소에 대한 이미지 업로드하기

```jsx
import React, { useContext } from 'react'
import { useNavigate } from 'react-router-dom'
import Input from '../../shared/components/FormElements/Input'
import Button from '../../shared/components/FormElements/Button'
import { VALIDATOR_REQUIRE, VALIDATOR_MINLENGTH } from '../../shared/util/validators'
import { useForm } from '../../shared/hooks/form-hook'
import './PlaceForm.css'
import { useHttpClient } from '../../shared/hooks/http-hook'
import { AuthContext } from '../../shared/context/auth-context'
import ErrorModal from '../../shared/components/UIElements/ErrorModal'
import LoadingSpinner from '../../shared/components/UIElements/LoadingSpinner'
import ImageUpload from '../../shared/components/FormElements/ImageUpload'

const NewPlace = () => {
  const navigate = useNavigate()
  const auth = useContext(AuthContext)
  const { isLoading, error, sendRequest, clearError } = useHttpClient()
  const [formState, inputHandler] = useForm(
    {
      title: {
        value: '',
        isValid: false,
      },
      description: {
        value: '',
        isValid: false,
      },
      address: {
        value: '',
        isValid: false,
      },
      image: {
        value: '',
        isValid: false,
      },
    },
    false
  )

  const placeSubmitHandler = async (event) => {
    event.preventDefault()
    try {
      const formData = new FormData()
      formData.append('title', formState.inputs.title.value)
      formData.append('description', formState.inputs.description.value)
      formData.append('address', formState.inputs.address.value)
      formData.append('creator', auth.userId)
      formData.append('image', formState.inputs.image.value)

      const responseData = await sendRequest('http://localhost:5000/api/places', 'POST', formData)
      navigate('/')
    } catch (error) {}
  }

  return (
    <>
      <ErrorModal error={error} onClear={clearError} />
      <form className="place-form" onSubmit={placeSubmitHandler}>
        {isLoading && <LoadingSpinner asOverlay />}
        <Input
          id="title"
          element="input"
          type="text"
          label="Title"
          validators={[VALIDATOR_REQUIRE()]}
          errorText="Please enter a valid title."
          onInput={inputHandler}
        />
        <ImageUpload id="image" onInput={inputHandler} errorText="이미지를 업로드 하세요" />
        <Input
          id="description"
          element="textarea"
          label="Description"
          validators={[VALIDATOR_MINLENGTH(5)]}
          errorText="Please enter a valid description (at least 5 characters)."
          onInput={inputHandler}
        />
        <Input
          id="address"
          element="input"
          label="Address"
          validators={[VALIDATOR_REQUIRE()]}
          errorText="Please enter a valid address."
          onInput={inputHandler}
        />
        <Button type="submit" disabled={!formState.isValid}>
          ADD PLACE
        </Button>
      </form>
    </>
  )
}

export default NewPlace
```

```jsx
  **폼에 관련된 초기화를 위해서 image 부분을 추가해주고**

	const [formState, inputHandler] = useForm(
    {
      title: {
        value: '',
        isValid: false
      },
      description: {
        value: '',
        isValid: false
      },
      address: {
        value: '',
        isValid: false
      },
      image : {
        value : '',
        isValid : false
      }
    },
    false
  );

**폼데이터로 값을 보내도록 해준다**

  const placeSubmitHandler = async event => {
    event.preventDefault();
    try {
      const formData = new FormData();
      formData.append('title',formState.inputs.title.value)
      formData.append('description',formState.inputs.description.value)
      formData.append('address',formState.inputs.address.value)
      formData.append('creator',auth.userId)
      formData.append('image',formState.inputs.image.value)

     const responseData = await sendRequest(
        'http://localhost:5000/api/places',
        'POST',
        formData
      );
      navigate('/');

    } catch (error) {

    }
  };

```

```jsx
// Express 프레임워크를 가져옵니다.
const express = require('express')
const { check } = require('express-validator')

// 사용자 컨트롤러 로직을 가져옵니다.
const usersController = require('../controllers/users-controllers')

// Express의 라우터를 생성합니다.
const router = express.Router()

// 파일 업로드 미들웨어를 가져옵니다.
const fileUpload = require('../middleware/file-upload')

// 사용자 목록을 반환하는 라우트
router.get('/', usersController.getUsers)

// 사용자 가입을 처리하는 라우트
router.post(
  '/signup',
  // 파일 업로드 미들웨어를 실행합니다. 하나의 이미지 파일만 처리하며, 'image'라는 필드에서 파일을 받습니다.
  fileUpload.single('image'),

  // 사용자 입력 유효성을 검사하는 미들웨어를 설정합니다.
  [
    // 'name' 필드가 비어있는지 확인합니다.
    check('name').not().isEmpty(),

    // 'email' 필드를 정규화하고 이메일 형식인지 확인합니다.
    check('email')
      .normalizeEmail() // Test@test.com => test@test.com 변환
      .isEmail(), // 이메일 형식인지 확인

    // 'password' 필드가 최소 6자 이상인지 확인합니다.
    check('password').isLength({ min: 6 }),
  ],

  // 가입 로직을 처리하는 컨트롤러 함수
  usersController.signup
)

// 사용자 로그인을 처리하는 라우트
router.post('/login', usersController.login)

// 라우터를 모듈로 내보냅니다.
module.exports = router
```

```jsx
서버의 장소 라우터에

// 파일 업로드 미들웨어를 가져옵니다.
const fileUpload = require('../middleware/file-upload');

fileUpload.single('image'),

// single('image') 부분은 하나의 파일만 업로드하며, 해당 파일은 클라이언트가 요청한 본문(body) 내의 'image' 키에서 찾아진다는 의미

를 추가해주고
```

```jsx
const { v4: uuid } = require('uuid')
const { validationResult } = require('express-validator')

// HttpError 모델을 불러와 사용합니다.
const HttpError = require('../models/http-error')
const getCoordsForAddress = require('../util/location')
const Place = require('../models/places')
const User = require('../models/user')
const { default: mongoose } = require('mongoose')

// 특정 장소 ID에 대한 정보를 반환하는 함수입니다.
const getPlacesById = async (req, res, next) => {
  // 요청 파라미터에서 장소 ID를 추출합니다.
  const placeId = req.params.pid

  let places

  try {
    places = await Place.findById(placeId)
  } catch (err) {
    const error = new HttpError('오류발생 장소를 찾지 못했습니다', 500)
    return next(error)
  }

  // 만약 장소를 찾지 못했다면 HttpError를 발생시키지 않고, next 함수를 통해 에러를 전달합니다.
  if (!places) {
    const error = new HttpError('유효한 장소 아이디를 찾지 못했습니다', 404)
    return next(error)
  }

  // 찾은 장소를 JSON 형태로 응답합니다.
  res.json({ places: places.toObject({ getters: true }) })
}

// 특정 사용자 ID에 대한 장소 정보를 반환하는 함수입니다.
const getPlaceByUserId = async (req, res, next) => {
  // 요청 파라미터에서 사용자 ID를 추출합니다.
  const userId = req.params.uid

  // let place;
  let userWithPlaces
  try {
    userWithPlaces = await User.findById(userId).populate('places')
  } catch (err) {
    const error = new HttpError('장소를 불러오는데 실패했습니다', 500)
    return next(error)
  }

  // 만약 사용자를 찾지 못했다면 HttpError를 발생시킵니다.
  if (!userWithPlaces || userWithPlaces.places.length === 0) {
    return next(new HttpError('유저를 찾을 수 없습니다', 404))
  }

  // 찾은 장소를 JSON 형태로 응답합니다.
  res.json({ places: userWithPlaces.places.map((place) => place.toObject({ getters: true })) })
}

const createPlace = async (req, res, next) => {
  // express-validator를 사용하여 요청의 유효성을 검사합니다.
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    console.log(errors)
    // 에러가 배열로 나와서 첫 번째 인덱스의 경로를 사용하여 클라이언트에게 에러를 전달합니다.
    next(new HttpError(`${errors.errors[0].path}는 비어있을 수 없습니다`, 422))
  }

  // 요청에서 필요한 데이터를 추출합니다.
  const { title, description, address, creator } = req.body

  let coordinates

  try {
    // 주소를 좌표로 변환하는 유틸리티 함수를 사용하여 좌표를 얻습니다.
    coordinates = await getCoordsForAddress(address)
  } catch (error) {
    // 좌표 얻기가 실패하면 에러를 핸들링하여 다음 미들웨어로 전달합니다.
    return next(error)
  }

  // 새로운 장소 객체를 생성합니다.
  const createPlace = new Place({
    title,
    description,
    address,
    location: coordinates, // 얻은 좌표 정보를 사용하여 새로운 장소의 위치를 설정합니다.
    image: req.file.path,
    creator,
  })

  let user
  try {
    // 생성자(creator) ID를 사용하여 해당 사용자를 데이터베이스에서 찾습니다.
    user = await User.findById(creator)
  } catch (err) {
    const error = new HttpError('새로운 장소 추가에 실패 했습니다', 500)
    return next(error)
  }

  // 찾은 사용자가 없을 경우 에러를 생성하여 핸들링합니다.
  if (!user) {
    const error = new HttpError('제공된 ID의 유저를 찾을 수 없습니다', 404)
    return next(error)
  }

  try {
    // Mongoose 세션을 시작하고 트랜잭션을 시작합니다.
    const sess = await mongoose.startSession()
    sess.startTransaction()

    // 새로운 장소를 데이터베이스에 저장합니다.
    await createPlace.save({ session: sess })

    // 사용자 문서에 새로운 장소의 ID를 추가합니다.
    user.places.push(createPlace)
    await user.save({ session: sess })

    // 트랜잭션을 커밋합니다.
    await sess.commitTransaction()
  } catch (err) {
    console.log(err)
    const error = new HttpError('새 장소 추가 실패', 500)
    // 저장 중에 에러가 발생하면 500 상태 코드와 함께 에러를 생성합니다.
    return next(error)
  }

  // 클라이언트에게 성공적인 응답을 전송합니다.
  res.status(201).json({ place: createPlace })
}

const updatePlaceById = async (req, res, next) => {
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    console.log(errors)
    throw new HttpError(`${errors.errors[0].path}는 비어있을 수 없습니다`, 422)
  }
  // 요청에서 필요한 데이터를 추출합니다.
  const { title, description } = req.body
  const placeId = req.params.pid

  let place
  try {
    place = await Place.findById(placeId)
  } catch (err) {
    const error = new HttpError('업데이트 실패', 500)
    return next(error)
  }

  place.title = title
  place.description = description

  try {
    await place.save()
  } catch (err) {
    const error = new HttpError('문제가 발생하였습니다 업데이트 할 수 없습니다', 500)
    return next(error)
  }

  // 클라이언트에게 업데이트된 장소 정보를 응답합니다.
  res.status(200).json({ place: place })
}

const deletePlaceById = async (req, res, next) => {
  // 요청 파라미터에서 장소의 ID를 가져옵니다.
  const placeId = req.params.pid
  let place

  // 장소를 찾아서 해당 ID로 검색한 결과를 가져옵니다.
  try {
    place = await Place.findById(placeId).populate('creator')
  } catch (err) {
    // 에러가 발생하면 500 상태 코드와 함께 에러 메시지를 반환합니다.
    const error = new HttpError('문제발생 삭제에 실패했습니다', 500)
    return next(error)
  }

  // 검색된 장소가 없으면 404 상태 코드와 함께 에러 메시지를 반환합니다.
  if (!place) {
    const error = new HttpError('장소 id 를 찾지 못하였습니다', 404)
    return next(error)
  }

  // MongoDB 세션을 사용하여 트랜잭션을 시작합니다.
  try {
    const sess = await mongoose.startSession()
    sess.startTransaction()

    // 검색된 장소를 삭제합니다.
    await place.deleteOne({ session: sess })

    // 해당 장소를 생성한 사용자의 데이터에서 해당 장소를 제거합니다.
    place.creator.places.pull(place)

    // 수정된 사용자 데이터를 저장합니다.
    await place.creator.save({ session: sess })

    // 트랜잭션을 커밋하여 변경 사항을 영구적으로 반영합니다.
    await sess.commitTransaction()
  } catch (err) {
    // 트랜잭션 도중 에러가 발생하면 500 상태 코드와 함께 에러 메시지를 반환합니다.
    const error = new HttpError('삭제에 실패했습니다', 500)
    return next(error)
  }

  // 모든 작업이 성공적으로 완료되면 200 상태 코드와 함께 성공 메시지를 반환합니다.
  res.status(200).json({ message: '삭제성공' })
}

// 외부에서 사용할 수 있도록 함수를 내보냅니다.
exports.getPlacesById = getPlacesById
exports.getPlaceByUserId = getPlaceByUserId
exports.createPlace = createPlace
exports.updatePlaceById = updatePlaceById
exports.deletePlaceById = deletePlaceById
```

```jsx
    // 새로운 장소 객체를 생성합니다.
    const createPlace = new Place({
        title,
        description,
        address,
        location: coordinates, // 얻은 좌표 정보를 사용하여 새로운 장소의 위치를 설정합니다.
        image: req.file.path,
        creator
    });

image: req.file.path 라는 새로운 값을 image 에 넣어준다
```

```jsx
import React, { useContext, useState } from 'react'

// 컴포넌트 및 훅 임포트
import Card from '../../shared/components/UIElements/Card'
import Button from '../../shared/components/FormElements/Button'
import Modal from '../../shared/components/UIElements/Modal'
import ErrorModal from '../../shared/components/UIElements/ErrorModal'
import LoadingSpinner from '../../shared/components/UIElements/LoadingSpinner'
import { AuthContext } from '../../shared/context/auth-context'
import { useHttpClient } from '../../shared/hooks/http-hook'

// CSS 파일 임포트
import './PlaceItem.css'

const PlaceItem = (props) => {
  // AuthContext로부터 사용자 인증 정보를 가져옴
  const auth = useContext(AuthContext)

  // useHttpClient 훅을 사용하여 HTTP 요청과 에러 처리를 담당하는 객체를 가져옴
  const { isLoading, error, sendRequest, clearError } = useHttpClient()

  // 지도 모달 열기/닫기를 위한 상태 관리
  const [showMap, setShowMap] = useState(false)

  // 삭제 확인 모달 열기/닫기를 위한 상태 관리
  const [showConfirmModal, setShowConfirmModal] = useState(false)

  // 지도 모달 열기 핸들러
  const openMapHandler = () => setShowMap(true)

  // 지도 모달 닫기 핸들러
  const closeMapHandler = () => setShowMap(false)

  // 삭제 확인 모달 열기 핸들러
  const showDeleteWarningHandler = () => {
    setShowConfirmModal(true)
  }

  // 삭제 확인 모달 취소 핸들러
  const cancelDeleteHandler = () => {
    setShowConfirmModal(false)
  }

  // 삭제 확인 모달에서 DELETE 버튼 클릭 시 처리 핸들러
  const confirmDeleteHandler = async () => {
    setShowConfirmModal(false)
    try {
      // 서버에 DELETE 요청을 보냄
      await sendRequest(`http://localhost:5000/api/places/${props.id}`, 'DELETE')
    } catch (error) {
      // 에러 발생 시 아무 동작 없음 (추가 로직 필요)
    }
    // 삭제된 장소의 ID를 onDelete 콜백 함수를 통해 상위 컴포넌트에 전달
    props.onDelete(props.id)
  }

  return (
    <>
      {/* 에러 모달 컴포넌트 */}
      <ErrorModal error={error} onClear={clearError} />

      {/* 지도 모달 컴포넌트 */}
      <Modal
        show={showMap}
        onCancel={closeMapHandler}
        header={props.address}
        contentClass="place-item__modal-content"
        footerClass="place-item__modal-actions"
        footer={<Button onClick={closeMapHandler}>CLOSE</Button>}
      >
        <div className="map-container">
          <h2>THE MAP</h2>
        </div>
      </Modal>

      {/* 삭제 확인 모달 컴포넌트 */}
      <Modal
        show={showConfirmModal}
        onCancel={cancelDeleteHandler}
        header="Are you sure?"
        footerClass="place-item__modal-actions"
        footer={
          <>
            <Button inverse onClick={cancelDeleteHandler}>
              CANCEL
            </Button>
            <Button danger onClick={confirmDeleteHandler}>
              DELETE
            </Button>
          </>
        }
      >
        <p>정말 삭제 하시겠습니까?</p>
      </Modal>

      {/* 장소 항목 */}
      <li className="place-item">
        {/* 카드 컴포넌트 */}
        <Card className="place-item__content">
          {isLoading && <LoadingSpinner asOverlay />}

          {/* 장소 이미지 */}
          <div className="place-item__image">
            <img src={`http://localhost:5000/${props.image}`} alt={props.title} />
          </div>

          {/* 장소 정보 */}
          <div className="place-item__info">
            <h2>{props.title}</h2>
            <h3>{props.address}</h3>
            <p>{props.description}</p>
          </div>

          {/* 장소 액션 버튼 */}
          <div className="place-item__actions">
            {/* 지도 보기 버튼 */}
            <Button inverse onClick={openMapHandler}>
              VIEW ON MAP
            </Button>

            {/* 사용자가 로그인 중일 때만 편집 및 삭제 버튼 표시 */}
            {auth.userId === props.creatorId && <Button to={`/places/${props.id}`}>EDIT</Button>}

            {auth.userId === props.creatorId && (
              <Button danger onClick={showDeleteWarningHandler}>
                DELETE
              </Button>
            )}
          </div>
        </Card>
      </li>
    </>
  )
}

export default PlaceItem
```

```jsx
<img src={`http://localhost:5000/${props.image}`} alt={props.title} />

이런식으로 장소 아이템.js 에서 이미지를 불러와 출력 가능하도록 수정해준다
```

# 장소가 삭제되면 이미지 삭제하기

```jsx
const { v4: uuid } = require('uuid')
const { validationResult } = require('express-validator')
const fs = require('fs')

// HttpError 모델을 불러와 사용합니다.
const HttpError = require('../models/http-error')
const getCoordsForAddress = require('../util/location')
const Place = require('../models/places')
const User = require('../models/user')
const { default: mongoose } = require('mongoose')

// 특정 장소 ID에 대한 정보를 반환하는 함수입니다.
const getPlacesById = async (req, res, next) => {
  // 요청 파라미터에서 장소 ID를 추출합니다.
  const placeId = req.params.pid

  let places

  try {
    places = await Place.findById(placeId)
  } catch (err) {
    const error = new HttpError('오류발생 장소를 찾지 못했습니다', 500)
    return next(error)
  }

  // 만약 장소를 찾지 못했다면 HttpError를 발생시키지 않고, next 함수를 통해 에러를 전달합니다.
  if (!places) {
    const error = new HttpError('유효한 장소 아이디를 찾지 못했습니다', 404)
    return next(error)
  }

  // 찾은 장소를 JSON 형태로 응답합니다.
  res.json({ places: places.toObject({ getters: true }) })
}

// 특정 사용자 ID에 대한 장소 정보를 반환하는 함수입니다.
const getPlaceByUserId = async (req, res, next) => {
  // 요청 파라미터에서 사용자 ID를 추출합니다.
  const userId = req.params.uid

  // let place;
  let userWithPlaces
  try {
    userWithPlaces = await User.findById(userId).populate('places')
  } catch (err) {
    const error = new HttpError('장소를 불러오는데 실패했습니다', 500)
    return next(error)
  }

  // 만약 사용자를 찾지 못했다면 HttpError를 발생시킵니다.
  if (!userWithPlaces || userWithPlaces.places.length === 0) {
    return next(new HttpError('유저를 찾을 수 없습니다', 404))
  }

  // 찾은 장소를 JSON 형태로 응답합니다.
  res.json({ places: userWithPlaces.places.map((place) => place.toObject({ getters: true })) })
}

const createPlace = async (req, res, next) => {
  // express-validator를 사용하여 요청의 유효성을 검사합니다.
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    console.log(errors)
    // 에러가 배열로 나와서 첫 번째 인덱스의 경로를 사용하여 클라이언트에게 에러를 전달합니다.
    next(new HttpError(`${errors.errors[0].path}는 비어있을 수 없습니다`, 422))
  }

  // 요청에서 필요한 데이터를 추출합니다.
  const { title, description, address, creator } = req.body

  let coordinates

  try {
    // 주소를 좌표로 변환하는 유틸리티 함수를 사용하여 좌표를 얻습니다.
    coordinates = await getCoordsForAddress(address)
  } catch (error) {
    // 좌표 얻기가 실패하면 에러를 핸들링하여 다음 미들웨어로 전달합니다.
    return next(error)
  }

  // 새로운 장소 객체를 생성합니다.
  const createPlace = new Place({
    title,
    description,
    address,
    location: coordinates, // 얻은 좌표 정보를 사용하여 새로운 장소의 위치를 설정합니다.
    image: req.file.path,
    creator,
  })

  let user
  try {
    // 생성자(creator) ID를 사용하여 해당 사용자를 데이터베이스에서 찾습니다.
    user = await User.findById(creator)
  } catch (err) {
    const error = new HttpError('새로운 장소 추가에 실패 했습니다', 500)
    return next(error)
  }

  // 찾은 사용자가 없을 경우 에러를 생성하여 핸들링합니다.
  if (!user) {
    const error = new HttpError('제공된 ID의 유저를 찾을 수 없습니다', 404)
    return next(error)
  }

  try {
    // Mongoose 세션을 시작하고 트랜잭션을 시작합니다.
    const sess = await mongoose.startSession()
    sess.startTransaction()

    // 새로운 장소를 데이터베이스에 저장합니다.
    await createPlace.save({ session: sess })

    // 사용자 문서에 새로운 장소의 ID를 추가합니다.
    user.places.push(createPlace)
    await user.save({ session: sess })

    // 트랜잭션을 커밋합니다.
    await sess.commitTransaction()
  } catch (err) {
    console.log(err)
    const error = new HttpError('새 장소 추가 실패', 500)
    // 저장 중에 에러가 발생하면 500 상태 코드와 함께 에러를 생성합니다.
    return next(error)
  }

  // 클라이언트에게 성공적인 응답을 전송합니다.
  res.status(201).json({ place: createPlace })
}

const updatePlaceById = async (req, res, next) => {
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    console.log(errors)
    throw new HttpError(`${errors.errors[0].path}는 비어있을 수 없습니다`, 422)
  }
  // 요청에서 필요한 데이터를 추출합니다.
  const { title, description } = req.body
  const placeId = req.params.pid

  let place
  try {
    place = await Place.findById(placeId)
  } catch (err) {
    const error = new HttpError('업데이트 실패', 500)
    return next(error)
  }

  place.title = title
  place.description = description

  try {
    await place.save()
  } catch (err) {
    const error = new HttpError('문제가 발생하였습니다 업데이트 할 수 없습니다', 500)
    return next(error)
  }

  // 클라이언트에게 업데이트된 장소 정보를 응답합니다.
  res.status(200).json({ place: place })
}

const deletePlaceById = async (req, res, next) => {
  // 요청 파라미터에서 장소의 ID를 가져옵니다.
  const placeId = req.params.pid
  let place

  // 장소를 찾아서 해당 ID로 검색한 결과를 가져옵니다.
  try {
    place = await Place.findById(placeId).populate('creator')
  } catch (err) {
    // 에러가 발생하면 500 상태 코드와 함께 에러 메시지를 반환합니다.
    const error = new HttpError('문제발생 삭제에 실패했습니다', 500)
    return next(error)
  }

  // 검색된 장소가 없으면 404 상태 코드와 함께 에러 메시지를 반환합니다.
  if (!place) {
    const error = new HttpError('장소 id 를 찾지 못하였습니다', 404)
    return next(error)
  }

  // 이미지 주소 가져오기 (req.file.path 로 이미지 주소 들어가 있음)
  const imagePath = place.image

  // MongoDB 세션을 사용하여 트랜잭션을 시작합니다.
  try {
    const sess = await mongoose.startSession()
    sess.startTransaction()

    // 검색된 장소를 삭제합니다.
    await place.deleteOne({ session: sess })

    // 해당 장소를 생성한 사용자의 데이터에서 해당 장소를 제거합니다.
    place.creator.places.pull(place)

    // 수정된 사용자 데이터를 저장합니다.
    await place.creator.save({ session: sess })

    // 트랜잭션을 커밋하여 변경 사항을 영구적으로 반영합니다.
    await sess.commitTransaction()
  } catch (err) {
    // 트랜잭션 도중 에러가 발생하면 500 상태 코드와 함께 에러 메시지를 반환합니다.
    const error = new HttpError('삭제에 실패했습니다', 500)
    return next(error)
  }

  fs.unlink(imagePath, (err) => {
    console.log(err)
  })

  // 모든 작업이 성공적으로 완료되면 200 상태 코드와 함께 성공 메시지를 반환합니다.
  res.status(200).json({ message: '삭제성공' })
}

// 외부에서 사용할 수 있도록 함수를 내보냅니다.
exports.getPlacesById = getPlacesById
exports.getPlaceByUserId = getPlaceByUserId
exports.createPlace = createPlace
exports.updatePlaceById = updatePlaceById
exports.deletePlaceById = deletePlaceById
```

```jsx
장소를 삭제하면 장소에 포함된 이미지도 삭제하고 싶다

그러나 반드시 그렇게 할 필욘 없다 스크립트를 작성해서 매달 삭제를 하게끔 할 수도 있고 직접 삭제를 하거나

머신 러닝 알고리즘을 위해 삭제하지 않고 그대로 둘 수도 있다

하지만 삭제하고 싶다면 어렵지 않게 가능하다

const fs = require('fs');

삭제를 위해서 fs 모듈을 불러와주고

// 이미지 주소 가져오기 (req.file.path 로 이미지 주소 들어가 있음)
const imagePath = place.image;

이미지의 주소를 가져와준다

fs.unlink(imagePath, err => {
		console.log(err);
});

그리고 삭제를 진행해주고 실패했을 시 오류를 출력해준다
```
