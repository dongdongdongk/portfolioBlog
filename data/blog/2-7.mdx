---
title: '서블릿2'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## 루트 경로 변경

```sql
콘솔창의 Server 에서 webmodule - edit 에 들어가면 루트 경로를 변경할 수 있다

portname 8080 을 80 으로 변경하면 http://localhost/ 로 사용가능
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380880/notion-blog/_/_/JSP/_2/Untitled.png)

---

## Parameter , Attribute

```sql
https://search.naver.com/search.naver?
where=nexearch&sm=top_hty&fbm=0&ie=utf8&query=%EB%89%B4%EC%A7%84%EC%8A%A4

파라미터 url 구분 ? 으로 표시

파라미터 구분 &

ex)http://localhost/product.do?name=test&price=20000

1.**Parameter**
	- Client에서 Server로 보내는 데이타
	- 이름과 값으로 구성
	- 이름 : String, 값 : String (이름과 값 모두 스트링 타입이다)

2. **Attribute(method)**
	- Server 내에서 서로 데이터를 주고 받을 때 사용
	- 내장객체에 존재
	- 이름과 값으로 구성
	- 이름 : String, 값 : Object

```

---

## GET,POST

```sql
요청을 보낼때 구성요소
필수
URL(ip:port/경로)
Method 형식중 하나  get or post 가 들어가야함

선택사항
파라미터(클라이언트가 서버로 보내는데이터)(이름=값)

1. GET
	- 단순 요청
	- 파라미터가 주소창에 노출(파라미터가 Header에 담김)
	- 작은 용량을 보낼 때

	A. Web Browser의 주소창에 직접 URL을 입력하는 경우
	B. A(link)태그 를 이용하는 경우
	C. Javascript의 location.href를 사용하는 경우
	D. Ajax 메서드에서 Get을 선택한 경우
	E. Form 태그 메서드에서 Get을 선택한 경우

2. POST
	- 보안에 민감한 요청
	- 파라미터가 주소창에 노출 X (파라미터가 Body에 담김)
	- 큰용량도 보낼 수 있음(이미지)

	A. Form 태그 메서드에서 Post를 선택한 경우
	B. Ajax 메서드에서 Post를 선택한 경우
```

---

## 개발자 모드에서 파라미터 확인하기

```sql
f12 를 눌러 개발자 모드에서 클라이언트 에서 서버로 보내는 파라미터를 확인할 수 있다.

ex> http://localhost/?name=iu&age=31

파라미터는 여러개 보낼 수 있으며 현재 보낸 파라미터는

name = iu

age = 31 이다.

*GET 방식의 파라미터로 주소창에 노출된다.

*파라미터와 url 구분은 ? 파라미터 끼리의 구분은 &
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380879/notion-blog/_/_/JSP/_2/Untitled_1.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380879/notion-blog/_/_/JSP/_2/Untitled_2.png)

---

## doGet, doPost 호출 방식

```sql
1.url 이 들어오고

2.HomeController 이 Service 가 req,res 매개변수를 가지고  메서드 호출

3. if req 가 Get이라면 doGet 아니라면 doPost (res 는 응답객체)

*request 매개변수에서 get매소드를 사용해서 정보를 꺼낼수도 있다

(ex .getMethod(); .getRequestURL();)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380879/notion-blog/_/_/JSP/_2/Untitled_3.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_16.png)

---

## Getparameter 로 request 값 받고 출력

```sql
http://localhost/?name=iu&age=31 를 입력받을시 아래

request.getParameter(); 로 이름과 값을 받아서 출력할 수 있다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380879/notion-blog/_/_/JSP/_2/Untitled_4.png)

```sql
http://localhost/?name=iu&age=31&money=100&money=200

위와 같이 money 의 값이 여러개 일때는 배열로 값을 받을 수 있다

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380879/notion-blog/_/_/JSP/_2/Untitled_5.png)

---

## 내장 객체

```sql
Tomcat(WAS) 가 생성하는 객체 (req,res) 이 둘을 **내장객체** 라고 한다

(우리가 선언하지 않은객체)

(HomeController,ProductController 은 우리가 선언했으니 내장객체가 아니다)

Life Cycle 중요 (생명주기) : 요청이 발생하면 생성되고 요청이 나가면 사라진다.

Request & Response : HttpServletRequest & Response : HttpServletResponse
	- 요청이 발생하면 생성되고, 응답이 나가면 소멸
	- RequestScope (Request영역)
	- 주요 메서드
	1) String value = getParameter("파라미터이름")           : Parameter 값 한개 반환
	2) String [] values = getParameterValues("파라미터이름") : 한개의 파라미터이름으로 여러개의 값이 올때
	3) Enumeration <String> en = getParameterNames()         : Parameter의 이름을 가지고 오고 싶을 때
	4) setAttribute("속성명", 값)                            : jsp로 보낼 데이터

```

---

## 내장 객체 경로

```sql
서버 내에서 이동한다

servlet => JSP (둘다 서버내에 있다 , 백엔드 )
서버 내에서 데이터를 이동시키고 있다 (req,res)

 **Attribute(method)**
	- **Server 내에서 서로 데이터를 주고 받을 때 사용**
	- 내장객체에 존재
	- 이름과 값으로 구성
	- 이름 : String, 값 : Object (자바의 모든 타입을 담을 수 있음)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380880/notion-blog/_/_/JSP/_2/Untitled_6.png)

```sql
이런식으로 request.setAttribute("list",ar); 을 JSP 로 보내면 서버내에서 테이터를

전송 하기 때문에 내장 객체를 사용한다 (Attribute)(JSP 에는 보이지는 않지만 req,res 가 존재
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380880/notion-blog/_/_/JSP/_2/Untitled_7.png)

```sql
위의 방식으로 사용하기 불편함이 있어 만들어 낸것이 JSP

JSP 에서는 자바 코드를 작성 가능한데 <% %> 사이에 작성한다.

HTML내에 Java코드 작성
JSP는 실행시 다시 Servlet으로 변환 됨

Java 코드 방식
1. 스크립틀릿
	<%  Java 코드 작성 %>    : 스크립틀릿
	<%= java의 변수나 값 %>  : HTML에 출력 (표현식)
	<%! 변수나 메서드 선언 %> : 선언문

2. JSP 주석
	<%--    --%>
```

![화면 캡처 2023-02-02 022744.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380878/notion-blog/_/_/JSP/_2/_ED_99_94_EB_A9_B4_EC_BA_A1_EC_B2_98_2023-02-02_022744.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380880/notion-blog/_/_/JSP/_2/Untitled_8.png)

```sql
String name = (String)request.getAttribute("list"); // 이름으로 구분해서 req 꺼내기

Attreibute 메소드 는 리턴타입이 object 이다

결과적으로 이를 String 타입으로 이름을 불러와 사용하려면 (String) 으로 String 타입으로

변환 시켜줘야 한다. (모든 오브젝트가 스트링은 아니다 =(다형성))

<%= %> 는 출력문
```

## JSP 반복문

```sql
<%
for(productDTO productDTO:ar) {
<%= %>
}
%>

반복문 내에서 출력문을 사용하려 하면 괄호의 끝을 위와 같이 인식해서 문제가 생긴다.

<%
for(ProductDTO productDTO:ar) {
<h3><%= productDTO.getProductNum() %> <h3>

<%} %>

이런식으로 사용해준다.
(쉽게 생각해서 그냥 끝 괄호에 <%} %> 해주자 애
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380880/notion-blog/_/_/JSP/_2/Untitled_9.png)

오늘 점심 추천
