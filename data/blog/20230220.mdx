---
title: '20230220'
date: '2026-09-02'
category: '풀스택/JavaScript'
tags: ['풀스택', 'JavaScript']
draft: false
summary: ''
---

```jsx
1. Java와는 상관이 없음

2. Web Browser에서 실행

3. OOP 언어중 하나

4. 인터프리터언어 (Java는 컴파일 언어)   //한줄한줄 실시간으로 읽어나감
	- 컴파일언어는 컴파일시 에러가 뜨면 실행 X (통번역)
	- 인터프리터언어는 실시간 컴파일 언어      (동시통역)
	- 실행중 오류(에러)가 있으면 그 때 중지

5. Web에서 동적인 요소 담당

6. JavaScript 용도
	- Element는 태그, 속성, contents로 구성 되어 있고
	- Element의 태그, 속성, contents를 변경, 삭제, 추가
	- Style(css) 변경, 삭제, 추가
```

### 1. 작성위치

```jsx
- css 작성위치와 비슷

1. Inline
	- 태그(Element)내에 script 속성에 작성

2. Internal
	- <head> 태그내에 <script>안에 자바스크립트</script> 태그내에 코드 작성
	- <body> 태그내에 <script>안에 자바스크립트</script> 태그내에 코드 작성

3. External                               //제일 많이 이용
	- 외부에 JS 파일을 만들고 import
	- <script src="js파일의 경로" type="text/javascript"></script>
	- import 하는 <script> 태그 내에 코드 작성 금지 (2번과 혼합해서 사용x)

- 통상 </body> 끝나기전 사용
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381276/notion-blog/_/JavaScript/_/20230220/Untitled.png)

```sql
js 는 여기서 오류를 확인
```

### 2. 변수와 데이터타입

```jsx
1. 데이터타입
	1) 숫자형     : Number
	2) 문자열     : String, 앞뒤로 " ", 또는 앞뒤로 ' '
	3) 논리형     : boolean
	4) Null       :
	5) Undefined  : 값이 없는 변수, 변수 사용시 초기화 하지 않으면 사용 할 때 에러
	6) Object     :

- 참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Data_structures

2. 변수 선언
	- 데이터타입 변수명;
	- JS는 Dynamic Type 방식(실행(값을 대입할때) 시에 데이터타입이 결정됨)

	1) var 변수명   : (지역변수, 전역변수 문제가 발생할 여지가 있음)
	2) let 변수명   : var 대신 사용
	3) const 변수명 : 상수 (java의 final)
```

### 3. 연산자

```jsx
참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Expressions_and_Operators

1. 산술연산자 : +, -, /, *, %
2. 비교연산자 : >, >=, <, <=, ==, !=, ===(값도 같고, 타입도 같으면 true), !==
3. 논리연산자 : &&, ||, !
4. 증감연산자 : ++, --
5. 타입연산자 : typeof data : data의 type명을 알고 싶을 때 사용
```

### 4. 제어문

```jsx
참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Control_flow_and_error_handling
참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Loops_and_iteration

1. 단일 if
		if(조건식){}

2. if(조건식){} else {}

3. if(조건식){} else if(조건식){} else {}

4. switch(){
		case 값1:
		break;

		case 값2:
		break;

		default :
		break;
 }

5. while(조건식){}

6. do {}while(조건식);

7. for(초기식;조건식;증감식){}

8. for(변수명 in 컬렉션변수명){
			//변수명에 인덱스 번호가 대입
	}
9. for(변수명 of 컬렉션변수명){
		//변수명에 컬렉션에서 하나 꺼낸 값이 대입
	}

10. 반복문의 보조문
	1) break;
	2) continue;
```

### 5. 배열 (Array)

```jsx
- 참조 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Global_Objects/Array

- 배열 생성시 크기를 지정하지 않음
- 객체

1. 빈 배열 생성
	1) let ar = new Array();    //쓰지 말것을 권장
	2) let ar = [];             //권장 사항

2. 데이터가 있는 배열 생성
	let ar = [1, 2, 'a', 3.12, true]

3. 주요 메서드
	1) push(data)    : 배열 끝에 요소를 추가하고 새로운 길이 (length) 반환
	2) pop()         : 배열 끝에 요소를 삭제하고 삭제한 요소를 반환
	3) unshift(data) : 배열 처음에 요소를 추가하고 새로운 길이 (length) 반환
	4) shift()       : 배열 처음 요소를 삭제하고 삭제한 요소를 반환
	5) sort()        : 정렬, 오름 차순
	6) reverse()     : 순서 변경
	7) foreach()     : 반복문 for 대신 사용가능한 함수
			 foreach(function(변수명1, 변수명2, 배열의변수명){
						-- 변수명1 : 배열에서 꺼낸 값
						-- 변수명2 : 인덱스번호
				});
	8) includes(찾을 Data): 배열에 해당 Data가 존재 여부 판단

4. 주요 변수
	1) lenth      : 배열의 길이를 담고 있는 변수
```

### 6. BOM and DOM

```jsx
JS는 화면을 구성하는 모든것들을 Object로 생성하고 관리
JS가 아닌 웹브라우저가 제공하는 객체

BOM(Browser Object Model)

1. window
	- 참조 : https://developer.mozilla.org/ko/docs/Web/API/Window
	- 최상위 객체(참조변수명은 생략 가능)
	a. document      : HTML 문서 관리   //가장 많이 사용
	b. location      : URL 관리
	c. history       : 뒤로, 앞으로 가기...
	d. navigator     : Web Browser 정보
	e. screen        : 클라이언트의 모니터 화면

3. location
	- 주요 변수
		1)href         : 변수에 저장된 URL주소로 요청이 발생

DOM (Document Object Model)
	- 프로그래밍 언어가 DOM 구조에 접근 할 수 있도록 방법을 제공하는 구조

```

```jsx
<h1 id="c1">SAMPLE</h1>    // h1 = 태그,  id = 속성,  sample = 콘텐츠  전체 = 엘리먼트

이 3가지를 모두 바꿀수있다. 순서는

1. 자바 스크립트로 긁어오는 작업이 필요하다.

const btn = document.getElementById('btn'); //가져오기만 한것
const btn2 = document.getElementById('btn2');
const btn3 = document.getElementById('btn3'); //const 는 자바의 final 과 비슷한것

const id = document.getElementById   // 아이디 선택자를 통해 가져온다 (여러종류가 있음)
```

```jsx
2.어떤걸 바꿀지 선택한다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381276/notion-blog/_/JavaScript/_/20230220/Untitled_1.png)

```jsx

let c1 = document.getElemntById("c1"); // 자바는 변수타입이 없다 let에 넣으면 자동으로 변환

//꺼내오는 작업이다 let c1이라는 변수명에 "c1" 이라는 아이디를 가져온다

console.log(c1.innerText);    //consoler.log 는 출력하는데 쓰는것

innerText는 텍스트만 출력하는것

innerHTML은 태그까지 모두 출력한다.

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381276/notion-blog/_/JavaScript/_/20230220/Untitled_2.png)

```jsx
btn.addEventListener("click, function(){   //addEventListener 은 때 라는 뜻으로 클릭할때
																					 // 클릭 할 때 등등
// 클릭할 때 내부의 내용 실행

})

c2.innerHTML= 이미지 경로를 덮어써서 이미지를 출력하게 된다

```
