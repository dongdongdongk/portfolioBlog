---
title: '폰트 추가하기'
date: '2026-09-02'
category: '게임 개발/Game Development in JS TS - The Complete Guide (w_'
tags: ['게임 개발', 'Game Development in JS TS - The Complete Guide (w_']
draft: false
summary: ''
---

```jsx
한글을 폰트 없이 그냥 사용하면 글자가 깨진다

이를 해결하기 위해 폰트를 로컬에 다운받아 사용하고

다운 받은 폰트를 적용하는 방법을 설명한다
```

# { 폰트 추가 하기 }

## 폰트 다운 받기

![image.png](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380512/notion-blog/_/Game_Development_in_JS_TS_-_The_Complete_Guide_w_/_/image.png)

```jsx
한글 폰트를 다운받고 파일을 원하는 경로에 넣어준다

ex) ./assets/font/BMJUA_ttf.ttf
```

## 폰트 적용 세팅

```jsx
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body {
        /* padding: 200px; */
        margin: 200px;
        @font-face {
            font-family: 'CustomFont';
            src: url('./assets/font/BMJUA_ttf.ttf') format('truetype');
            font-weight: normal;
        }
      }
    </style>
  </head>
  <body>
  </body>
</html>

```

```jsx
 index.html 의 head 에 style 에 위와 같이 코드를 추가해 준다
```

```jsx
        @font-face {
            font-family: 'CustomFont';
            src: url('./assets/font/BMJUA_ttf.ttf') format('truetype');
            font-weight: normal;
        }
```

```jsx
여기서 url 은 로컬이 경로이면 format 은 폰트 포멧이며

font-family 는 내가 사용할 폰트에 붙여주는 이름이다
```

## 폰트 사용하기

```jsx
// import Phaser from "phaser";
import BaseScene from './BaseScene'

class CreditsScene extends BaseScene {
  constructor(config) {
    super('CreditsScene', { ...config, canGoBack: true })

    this.menu = [
      { scene: null, text: '플레이해 주셔서 감사합니다.' },
      { scene: null, text: '-DongK-' },
    ]
  }

  create() {
    super.create()

    this.createMenu(this.menu, (menuItem) => this.setupCreditsStyle(menuItem))
  }

  setupCreditsStyle(menuItem) {
    const textGo = menuItem.textGo
    textGo.setInteractive()
    textGo.setStyle({
      fontFamily: 'CustomFont',
      fontSize: '40px',
      fill: '#000', // 흰색 텍스트
      align: 'center',
    })
  }
}

export default CreditsScene
```

```jsx
위와 같이 setupCreditStyle 함수를 만들어서 fontFamily 에 아까 넣어주고 설정해줬던

폰트 이름을 넣어주고 사용한다
```
