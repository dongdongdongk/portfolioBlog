---
title: '33. 자바 메소드 (3)'
date: '2026-09-02'
category: '백엔드/JAVA'
tags: ['백엔드', 'JAVA']
draft: false
summary: ''
---

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled.png>)

```java
//객체 내부에서는 리턴값이 있거나 없거나 이렇게 호출이 가능하다.
매소드( 매개값1, 매개값2);

// 매소드가 리턴값이 없거나(void) 있어도 받고 싶지 않은경우

public class CalssName { //ClassName 이라는 class선언
	void method1( String p1 , int p2) { // return 값이 없는 method1 선언

내용

}

	void method2() {
		method1( "홍길동", 100); //method2 에서 method1을 호출하고 method1 에 값을 대입해 실행
	}
}

// 클래스 내부여서 method1 이렇게 호출할수 있다.
```

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%201.png>)

```java
// 리턴값 있는 메소드 호출 리턴값 받고 싶은 경우

public class ClassName {
	int method1(int x, int y) { // 2개의 매개값을 받아서
		int result = x + y; // 더하기 한뒤 result 에 저장
		return result; // result 값을 반환
  }

	void method2() {
		int result1 = method1(10, 20); //매개값 10,20로 method1 계산뒤 result 1에 저장
		double result2 = method1(10, 20);// result2 에 저장
	}

 }
// 결과는 result1 은 30 이 되고
// result2 는 타입이 double 이기 때문에 30.0 이 된다.
```

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%202.png>)

**실행Class**

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%203.png>)

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%204.png>)

```java
// 객체 Class 외부에서 객체를 호출할때
Car myCar = new Car(); // 객체 생성

myCar.keyTurnOn(); //return 값이 없을때 변수 선언 x
myCar.run();

int speed = myCar.getSpeed(); //return 값이 있을때 변수 선언후 메소드 호출
```

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%205.png>)

**실행Class**

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%206.png>)

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%207.png>)

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%208.png>)

```java
int plus(int x, int y) { // 변수의 타입,개수,순서 중 하나라도 달라야 한다.
	int result = x + y;
	return result;
}

double plus(double x, double y) {
	double result = x + y;
	return result;
}
```

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%209.png>)

```java
//매개변수 값
plus(10, 20);

plus(10.5, 20.3);

int x = 10;
double y = 20.3;
plus(x, y);

//오버로딩 메소드

int plus(int x, int y) { //plus(10, 20); 이 들어간다
	int result = x + y;
	return result;
}

double plus(double x, double y) { //plus(10.5, 20.3); 이 들어간다
	double result = x + y;
	return result;
}

int x 와 double y 값이 들어갈 경우? double plus 식을 사용한다

JVM은 호출하는 **매개변수** 값,타입을 보고 메소드를 선택한다.
최대한 값이 들어갈수 있는(int 는 double 에 들어갈수 있다.) 메소드 타입을 선택한다.
```

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%2010.png>)

```java
int divide(int x, int y) { }

double divide(int boonja, int boonmo) { }

//위와 같이 매개변수 타입, 개수 ,순서 같은 경우에는 매개변수 이름 달라도 오버로딩 x
```

- println 이 계속 사용될 수 있었던 이유는 괄호 안의 매개변수가 항상 달랐기 때문이다.

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%2011.png>)

**실행 Class**

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%2012.png>)

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%2013.png>)

![Untitled](<33%20%EC%9E%90%EB%B0%94%20%EB%A9%94%EC%86%8C%EB%93%9C%20(3)/Untitled%2014.png>)

```java
선언부 : int divide(int x, int y) { }
```

```java
보이드 : void run() { }
```

```java
 매개변수 : double avg(int x, int y) { }  //  int x 와 int y 가 매개변수
```

```java
리턴문 : int plus(int x, int y) {
						int result = x + y;
						return rsult;   //result 값을 plus 메소드에 저장
				}

				void run() {

				return              // void 에서도 사용할수 있다 여기서는 메소드 종료 기능으로 이용
				}


```

```java
호출 : 객체 클래스 외부에서 호출 Calculator myCalc = new Calculator();

       객체 내부 호출 int speed = myCar.getSpeed();
```

```java
오버로딩 :
int plus(int x, int y) { //plus(10, 20); 이 들어간다
	int result = x + y;
	return result;
}

double plus(double x, double y) { //plus(10.5, 20.3); 이 들어간다
	double result = x + y;
	return result;
}

//매개변수가 다른 타입 개수 순서중 하나라도 달라야 한다.
```
