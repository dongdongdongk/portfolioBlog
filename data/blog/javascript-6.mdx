---
title: '국비 JavaScript 정리'
date: '2026-09-02'
category: '풀스택/JavaScript'
tags: ['풀스택', 'JavaScript']
draft: false
summary: ''
---

# JavaScript 기본

```sql
<h1 id="c1">SAMPLE</h1>

 // h1 = 태그,  id = 속성,  sample = 콘텐츠  전체 = 엘리먼트

이 3가지를 모두 바꿀수있다.
```

## console.log();

```sql
console.log(result);

console.log(출력문)

"" 문자열 또는 변수등을 출력한다 (f12 관리자 모드에서 확인가능)
```

# Element 선택

```jsx
 1. Element 선택(Selector)

	1) ID 선택자
		- window.document.getElementById('id명')

	2) Class 선택자
		- window.document.getElementsByClassName('클래스명')
		- 리턴 타입은 유사배열임, 클래스명이 한개라도 유사배열로 리턴

	3) Tag 선택자
		- window.document.getElementsByTagName('Tag명')
		- 리턴 타입은 배열임, 클래스명이 한개라도 배열로 리턴

	4) querySelectorAll
		- document.querySelectorAll("css 선택자")
		- 리턴은 배열임
		- 주로 클래스나 여러개를 선택할 때 사용
		- :hove, ::after 등 가상선택자(클래스)를 사용 가능

	5) querySelector
		- document.querySelectorAll("css 선택자")[0] 와 같음
		- 주로 id 선택자로 많이 사용
		- querySelectorAll()[0]은 모든 요소를 검색하고 첫번째 요소를 반환 하지만
		- querySelector는 요소를 찾으면 그 즉시 검색을 멈춤 그래서 querySelectorAll보다 더 빠름
		- :hove, ::after 등 가상선택자(클래스)를 사용 가능

```

## getElementById

```sql
const btn = document.getElementById('btn');

const 변수명 = document.getElementById('id');

오브젝트를 가져오는 기능
```

## getElementsByClassName

```sql
const food = document.getElementsByClassName("food");

const 변수명 = document.getElementsByClassName("class 이름");

클래스로 불러오기 (여러개를 동시에 불러올 때 배열로 받음)

값이 하나라도 배열에 담김
```

# Event

```sql
사용자의 행위, 클릭, 입력, 마우스움직, 페이지 닫기, 열기, 스크롤, 사이즈변경
에 따라 내용을 실행시킴

d1.addEventListener("click",function(){});

변수.addEventListener("이벤트",function(){});

 키보드 이벤트
	1) keydown           : key를 눌렀을 때, 특수키(crtl, alt...한글) 포함
	2) keypress          : key를 눌렀을 때, 특수키(crtl, alt...한글) 제외
	3) **keyup             : key를 눌렀다가 뗏을 때  //많이 사용**

Event 등록
	1) Element의 속성 형식으로 등록
		- on이벤트명 = "함수호출"
		ex) <button onclick="fn1()">CLICK</button>

	2) JS 파일에서 코드 작성
		- 선택자.on이벤트명 = 익명함수 선언 또는 함수 호출 (함수 호출 시에는 () 생략 해야 함)

	3) J**S 파일에 작성   //유자보수에 좋음
		- 선택자.addEventListener("이벤트명", 익명함수선언 또는 함수 호출)**

C. Event 강제 실행
		선택자.이벤트명();
```

## addEventListener

```sql
btn3.addEventListener("click",function(){

 let c4 = document.getElementById('c4'); //이벤트가 발생될 때 실행
    console.log(c4.value);

})

변수.addEventListener("Event 종류",function(){})

```

```sql
const d1 = document.getElementById("d1");
const input1= document.getElementById("input1");
const input2= document.getElementById("input2");

d1.addEventListener("click",function(){

console.log("click event");

});

d1.addEventListener("mouseenter", function(){ //마우스가 위로 갔을때

    console.log("Mouse Enter Event");

});

input1.addEventListener("keyup",function(){ //키를 누르고 땔때

    console.log("keyup Event");
});

input2.addEventListener("focus", function(){ //포커스 입력 깜박 들어갔을때

    console.log("focus Event");

});

input2.addEventListener("blur", function(){ //포커스를 잃어버렸을때

    console.log("blur Event");

});

input2.addEventListener("change", function(){ //포커스를 잃어버렸을때

    console.log("change Event");

});

btn.onclick=function(){} // 다른형식
```

## innerText, innerHTML

```sql
btn.addEventListener("click", function(){

    let c2 = window.document.getElementById("c2");
    console.log(c2.innerText);
    console.log(c2.innerHTML);
    //c2.innerText='text';
    c2.innerHTML='<img src="../resources/images/test1.jpg">';
});

선택자로 선택한 Element의 내부에 있는 Contents를 가지고 오거나 변경

	A. GET (값 가져오기)
	1) 선택자.innerHTML : 변수, 선택한 Element의 자식 태그 들 전부 반환
	2) 선택자.innerText : 변수, 선택한 Element의 내부의 모든 Text 반환

	c2.innerText

	변수.innerText

	B. SET (값 대입하기)
	1) 선택자.innerHTML=값
	2) 선택자.innerText=값

	 c2.innerHTML='<img src="../resources/images/test1.jpg">';

   변수.innerHTML='<img src="../resources/images/test1.jpg">';
```

## getAttribute

```sql
3. 선택자로 선택한 Element의 속성을 값을 가지고 오거나 변경

	v = c3.getAttribute('title');
	변수 = id.getAttribute('title');

	A. GET
		1) 선택자.속성명
		2) 선택자.getAttribute("속성명");

	B. SET
		1) 선택자.속성명=값;
		2) 선택자.setAttribute("속성명", "값");/code
```

## getElementsByClassName

```jsx
class 속성
		참고 : https://developer.mozilla.org/ko/docs/Web/API/Element/classList

		1) class의 이름(값) 가져오기
				a. 선택자.className : 변수, 선택자에 클래스의 값이 한개 일때
				b. 선택자.classList : 변수, 선택자에 클래스의 값이 여러개 일때, 배열

		2) class의 이름(값) 변경하기
				a. 선택자.className = 값    : 한개
				b. 선택자.classList.replace('변경전 클래스명', '변경할 클래스명') : 여러개중 하나 변경

		3) class의 이름(값) 추가
				a. 선택자.classList.add('추가할클래스명')                       : 한개 추가
				b. 선택자.classList.add('추가할클래스명1', '추가할클래스명2')    : 여러개 추가

		4) class의 이름(값) Toggle
				- 클래스명이 없으면 추가, 있으면 삭제
				a. 선택자.classList.toggle('클래스명')
				b. 선택자.classList.toggle('클래스명', 조건식 또는 boolean)

		5) class의 이름(값) 삭제
				a. 선택자.classList.remove('삭제할 클래스명')
				b. 선택자.classList.remove('삭제할 클래스명1', '삭제할 클래스명2')

		6) class의 이름이 여러개 일때 원하는 클래스 이름이 있는지 확인
				a. 선택자.classList.contains(클래스명)
```

## function

```sql

// function 선언

f1();

function f1 () {  //함수는 호출이 필요
    console.log("function Test");
}

//매개변수 선언시 let,const 는 생략 해야함
function f2(n1, n2) {
    return n1+n2;
}

//fuction 호출
f1();

let result = f2(1,2)
console.log(result);

let 변수 = 함수명(매개변수)
console.log(변수);

* function 은 선언전에 호출할 수 있다. (선언위에 호출이 가능)

```

## 명명함수

```sql
const d1 = document.getElementById("d1");

function callback1 (){
    console.log("CallBack1");
}

let callback2 = function() {
    console.log("Callback2")
}

//다른 곳에서도 같은 함수를 사용하고 싶을 때 사용
d1.addEventListener("click",callback1());
d1.addEventListener("click",callback2());
```

## 익명함수

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381287/notion-blog/_/JavaScript/_/_JavaScript_/Untitled.png)

```jsx
익명함수
			- 이름이 없는 함수  //뭔가 일어났을 때 일어나는 함수
			- 콜백함수용, 다른 함수의 매개변수의 값으로 함수를 넣을 수도 있음
			- 함수를 선언하고 호출
```

## 전역변수

```sql
//전역변수
let n = 'iu';

// function 선언

function f1 () {  //함수는 호출이 필요
    console.log("function Test");
    console.log(n); //전역 변수를 통해서 출력
}

* 전역 변수는 다른 js파일 에서도 사용 가능하다.
```

# Event 전달(위임)

## 버블링,캡쳐링

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381286/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_1.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381286/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_2.png)

```jsx

	버블링 과 캡처링

	default는 버블링

	* 자식부터 시작한다는 것은 내부 실행문 부터 시작된다는것

1. **버블링**

	- 중접된 Element 들에서 같은 이벤트가 적용 되었을때

	- 자식 부터 실행하고 부모로 전파 되는것

	자식 엘리먼트에 이벤트가 발생하면 할당된 핸들러(callback)가 작동하고 부모의 핸들러가 작동
	addEventListener('이벤트명',function(){},false)    false만 작성 가능

```

```jsx

2. **캡처링**

	- 중접된 Element 들에서 같은 이벤트가 적용 되었을때

	- 부모 부터 실행하고 자식으로 전파 되는것

	이벤트 전달이 버블링 반대
	addEventListener('이벤트명', function(){},true)     true만 작성 가능

```

## 캡처링 중단

```jsx

	3. 버블링, 캡처링 중단
	- 이벤트 전달을 중단 시키기
	- 꼭 필요한 경우를 제외하고 버블링 중단은 안하는 것을 권장
		1) event.stopPropagation(); //현재발생한 이벤트

		d2.addEventListener("click", function(event){
		    event.stopPropagation();
		    console.log("p click");
		}, false);



		2) event.stopImmediatePropagation(); //현재 이벤트 외에도 모두
		해당 Element에 있는 모든 이벤트 전달을 중단 할 때 사용


4. Element가 가지고 있는 기본 동작(event, a, submit,,,)을 중단

		1) preventDefault()

		- Element가 가지고 있는 기본 동작(event, a, submit,,,)을 중단 시키는 메서드

		<a href="https://www.google.com" id="google">GOOGLE</a>

			const google = document.getElementById("google");

			google.addEventListener("click", function(event){
		    alert("Go Google");
		    event.preventDefault();
			});



```

### Event 객체

```jsx
이벤트가 발생하면 관련정보는 event 객체에 저장

주요 속성(변수)
1. type          : 이벤트 타입(이벤트 이름)
2. currentTarget : 현재 이벤트가 발생한 element (this)
3. target        : 실제 이벤트가 발생한 element (this가 아닐 수도 있음,

currentTarget과 다를 수 있음)

주요 메서드
1. stopPropagation()     : 버블링, 캡쳐링 모두 취소

**예를들어 div 내부의 버튼을 클릭하면

currentTarget 은 div

target 은 buttone 이다**
```

# Element 추가 OR 삭제

## append,prepend

```jsx
- Element 내부에 새로운 Element를 추가
- innerHTML은 추가가 아니라 기존의 내용을 지우고 덮어 쓰기

1. 선택자.append()   : 선택자 내부에 기존 내용 밑에 추가
2. 선택자.prepend()  : 선택자 내부에 기존 내용 위에 추가 (태그 내부가 아니라 위에(전에))
3. 선택자.before()   : 선택자 외부 위에 추가
4. 선택자.after()    : 선택자 외부 밑에 추가

-- 삭제
5. 선택자.remove()                   : 선택자를 포함 자식 element 모두 삭제
6. 부모선택자.removeChild(자식선택자) : 부모내의 자식 element만 삭제

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381286/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_3.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381286/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_4.png)

```jsx

  JS에서 Element 생성

	1) 새로운 Element 생성
		document.createElement('새로만들 태그명')

	2) 생성한 Element에 Text Contents 추가
		document.createTextNode("추가할 Text")

	3) 생성한 자식 Element를 생성한 부모 Element내에 추가
		생성한부모Element.appendChild(자식Element);

	4) Attribute(속성) 생성
		document.createAttribute("속성명")
		변수명.value="속성의값"
		적용할Element변수명.setAttributeNode(생성한속성) //위에서 만든 속성을 넣은 변수를 Element

		에 적용

<li><button>click</button></li>
위의 예제를 만드는 과정을 설명한다. 위 3가지를 모두 만들어 조립하는 과정이다

const add = document.getElementById("add");   // Html 의 id add 속성을 선택하여 add 변수할당
const list = document.getElementById("list"); // Html 의 id list 에 list 변수 할당

add.addEventListener('click', function() {  // 변수 add에 클릭 이벤트 할당

let li = document.createElement('li');     // li변수에 li 앨리먼트 생성
let button = document.createElement("button") // button 변수에 button 앨리먼트 생성
let text = document.createTextNode('click'); // text 변수에 click text 콘텐츠 추가

button.appentChild(text); //button 앨리먼트에 text 자식 노드 추가
li.appendChild(button);  // li 에 button 자식 노드 추가 (완성)

list.append(li);   //list 에 li 노드 추가

* 추가적으로 속성을 넣고싶다면 (ex 버튼에 색상)

let attr = document.createAttribute("class"); //attr 변수에 class 속성을 만들어주고 (다른것도 가능)
attr.value = 'btn';  // attr 변서 (class) dp btn( 색상 속성이다 ) 를 넣어준다

button.setAttributeNode(attr); // button 에 속성 노드 넣기

 --------------------------------------------------------------------------------------------

btn.addEventListener("click", function(){
    d1.innerHTML='<img src="/resources/images/title1.jpg">'
});

add.addEventListener("click", function(){
    let li = document.createElement("li"); //<li></li>
    let t = document.createTextNode("test");//test
    li.appendChild(t);                      //<li>test</li>
    list.append(li);
});

add2.addEventListener("click", function(){
    let li = document.createElement("li"); //<li></li>
    let h = document.createElement("h3");  //<h3></h3>
    let t = document.createTextNode("Hello"); //Hello
    h.appendChild(t);                       //<h3>Hello</hello>
    li.appendChild(h);                       //<li><h3>Hello</hello></li>
    list2.prepend(li);
})

//add3을 클릭하면 div#d2 내에 input Element를 Append
add3.addEventListener("click", function(){
    let input = document.createElement("input");//<input>
    let ch = document.createAttribute("type");  //type=
    ch.value="checkbox";                        //type="checkbox"
    input.setAttributeNode(ch);                 //<input type="checkbox">
    d2.appendChild(input);
});

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381287/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_5.png)

(만든 변수를 재 활용할 수 도 있다 .( attr 변수를 활용해 clas 를 넣었다 for을 넣었다 type을 넣었다

하고 있다.)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381287/notion-blog/_/JavaScript/_/_JavaScript_/Untitled_6.png)

# data-\*\*\*

```jsx
  **개발자가 자신이 원하는 속성을 마음대로 만들 수 있는 기능이다**

- HTML5에 추가된 Element의 속성
- 개발자가 element의 속성값으로 data를 넣고 싶을 때 사용
- 사용자 정의 속성

data-속성명

ex) data-age="20"
ex) data-my-name="iu"
```

# varStratus

```jsx

status 는 for문의 돌아가는 상태를 알 수 있게 체크하여 준다

c. c:forEach
				1) 숫자를 반복
					<c:forEach begin="시작번호" end="끝번호" step="증가값" var="숫자를담을변수명">
					- end 이하값
					- step 생략가능, 기본은 1, 0 이하값은 X
					<c:forEach begin="10" end="1" step="-1" var="숫자를담을변수명"> : 사용불가

				2) Collection 반복
					<c:forEach items="Collection의 속성명" var="DTO를 담을 변수명" varStatus="상태를 담을 변수명">

					- **varStatus (숫자가 아닌 목록을 가져올때 사용한다) ex)items=""**

						-- 필수는 아님, 필요하면 사용
						a. 현재 아이템 : ${pageScope.상태를담을변수명.current}
						b. 인덱스번호  : ${pageScope.상태를담을변수명.index}
						c. 순서 번호   : ${pageScope.상태를담을변수명.count}
						d. 처음 ??     : ${pageScope.상태를담을변수명.first}
						e. 마지막 ??   : ${pageScope.상태를담을변수명.last}
						f. 시작값      : ${pageScope.상태를담을변수명.begin}
						g. 끝값        : ${pageScope.상태를담을변수명.end}
						h. 증가값      : ${pageScope.상태를담을변수명.step}

						예제)

						<c:foreach items=”${items}” var=”item” varStatus=”status”>

						${status.current}<br/>      <!– 현재 아이템 –>

						${status.index}<br/>        <!– 0부터의 순서 –>

						${status.count}<br/>        <!– 1부터의 순서 –>

						${status.first}<br/>          <!– 현재 루프가 처음인지 반환 –>

						${status.last}<br/>           <!– 현재 루프가 마지막인지 반환 –>

						${status.begin}<br/>        <!– 시작값 –>

						${status.end}<br/>           <!– 끝값 –>

						${status.step}<br/>          <!– 증가값 –>

						< /c:forEach>



						EX) items = [0,1,2,3,4,5]

						<c:foreach items=”${items}” var=”item” varStatus=”status”>

						${status.current}<br/>      <!– 현재 아이템 –>

						${status.index}<br/>         0,1,2,3,4,5

						${status.count}<br/>        1,2,3,4,5,6

						${status.first}<br/>           index == 0일 때 true

						${status.last}<br/>           index == 5일 때 true

						${status.begin}<br/>        <!– 시작값 –>

						${status.end}<br/>          <!– 끝값 –>

						${status.step}<br/>         <!– 증가값 –>

						< /c:forEach>
```

# 제어문

```jsx
참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Control_flow_and_error_handling
참고 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Guide/Loops_and_iteration

1. 단일 if
		if(조건식){}

2. if(조건식){} else {}

3. if(조건식){} else if(조건식){} else {}

4. switch(){
		case 값1:
		break;

		case 값2:
		break;

		default :
		break;
 }

5. while(조건식){}

6. do {}while(조건식);

7. for(초기식;조건식;증감식){}

8. for(변수명 in 컬렉션변수명){
			//변수명에 인덱스 번호가 대입
	}   //클래스의 멤버들을 순환해서 뽑아 올 때 사용 <많이 사용은 x>

9. for(변수명 of 컬렉션변수명){
		//변수명에 컬렉션에서 하나 꺼낸 값이 대입
}

10. 반복문의 보조문
	1) break;
	2) continue;

```

```jsx
예제)
let ar = [1,2,3];

console.log('Old')
for(let i=0;i<ar.length;i++){
    console.log(ar[i]);
}

console.log('For of');
for(let a of ar){
    console.log(a);
}

console.log('For in');
for(let j in ar){
    console.log(j);
    console.log(ar[j]);
}
```

# 배열 (Array)

```jsx
- 참조 : https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Global_Objects/Array

- 배열 생성시 크기를 지정하지 않음
- 객체

1. 빈 배열 생성
	1) let ar = new Array();    //쓰지 말것을 권장
	2) let ar = [];             //권장 사항

2. 데이터가 있는 배열 생성
	let ar = [1, 2, 'a', 3.12, true]

3. 주요 메서드

	1) push(data)    : 배열 끝에 요소를 추가하고 새로운 길이 (length) 반환

	2) pop()         : 배열 끝에 요소를 삭제하고 삭제한 요소를 반환

	3) unshift(data) : 배열 처음에 요소를 추가하고 새로운 길이 (length) 반환

	4) shift()       : 배열 처음 요소를 삭제하고 삭제한 요소를 반환

	5) sort()        : 정렬, 오름 차순

	6) reverse()     : 순서 변경

	7) foreach()     : 반복문 for 대신 사용가능한 함수
			 foreach(function(변수명1, 변수명2, 배열의변수명){
						-- 변수명1 : 배열에서 꺼낸 값
						-- 변수명2 : 인덱스번호
				});

	8) includes(찾을 Data): 배열에 해당 Data가 존재 여부 판단

4. 주요 변수
	1) lenth      : 배열의 길이를 담고 있는 변수
```

```jsx
예제)

console.log('For each')
ar.forEach(function(v, i, list){
    console.log('v : ', v, ' i : ', i, ' ar : ', list);

});
```

# Object , Class

```jsx
1. Object
	1) 선언
		const v1 = {key:value, key2:value,...}
		const v2 = {
				key1:value1,
				key2:value2,
				...
		}

	2) 속성사용
	변수명.속성명
	변수명["속성명"]
	변수명.메서드명()

	3) 메서드
	const v3 = {
			key1:value1,
			key2:value2,
			method1:function(){
					...
			},
	}

2. class
//class 클래스명{
    //멤버변수선언
    //멤머메서드선언
    //생성자선언

//}
//Car class 선언
//brand, color 멤버변수 선언
//move 메서드 선언
class Car {

    name="test";
    // #변수명 : private에 의미
    #gas="Fuel";

    //클래스 변수
    static country="Korea";
    static #gear=4;

    //생성자
    //인스턴스를 생성하고 변수의 초기화
    constructor(brand, color){
        this.brand=brand;
        this.color=color;

    }

    //method
    move(){
        console.log(this.brand, this.color, this.#gas);
        console.log("Gear => ", Car.#gear);
    }
}

//Object, Instance
let car1 = new Car("Audi", "Red", "Oil");
console.log("Car1 ", typeof car1);
console.log("Car1 ", car1);
//멤버 추가 가능
car1.price=3000;
console.log("Price => ", car1.price);
console.log("Gas => ", car1.gas);
car1.move();
console.log("Country => ", Car.country)

class Human{
    #name;
    #age;
    constructor(){}

    //getter
    get name(){
        //get 키워드
        //getter는 return을 꼭 명시
        return this.#name;
    }

    //setter
    set name(name){
        this.#name=name;
    }

    info(){
        console.log(this.#name, this.#age);
    }

}

const h1 = new Human();

//setter 호출, 함수 호출 형식이 아님
//변수명.setter명 = 값  , setter가 호출 됨
//h1.name("Test");
h1.name="Test";

//getter 호출, 함수 호출 형식이 아님
//변수명.getter명 = 값  , getter가 호출 됨
//const name=h1.name();
const name = h1.name;
console.log("Name : ", name);

h1.info();
```

```jsx
**예제**

let car = {
    brand:'AUDI',
    price:3000,
    info:function(){
        console.log(this.brand, this.price);
    }
}

//class 선언
class Student{
    #n='';
    #kor;

    set n(n){
        this.#n=n;
    }

    get n(){
        return this.#n;
    }

    set kor(kor){
        this.#kor=kor;
    }

    get kor(){
        return this.#kor;
    }

    info(){
        console.log(this.#n, this.#kor);
    }

}

const btn1 = document.getElementById('btn1');

btn1.addEventListener('click', function(){
    // console.log(car.brand);
    // car.info();

    // car.color='RED';
    // console.log('For in');
    // for(let v in car){
    //     console.log(car[v]);
    // }
    let s = new Student();
    s.n='iu';
    s.kor=100;
    s.eng=100;
    s.info();
    console.log(s.eng);
    console.log(s.n, s.kor);

    let s1 = new Student();

})
```

# Ajax(Asynchronous JavaScript And XML)

```jsx

**비동기 방식**

1. XMLHttpRequest
let xhttp = new XMLHttpRequest();

					**POST 방식**
            xhttp.open("POST", "../noticeReply/update");

            //요청 header 정보
            xhttp.setRequestHeader("Content-type", "application/x-www-form-urlencoded");

						//POST 파라미터 전송
            xhttp.send("replyNum="+replyNum+"&contents="+contents);


            xhttp.onreadystatechange=function(){   // 상태가 변할때 적용
                if(this.readyState==4 && this.status==200){  // 4번이 완성 4쉐이크 200 은 정상응답(오류 400,404등등)
                    if(this.responseText.trim()=='1'){  //responseText 응답을 담는곳
                        alert('수정 성공');
                        document.querySelector("#up"+replyNum).innerHTML=contents;
                    }else {
                        alert('수정 실패');
                    }
                }

					**GET 방식**

					xhttp.open("GET" , "notice/list?page=1"); // GET이면 ? 파라미터 넘겨주기

					xhttp.send(); //비워줌

					xhttp.addEventListener('readystatechange', function() { //상태가 변할떄 적용
							if(this.readyState==4 && this.status==200) {
								console.log("정상 요청 응답종료");
								console.log(this.responseText);

							}

					});

2. fetch
	 fetch(url, [options])
	 - url : 요청 URL
	 - options : method나, header등 지정

	a. options
		 method: 메서드 형식
		 headers : 헤더 값 전달( {content-type:'application/json'}
		 body:요청시 전달할 값
     mode:CORS값 설정 (cors, no-cors, same-origin)
		 credencial : 자격 증명을 위한 옵션(include, same-origin, omit, default=same-origin)
		 cache: 캐시 사용 여부 (no-cache, reload, force-cache, only-if-cache)

	b. 요청 형식
		fetch(URL, {                       //js에 {} 는 객체라는의미
				method:"",
				headers: {},
			  body: JSON.stringfy(data), //JSON data를 String type으로 변환
		})

	c. 결과처리
		result.text()
		응답을 읽고 텍스트를 반환

		result.json()
		응답을 읽고 JSON으로 반환

		result.blob()
		응답을 읽고 BLOB(바이너리 데이터)형태로 반환



```

### 화살표 함수(Arrow Function)

```jsx
let f1 = function (num) {
  console.log('t1 function : ', num)
}

f1(3)

let f2 = (num) => {
  console.log('t2 function : ', num)
}

let f3 = () => console.log('f3 function')

f2(4)
f3()

btn.addEventListener('click', (e) => {
  //function 생략  하고  () => 로  대채사용
})

v3.click(function () {})
v4.click(() => {})

$.ajax({
  success: (result) => {},
})
```

# JSON

```jsx
JavaScript Object Notation

1. JSON은 다양한 프로그래밍 언어에서 사용되며, 웹 개발에서 특히 많이 사용.

2. JSON은 문자열 형태로 존재

3.

다음은 실제로 JSON 데이터를 사용하는 몇 가지 예시

참조
https://developer.mozilla.org/ko/docs/Learn/JavaScript/Objects/JSON

JavaScript
	1) JSON 형식의 문자열을 JSON 객체로 변환
		 JSON.parse("문자열");

	2) JSON 객체를 JSON형식의 문자열로 변환
		 JSON.stringify(JSON객체)

형태 : key : value

{key:value, key:value}

[{key:value, key:value}, {key:value, key:value}]

숫자, boolean, string

1,     false,  'test'

[숫자
```
