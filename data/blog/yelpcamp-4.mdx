---
title: 'YelpCamp리뷰 모델 추가하기'
date: '2026-09-02'
category: '풀스택/The Web Developer 부트캠프 2022'
tags: ['풀스택', 'The Web Developer 부트캠프 2022']
draft: false
summary: ''
---

# 리뷰 모델 정의하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381397/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled.png)

```jsx
const mongoose = require('mongoose')
const Schema = mongoose.Schema

const reviewSchema = new Schema({
  body: String,
  rating: Number,
})

module.exports = mongoose.model('Review', reviewSchema)
```

```jsx
리뷰라는 새로운 모델을 정의해준다

( 대문자로 시작하는 단수의 "Review" )
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381394/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_1.png)

```jsx
const mongoose = require('mongoose') // 몽구스 연결

const Schema = mongoose.Schema // 몽구스 스키마를 스키마로 저장 ( 자주 사용하기 위해 단축키 느낌 )

const CampGroundSchema = new Schema({
  // CampGround 스키마 정의
  title: String,
  image: String,
  price: Number,
  description: String,
  location: String,
  reviews: [
    {
      type: Schema.Types.ObjectId,
      ref: 'Review',
    },
  ],
})

module.exports = mongoose.model('CampGround', CampGroundSchema) // 모델 내보내기
```

```jsx
캠프 모델에 리뷰 배열을 추가해준다

이번에는 one to many 관계로 사용한다 ( few 가 아니기 때문에 캠프 모델이 수정이 필요한 것 )
```

## 리뷰 폼 만들기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381395/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_2.png)

```jsx
예전에 만들어둔 캠프 상세 페이지에 댓글 폼을 만들어준다

리뷰를를 등록하는 주소는

/campground/:id/review

로 id 는 현재 캠프의 id 가 들어가도록 params 를 사용해준다
```

```jsx
import { useEffect, useState } from 'react'
import {
  MDBCard,
  MDBCardImage,
  MDBCardBody,
  MDBCardTitle,
  MDBCardText,
  MDBCardLink,
  MDBListGroup,
  MDBListGroupItem,
  MDBRipple,
  MDBBtn,
} from 'mdb-react-ui-kit'
import Col from 'react-bootstrap/Col'
import Form from 'react-bootstrap/Form'
import InputGroup from 'react-bootstrap/InputGroup'
import Row from 'react-bootstrap/Row'
import FloatingLabel from 'react-bootstrap/FloatingLabel'
import { useParams, Link, Navigate, useNavigate } from 'react-router-dom'
import axios from 'axios'
import { Button } from 'bootstrap'

const CampGroundDetail = () => {
  const params = useParams()
  const [camp, setCamp] = useState({})
  const navigate = useNavigate()
  const [validated, setValidated] = useState(false)
  //상태 초기화
  const [formData, setFormData] = useState({
    body: '',
    rating: 2,
  })

  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
        setCamp(response.data)
      } catch (error) {
        console.log('FAIL LOAD CAMPGROUND DETAIL')
        setCamp([])
      }
    }
    fetchData()
  }, [])

  // 폼 제출 핸들러
  const onSubmitHandler = async (e) => {
    e.preventDefault()

    const form = e.currentTarget

    if (form.checkValidity() === false) {
      setValidated(true)
      return
    }

    setValidated(false)

    // 폼 데이터 전송
    try {
      await axios.post(`http://localhost:4000/campground/${params.id}/review`, formData)
    } catch (error) {
      console.error('ReviewFail', error)
    }
  }

  // 캠프 삭제
  const deleteCamp = async () => {
    try {
      await axios.delete(`http://localhost:4000/campground/${params.id}`)
      navigate('/campground')
    } catch (error) {
      console.error('DELETE FAIL', error)
    }
  }

  // 입력값 변경 핸들러
  const handleChange = (e) => {
    const { name, value } = e.target
    setFormData((prevDate) => ({
      ...prevDate,
      [name]: value,
    }))
  }

  return (
    <>
      {camp ? (
        <div className="row">
          <div className="offset-3 col-6">
            <MDBCard>
              <MDBRipple rippleColor="light" rippleTag="div" className="bg-image hover-overlay">
                <MDBCardImage src={camp.image} fluid alt="..." />
                <a>
                  <div
                    className="mask"
                    style={{ backgroundColor: 'rgba(251, 251, 251, 0.15)' }}
                  ></div>
                </a>
              </MDBRipple>
              <MDBCardBody>
                <MDBCardTitle>{camp.title}</MDBCardTitle>
                <MDBCardText>{camp.description}</MDBCardText>
              </MDBCardBody>
              <MDBListGroup flush>
                <MDBListGroupItem className="text-muted">{camp.location}</MDBListGroupItem>
                <MDBListGroupItem>${camp.price}/night</MDBListGroupItem>
                <MDBListGroupItem>Vestibulum at eros</MDBListGroupItem>
              </MDBListGroup>
              <MDBCardBody>
                <Link to={`/campground/${params.id}/edit`}>
                  <MDBBtn className="btn-info me-2">Edit</MDBBtn>
                </Link>
                <MDBBtn onClick={deleteCamp} className="btn-danger">
                  Delete
                </MDBBtn>
              </MDBCardBody>
            </MDBCard>
            {/* 폼제출 */}
            <Form noValidate validated={validated} onSubmit={onSubmitHandler}>
              <Row className="mt-3">
                <Form.Group as={Col} md="12" controlId="body">
                  <Form.Label>Review</Form.Label>
                  <InputGroup hasValidation>
                    <FloatingLabel controlId="Review" label="Review">
                      <Form.Control
                        as="textarea"
                        placeholder="Leave a review here"
                        value={formData.body}
                        onChange={handleChange}
                        name="body"
                        style={{ height: '100px' }}
                        required
                      />
                    </FloatingLabel>
                  </InputGroup>
                </Form.Group>
                <div>
                  <label htmlFor="rating" className="form-label">
                    Rating
                  </label>
                  <input
                    type="range"
                    className="form-range"
                    min="0"
                    max="5"
                    id="rating"
                    name="rating"
                    value={formData.rating}
                    onChange={handleChange}
                  ></input>
                </div>
              </Row>
              <MDBBtn className="btn-success mt-3">Submit</MDBBtn>
            </Form>

            <Link to={'/campground'}>Back To CampGround</Link>
          </div>
        </div>
      ) : (
        <p>캠프를 불러오는 중입니다...</p>
      )}
    </>
  )
}

export default CampGroundDetail
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381396/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_3.png)

```jsx
// 리뷰 추가
app.post(
  '/campground/:id/review',
  validateReview,
  catchAsync(async (req, res) => {
    const { id } = req.params
    const campground = await CampGround.findById(id)
    const { body, rating } = req.body
    const review = new Review({ body, rating })
    campground.reviews.push(review._id)
    await review.save()
    await campground.save()
    res.send(campground)
  })
)
```

```jsx
index.js 에서는 review 폼에서 받은 데이터를 push 하여 campground 에 넣어주고 저장해준다

* **꼭 리뷰와 campground 모두 저장해 주어야한다 ( 왜냐하면 둘은 서로 연결되어 있다 )**
```

# 리뷰 유효성 검사

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381396/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_4.png)

```jsx
const Joi = require('joi')

module.exports.campgroundSchema = Joi.object({
  title: Joi.string().required(),
  price: Joi.number().required().min(0),
  description: Joi.string().required(),
  location: Joi.string().required(),
  image: Joi.string().required(),
})

module.exports.reviewSchema = Joi.object({
  rating: Joi.number().required().min(0).max(5),
  body: Joi.string().required(),
})
```

```jsx
Joi 를 이용해서 유효성 검사를 실시해준다

( 클라이언트 검사도 실시해주자 )

```

## index.js

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381396/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_5.png)

```jsx
const { campgroundSchema , reviewSchema } = require("./validate")

reviewSchema 를 추가해주고
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381396/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_6.png)

```jsx
const validateReview = (req, res, next) => {
  const { error } = reviewSchema.validate(req.body, { abortEarly: false })
  if (error) {
    const errorMessage = error.details.map((detail) => detail.message).join(',')
    throw new ExpressError(errorMessage, 400)
  } else {
    next()
  }
}
```

```jsx
validateReview 미들웨어를 작성해준다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381396/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_7.png)

```jsx
이후 라우터에 validateReview 를 추가해준다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381397/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_8.png)

## 리뷰 출력

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381397/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_9.png)

```jsx
import { useEffect, useState } from 'react'
import {
  MDBCard,
  MDBCardImage,
  MDBCardBody,
  MDBCardTitle,
  MDBCardText,
  MDBCardLink,
  MDBListGroup,
  MDBListGroupItem,
  MDBRipple,
  MDBBtn,
} from 'mdb-react-ui-kit'
import Col from 'react-bootstrap/Col'
import Form from 'react-bootstrap/Form'
import InputGroup from 'react-bootstrap/InputGroup'
import Row from 'react-bootstrap/Row'
import FloatingLabel from 'react-bootstrap/FloatingLabel'
import { useParams, Link, Navigate, useNavigate } from 'react-router-dom'
import axios from 'axios'
import { Button } from 'bootstrap'

const CampGroundDetail = () => {
  const params = useParams()
  const [camp, setCamp] = useState({})
  const [reviews, setReviews] = useState([]) // 리뷰 상태 추가
  const [validated, setValidated] = useState(false)
  //상태 초기화
  const [formData, setFormData] = useState({
    body: '',
    rating: 2,
  })

  const navigate = useNavigate()
  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
        setCamp(response.data)
        setReviews(response.data.reviews)
      } catch (error) {
        console.log('FAIL LOAD CAMPGROUND DETAIL')
        setCamp({})
      }
    }
    fetchData()
  }, [params.id])

  // 폼 제출 핸들러
  const onSubmitHandler = async (e) => {
    e.preventDefault()

    const form = e.currentTarget

    if (form.checkValidity() === false) {
      setValidated(true)
      return
    }

    setValidated(false)

    // 폼 데이터 전송
    try {
      await axios.post(`http://localhost:4000/campground/${params.id}/review`, formData)
      // 새로운 리뷰를 추가한 후에 다시 데이터를 불러와서 업데이트
      const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
      // 인풋창 초기화
      setFormData({ body: '', rating: 2 })
      setReviews(response.data.reviews)
    } catch (error) {
      console.error('ReviewFail', error)
    }
  }

  // 캠프 삭제
  const deleteCamp = async () => {
    try {
      await axios.delete(`http://localhost:4000/campground/${params.id}`)
      navigate('/campground')
    } catch (error) {
      console.error('DELETE FAIL', error)
    }
  }

  // 입력값 변경 핸들러
  const handleChange = (e) => {
    const { name, value } = e.target
    setFormData((prevDate) => ({
      ...prevDate,
      [name]: value,
    }))
  }

  return (
    <>
      {camp ? (
        <div className="row">
          <div className="offset-3 col-6">
            <MDBCard>
              <MDBRipple rippleColor="light" rippleTag="div" className="bg-image hover-overlay">
                <MDBCardImage src={camp.image} fluid alt="..." />
                <a>
                  <div
                    className="mask"
                    style={{ backgroundColor: 'rgba(251, 251, 251, 0.15)' }}
                  ></div>
                </a>
              </MDBRipple>
              <MDBCardBody>
                <MDBCardTitle>{camp.title}</MDBCardTitle>
                <MDBCardText>{camp.description}</MDBCardText>
              </MDBCardBody>
              <MDBListGroup flush>
                <MDBListGroupItem className="text-muted">{camp.location}</MDBListGroupItem>
                <MDBListGroupItem>${camp.price}/night</MDBListGroupItem>
                <MDBListGroupItem>Vestibulum at eros</MDBListGroupItem>
              </MDBListGroup>
              <MDBCardBody>
                <Link to={`/campground/${params.id}/edit`}>
                  <MDBBtn className="btn-info me-2">Edit</MDBBtn>
                </Link>
                <MDBBtn onClick={deleteCamp} className="btn-danger">
                  Delete
                </MDBBtn>
              </MDBCardBody>
            </MDBCard>
            {/* 폼제출 */}
            <Form noValidate validated={validated} onSubmit={onSubmitHandler}>
              <Row className="mt-3">
                <Form.Group as={Col} md="12" controlId="body">
                  <Form.Label>Review</Form.Label>
                  <InputGroup hasValidation>
                    <FloatingLabel controlId="Review" label="Review">
                      <Form.Control
                        as="textarea"
                        placeholder="Leave a review here"
                        value={formData.body}
                        onChange={handleChange}
                        name="body"
                        style={{ height: '100px' }}
                        required
                      />
                    </FloatingLabel>
                  </InputGroup>
                </Form.Group>
                <div>
                  <label htmlFor="rating" className="form-label">
                    Rating
                  </label>
                  <input
                    type="range"
                    className="form-range"
                    min="0"
                    max="5"
                    id="rating"
                    name="rating"
                    value={formData.rating}
                    onChange={handleChange}
                  ></input>
                </div>
              </Row>
              <MDBBtn className="btn-success mt-3">Submit</MDBBtn>
            </Form>
            <h2>Reviews</h2>
            {reviews.map((review) => (
              <div key={review._id}>
                <p>{review.body}</p>
                <p>Rating: {review.rating}</p>
                <hr />
              </div>
            ))}

            <Link to={'/campground'}>Back To CampGround</Link>
          </div>
        </div>
      ) : (
        <p>캠프를 불러오는 중입니다...</p>
      )}
    </>
  )
}

export default CampGroundDetail
```

```jsx
map 을 사용해서 review 의 값을 반복해서 출력한다
```

# 리뷰 스타일링

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381394/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_10.png)

```jsx
import { useEffect, useState } from 'react'
import {
  MDBCard,
  MDBCardImage,
  MDBCardBody,
  MDBCardTitle,
  MDBCardText,
  MDBCardLink,
  MDBListGroup,
  MDBListGroupItem,
  MDBRipple,
  MDBBtn,
} from 'mdb-react-ui-kit'
import Col from 'react-bootstrap/Col'
import Form from 'react-bootstrap/Form'
import InputGroup from 'react-bootstrap/InputGroup'
import Row from 'react-bootstrap/Row'
import FloatingLabel from 'react-bootstrap/FloatingLabel'
import { useParams, Link, Navigate, useNavigate } from 'react-router-dom'
import axios from 'axios'
import { Button } from 'bootstrap'

const CampGroundDetail = () => {
  const params = useParams()
  const [camp, setCamp] = useState({})
  const [reviews, setReviews] = useState([]) // 리뷰 상태 추가
  const [validated, setValidated] = useState(false)
  //상태 초기화
  const [formData, setFormData] = useState({
    body: '',
    rating: 2,
  })

  const navigate = useNavigate()
  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
        setCamp(response.data)
        setReviews(response.data.reviews)
      } catch (error) {
        console.log('FAIL LOAD CAMPGROUND DETAIL')
        setCamp({})
      }
    }
    fetchData()
  }, [params.id])

  // 폼 제출 핸들러
  const onSubmitHandler = async (e) => {
    e.preventDefault()

    const form = e.currentTarget

    if (form.checkValidity() === false) {
      setValidated(true)
      return
    }

    setValidated(false)

    // 폼 데이터 전송
    try {
      await axios.post(`http://localhost:4000/campground/${params.id}/review`, formData)
      // 새로운 리뷰를 추가한 후에 다시 데이터를 불러와서 업데이트
      const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
      // 인풋창 초기화
      setFormData({ body: '', rating: 2 })
      setReviews(response.data.reviews)
    } catch (error) {
      console.error('ReviewFail', error)
    }
  }

  // 캠프 삭제
  const deleteCamp = async () => {
    try {
      await axios.delete(`http://localhost:4000/campground/${params.id}`)
      navigate('/campground')
    } catch (error) {
      console.error('DELETE FAIL', error)
    }
  }

  // 입력값 변경 핸들러
  const handleChange = (e) => {
    const { name, value } = e.target
    setFormData((prevDate) => ({
      ...prevDate,
      [name]: value,
    }))
  }

  return (
    <>
      {camp ? (
        <div className="row">
          <div className="col-6">
            <MDBCard>
              <MDBRipple rippleColor="light" rippleTag="div" className="bg-image hover-overlay">
                <MDBCardImage src={camp.image} fluid alt="..." />
                <a>
                  <div
                    className="mask"
                    style={{ backgroundColor: 'rgba(251, 251, 251, 0.15)' }}
                  ></div>
                </a>
              </MDBRipple>
              <MDBCardBody>
                <MDBCardTitle>{camp.title}</MDBCardTitle>
                <MDBCardText>{camp.description}</MDBCardText>
              </MDBCardBody>
              <MDBListGroup flush>
                <MDBListGroupItem className="text-muted">{camp.location}</MDBListGroupItem>
                <MDBListGroupItem>${camp.price}/night</MDBListGroupItem>
                <MDBListGroupItem>Vestibulum at eros</MDBListGroupItem>
              </MDBListGroup>
              <MDBCardBody>
                <Link to={`/campground/${params.id}/edit`}>
                  <MDBBtn className="btn-info me-2">Edit</MDBBtn>
                </Link>
                <MDBBtn onClick={deleteCamp} className="btn-danger">
                  Delete
                </MDBBtn>
              </MDBCardBody>
            </MDBCard>

            <Link to={'/campground'}>Back To CampGround</Link>
          </div>
          <div className="col-6">
            {/* 폼제출 */}
            <h1>Leave a Review</h1>
            <Form noValidate validated={validated} onSubmit={onSubmitHandler}>
              <Row className="mt-3">
                <div>
                  <label htmlFor="rating" className="form-label">
                    Rating
                  </label>
                  <input
                    type="range"
                    className="form-range"
                    min="0"
                    max="5"
                    id="rating"
                    name="rating"
                    value={formData.rating}
                    onChange={handleChange}
                  ></input>
                </div>
                <Form.Group as={Col} md="12" controlId="body">
                  <Form.Label>Review</Form.Label>
                  <InputGroup hasValidation>
                    <FloatingLabel controlId="Review" label="Review">
                      <Form.Control
                        as="textarea"
                        placeholder="Leave a review here"
                        value={formData.body}
                        onChange={handleChange}
                        name="body"
                        style={{ height: '100px' }}
                        required
                      />
                    </FloatingLabel>
                  </InputGroup>
                </Form.Group>
              </Row>
              <MDBBtn className="btn-success mt-3">Submit</MDBBtn>
            </Form>
            <div className="mt-4">
              <h2>Reviews</h2>
              {reviews.map((review) => (
                <div className="card mb-3">
                  <div className="card-body">
                    <div key={review._id}>
                      <h5 className="card-title">Rating : {review.rating}</h5>
                      <p className="card-text">Review :{review.body}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      ) : (
        <p>캠프를 불러오는 중입니다...</p>
      )}
    </>
  )
}

export default CampGroundDetail
```

# 리뷰 삭제

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381395/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_11.png)

## index.js

```jsx
// 리뷰 삭제
app.delete(
  '/campground/:id/reviews/:reviewId',
  catchAsync(async (req, res) => {
    const { id, reviewId } = req.params
    await CampGround.findByIdAndUpdate(id, { $pull: { reviews: reviewId } })
    await Review.findByIdAndDelete(reviewId)
    res.send('Delete Test')
  })
)
```

```jsx
리뷰를 삭제하기 위해서는 campground 의 :id 와  리뷰의 id 인 :reviewId 가 필요하다

( :id 는 캠프의 Id 배열의 참조값을 지우기 위해서 필요하고 :reviewId 는 댓글 자체를 지우는데 필요하다 )

id, reviewId 를 req.params 으로 가져오고

CampGround.findByIdAndUpdate(id, { $pull : {reviews : reviewId}}) 에서는

해당 캠프그라운드의 reviews 배열에서 reviewId를 제거한다

그리고 다음줄에

Review 모델에서 reviewId에 해당하는 리뷰를 삭제한다

* **$pull은 배열에서 특정 값 또는 조건과 일치하는 요소를 제거하는 MongoDB 연산자 Mongo 에서 추천하는 방법이다**
```

## 리액트 페이지

```jsx
import { useEffect, useState } from 'react'
import {
  MDBCard,
  MDBCardImage,
  MDBCardBody,
  MDBCardTitle,
  MDBCardText,
  MDBCardLink,
  MDBListGroup,
  MDBListGroupItem,
  MDBRipple,
  MDBBtn,
} from 'mdb-react-ui-kit'
import Col from 'react-bootstrap/Col'
import Form from 'react-bootstrap/Form'
import InputGroup from 'react-bootstrap/InputGroup'
import Row from 'react-bootstrap/Row'
import FloatingLabel from 'react-bootstrap/FloatingLabel'
import { useParams, Link, Navigate, useNavigate } from 'react-router-dom'
import axios from 'axios'
import { Button } from 'bootstrap'

const CampGroundDetail = () => {
  const params = useParams()
  const [camp, setCamp] = useState({})
  const [reviews, setReviews] = useState([]) // 리뷰 상태 추가
  const [validated, setValidated] = useState(false)
  //상태 초기화
  const [formData, setFormData] = useState({
    body: '',
    rating: 2,
  })

  const navigate = useNavigate()
  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
        setCamp(response.data)
        setReviews(response.data.reviews)
      } catch (error) {
        console.log('FAIL LOAD CAMPGROUND DETAIL')
        setCamp({})
      }
    }
    fetchData()
  }, [params.id])

  // 폼 제출 핸들러
  const onSubmitHandler = async (e) => {
    e.preventDefault()

    const form = e.currentTarget

    if (form.checkValidity() === false) {
      setValidated(true)
      return
    }

    setValidated(false)

    // 폼 데이터 전송
    try {
      await axios.post(`http://localhost:4000/campground/${params.id}/review`, formData)
      // 새로운 리뷰를 추가한 후에 다시 데이터를 불러와서 업데이트
      const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
      // 인풋창 초기화
      setFormData({ body: '', rating: 2 })
      setReviews(response.data.reviews)
    } catch (error) {
      console.error('ReviewFail', error)
    }
  }
  // 리뷰 삭제 핸들러
  const deleteReview = async (reviewId) => {
    try {
      await axios.delete(`http://localhost:4000/campground/${params.id}/reviews/${reviewId}`)
      // 리뷰를 삭제한 후에 다시 데이터를 불러와서 업데이트
      const response = await axios.get(`http://localhost:4000/campground/${params.id}`)
      setReviews(response.data.reviews)
    } catch (error) {
      console.error('Review Delete Fail', error)
    }
  }

  // 캠프 삭제
  const deleteCamp = async () => {
    try {
      await axios.delete(`http://localhost:4000/campground/${params.id}`)
      navigate('/campground')
    } catch (error) {
      console.error('DELETE FAIL', error)
    }
  }

  // 입력값 변경 핸들러
  const handleChange = (e) => {
    const { name, value } = e.target
    setFormData((prevDate) => ({
      ...prevDate,
      [name]: value,
    }))
  }

  return (
    <>
      {camp ? (
        <div className="row">
          <div className="col-6">
            <MDBCard>
              <MDBRipple rippleColor="light" rippleTag="div" className="bg-image hover-overlay">
                <MDBCardImage src={camp.image} fluid alt="..." />
                <a>
                  <div
                    className="mask"
                    style={{ backgroundColor: 'rgba(251, 251, 251, 0.15)' }}
                  ></div>
                </a>
              </MDBRipple>
              <MDBCardBody>
                <MDBCardTitle>{camp.title}</MDBCardTitle>
                <MDBCardText>{camp.description}</MDBCardText>
              </MDBCardBody>
              <MDBListGroup flush>
                <MDBListGroupItem className="text-muted">{camp.location}</MDBListGroupItem>
                <MDBListGroupItem>${camp.price}/night</MDBListGroupItem>
                <MDBListGroupItem>Vestibulum at eros</MDBListGroupItem>
              </MDBListGroup>
              <MDBCardBody>
                <Link to={`/campground/${params.id}/edit`}>
                  <MDBBtn className="btn-info me-2">Edit</MDBBtn>
                </Link>
                <MDBBtn onClick={deleteCamp} className="btn-danger">
                  Delete
                </MDBBtn>
              </MDBCardBody>
            </MDBCard>

            <Link to={'/campground'}>Back To CampGround</Link>
          </div>
          <div className="col-6">
            {/* 폼제출 */}
            <h1>Leave a Review</h1>
            <Form noValidate validated={validated} onSubmit={onSubmitHandler}>
              <Row className="mt-3">
                <div>
                  <label htmlFor="rating" className="form-label">
                    Rating
                  </label>
                  <input
                    type="range"
                    className="form-range"
                    min="0"
                    max="5"
                    id="rating"
                    name="rating"
                    value={formData.rating}
                    onChange={handleChange}
                  ></input>
                </div>
                <Form.Group as={Col} md="12" controlId="body">
                  <Form.Label>Review</Form.Label>
                  <InputGroup hasValidation>
                    <FloatingLabel controlId="Review" label="Review">
                      <Form.Control
                        as="textarea"
                        placeholder="Leave a review here"
                        value={formData.body}
                        onChange={handleChange}
                        name="body"
                        style={{ height: '100px' }}
                        required
                      />
                    </FloatingLabel>
                  </InputGroup>
                </Form.Group>
              </Row>
              <MDBBtn className="btn-success mt-3">Submit</MDBBtn>
            </Form>
            <div className="mt-4">
              <h2>Reviews</h2>
              {reviews.map((review) => (
                <div className="card mb-3">
                  <div className="card-body">
                    <div key={review._id}>
                      <h5 className="card-title">Rating : {review.rating}</h5>
                      <p className="card-text">Review :{review.body}</p>
                    </div>
                    <button
                      className="btn btn-sm btn-danger"
                      onClick={() => deleteReview(review._id)}
                    >
                      Delete
                    </button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      ) : (
        <p>캠프를 불러오는 중입니다...</p>
      )}
    </>
  )
}

export default CampGroundDetail
```

```jsx
**삭제 함수를 호출하는 버틑에 onClick**

onClick={() => deleteReview(review._id)}은 React에서 이벤트 핸들러를 정의하는 방식 중 하나이다.

이렇게 하는 이유는 함수를 직접 호출하지 않고, 새로운 함수를 생성하여 이벤트 핸들러로 전달하기 때문이다.

만약 onClick={deleteReview(review._id)}와 같이 직접 함수를 호출하면 렌더링될 때마다 함수가 호출되어

원하는 시점에 삭제되는 것이 아니라 즉시 함수가 실행된다.

하지만 onClick={() => deleteReview(review._id)}와 같이 함수를 새로 생성하면,

해당 함수는 클릭 이벤트가 발생할 때만 실행되기 때문에 의도한 시점에 함수가 호출된다.

**리뷰 삭제 핸들러**

await axios.delete(`http://localhost:4000/campground/${params.id}/reviews/${reviewId}`);

2개의 params 를 사용해서 호출을 한다
```

## 캠핑장 삭제시 연관 데이터 삭제

```jsx
캠핑장을 삭제하고 나면

댓글 내용이 그대로 남아있게 된다 이 내용까지

미들웨어를 활용해서 삭제해보도록 한다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381395/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_12.png)

```jsx
공식 문서를 찾아가서

우리가 사용 할 findByIdAndDelete() 메서드를 찾는다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381395/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_13.png)

```jsx
클릭해서 보면 이메서드는 findOneAndDelete() 미들웨어를 사용한다고 적혀있다

이걸 몽구스 미들웨어에 적용 시켜줘야한다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381395/notion-blog/_/The_Web_Developer_2022/node_js/YelpCamp_/Untitled_14.png)

```jsx
Campground 모델을 찾아간 뒤 미들웨어를 작성해준다 ( 아까 문서에서 찾은 미들웨어를 적어준다

현재는 삭제한 뒤 리뷰를 지워야 하기 때문에 post 를 사용한다
```

```jsx
const mongoose = require('mongoose') // 몽구스 연결
const { Schema } = mongoose // 몽구스 스키마를 스키마로 저장 ( 자주 사용하기 위해 단축키 느낌 )
const Review = require('./review') // 다른 파일에서 정의한 Review 모델을 불러온다

const CampGroundSchema = new Schema({
  // CampGround 스키마 정의
  title: String,
  image: String,
  price: Number,
  description: String,
  location: String,
  reviews: [
    {
      type: Schema.Types.ObjectId,
      ref: 'Review',
    },
  ],
})

CampGroundSchema.post('findOneAndDelete', async function (doc) {
  if (doc) {
    await Review.deleteMany({
      _id: {
        $in: doc.reviews,
      },
    })
  }
})

module.exports = mongoose.model('CampGround', CampGroundSchema) // 모델 내보내기
```

```jsx

해당 캠핑장 문서가 삭제될 때, 그 캠핑장과 연관된 리뷰들도 함께 삭제한다.

( 현재 post 를 사용하고 있기 때문에 doc 에는 함수가 아닌 데이터가 들어가 있을것이다 )

$in 연산자를 사용하여 doc.reviews 배열에 있는 ObjectId들을 가진 리뷰들을 한 번에 삭제

if(doc)로 모두 감싸는 이유는 가끔은 찾을 수 없는 걸 삭제하려고 하면

삭제되지 않을 때가 있다
```
