---
title: '2023-04-26(Schedule, 메일 발송하기 (Google SMTP))'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

# 리눅스 환경 배포 및 서버

```markdown
리눅스

- CI/CD : 지속적인 통합, 지속적인 서비스 제공, 지속적인 배포
```

```markdown
< CI/CD를 이용하는 방법 >

- Github action Aws S3
  GitHub Actions은 GitHub에서 제공하는 자동화 서비스로,
  AWS S3와의 통합을 통해 GitHub 저장소에 변경 사항이 발생할 때마다
  AWS S3에 자동으로 파일을 업로드하거나 다운로드하는 작업을 자동으로 수행
```

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled.png>)

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%201.png>)

---

---

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%202.png>)

## 로드 밸런싱

```markdown
- 로드 밸런싱
  네트워크 트래픽을 여러 대의 서버 또는 컴퓨팅 리소스 사이에 분산시켜
  작업 부하를 균형있게 분산하는 기술서
```

```markdown
유저가 서버에 들어가 로그인하면 (REQUEST) SESSION ID 생성
서버는 유저에게 응답으로(RESPONSE) COOKIE에 SESSION ID 담아 응답

서버를 여러대 놓고 회원이 접속했을때 최초 서버에 들어갔다가 로그아웃 후
타 서버에 들어가면 인식하지 못한다. (세션ID 존재 X)

- 해결방법

1. 세션 ID를 저장하는 서버를 따로 생성 (EX) 서버:레디스(Redis))
2. 토큰을 이용하는 방식 (세션 사용X)

- 암호화 해서 보냄
```

![<1. 세션 ID를 저장하는 서버를 따로 생성>](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%203.png>)

<1. 세션 ID를 저장하는 서버를 따로 생성>

![<2. 토큰을 이용하는 방식 (세션 사용X)>](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%204.png>)

<2. 토큰을 이용하는 방식 (세션 사용X)>

# Schedule(수정)

```jsx
특정 시간이나 주기적으로 반복되는 작업을 해야 할 때
```

## 1. 사용 설정

```jsx
App내에 main 메서드가 있는 클래스의 선언부에 Schedule 기능 사용 선언

@EnableScheduling
public class StudyBootApplication {...}
```

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%205.png>)

## 2. Schedule을 구현한 Class 생성

```jsx
1. 클래스를 생성
2. 반복 작업 할 Method를 생성
3. 메서드 선언부에 @Scheduled를 선언하고 반복주기를 설정
```

1.클래스를 생성

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%206.png>)

## 3. 반복 주기

```jsx
1. fixRate
	- 고정(일정) 간격으로 반복 실행
	- 서버가 실행 되고 실행
	- 해당 메서드가 언제 종료 되는지 상관 없음
	- fixRate=밀리세컨즈 or fixRateString="밀리세컨즈"

2. fixDelay
	- 고정(일정) 간격으로 반복 실행
	- 해당 메서드가 종료 후 일전 간격 실행
	- fixDelay=밀리세컨즈 or fixDelayString="밀리세컨즈"

3. 공통사항
	- 서버가 실행 하고 처음 시작 간격을 설정
	- initialDelay
	- initialDelayString = "1000"

4. **Cron  (많이 사용)**
	- Server(Linux)의 일정 주기별 반복 실행
	- Linux에서는 Crontab과 같음
	- 총 6개의 단위로 이루어짐 ( 초  분  시  일  월  요일)
	- 초(0-59) 분(0-59) 시(0-23) 일(1-31) 월(1-12) 요일(0-7)
	- 요일은 0,7은 일요일, 월요일 1, 화요일 2,...,토요일 6
	- * 은 모든것 (매초, 매분, 매시, 매일, 매월,,...)

*  *  *  *  *  *     : 매초마다 실행, 1초간격 실행
10 *  *  *  *  *     : 매시 매분 10초에 실행, 1시 0분 10초, 1시 1분 10초,
5  50 *  *  *  *     : 매시 50분 5초에 실행, 1시 50분 5초, 2시 50분 5초
0  0  0  *  *  *     : 매일 자정마다 실행
0  0  0  1  *  *     : 매월 1일 자정마다 실행
0  0  0  24 12 *     : 매년 12월 24일 자정마다 실행
0  0  9  *  *  1     : 매주 월요일 아침 9시 정각마다 실행
20 30 */3 * *  *     : 매일 3시간 간격으로 실행 (0시 30분 20초, 3시 30분 20초..)
0  0  0  *  * 6-7    : 매주 토-일 자정에 실행
0  0  9-18 * * 1-5   : 매주 월-금요일까지 오전 9시-18시까지 매시 정각 마다 실행
0  0  9-18/4 * * 1-5 : 매주 월-금요일까지 오전 9시-18시까지 4시간 간격으로 실행
0  0  0  *  * 1,3,5  : 월, 수, 금요일 자정마다 실행

ex) */10 * * * * *    -  10 초 간격으로 실행
```

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%207.png>)

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%208.png>)

cron 사용

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%209.png>)

매초마다 실행, 1초간격 실행

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2010.png>)

매시 매분 50초에 실행

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2011.png>)

간격을 나타낼 때 \*/3

## 예제1) 회원가입 한 유저들중에 오늘 생일인 사람 공지사항에 ‘생일이에요’ 글쓰기 ( 특정시간 -> 10분에)

```java
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iu.base.member.MemberDAO">

	<select id="getBirth" resultType="MemberVO">
		SELECT NAME, EMAIL FROM MEMBER
		WHERE DATE_FORMAT(birth, '%M-%D')=DATE_FORMAT(now(), '%M-%D') // birth 컬럼의 값을 '%M-%D' 형식으로 변환 (생일을 월,일 형식으로 변환)
	</select>                                                       // 앞서 변환한 생일과 오늘 날짜를 비교하여 일치하는 경우에만 해당 회원을 조회

	<update id="setEnabled">
	UPDATE MEMBER SET ENABLED = 0
	WHERE USERNAME
	IN
		( SELECT userName FROM MEMBER
		  WHERE TIMESTAMPDIFF(DAY, lastTime, now()) >= 3)
<!--
	<![CDATA[
 		UPDATE MEMBER SET ENABLED=0
		WHERE LASTTIME <= now()-INTERVAL 3 DAY
	]]>
 -->
	</update>

	<update id="setLastTimeUpdate" parameterType="MemberVO">        // 마지막으로 접속한 시간을 lastTime 에 저장
		UPDATE MEMBER SET LASTTIME=now() WHERE USERNAME=#{userName}
	</update>

	<select id="getMembers" resultType="MemberVO">
		SELECT USERNAME FROM MEMBER
	</select>

	<insert id="setJoin" parameterType="MemberVO">
		INSERT INTO MEMBER (USERNAME,PASSWORD, NAME, EMAIL, BIRTH, ENABLED)
		VALUES(#{userName}, #{password}, #{name}, #{email}, #{birth}, #{enabled})
	</insert>

	<insert id="setMemberRole" parameterType="Map">
		INSERT INTO MEMBER_ROLE (USERNAME, NUM)
		VALUES(#{userName}, #{num})
	</insert>

	<select id="idDuplicateCheck" resultType="MemberVO" parameterType="MemberVO">
		SELECT USERNAME FROM MEMBER WHERE USERNAME=#{userName}
	</select>

	<select id="getLogin" parameterType="MemberVO" resultMap="getLoginResult">
		SELECT M.userName, M.email, R.num, R.ROLENAME
		FROM MEMBER M
			 INNER JOIN
			 MEMBER_ROLE MR
			 ON M.USERNAME = MR.USERNAME
			 INNER JOIN
			 ROLE R
			 ON MR.NUM = R.NUM
		WHERE M.USERNAME=#{userName} AND M.PASSWORD=#{password}
	</select>

	<resultMap type="MemberVO" id="getLoginResult">
		<id property="userName" column="USERNAME"/>
		<result property="email" column="EMAIL"/>
		<collection property="roleVOs" javaType="List" ofType="RoleVO">
			<id property="num" column="NUM"/>
			<result property="roleName" column="ROLENAME"/>
		</collection>

	</resultMap>





</mapper>
```

```java
package com.iu.base.util;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import com.iu.base.board.notice.NoticeDAO;
import com.iu.base.board.notice.NoticeVO;
import com.iu.base.member.MemberDAO;
import com.iu.base.member.MemberVO;

import lombok.extern.slf4j.Slf4j;

@Slf4j
@Component
public class TestSchedule {
	@Autowired
	private MemberDAO memberDAO;
	@Autowired
	private NoticeDAO noticeDAO;
	@Autowired
	private MailManager mailManager;

	//@Scheduled(cron = "40 * * * * *")
	public void test() throws Exception {
		List<MemberVO> ar= memberDAO.getBirth();

		for(MemberVO memberVO:ar) {
			mailManager.send(memberVO.getEmail(), "생일축하", "<h1>생일축하</h1>");
		}



// 생일축하 게시물 생성

//		List<MemberVO> ar= memberDAO.getBirth();
//
//		StringBuffer sb = new StringBuffer();  // StringBuffer 문자열 추가할 때 사용
//		sb.append("오늘은 ");
//
//		for(MemberVO memberVO:ar) {
//			sb.append(memberVO.getName());  // 멤버 네임을 추가
//			sb.append(", ");
//		}
//
//		sb.append(" 생일 입니다. 생일빵 !!");
//
//
//		NoticeVO noticeVO = new NoticeVO();
//
//		noticeVO.setTitle("생축");
//		noticeVO.setWriter("사장님");
//		noticeVO.setContents(sb.toString());  // 저장된 게시물 내용을 (sb) toString 으로 문자열로 변환하여 저장
//
//		noticeDAO.setInsert(noticeVO); // noticeDAO 객체의 setInsert 메서드를 호출하여, noticeVO 객체에 저장된 정보를 이용하여 생일 축하 게시물을 생성


	}

}
```

## 예제2) 마지막으로 로그인한지 3일이 지나면 enabled 0으로 변경 ( 특정시간 -> 추천 새벽 4시)

```java

<update id="setEnabled">               // 휴면계정 만들기 2가지 방법존재

<!-- 1번방법-->

	UPDATE MEMBER SET ENABLED = 0
	WHERE USERNAME
	IN
		( SELECT userName FROM MEMBER
		  WHERE TIMESTAMPDIFF(DAY, lastTime, now()) >= 3)

<!-- 2번방법-->

	<![CDATA[                             // <![CDATA[ 쿼리 ]]> 내부의 쿼리문에 <, > 등 등호를 사용할 수 있게해줌
 		UPDATE MEMBER SET ENABLED=0
		WHERE LASTTIME <= now()-INTERVAL 3 DAY
	]]>

</update>
```

```java
TIMESTAMPDIFF : 시간 차이를 계산하는 함수로, 첫 번째 매개변수에는 시간 단위를 지정하고, 두 번째 매개변수와 세 번째 매개변수에는 비교할 시간 값을 지정

ex) DAY = 시간 차이를 일(day) 단위로 계산하도록 지정

	  lastTime , now() (현재시간) >= 3       :     lastTime 컬럼 값과 현재 시간 값(now()) 간의 차이를 계산하여 그 차이가 3일 이상인 경우를 찾기 위한 조건문
```

# 구글링 후 다른 설정 파일일때

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2012.png>)

```markdown
gradle로 나와있다. 하지만 난 maven이다
이땐 mvn repositery 들어가서 찾아본다.
여긴 "start mail" 검색해서 dependency 추가
```

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2013.png>)

```markdown
yml은 application.properties 로 바뀔 때 : 으로 이어져 있는게 한줄로 . 으로 이어줌
```

# **스프링부트 메일 발송하기 (Google SMTP)**

## 구글 설정

### **1. Google 홈페이지 → Google 계정 관리(우측상단)**

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2014.png>)

### **2. 보안 → 앱 비밀번호**

```java
앱 비밀번호는 위에 2단계 인증을 해야 생성됩니다.
```

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2015.png>)

### **3. 메일 → Windows 컴퓨터 선택**

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2016.png>)

### **4. 생성된 앱 비밀번호를 메모해둔다. (smtp 설정에 사용될 예정)**

![Untitled](<2023-04-26(Schedule,%20%EB%A9%94%EC%9D%BC%20%EB%B0%9C%EC%86%A1%ED%95%98%EA%B8%B0%20(Google%20SMTP))/Untitled%2017.png>)

### **5. 빠른 설정(우측상단 톱니바퀴)** → **모든 설정 보기**

**모든 설정 보기**

![](https://blog.kakaocdn.net/dn/bz65cw/btq6fUtKm5G/Pb8RXyj685Tvq4lyOeSjz0/img.png)

### **6. 전달 및 POP/IMAP(탭)** → **모든 메일에 POP 사용하기** → **IMAP 사용** → **변경사항 저장**

![](https://blog.kakaocdn.net/dn/CB76s/btq6fO8hXa6/XV8X7fToUWGOu3SaXU4PgK/img.png)

## 자바 설정

### 1.의존성 추가

```java
<!-- https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-mail -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-mail</artifactId>
    <version>2.7.3</version>
</dependency>
```

### 2.**application.properties 설정**

```java
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=<내 구글 계정>
spring.mail.password=<내 구글 계정 패스워드>
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

* 주의
반드시 application.properties 파일은 .gitingore에 추가해주기
```

### 3. 파일 매니저 생성

```java
**MailManager**

package com.iu.base.util;

import javax.mail.internet.InternetAddress;
import javax.mail.internet.MimeMessage;
import javax.mail.internet.MimeMessage.RecipientType;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Component;

@Component
public class MailManager {

	@Value("${spring.mail.username}")      // application.properties 에서 값 가져오기
	private String sender;

	@Autowired
	private JavaMailSender javaMailSender;          // 자바 메일센더 인터페이스 생성

	public void send(String to, String sub, String con) throws Exception {
//		HTML 태그 사용해서 전송하는 방법
		MimeMessage mimeMessage = javaMailSender.createMimeMessage();  // 이 클래스를 사용하면 태그 변화 가능  ex)<h1></h1>
		mimeMessage.setFrom(sender);
		mimeMessage.addRecipient(RecipientType.TO, new InternetAddress(to));
		mimeMessage.setSubject(sub);
		mimeMessage.setText(con);
		javaMailSender.send(mimeMessage);

//		HTML태그를 무시하고 적은 그대로 Text로 전송
//		SimpleMailMessage mailMessage = new SimpleMailMessage();
//		mailMessage.setFrom(sender);
//		mailMessage.setTo(to);
//		mailMessage.setSubject(sub);
//		mailMessage.setText(con);
//		javaMailSender.send(mailMessage);
	}

}
```

```java
package com.iu.base.util;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import com.iu.base.board.notice.NoticeDAO;
import com.iu.base.board.notice.NoticeVO;
import com.iu.base.member.MemberDAO;
import com.iu.base.member.MemberVO;

import lombok.extern.slf4j.Slf4j;

@Slf4j
@Component
public class TestSchedule {
	@Autowired
	private MemberDAO memberDAO;
	@Autowired
	private NoticeDAO noticeDAO;
	@Autowired
	private MailManager mailManager;

	//@Scheduled(cron = "40 * * * * *")
	public void test() throws Exception {
		List<MemberVO> ar= memberDAO.getBirth();  // 쿼리문에 email 도 가져오게 되어있다.

		for(MemberVO memberVO:ar) {
			mailManager.send(memberVO.getEmail(), "생일축하", "<h1>생일축하</h1>");    // 누구에게(getEmail),제목,내용
		}
```
