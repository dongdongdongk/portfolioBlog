---
title: 'AWS 시험'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

1.다음 중 소스 파일을 이용한 설치 방법이 나머지 셋과 다른 것은?(2)

```xml
1.	Apache httpd
2.	MySQL
3.	PHP
4.	Nmap
```

2.다음 중 데비안 계열 리눅스에서 사용하는 패키지 관리 도구로 가장 알맞은 것은?(3)

```xml
1.	rpm
2.	yum
3.	dpkg
4.	YaST
```

3.다음 중 yum을 이용해서 nmap 패키지를 제거하는 명령으로 알맞은 것은?(3)

```xml
1.	yum delete nmap
2.	yum clean nmap
3.	yum remove nmap
4.	yum destory nmap
```

4.다음 중 온라인 기반 패키지 관리 도구로 거리가 먼 것은?(4)

```xml
1.	apt-get
2.	yum
3.	zypper
4.	YaST
```

5.다음 중 의존성이 있는 httpd 패키지를 강제로 제거하는 명령으로 알맞은 것은?(4)

```xml
1.	rpm –r httpd --force
2.	rpm –r httpd --nodeps
3.	rpm –e httpd --force
4.	rpm –e httpd --nodeps
```

6.다음 중 vi 편집기에서 변경된 내용을 저장하지 않고 종료하는 명령으로 알맞은 것은?(2)

```xml
1.	:w!
2.	:q!
3.	:x!
4.	:e!
```

7.다음 중 cron을 이용해서 매주 1회만 작업 스크립트를 실행하려고 할 때 ( 괄호 ) 안에 들어갈 내용을 알맞은 것은?(3)

```xml
1.	4 0 * 1 *
2.	4 0 1 * *
3.	4 0 * * 2
4.	4 0 * 2 *
```

8.다음 중 디스크 용량 단위를 적은 순서부터 큰 순서로 바르게 나열한 것은?(1)

```xml
1.	GB＜TB＜PB＜EB
2.	TB＜GB＜PB＜EB
3.	GB＜TB＜EB＜PB
4.	TB＜GB＜EB＜PB
```

9.다음 설명에 해당하는 빅데이터 관련 기술로 알맞은 것은?(1)

```xml
대량의 자료를 처리할 수 있는 큰 컴퓨터 클러스터에서 동작하는
분산 응용 프로그램을 지원하는 프리웨어 자바 소프트웨어 프레임워크이다
```

```xml
1.	Hadoop
2.	NoSQL
3.	R
4.	Anisible
```

10.다음 중 FTP 서비스에서 사용하는 포트 번호에 대한 설명으로 알맞은 것은?(3)

```xml
1.	FTP 서비스는 20번 포트를 사용해서 데이터 전송 및 제어를 관리한다.
2.	FTP 서비스는 21번 포트를 사용해서 전송 및 제어를 관리한다.
3.	FTP 서비스는 20번 포트로 데이터를 전송하고, 21번 포트로 제어한다.
4.	FTP 서비스는 20번 포트로 제어하고, 21번 포트로 데이터를 전송한다.
```
