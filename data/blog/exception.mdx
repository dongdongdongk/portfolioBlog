---
title: 'Exception 유형'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## Server Error (BackEnd)

## NullpointerException

```sql
객체를 만들지 않고 사용하려 할 때 발생

String str,

Str.subStirng();

```

```sql
private ProductDAO productDAO;
```

## DefendencyExcption

```sql
@Autowired
private ProductDAO productDAO;

주입 하려는 객체가 생성되지 않았다

해당 클래스의 선어부에 객체 생성하는 Annotation 이 있는가?

해당 클래스가 BasePackage 또는 하위에 속해 있는가?

context.xml 에 bean 태그로 선언이 되어 있는가?

context.xml 파일의 위치와 이름을 web.xml에 등록 되어 있는가?
```

## Front Error

## 404 Not Found

```sql
자원의 위치를 차증ㄹ 수 없다. (URL 주소의 문제)

URL과 맵핑 되는 Controller 의 메서드가 있는가??

Controller 에서 연결하는 JSP 파일이 있는가??

Controller 에서 redirect 하는 URL 과 맵핑 되는 Controller의 메서드가 있는가
```

EL 삼항 연산자 코드예시

<label for="bs2">판매중단</label>
<input id="bs2" type="radio" checked = "${dto.bookSale eq '0' ? 'checked':''}" name="bookSale" value="0">
