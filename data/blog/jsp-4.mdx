---
title: 'JSP에서 다음 페이지로 데이터를 전달시키는 4가지 방법'
date: '2026-09-02'
category: '백엔드/JAVA'
tags: ['백엔드', 'JAVA']
draft: false
summary: ''
---

```html
* 두번째, 세번째 방식은 get 방식으로 보내는 것과 같음
```

# **첫번째 방법**

```html
<form>
  태그의 자식으로 <input type="hidden" name="" value="" /> 이런 요소를  추가하여, 보내고 싶은 값을
  value 속성에 넣어주고, 이동한 페이지에서 request 객체를 통해 접근하는 방법
</form>
```

```html
<!-- 첫 번째 페이지 (form을 통해 데이터를 전송) -->
<form action="second_page.jsp" method="post">
  <input type="hidden" name="myValue" value="Hello World" />
  <input type="submit" value="두 번째 페이지로 이동" />
</form>

<!-- 두 번째 페이지 (request 객체를 통해 값에 접근) -->
<% String myValue = request.getParameter("myValue"); %>
<p>전달받은 값: <%= myValue %></p>
```

# **두번째 방법**

```html
response.sendRedirect("파일경로?변수=" + 변수값)의 인자에 붙여주는 방법 -> 이동한 페이지에서 request
객체를 통해 접근
```

```java
// 첫 번째 페이지 (Java Servlet)
String myValue = "Hello World";
response.sendRedirect("second_page.jsp?myValue=" + myValue);

// 두 번째 페이지 (request 객체를 통해 값에 접근, JSP 사용)
<%
  String myValue = request.getParameter("myValue");
%>
<p>전달받은 값: <%= myValue %></p>
```

# **세번째 방법**

```html
<a href="adadaad?변수=<%=값 %>"> 이동하기 </a>
```

```java
<!-- 첫 번째 페이지 -->
<a href="second_page.jsp?myValue=Hello+World">두 번째 페이지로 이동</a>

<!-- 두 번째 페이지 (request 객체를 통해 값에 접근, JSP 사용) -->
<%
  String myValue = request.getParameter("myValue");
%>
<p>전달받은 값: <%= myValue %></p>
```

# **네번째 방법**

```html
session객체에 붙여주는 방법
```

```java
// 첫 번째 페이지 (Java Servlet)
String myValue = "Hello World";
HttpSession session = request.getSession();
session.setAttribute("myValue", myValue);
response.sendRedirect("second_page.jsp");

// 두 번째 페이지 (세션 객체를 통해 값에 접근, JSP 사용)
<%
  String myValue = (String)session.getAttribute("myValue");
%>
<p>전달받은 값: <%= myValue %></p>
```
