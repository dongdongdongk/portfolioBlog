---
title: '서블릿4'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## Controller 에 파라미터 값을 받는 방법

```java

(재)**Parameter**
	- Client에서 Server로 보내는 데이타
	- 이름과 값으로 구성
	- 이름 : String, 값(value) : String (이름과 값 모두 스트링 타입이다)
```

```java
**1.매개변수로 내장객체 req,res 를 선언해서 파라미터 값 받기**

@RequestMapping(value="detail")
**//DispacherServlet 에서 req,res 를 요청하여 매개변수로 받음**
public String getProductDetail(HttpServletRequest request, HttpServletResponse response){
System.out.println("Product Detail");
String num = request.getParameter("num"); // 파라미터는 String 타입으로 모두 받음

prductDTO productDTO = new ProductDTO();
productDTO.setProductNum(Long.parseLong(num)); //String 타입으로 받은 파마미터를 Long으로

System.out.println(productDTO != null);

return "product/productDetail";
}

```

```java
2.**매개변수를 파라미터 명으로 받고 받음과 동시에 타입을 변환시킴**

@RequestMapping(value="detail")
public String getProductDetail**(long num )**{ // 파라미터 이름과 동일하게 변수를 받아 long변환
System.out.println("Product Detail");

//String num = request.getParameter("num"); // 파라미터는 String 타입으로 모두 받음
**productDTO.setProductNum(num)** // num을 처음부터 매개변수에 long으로 변환시켜 받음

prductDTO productDTO = new ProductDTO();
productDTO.setProductNum(Long.parseLong(num));
productDTO = productService.getProductDetail(productDTO);

System.out.println(productDTO != null);

return "product/productDetail";
}

```

```java
**@RequestParam 을 이용해서 파라미터의 이름이 다르게 받아줄 수도 있다.
//"a" 의 파라미터 값을 받으면 Integer age에 넣어줘라**

@RequestMapping(value="detail")
public String getProductDetail**(long num, String name, @RequestParam(value =  "a") Integer age)

추가적으로
(value = "a", defaultValue = "1", required = false)

defaultValue : 값이 넘어오지 않으면 1로 하라

required : false 로 해놓으면 값이 오지 않으면 받지 않겠다 (null 을 피할 수 있음)

-----------------------------------------------------------------------------------

같은 이름의 값이 여러개 들어오면 배열로 받아준다.**
public String getProductDetail**(int [] num )**
```

```java
3.**파라미터 이름이 속해있는 DTO로 선언하기
파라미터의 이름과 setter의 이름이 같아야함**

@RequestMapping(value="detail")
public String getProductDetail**(ProductDTO productDTO )**{
System.out.println("Product Detail");

//String num = request.getParameter("num");
productDTO.setProductNum(num)

//prductDTO productDTO = new ProductDTO();
//productDTO.setProductNum(Long.parseLong(num));
productDTO = productService.getProductDetail(productDTO);
System.out.println(productDTO != null);

return "product/productDetail";
}

**jsp 에서 멤버 변수와 동일하게 파라미터 이름을 설정하고 Controller 에서 DTO 를 매개변수로
주면 자동으로 찾아 넣어준다 ( 이때 DTO에는 동일한 이름의 변수가 있어야 한다)

*실제로는 setter 의 이름과 파라미터 이름이 같아야한다.

*DTO 의 변수명은 테이블의 컬럼명과 같아야한다.(그래야 편함)**
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380886/notion-blog/_/_/JSP/_4/Untitled.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380882/notion-blog/_/_/JSP/_4/Untitled_1.png)

---

## Controller 에서 jsp 로 값 보내기

### Model 사용하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380883/notion-blog/_/_/JSP/_4/Untitled_2.png)

```java
Model  이라는 jsp로 데이터를 전달할때 사용하는 클래스를 이용한다 (req 랑 동일한 기능)

model.addattribute("이름","value")

*서버 내에서 서버로 보내는 것을 attribute

*Model은 return 을 하지 않아도 jsp로 간다
```

### ModelAndView 사용하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380883/notion-blog/_/_/JSP/_4/Untitled_3.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380883/notion-blog/_/_/JSP/_4/Untitled_4.png)

```java
ModelAndView 를 사용할수도 있다.

mv.setViewName("product/productList"); //보낼 주소값
mv.addObject("이름",value); // addattribute 와 같은기능

*매개변수로 ModelAndView 를 선언하면 DS 가 보내준다 ( ModelAndView mv = new ModelAndView)
```

### jsp 에서 Attribute 출력

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380885/notion-blog/_/_/JSP/_4/Untitled_5.png)

```java
<% %> JAVA 코드 작성 방식으로 Attribute 를 받아온뒤
Attribute 는 Object 타입이기 때문에 형변환을 해준다 (ProductDTO)

<% ProductDTO productDTO = (ProductDTO)request.getAttribute("이름");%>
//출력문
<h3><%=productDTO.getProductName() %></h3>
<h3><%=prodcutDTO.getProductDetail() %></h3>
```

---

## EL(Expression Language)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380885/notion-blog/_/_/JSP/_4/Untitled_6.png)

```java
EL
-<% %> 는 예전 방식 EL을 사용한다
-출력 전용
-if, for 은 없다
-꺼내오는 작업이 필요없다. ex)<% ProductDTO productDTO = (ProductDTO)request.getAttribute("이름");%>

-- 내장객체의 LifeCycle
-- pageScope -> requestScope -> sessionScope -> applicationScope
(지금 사용하는 scope 명 requestScope)

**문법 : ${scope명 , 속성명}
       ${request,setAttribute("deo",noticeDTO);
			 (Attribute에 DTO가 있을때)

	     $(scope명,속성명,getter 메서드에서 get을 지우고 첫 글자를 소문자로 바꾼것
			 $(requestScope.dto.writer

Scope 명은 생략가능**

```

```html
<h3>${requestScope.dto.productName}</h3>
<h3>${dto.productDetail}</h3>
<!-- scope 명 생략가능 -->
<h3>${dto.productJumsu * dto.productNum}</h3>
<!-- 연산자를 사용하여 두 값을 곱하기 -->
```

---

## JSTL(JSP STANDARD Tag Library)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380885/notion-blog/_/_/JSP/_4/Untitled_7.png)

```html
예전에 배운 JSP 의 반복문의 불편함을 해결해주는 라이브러리 이다. 주로 사용하는 기능은 Core Tag
(다른건 다른곳에서 해결 가능) - 스크립 틀릿을 사용하지 않음 1) JSTL 태그 a. Core Tag : 변수관리,
제어문등을 제공 접두사(prefix, 태그명) : c b. Function Tag : 문자열 조작, 문자열 길이등 함수제공
접두사 : fn c. Formatting Tag : 숫자, 날짜, 통화단위 등을 지원 : fmt d. XML Tag : XML 관련 : x e.
SQL Tag : SQL 지원 : sql
```

### Core Tag 사용 순서

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380885/notion-blog/_/_/JSP/_4/Untitled_8.png)

```html
Core Tag 사용순서 1. 사용하려는 JSP page 마다 별로 선언부에 JSTL 라이브러리 선언 <%@ taglib
prefix="c" uri="http://java.sun.com/jsp/jstl/core" %> 2.<c:태그명
  items="${Collection의 속성명(배열,리스트)"
  var="DTO를 담을 변수명"
>
  ex<c:forEach items="${list}" var="dto">
    (Controller 의 이름이 items) var = 자유롭게

    <h3>${pageScope.dto.prodcutName}</h3>
    <h3>${dto.productJumsu}</h3>
    <!-- scope 명 생략가능 -->

    <c:/태그명> if : if문 forEach : 반복문 choose : 스위치문
  </c:forEach></c:태그명
>
```

### Ex 최종출력

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380885/notion-blog/_/_/JSP/_4/Untitled_9.png)

```html
<c:forEach items="$(list)" var="dto">
  <tr>
    <td><a href="./detail?prodcutName=${dto.productNum}">${pageScope.dto.productName}</a></td>
    <!-- 클릭시 productName 리스트를 출력-->
    <td>${dto.productJumsu}</td>
  </tr>
  <c:forEach></c:forEach
></c:forEach>
```

## JSTL-<c:forEach>

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380882/notion-blog/_/_/JSP/_4/Untitled_10.png)

```java
자바의 반복문 for 과 비슷한 문법이다
<c:forEach items = "${list}" var = "dto">   //list 는 Controller 에서 가져온 리스트
																						//dto 는 page scope 로 이 페이지에서만 유지되는
<c:forEach>                                 //변수이다
```

## JSTL-<c:if>

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380882/notion-blog/_/_/JSP/_4/Untitled_11.png)

```java
자바의 if 문과 같은 문법이다

<c:if test = "${not empty dto}">    // dto 가 null 이 아니라면 실행
                                    //  "${dto.bookSale eq 1} 다양한 연산자 가능
</c:if>
```

## JSTL-<c:choose>

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380882/notion-blog/_/_/JSP/_4/Untitled_12.png)

```java
자바의 switch 문과 비슷한 문법이다

<c:choose>
		<c:when test = "${dto.bookSale eq 1}"> 판매중 </c:when> //
		<c:otherwise> 판매중단 </c:otherwise>
</c:choose>

```

---

## html-Table

```html
1. 표를 만들 때 사용

<table>
  : 표를 감싸는 태그
  <tr>
    : 표의 행 (row)
    <td></td>
    : 표의 열 (column)
  </tr>
</table>

2. 표를 생성시 제목과 내용을 분리
<table>
  <thead>
    <tr>
      <th>제목1</th>
      <th>제목2</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>내용1</td>
      <td>내용2</td>
    </tr>
  </tbody>

  <tfoot>
    <tr>
      <td>foot1</td>
      <td>foot2</td>
      >
    </tr>
  </tfoot>
</table>

3. 컬럼 확장(합치기) 1) Column 확장 (가로로 확장, 옆으로)
<td colspan="합치는 총 갯수">2) Row 확장 (셀로로 확장, 위아래)</td>

<td rowspan="합치는 총 갯수"></td>
```

---

## html-Form

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380882/notion-blog/_/_/JSP/_4/Untitled_13.png)

```html
- 사용자로부터 입력 받거나 선택 할 때 사용 - 대부분이 서버로 전송 하는 데이터를 입력할 양식 - form
태그 영역 내의 것들을 서버로 전송 - form 태그 영역 내에는 input 태그들 사용

<form action="요청할 URL주소" method="요청할메서드형식"></form>

- method속성은 생략 가능 ( Default는 GET) - GET 또는 POST로 변경 가능 *Controller 에서 method =
requestMetod.GET or POST 로 form 에서 보낸 메서드 형식 선택 가능 (기본은 GET)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380883/notion-blog/_/_/JSP/_4/Untitled_14.png)

---

## html-입력태그

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380883/notion-blog/_/_/JSP/_4/Untitled_15.png)

```html
- 주로 form 태그 내에서 사용 - form 태그 없이 사용도 가능 (JavaScript와 같이 사용) - 사용자로 부터
데이터를 입력 받는 태그 - type이라는 속성값에 따라서 입력 양식이 달라짐 1. input
<input type="값" />

1) type의 종류 a. text : 짧은 Text에 주로 사용 b. password : 입력 되는 데이터를 * 로 표시 c. file :
파일 업로드시 파일을 선택 d. date : 날짜 입력 또는 선택 e. number : text와 비슷 , 숫자만 입력 f.
email : 이메일 양식으로 입력 g. tel : 전화번호 양식으로 입력 h. hidden : 사용자에게는 보이지 않고
서버로 전송 i. radio : 여러개 중에서 하나를 선택 할 때 : 그룹 별로 name 속성의 값을 일치시켜야 함 :
Server로 전송시 입력 할수 없으니 개발자가 미리 value에 속성에 값을 대입 j. checkbox : 여러개중에서
다중 선택 : 나머지는 Radio와 거의 같음 2. textarea - 비교적 긴글을 입력할 때 사용
<textarea rows="" cols=""></textarea>

3. select - 여러개 중에서 하나를 선택
<select name="">
  <option value="">값1</option>
  <option value="">값2</option>
  <option value="">값2</option>
</select>

4. 공통속성 1) value : 입력 태그에 입력된 값이 저장되는 속성 파라미터의 값으로 서버로 전송 2) **name
: 파라미터의 이름이 됨** 3) readonly : 입력(선택)불가, 읽기만 가능, 서버로 전송 가능 4) disabled :
입력(선택)불가, 읽기만 가능, 서버로 전송 불가 5. 입력 태그별 속성 1) 사용자 타이핑하는 태그 a.
placeholder : 입력양식에 hint 제공
```
