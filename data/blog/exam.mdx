---
title: 'exam'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

1. (가)영역처럼 설정이 되어 있을 때 WriteArticleServiceImpl 클래스의 생성자를 구현하시오

```bash
내용
class="chap01.WriteArticleServiceImpl">

(가)

----------------------------------------------------------------
정답

class WriteArticleServiceImpl {

   OracleArticleDao dao;

    public WriteArticleServiceImpl(OracleArticleDao dao) {

        this.dao = dao;

     }

}
```

2.위의 [문항1]의 xml을 참조하는 main 소스입니다. Ⓐ의 빈칸에 들어갈 단어를 쓰세요

```bash
String config = "classpath:applicationContext.xml";

AbstractApplicationContext ctx =

new GenericXmlApplicationContext(config);

WriteArticleService articleService =

ctx.getBean("Ⓐ",WriteArticleService.class);

articleService.write(new Article());

----------------------------------------------------------------
정답
writeArticleService
```

3.스프링의 개념 중 공통기능을 표현하기 위한 것이 AOP입니다.

AOP는 ( ) Oriented Programming 의 약자입니다. ()안에 맞는 단어를 쓰시오

```bash
----------------------------------------------------------------
정답
Aspect
```

4.스프링은 의존 관계가 자기 자신이 아닌 외부에 의해서 설정 됩니다.

DI는 ( ) Injection의 약자입니다. ( )안에 맞는 단어를 쓰시오.

```bash
----------------------------------------------------------------
정답
Dependency
```

5.Mybatis를 이용하여 프로그래밍하려고 한다.

다음과 같은 인터페이스 방식으로 하고자 할 때. 인터페이스의 네임 스페이스를 쓰시오.

```bash
package mapper;

...

public interface BoardMapper {

@Select("select ifnull(max(num),0) from board")

int maxnum();

}

----------------------------------------------------------------
정답
mapper.BoardMapper
```

6.다음은 소스의 일부입니다. 해당 클래스를 정상적으로 사용한 문장을 고르시오.

단 보기의 소스에서는 컴파일 오류는 없습니다.

```bash
@Component
public class HomeController {... }
1번	context.getBean("HomeController", Component.class);
2번	context.getBean("HomeController", HomeController.class);
3번	context.getBean("homeController", Component.class);
4번	context.getBean("homeController", HomeController.class);

----------------------------------------------------------------
정답
4
```

7.Maven 프로젝트로 실행 하고자 할때 jar 파일을 설정하는 xml의 이름은 무엇인가

```bash
1번	maven.xml
2번	pom.xml
3번	sitemesh.xml
4번	web.xml

----------------------------------------------------------------
정답
2
```

8.스프링 컨테이너와 관련있는 인터페이스의 이름이 아닌것을 고르시오

```bash
1번	BeanFactory
2번	ApplicationContext
3번	WebApplicationContext
4번	BeanApplicationContext

----------------------------------------------------------------
정답
4
```

9.mybatis를 사용하여 db의 정보를 읽어 올 때,

조회될 레코드가 여러건인 경우 사용되는 메서드이름은 무엇인가.

```bash
1번	selectList
2번	selectOne
3번	select
4번	selectObject

----------------------------------------------------------------
정답
1
```

10.다음은 빈클래스입니다. 클래스에서 구현된 프로퍼티 이름중 맞는 것을 고르시오

```bash
public class MessageBean {

private String messsage

public String getMsg() {

return messsage

}

public void setMsg(String messsage) {

this.messsage = messsage

}

}

1번	get 프로퍼티 : message, set 프로퍼티 : message
2번	get 프로퍼티 : msg, set 프로퍼티 : message
3번	get 프로퍼티 : message, set 프로퍼티 : msg
4번	get 프로퍼티 : msg, set 프로퍼티 : msg

----------------------------------------------------------------
정답
4
```
