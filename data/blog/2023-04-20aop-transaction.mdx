---
title: '2023-04-20(AOP, Transaction)'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

# AOP

```java

(대형 프로젝트에 많이 사용하고 크게 사용 할 일이 많은건 아니다)
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%201.png>)

```sql
반복 되는 클래스나 메서드를 넣어두고 재활용 하는 특징
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%202.png>)

```sql
오브젝트 언어를 조금더 오브젝트 스럽게 해주기 위해서 (위의 반복되는 코드를 수정)
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%203.png>)

```sql
핵심 코드를 보고있다가 코드가 실행될 때 반복되는 코드를 실행 해주는것
```

# AOP 사용순서

## 1.API 준비

```jsx
<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-aop</artifactId>
				<!-- 버전 생략 -->
</dependency>
```

## 2. AOP 사용

```jsx
main method가 있는 클래스 선언부에
@EnableAspectJAutoProxy 선언(생략 가능)

*** 실패하면 아래 코드

@SpringBootApplication
//@EnableAspectJAutoProxy
public class StudyBootApplication {

	public static void main(String[] args) {
		SpringApplication.run(StudyBootApplication.class, args);
	}

}
```

## 3. AOP 주요 용어

```jsx
1. advice
	 - 핵심 로직이 진행 될 때 실행해야할 공통 로직

2. point-cut
	 - 핵심 로직

3. join point
	 - 공통로직을(advice) 핵심로직(point-cut)에 언제 실행 할 것이냐 결정
	 1) Before          : 핵심로직 실행 전
	 2) AfterReturning  : 핵심로직 실행 후, 정상적인 종료일 때만
	 3) AfterThowing    : 핵심로직 실행 후, 예외가 발생했을 때만
	 4) After           : AfterReturning + AfterThowing
	 5) Around          : Before + After

4. Aspect (실제로 연결하는것) (위의 3가지는 셋팅)
	- Advice 를 언제(join-point) 누구에게(point-cut) 실행 하는 것을 설정

5. Weaving
	- Advice를 point-cut에 적용하는 행위
6. proxy
```

## 5. 예시

```jsx
@Component
@Slf4j
@Aspect
public class Card {    // advice


	@Before("execution(* com.iu.home.aop.test.Transeport.airplane())")
	public void before() {
		log.info(" ------------Before --------- ");
	}
	@After("execution(* com.iu.home.aop.test.Transeport.get*())")
	public void after() {
		log.info(" ------------After --------- ");
	}


	@Around("execution(* com.iu.home.aop.test.Transeport.take*())")
	public Object cardCheck(ProceedingJoinPoint joinPoint) throws Throwable {
		log.info("--- 삐빅 승차 입니다 -----");
		Object obj = joinPoint.proceed();
		log.info("--- 삐빅 하차 입니다 -----");
		return obj;

	}

}
```

## 4. Execution

```jsx
execution(접근지정자(생략가능) 리턴타입 패키지명.클래스명.메서드명(매개변수타입) )

- execution(public *.*)         : 모든 public 메서드
- execution( * set*())          : 모든 set으로 시작하는 메서드들 중 매개변수가 없는것
- execution( * set*(*))         : 모든 set으로 시작하는 메서드들 중 매개변수가 한개 선언한것
- execution( * set*(*,*))       : 모든 set으로 시작하는 메서드들 중 매개변수가 두개 선언한것
- execution( * set*(Integer, *)): 모든 set으로 시작하는 메서드들 중 매개변수가 두개 선언한것, 첫번째는 Integer
- execution( * set*(..))        : 모든 set으로 시작하는 메서드들 중 매개변수가 0개 이상 상관 X
- execution( * com.iu.home.service.TestService.*()) :      (모든 메서든데 매개변수가 없는것)
- execution( * com.iu.home.board.*.*()) :
- execution( * set*() || execution(* set*(*))) :
- execution( * set*() && execution(* set*(*))) :

```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%204.png>)

```sql
Advice 를 언제(join-point) 누구에게(point-cut) 실행 하는 것을 설정
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%205.png>)

```java
 advice 와 시기 (@Befor)가 결정되었다
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%206.png>)

```java
위 경로의 모든 use 로 시작하는 메서드를 실행
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%207.png>)

```java
point-cut 핵심기능
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%208.png>)

```java
정상 종료 되었을 때
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%209.png>)

```java
랜덤으로 1, 2 나오게 해서 익셉션 발생 시키기
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2010.png>)

```java
@Around 실습  실행 전,후
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2011.png>)

```java
탈때 내릴때 log. 둘다 줘보기

	@Around("execution(* com.iu.home.aop.test.Transeport.take*())")
	public Object cardCheck(ProceedingJoinPoint joinPoint) throws Throwable {
		log.info("--- 삐빅 승차 입니다 -----");
		Object obj = joinPoint.proceed();
		log.info("--- 삐빅 하차 입니다 -----");
		return obj;

	}

*** 이걸 사용해서 둘의 순서를 설정

ProceedingJoinPoint joinPoint 를 객체화
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2012.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2013.png>)

```java
object 타입으로 리턴 해줘야 하고
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2014.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2015.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2016.png>)

```java
버스 와 지하철 에서 받은 리턴으로 joinPoint 에 대
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2017.png>)

```java
실행하는 point cut 의 리턴을 받아서
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2018.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2019.png>)

```java
매개변수를 받아보기
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2020.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2021.png>)

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2022.png>)

```java
무슨 타입이 올지 모르니까 오브젝트로 받음

for 문을 돌려서 args 찍어보기

매개변수를 받아야 하기 때문에 (..) 으로 경로 변경

******

로그 기록을 공통적으로 찍고 싶을때 AOP 를 많이 사용한다.

```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2023.png>)

```java
(ex notice 테이블에 인서트를 했는데 notice 파일에서 익셉션이 발생했을 때 )

(ex 구매내역은 남지만 포인트가 익셉션으로 올라가지 않을때)

(ex A 통장에서 돈을뺴서 B에 넣으려고 하는데 A에서 빠지기만 하고 B에서 익셉션이 발생해서 문제가 발생했을 때 )

이 모든 과정을 하나의 단위로  **트렌젝션** 이라고 한다.

인서트,업데이트,딜리트 이런 종류가 두개 이상 들어가면 트렌젝션
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2024.png>)

```java
서비스 클래스에서 set으로 시작하는 애들중에 익셉션이 발생 했을 때
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2025.png>)

```java
모든 패키지에 서비스 패키지에 set 에 모든것 그러나 이렇게 모든곳에 적어주기 힘드니까 다른 기능이 있다
```

# Transaction 처리

```jsx
Spring에서는 AOP를 이용해서 Transction 처리
Legacy에서는 XML파일, Annotation에 설정
```

## 1. 설정

```jsx
1. main method가 있는 클래스 선언부
	 - @EnableTransactionManagement 선언
	 - 생략 해도 가능

2. Service class에 Transaction 처리
	 @Transactional(rollbackFor=Exception.class)

	 a. 메서드 선언부
	 b. 클래스 선언부     (여기까지)
	 c. Interface 선언부 (이건 오류가 많다)

	 - 메서드는 public 이어야 함

3. application.properties
	 - Service에 @Transactional 사용시
	 - Service class가 부모 없이 단독 사용시 에러 방지
	 spring.aop.proxy-target-class=true
```

![Untitled](<2023-04-20(AOP,%20Transaction)/Untitled%2026.png>)

```java
귀찮으면 class 에 줄수도있다.
```

@Transactional

### 예제

```java
**NoticeService**

package com.iu.base.board.notice;

import java.util.List;
import java.util.Random;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.multipart.MultipartFile;

import com.iu.base.board.BoardFileVO;
import com.iu.base.board.BoardService;
import com.iu.base.board.BoardVO;
import com.iu.base.util.FileManager;
import com.iu.base.util.Pager;

import lombok.extern.slf4j.Slf4j;

@Service
@Slf4j
@Transactional(rollbackFor=Exception.class)                                        // 익셉션 발생시 전부 롤백 트랜잭션
public class NoticeService implements BoardService{

	@Autowired
	private NoticeDAO noticeDAO;

	@Value("${app.upload.notice}")
	private String path;

	@Autowired
	private FileManager fileManager;

	@Override
	public List<BoardVO> getList(Pager pager) throws Exception {
		// TODO Auto-generated method stub
		pager.makeStartRow();
		pager.makeNum(noticeDAO.getTotalCount(pager));
		return noticeDAO.getList(pager);
	}

	@Override
	public BoardVO getDetail(BoardVO boardVO) throws Exception {
		// TODO Auto-generated method stub
		return noticeDAO.getDetail(boardVO);
	}
	@Override
	public BoardFileVO getFileDetail(BoardFileVO boardFileVO) throws Exception {
		// TODO Auto-generated method stub
		return noticeDAO.getFileDetail(boardFileVO);
	}

	@Override
	public int setInsert(BoardVO boardVO,MultipartFile[] multipartFiles) throws Exception {
		int result = noticeDAO.setInsert(boardVO);
		log.error("Num============>{}",boardVO.getNum());

		Random random = new Random();
		int num = random.nextInt(1);

		if(num==0) {
			throw new Exception();
		}

		if(multipartFiles != null) {
			for(MultipartFile multipartFile:multipartFiles) {
				//조건식 또는 =>multipartFile.getSize() != 0
				if(!multipartFile.isEmpty()) {
					String fileName = fileManager.saveFile(path, multipartFile);
					BoardFileVO boardFileVO = new BoardFileVO();
					boardFileVO.setFileName(fileName);
					boardFileVO.setOriName(multipartFile.getOriginalFilename());
					boardFileVO.setNum(boardVO.getNum());
					result = noticeDAO.setBoardFileAdd(boardFileVO);
				}
			}
		}
		return result; //noticeDAO.setInsert(boardVO);
	}

	@Override
	public int setUpdate(BoardVO boardVO) throws Exception {
		// TODO Auto-generated method stub
		return 0;
	}

	@Override
	public int setDelete(BoardVO boardVO) throws Exception {
		// TODO Auto-generated method stub
		return 0;
	}


}
```
