---
title: '자바 많이 사용하는 함수'
date: '2026-09-02'
category: '백엔드/JAVA'
tags: ['백엔드', 'JAVA']
draft: false
summary: ''
---

# 유틸

```sql
import java.util.*;
import java.io.*;
```

# **1. 변수 선언**

```sql
String[] arr1 = new String[5];
int[] arr2 = {1, 2, 3};

int N = 3;
int[] arr3 = new int[N];
```

# **2. Arrays**

```sql
int arr[] = {10, 8, 11, 2, 3, 0};

// 1. 오름차순 {0, 2, 3, 8, 10, 11}
Arrays.sort(arr1);

// 2. 내림차순 {11, 10, 8, 3, 2, 0}
Arrays.sort(arr1, Collections.reverseOrder());

// 3. 일부만 정렬 {2, 8, 11, 10, 3, 0} (0~4만 정렬)
Arrays.sort(arr1, 0, 4)

// 4. 오름차순 정렬하면 binary search로 특정 값을 찾을 수 있다.
Arrays.binarySearch(arr1, 2);

// 5. 배열을 어레이리스트로 변환할 떼!
List list = Arrays.asList(arr1);

// 6. 배열의 특정 범위 자르기
int tmp[] = Arrays.copyOfRange(arr1, 0, 3);
```

# **3. length / length() / size()**

```sql
* length: 배열의 길이 (arr.length)

* length(): String related object (str.length())

* size(): Collections object (list.size())

// 1. length
int[] arr = new arr[3];
System.out.println(arr.length);

// 2. length()
String str = "java";
System.out.println(str.length());

// 3. size()
ArrayList<Integer> list = new ArrayList<>();
System.out.println(list.size());
```

# **4. String**

```sql
String str = "hello world";

// 1. 자르기
str.split(" ");
str.substring(0, 5);
for(int i = 0; i < str.length(); i++) str.charAt(i);

// 1-1. 문자열을 배열로 만들고 싶을 때
String str = "12345";
String[] Arr = str.split("");

// 대소문자 변경
str = str.toUpperCase();		// HELLO WORLD
str = str.toLowerCase();		// hello world

// 한번 쓴 문자열은 변경 불가. substring 이용해서 새로운 변수로 선언해야함
String name="starfucks";
String newname=name.substring(0,4)+'b'+name.substring(5);	// starbucks

// 문자열이 특정 문자로 시작되는지 판별
startsWith(str)
String text = "Hello World";
boolean startsWithHello = text.startsWith("Hello");
System.out.println("startsWithHello: " + startsWithHello); // 출력: true

// 문자열이 특정 문자로 끝나는지 판별
endsWith(str)
String fileName = "apple.jpg";
boolean endsWithJpg = fileName.endsWith(".jpg");
System.out.println("endsWithJpg: " + endsWithJpg); // 출력: true

// 문자열 값 비교
equals(str)
String text1 = "Hello";
String text2 = "Hello";
boolean areEqual = text1.equals(text2);
System.out.println("areEqual: " + areEqual); // 출력: true
```

## **contains(str)**

```sql
특정 문자열이 포함되어 있는지 확인

String text = "Hello World";
boolean containsWorld = text.contains("World");
System.out.println("Contains 'World': " + containsWorld); // 출력: true

String text = "Programming is fun";
boolean containsJava = text.contains("Java");
System.out.println("Contains 'Java': " + containsJava); // 출력: false
```

## **String.valueOf(str)**

```sql
지정된 값을 String으로 변환

int number = 42;
String numberAsString = String.valueOf(number);
**System.out.println("Number as String: " + numberAsString); // 출력: "42"**
```

## **charAt(index)**

```sql
문자열 특정 위치에 있는 문자 반환

String text = "Hello";
char character = text.charAt(2);
System.out.println("Character at index 2: " + character); // 출력: "l" "Hello" 문자열에서 인덱스 2에 있는 문자를 가져오는 예제
```

## **indexOf(str)**

```sql
특정 문자열이 대상 문자열의 몇 번째 인덱스에 위치하는지 반환

String text = "Hello World";
int index = text.indexOf("World");
System.out.println("Index of 'World': " + index); // 출력: 6
```

## **substring**

```sql
지정한 범위에 속하는 문자열 반환

//Hello World 문자열에서 인덱스 0부터 5까지의 부분 문자열을 추출하는 예제
String text = "Hello World";
String substring = text.substring(0, 6);
System.out.println("Substring: " + substring); // 출력: "Hello "

//인덱스 5부터 끝까지의 부분 문자열을 추출하는 예제
String text = "Java Programming";
String substring = text.substring(5);
System.out.println("Substring: " + substring); // 출력: "Programming"
```

## **replace(beforeStr, afterStr)**

```sql
특정 문자열을 새로운 문자열로 치환

String text = "I love programming";
String replacedText = text.replace("love", "enjoy");
System.out.println("Replaced Text: " + replacedText); // 출력: "I enjoy programming"

String text = "Apples are red, apples are juicy";
String replacedText = text.replace("apples", "oranges");
System.out.println("Replaced Text: " + replacedText); // 출력: "Oranges are red, oranges are juicy"
```

## **toLowerCase(),toUpperCase()**

```sql
문자열을 소문자로 변환, 문자열을 대문자로 변환

String text = "Hello World";
String lowercaseText = text.toLowerCase();
System.out.println("Lowercase Text: " + lowercaseText); // 출력: "hello world"

String text = "Hello World";
String uppercaseText = text.toUpperCase();
System.out.println("Uppercase Text: " + uppercaseText); // 출력: "HELLO WORLD"
```

## **length**

```sql
문자열의 길이를 반환

String text = "Hello";
int length = text.length();
System.out.println("Length of 'Hello': " + length); // 출력: 5
```

## **trim()**

```sql
문자열의 앞뒤 공백 제거

String text = "   Hello World   ";
String trimmedText = text.trim();
System.out.println("Trimmed Text: " + trimmedText); // 출력: "Hello World"
```

## split

```sql
**개념**

split 함수는 입력받은 정규표현식 또는 특정 문자를 기준으로 문자열을 나누어 배열(Array) 에 저장하여 리턴합니다.
```

```sql
**기본 문법**

String[] parts = inputString.split(delimiter); // 배열로 반환 된다는걸 기억

* inputString은 분할하려는 문자열, delimiter는 문자열을 나눌 구분자
```

```sql
**예제**

String str = "010-1234-5678";
String[] mobNum = str.split("-");
String ret1 = mobNum[0];
String ret2 = mobNum[1];
String ret3 = mobNum[2];

System.out.println("ret1 = "+ret1);
System.out.println("ret2 = "+ret2);
System.out.println("ret3 = "+ret3);

//결과

ret1 = 010

ret2 = 1234

ret3 = 5678

///////////

String str2 = "서울,대전,대구,부산,인천,울산";
String[] cityArr = str2.split(",");

for (int i = 0; i < cityArr.length; i++){
    System.out.println(cityArr[i]);
}

//결과

서울
대전
대구
부산
인천
울산

//////////////////////

// 인자를 두개 받는 방법

String str2 = "81-2-010-1234-5678";
String[] arr = str2.split("-", 2);
System.out.println("ret4 = "+arr[0]);
System.out.println("ret5 = "+arr[1]);

* 위와 같은 String 값이 있다고 했을때 "-" 를 인자 값으로 split 하면서 배열의 크기는 2로 한정

ret4 = 81

ret5 = 2-010-1234-5678

String str2 = "81-2-010-1234-5678";
String[] arr2 = str2.split("-", 4);
System.out.println(arr2.length);
for (int i = 0; i < arr2.length; i++){
    System.out.println("ret"+i+" = "+arr2[i]);
}

ret0 = 81

ret1 = 2

ret2 = 010

ret3 = 1234-5678
```

## format

```sql
이 메서드는 문자열을 지정된 형식으로 생성합니다. {}로 표시된 위치에 인수를 대체합니다. 이 메서드는 Java 5부터 지원됩니다.

String name = "Alice";
int age = 30;
String formattedString = String.format("이름: %s, 나이: %d", name, age);
System.out.println(formattedString);

// String.format 메서드를 사용하여 문자열 내에서 %s와 %d를 사용하여 문자열과 정수를 대체합니다.
```

## **append**

```sql
append 메서드는 StringBuilder 또는 StringBuffer에 문자열을 추가합니다.

StringBuilder는 가변 문자열을 처리할 때 사용하며, StringBuffer는 스레드로부터 안전한 가변 문자열 처리를 위해 사용됩니다.

StringBuilder stringBuilder = new StringBuilder("Hello");
stringBuilder.append(" World");
System.out.println(stringBuilder.toString());

//StringBuilder 객체에 문자열 "Hello"를 초기화하고, append 메서드를 사용하여 " World"를 추가합니다.
```

## **isEmpty**

```sql
isEmpty 메서드는 문자열이나 컬렉션이 비어 있는지 확인합니다. 비어 있으면 true를 반환하고, 그렇지 않으면 false를 반환합니다.

String emptyString = "";
boolean isEmpty = emptyString.isEmpty();
System.out.println("문자열이 비어있는가? " + isEmpty); // true
```

## **compareTo**

```sql
compareTo 메서드는 객체를 비교하여 정렬 순서를 결정합니다. 객체가 동일한 경우 0을 반환하고, 비교 대상보다 작으면 음수, 크면 양수를 반환합니다

String str1 = "Apple";
String str2 = "Banana";
int result = str1.compareTo(str2);
if (result < 0) {
    System.out.println(str1 + " comes before " + str2);
} else if (result > 0) {
    System.out.println(str2 + " comes before " + str1);
} else {
    System.out.println("Both strings are equal.");
}
```

## **toString()**

```sql
toString 메서드는 객체의 문자열 표현을 반환합니다. 주로 디버깅 및 로깅 목적으로 사용

Person person = new Person("Alice", 30);
System.out.println(person.toString());

// 오버라이드 해서 원하는 형식으로 출력하는데 많이 사용
class Person {
    private String name;
    private int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    @Override
    public String toString() {
        return "Person [name=" + name + ", age=" + age + "]";
    }
}

public class Main {
    public static void main(String[] args) {
        Person person = new Person("Alice", 30);
        System.out.println(person.toString());
    }
}
위의 예제에서 Person 클래스의 toString() 메서드가 오버라이드되었으며, toString() 메서드에서는 객체의 name과 age 속성을 포함한 문자열을 반환하고 있습니다. 따라서 위 코드를 실행하면 다음과 같은 결과가 출력됩니다:

css
Copy code
Person [name=Alice, age=30]
toString() 메서드를 오버라이드하면 객체의 내용을 원하는 형식으로 문자열로 표현할 수 있으므로 디버깅 및 로깅 목적으로 유용하게 활용됩니다.
```

# **5. HashMap**

```sql
// 1. 선언
HashMap<String, Integer> hm = new HashMap<>();

// 2. key-value 넣기
hm.put("java", 0);

// 3. 키로 값 가져오기
hm.get("java");

// 4. containsKey()로 존재유무 확인
if (!hm.containsKey("java")) hm.put("java", 1);

// 5. 특정 키가 없으면 값 설정, 있으면 기존 값 가져오는 함수
hm.put("java", hm.getOrDefault("java", 3);

// 6. keySet() 함수로 맵 순회
for(String key : hm.KeySet()) {
	hm.get(key);
}
```

# **6. ArrayList**

```sql
// 1. 선언
ArrayList<String> list = new ArrayList<>();

// 2. 삽입
list.add("java");			// {"java"}
list.add(0, "ryu");			// {"ryu", "java"} (0번째 인덱스에 삽입)

// 3. 수정
list.set(1, "c++");			// {"ryu", "c++"}

// 4. 삭제
list.remove(1);				// {"ryu"}

// 5. 값 존재 유무 확인
list.contains("java");		// false
list.indexOf("ryu");		// 0 존재하면 인덱스 리턴

// 6. iterator 사용
Iterator it = list.iterator();

// 6-1. 인덱스 오름차순 순회
while (it.hasNext()) {
	...
}

// 6-2. 인덱스 내림차순 순회
while (it.hasPrevious()) {
	...
}

// 7. 중복없이 값을 넣고 싶을 때
if (list.indexOf(value) < 0) {	// 없으면 -1을 리턴하기 때문에
	list.put(value);
}

// 8. 리스트 값 하나씩 가져올 때 (int 일 경우)
for(int i = 0; i < list.size(); i++) {
	list.get(i).intValue();
}
```

# **7. Queue**

```sql
// 1. 선언
Queue<Integer> q = new LinkedList<>();		// linked list로 선언해야함

// 2. 삽입
q.add(10);			// {10}
q.offer(2);			// {10, 2}

// 3. 프론트값 반환
q.peek();			// 10

// 4. 삭제
q.remove();
q.poll();

// 5. 초기화
q.clear();

// 6. 비었는지
q.isEmpty();

// 7. pair 같은 경우는 그냥 구현해서 사용
static class Node{
        int y;
        int x;
        int dist;

        Node(int y,int x,int dist){
            this.y=y;
            this.x=x;
            this.dist=dist;
       }
   }

Queue<Node> queue=new LinkedList<>();
queue.add(new Node(1,2,3));
Node node= queue.poll();
```

# **8. 우선순위 큐**

```sql
// 1. 선언
PriorityQueue<Integer> pq = PriorityQueue<Integer>();	// 최소힙
PriorityQeueu<Integer> pq=PriorityQueue<Integer>(Collections.reverseOrder());	// 최대힙

// 2. 삽입
pq.add(3);

// 3. 삭제
pq.remove();

// 4. root 값 추출
pq.peek();

// 5. pair 사용 시
import java.io.IOException;
import java.util.PriorityQueue;

public class PQ {

    static class Node{
        int y;
        int x;

        Node(int y,int x){
            this.y=y;
            this.x=x;
        }

        // 비교 함수 만들어야함!!
        public int compareTo(Node p) {
            if(this.y < p.x) {
                return -1; // 오름차순
            }
            else if(this.y == p.y) {
                if(this.x < p.x) {
                    return -1;
                }
            }
            return 1;
        }
    }

    public static void main(String[] args) throws IOException{

        PriorityQueue<Node> pq1=new PriorityQueue<>(Node::compareTo);
        pq1.add(new Node(1,2));
        pq1.add(new Node(1,1));
        pq1.add(new Node(2,3));
        pq1.add(new Node(2,1));

        while(!pq1.isEmpty()){
            Node node=pq1.peek();
            System.out.println(node.y+" "+node.x);
            pq1.remove();
        }
    }
}
```

# **8. Math 라이브러리**

```sql
// 1. 최대 최소
Math.max(10, 2);
Math.min(10, 2);

// 2. 절대값
Math.abs();

// 3. 올림 내림 반올림
Math.ceil(-3.2);		// -3
Math.floor(-3.2);		// -4
Math.round(-3.26);		// -3	첫째자리에서 반올림

// 3-1. 소수 둘째, 셋째 자리에서 반올림 하고 싶다면
double a = 1.23456;
String b = String.format("%.1f", a);	// .1f는 둘째자리에서 반올림

// 4. 제곱 제곱근
Math.pow(2, 2);		// 2^2 = 4
Math.sqrt(4);		// 2
```
