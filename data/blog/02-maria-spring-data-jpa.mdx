---
title: '02 Maria 데이터베이스와 Spring Data JPA'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

```java
책의 예제는 MariaDB 서버 와 HeidiSQL 프로그램을 사용한다.

설치하고 설정해주자
```

# Spring Data JPA 를 이용하는 프로젝트의 생성

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381180/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled.png)

```java
기본 세팅은 이런식으로 해주고
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381170/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_1.png)

```java
Spring Data JPA 와 MariaDB Driver 을 추가해준다
```

## Auto Configuration

```java
스프링 부트는 Auto Configuration 이라고 불리는 자동 설정 기능이 있다

자동설정 기능이란건 특정한 라이브러리가 있다면 이에 관련된 설정을 자동으로 추가해주는것

Ex) Spring Data Jpa 라는 의존성을 추가했기 때문에 이와 관련된 설정이 자동으로 추가됨
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_2.png)

```java
이제 프로젝트를 만들고 어플리케이션을 시작하면 이런 오류가 발생한다.

이 문제를 해결하기 위해서는 두가지 설정을 해줘야 하는데

1. MariaDB 를 위한 JDBC 드라이버

2. 스프링 부트 프로젝트 내 MariaDB 설정( 프로젝트 생성 시 선택 가능)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381176/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_3.png)

```java
MariaDB 드라이버는 처음 프로젝트 생성 시에 선택하여 추가할 수 있지만 현재 데이터 베이스 버전과 맞지 않으면 직접 검색해서 추가해준다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_4.png)

```java
메이븐 저장소에서 MariaDB 관련 드라이버를 선택하고 Gradle 관련 설정을 복사하고
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381179/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_5.png)

```java
복사한 내용을 build.gradle 파일의 dependencies 항목에 추가해준다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381180/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_6.png)

```java
dependencies 를 추가해주면 오른쪽 위에 코끼리 모양의 라이브러리 반영을 할것인지 버튼이 생긴다. 눌러준다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381180/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_7.png)

```java
스프링 프로젝트의 데이터베이스 설정은 프로젝트 내에 생성된 application properties 파일을 사용하거나 별도의 클래스 파일을 작성해 사요할 수 있다 (이 책에서는 application properties 를 사용)

application properties 파일의 설정은 기존의 XML 이나 Java 설정에 비해서 단순하다 스프링 부트는 어지간한 설정은 파일을 수정하는 것만으로 설정 가능하다

(설정 파일은 properties 파일로 만들거나 yml 파일로 만들어서 사용한다 *이책은 자동으로 생성되는 properties 파일 포맷 사용 )

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381180/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_8.png)

```java
spring.datasource.url=jdbc:mariadb://localhost:3306/delivery
spring.datasource.username=root
spring.datasource.password=kdh930902
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

application properties 에 DB 설정을 추가해준다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381180/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_9.png)

```java
External Libraries 를 사용하면 어떤 라이브러리가 추가되었는지 확인 가능하다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381170/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_10.png)

```java
실행 로그를 확인하면 HikariPool 로 시작하는 부분이 보이는데 이는 스프링 부트가 기본적으로 이용하는 커넥션 풀(Connection Pool) 이 HikariCP 라이브러리를 이용하기 때문이다

기존의 스프링 프로젝트를 생성하였다면 직접 추가하고 설정해주어야 하지만 스프링 부트는 자동으로 설정된다.
```

---

# Spring Data JPA 소개

```java
* ORM 기술을 Java 언어에 맞도록 스펙으로 정리한것이 JPA

* JPA 에는 여러 종류의 구현체들이 존재

* JPA 중에 가장 인지도 높은 구현체는 Hibernate 프레임 워크

* 스프링은 Hibernate 와 연동이 쉽게 중간에 연결 라이브러리를 제공 (Spring Data JPA)
```

```java
JPA(Java Persistence API) 는 Java 언어를 통해서 데이터 베이스 같은 영속 계층을 처리하고자 하는 스펙

JPA 를 이해하기 위해서는 우선 ORM 이라는 기술에 대해서 알아야 한다 (Object-Relational Mapping)

* **영속 계층**은 주로 데이터베이스와 관련된 작업을 수행하며, 데이터베이스 관리 시스템(DBMS)을 사용하여 데이터를 저장하고 검색
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381172/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_11.png)

# ORM 과 JPA

```java
* 객체(Object) 와 객체(entity)

* 데이터를 보관하는 구조체라는 공통점

* 객체지향에서는 참조 관계형 데이터베이스 에서는 관계

* ORM 은 객체지향 구조를 관계형 구조로 매핑 하는 기술
```

```java
ORM 은 간단하게 말하면 객체지향 패러다임을 데이터 베이스에 보존하는 기술 이라고 할 수 있다

ORM 의 시작은 아주 단순해서 객체지향 구조가 관계형 데이터 베이스와 유사하다는 점에서 시작한다

객체지향 언어중에 Class 를 사용하는 언어는 특히 그러한데 클래스를 사용하는 언어들은 데이터의 구조를 잡기 위해서 클래스를 설계한다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381172/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_12.png)

# Spring Data JPA 와 JPA

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381172/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_13.png)

```java
* JPA 구현체인 Hibernate 를 좀더 쉽게 사용할 수 있는 라이브러리

* MyBatis 에서 Spring - MyBatis 와 동일한 역할
```

# 엔티티 클래스와 JpaRepository

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381172/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_14.png)

```java
@Entity : 해당 클래스가 엔티티 클래스 임을 표시 한개 이상의 테이블 생성

@Table : 관계형 데이터베이스에 생성되는 테이블과 관련된 설정

@id : PK관련 설정 자동으로 생성되는 방식은 @GeneratedValue 사용
```

```java
**@Entity**
엔티티 클래스는 반드시 @Entity 라는 어노테이션을 추가 해야만 한다 이는 해당 클래스가 엔티티를 위한 클래스 이며 해당 클래스의 인스턴스 들이 JPA 로 관리되는 엔티티 객체 라는것을 의미한다

또한 @Entity 가 붙은 클래스는 옵션에 따라 자동으로 테이블을 생성할 수도 있고 이 경우 @Entity 가 있는 클래스의 멤버 변수에 따라서 자동으로 컬럼들도 생성된다

**@Table**
@Entity 어노테이션과 같이 사용할 수 있는 어노테이션으로 데이터 베이스 상에서 엔티티 클래스를 어떠한 테이블로 생성할 것인지에 대한 정보를 담기위한 어노테이션

예를들어 @Table(name= "t_memo") 라고 작성하면 테이블 이름이 t_memo 로 생성된다 (단순히 테이블 이름뿐 아니라 인덱스 등을 생성하는 설정도 가능하다 )

**@id 와 @GeneratedValue**
@Entity 가 붙은 클래스는 PK에 해당하는 특정 필드를 @id 로 지정해야 한다

@id 가 사용자가 입력하는 값을 사용하는 경우가 아니면 자동으로 생성되는 번호를 사용하기 위해 @GeneratedValue 라는 어노테이션을 활용한다

@GeneratedValue(strategy = GenerationType.IDENTITY) 는 PK 를 자동으로 생성할 때 사용한다

(키 생성 전략 이라고 한다) (연결되는 데이터 베이스가 오라클이면 별도의 번호를 위한 별도의 테이블을 생성하고 mysql 이나 mariaDB 이면 auto increment 를 기본으로 사용해서 새로운 레코드가 기록될 때 마다 다른 번호를 가질 수 있도록 처리된다 )

키 생성 전략은 크게 다음과 같다

* AUTO(default) - JPA 구현체 (스프링 부트에서는 Hibernate) 가 생성 방식을 결정

* IDENTITY - 사용하는 데이터베이스가 키 생성을 결절 (MySQL 이나 MariaDB 의 경우 auto increment 방식을 사용)

* SEQUENCE - 데이터 베이스의 sequence 를 이용해서 키를 생성 (@SequenceGenerator 과 같이 사용)

* TABLE - 키 생성 전용 테이블을 생성해서 키 생성 (@TableGenerator 과 함께 사용)


**@Column**
추가적인 필드(칼럼) 이 필요한 경우에도 마찬가지로 어노테이션을 활용한다 이때는 @Column 을 이용해서 다양한 속성을 지정 가능하다 주로 nullable, name, length 등을 이용해서 데이터 베이스 칼럼 정보를 제공한다

속성중에 columnDefinition 을 이용하면 기본값을 지정할 수도 있다(시간에 대한 기본값은 뒷쪽에서)

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381172/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_15.png)

```java
위와 같이 내용을 수정해준다.

Lombok 의 @Getter를 이용해서 Getter 메서드를 생성하고 @Builder 를 이용해서 객체를 생성할 수 있게 처리한다.

Builder 를 이용하기 위해서는 @AllArgsConstructor @NoArgsConstructor 를 항상 같이 처리해야 컴파일 오류가 생기지 않는다.
```

### @Column 속성

| **속성** | **기능**                                     | **기본값**       |
| -------- | -------------------------------------------- | ---------------- |
| name     | 필드와 매핑할 테이블의 컬럼 이름을 지정한다. | 객체의 필드 이름 |

| insertable
(거의 사용하지 않음) | 엔티티 저장 시 이 필드도 같이 저장한다.
false로 설정하면 이 필드는 데이터베이스에 저장하지 않는다.
false 옵션은 읽기 전용일 때 사용한다 | true |
| updateable | 엔티티 수정 시 이 필드도 같이 수정한다.
false로 설정하면 데이터베이스에 수정하지 않는다.
false 옵션은 읽기 전용일 때 사용한다 | true |
| table
(거의 사용하지 않음) | 하나의 엔티티를 두 개 이상의 테이블에 매핑할 때 사용한다.(@SecondaryTable 사용)
지정한 필드를 다른 테이블에 매핑할 수 있다. | 현재 클래스가 매핑된 테이블 |
| nullable (DDL) | DDL 생성 시 null 값의 허용 여부를 설정한다.
false로 설정하면 not null 제약조건이 붙는다. | true |
| unique (DDL) | @Table의 uniqueConstraints와 같으나 한 컬럼에 간단히 유니크 제약조건을 걸 때 사용한다. | false |
| columnDefinition (DDL) | 데이터베이스 컬럼 정보를 직접 줄 수 있다. | 자바 필드의 타입과,
데이터베이스 방언 설정 정보를 사용해
적절히 생성 |
| length (DDL) | 문자 길이 제약조건, String 타입에만 사용한다. | 255 |
| precision, scale (DDL) | BigDecimal 타입(혹은 BigInteger)에서 사용한다.
precision은 소수점을 포함한 전체 자리수를,
scale은 소수의 자리수다. | precision = 0,
scale = 0 |

### Tip

```java
@Column 과 반대로 데이터베이스에 칼럼으로 생성되지 않는 필드는 @Transient 어노테이션을 적용한다

또한 @Column 으로 기본값을 지정하기 위해서는 columnDefinition 을 이용하기도 한다

@Column(columnDefinition = "varchar(255) default 'Yes'")
```

# Spring Data JPA를 위한 스프링 부트 설정

```java
지금까지 설정으로 프로젝트 실행은 문제가 없지만 자동으로 필요한 테이블을 생성하거나 JPA 를 이용할 떄 발생하는 SQL 등을 확인하기 위해서는 약간의 추가설정이 필요하다

application.properties 파일에 다음 내용을 추가

#jpa sql 출력
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=update
프로젝트 실행시에 자동으로 DDL(create,alter, drop 등) 을 생성할것인지를 결정하는 설정 설정값은 create,update,create-drop,validate 가 있다

예를들어 create 는 매번 테이블 생성을 새로 시도한다 예제의 update 는 변경이 필요한 경우에는 alter 로 변경되고 테이블이 없는경우 create 가 되도록 설정

spring.jpa.properties.hibernate.format_sql=true
실제 JPA 구현체인 Hibernate 가 동작하면서 발생하는 SQL 을 포맷팅 해서 출력한다 (실행되는 SQL 의 가독성을 늘려줌)

spring.jpa.show-sql=true
JPA 처리 시에 발생하는 SQL을 보여줄 것인지를 결정
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_16.png)

```java
실행시 자동으로 테이블이 생성되는걸 확인 가능하다.
```

# JpaRepository 인터페이스

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_17.png)

```java
JpaRepository는 데이터베이스 테이블과 관련된 기본적인 CRUD(Create, Read, Update, Delete) 작업을 추상화하여 제공한다.

따라서, 개발자는 데이터베이스 작업을 수행하기 위해 SQL 쿼리를 직접 작성하거나 JDBC 코드를 작성할 필요가 없다.

이러한 CRUD 작업은 JpaRepository 인터페이스에 정의된 메서드를 통해 간단하게 수행할 수 있다.
```

```java
* Spring Data JPA 는 JPA 를 쉽게 사용할 수 있는 API 의 일부로 Repository 라는 타입의 기능을 제공

* 검색/정렬 기능

* 인터페이스만 작성하면 동적으로 객체가 생성되는 방식 (동적 프록시)
```

```java
Spring Data JPA 는 JPA의 구현체인 Hibernate 를 이용하기 위한 여러 API 를 제공한다

그중 가장 많이 사용할것이 바로 JpaRepository 라이브러리 인터페이스다.
```

```java
일반적인 기능만을 사용할 때는 CrudRepository 를 사용하는 것이 좋고 모든 기능을 사용하고 싶을 때는 JpaRepository 를 이용한다

(특별한 경우가 아니라면 JpaRepository 를 이용하는 것이 무난한 선택)
```

## JpaRepository 사용하기

```java
JpaRepository 는 인터페이스 이고 Spring Data Jpa는 이를 상속하는 인터페이스를 선언하는 것만으로 모든 처리가 끝난다.

(실제 동작 시에는 스프링이 내부적으로 해당 인터페이스에 맞는 코드를 생성하는 방식을 이용한다)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_18.png)

```java
프로젝트 내에 repository 패키지를 생성하고 MemoRepository 인터페이스를 추가한다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_19.png)

```java
MemoRepository 는 인터페이스 자체이고 JpaRepository 인터페이스를 상속 하는 것만으로 모든 작업이 끝난다

JpaRepository 를 사용할 때는 아래와 같은 형식으로 사용한다.

JpaRepository<@Entity 의 클래스타입 , @id(pk) 타입)>      Ex) JpaRepository<Memo, Long>

JPA 는 인터페이스를 선언하면 자동으로 스프링 bean 에 등록된다 (스프링이 내부적으로 인터페이스 타입에 맞는 객체를 생성해서 빈으로 등록한다)
```

## 테스트 코드를 통한 CRUD 연습

```java
MemoRepository 를 이용해서 작성된 테이블에 SQL 없이 CRUD 작업을 테스트 해본다.
```

```java
JpaRepository 는 다음과 같은 메서드를 활용한다

* insert 작업 : save(엔티티 객체)

* select 작업 : findById(키 타입), getOne(키 타입)

* update 작업 : save(엔티티 객체)

* delete 작업 : deleteById(키 타입, delete(엔티티 객체)
```

```java
특이하게도 insert 와 update 작업에 사용하는 메서드가 동일하게 save() 를 이용하는데 이는 JPA 구현체가 객체를 비교하고 없다면 insert, 존재하면 update 를 동작시키는

방식으로 동작하기 때문이다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_20.png)

```java
본격적인 테스트에 앞서서 실제로 MemoRepository가 정상적으로 스프링에서 처리되고 의존성 주입에 문제가 없는지를 확인하는 것이 좋다

testClass 메서드는 MemoRepository 인터페이스 타입의 실제 객체가 어떤것인지 확인한다 스프링 내부적으로 해당 클래스를 자동으로 생성하는데 (AOP 기능) 이때 클래스 이름을 확인해보자

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_21.png)

```java
테스트 코드는 위와같이 작성한적 없는 클래스 이름이 출력되는걸 확인할 수 있다 (getClass = 현재 참조하고 있는 클래스 확인, getName 클래스의 이름 가져오기)

(동적 프록시 라는 방식으로 만들어진다)
```

## 등록 작업 테스트

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_22.png)

```java
100개의 새로운 Memo 를 생성하고 MemoRepository 를 이용하여 insert 하는것

(memoText 는 NotNull 조건이므로 반드시 데이터를 넣어주고 테스트를 진행하는것)
```

```java

기존 반복문을 사용하는것이 아니라 Stream 을 사용하여 반복한다 ( 더 짧고 보기 좋지만 꼭 좋은건 아니다 상황에 맞게 사용)

@Test
public void testInsertDummies() {
    for (int i = 1; i <= 100; i++) {
        Memo memo = Memo.builder().memoText("Sample......" + i).build();
        memoRepository.save(memo);
    }
}

변환 ->

@Test
    public void testInsertDummies() {
        IntStream.rangeClosed(1,100).forEach(i -> {                                  // IntStream: 정수 값의 스트림을 생성하는 클래스 rangeClosed 메서드는 지정된 범위 내의 정수 값을 생성
            Memo memo = Memo.builder().memoText("Sample......" + i).build();
            memoRepository.save(memo);
        });
    }
```

## 조회 작업 테스트

```java
조회 작업 테스트는 findById() 를 사용하여 처리 (getOne() 이란것도 있지만 2.5.0 버전에서 사라짐)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_23.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381173/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_24.png)

```java
@Test
    public void testSelect() {
        //데이터 베이스에 존재하는 mno
        Long mno = 100L;           // id 가 100인걸 찾기위해

        Optional<Memo> result = memoRepository.findById(mno);   // Optional 사용 findById 로 id 100번 찾기
        System.out.println("============================");

        if(result.isPresent()) {                  // isPresent(): 이 메서드는 Optional 클래스의 메서드 중 하나로, Optional 객체 안에 값이 존재하는지 여부를 확인하는 역할을 값이 존재하면 true를 반환하고, 값이 없으면 false를 반환
            Memo memo = result.get();  // result 객체 안에 있는 값을 get() 메서드를 사용하여 가져옴
            System.out.println(memo);
        }
    }
```

```java
findById() 의 경우 java.utill 패키지의 Optional 타입으로 반환되기 때문에 결과가 존재하는지를 체크하는 형태로 작성하게 됨
```

**Optional이란? Optional 개념 및 사용법**

## 수정 작업 테스트

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381174/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_25.png)

```java
@Test
    public void testUpdate() {
        Memo memo = Memo.builder().mno(100L).memoText("Update Text").build();
        System.out.println(memoRepository.save(memo));
    }
```

```java
수정 작업은 등록 작업과 동일한 save() 를 이용해서 처리

(내부적으로 해당 엔티티의 @id 값이 일치하는지 확인해서 insert 혹은 update 작업을 처리

testUpdate 를 보면 100번의 Memo 객체를 만들고 save() 를 호출
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381174/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_26.png)

```java
호출 결과를 보면 내부적으로 select 쿼리로 해당 번호의 Memo 객체를 확인하고 이를 update 하는것을 알 수 있음

JPA는 엔티티 객체들을 메모리상에 보관하려고 하기 때문에 특정한 엔티티 객체가 존재하는지 확인하는 select 가 먼저 실행되고 @id 를 가진 객체가 있다면 update 없다면 insert 를 실행하게 됨
```

## 삭제 작업 테스트

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381174/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_27.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381174/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_28.png)

```java
@Test
    public void testDelete() {
        Long mno = 100L;
        memoRepository.deleteById(mno);
    }
```

```java
위와 동일하게 삭제하려는 번호(mno) 의 엔티티 객체가 있는지 확인하고 이를 삭제하려고 함 (select 이후 delete)

*deleteById() 의 리턴 타입은 void 이고 만일 해당 데이터가 존재하지 않으면 org.springframework.dao.EmptyResultDataAccessException 이 발생
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381174/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_29.png)

# 페이징 처리

```java
Spring Data Jpa 를 이용할 때 페이지 처리는 반드시 0 부터 시작한다는 점을 기억
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381176/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_30.png)

```java
		@Test
    public void testPageDefault() {
        //1페이지 10개
        Pageable pageable = PageRequest.of(0,10);       // 1페이지 , 10개의 데이터 가져오기
        Page<Memo> result = memoRepository.findAll(pageable);
        System.out.println(result);

    }
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381176/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_31.png)

```java
import 시에 org.springframework.data 관련 클래들을 사용하도록 주의해준다

PageRequest.of() 를 사용하여 한페이지에 10개의 데이터를 가져오도록 설정해줬다

(Page타입이 흥미로운 점은 단순 해당 목록을 가져오는게 아니라 페이지 처리에 필요한 전체 데이터 개수를 가져오는 쿼리도 같이 실행한다는 것이다 (데이터가 부족하면 실행하지 않는다))
```

```java
첫번째 쿼리를 보면 MariaDB에서 페이징 처리에 사용하는 limit 구문이 사용되는 것을 볼 수 있고

두번째 쿼리에서는 count() 를 이용해서 전체 개수를 처리하는것을 볼 수 있다

findAll() 에 Pageable 타입의 파라미터를 전달하면 페이징 처리에 관련된 쿼리를 실행하고 이 결과물을 이용해서 리턴 타입으로 지정된 Page<엔티티 타입> 객체로 저장
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381176/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_32.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381176/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_33.png)

```java
@Test
    public void testPageDefault() {
        //1페이지 10개
        Pageable pageable = PageRequest.of(0,10);   // 현재 페이지 0 ,  페이지 데이터 10
        Page<Memo> result = memoRepository.findAll(pageable);
        System.out.println(result);
        System.out.println("=====================================");
        System.out.println("TotalPage : " + result.getTotalPages()); // 총 페이지
        System.out.println("TotalCount : " + result.getTotalElements()); // 전체개수
        System.out.println("Page Number : " + result.getNumber()); // 현재 페이지 번호
        System.out.println("Page Size : " + result.getSize()); // 페이지당 데이터 개수
        System.out.println("has next Page? : " + result.hasNext()); // 다음 페이지 존재여부
        System.out.println("first page? : " + result.isFirst()); // 시작 페이지(0) 인가
        //실제 페이지 데이터 불러오기
        System.out.println("=====================================");
        for(Memo memo : result.getContent()) {
            System.out.println(memo);
        }
    }
```

```java
Page<엔티티 타입> 은 쿼리 결과를 사용하기 위해 여러 메서드를 지원함

실제 데이터 페이지를 처리하는 것은 getContent() 를 이용해서 List<엔티티 타입> 으로 처리하거나 Stream<엔티티 타입> 을 반환하는 get() 을 이용할 수 있다
```

# 정렬 조건 추가하기

```java
페이징 처리를 담당하는 PageRequest 에는 정렬과 관련된 org.springframework.data.domain.Sort 타입을 파라미터로 전달할 수 있다.

Sort 는 한개 혹은 여러 개의 필드 값을 이용해서 순차적 정렬 asc 이나 역순으로 정렬 desc 를 지정할 수 있다
```

람다식 사용코드

```java
		@Test
    public void testSort() {
        Sort sort1 = Sort.by("mno").descending();
        Pageable pageable = PageRequest.of(0, 10, sort1);
        Page<Memo> result = memoRepository.findAll(pageable);
        result.get().forEach(memo -> {
            System.out.println(memo);
        });
    }
```

[**람다식(Lambda Expressions) -> 사용법 & 예제**](<../../JAVA/%EC%9E%90%EB%B0%94/%EB%9E%8C%EB%8B%A4%EC%8B%9D(Lambda%20Expressions)%20-%20%EC%82%AC%EC%9A%A9%EB%B2%95%20&%20%EC%98%88%EC%A0%9C%203c8462dead0648f3b25d0fd9cd801fcc.md>)

기본 코드

```java
@Test
public void testSort() {
    Sort sort1 = Sort.by("mno").descending();    // mno 필드의 값을 역순으로 정렬하도록
    Pageable pageable = PageRequest.of(0, 10, sort1);
    Page<Memo> result = memoRepository.findAll(pageable);
    List<Memo> memoList = result.getContent();

    for (Memo memo : memoList) {
        System.out.println(memo);
    }
}
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_34.png)

```java
mno 필드 값을 역순(desc) 으로 정렬하도록 Sort.by(필드).desending() 을 이용하는 것을 볼 수 있다.

(쿼리 또한 order by 가 추가됨)
```

### 정렬 조건 and로 추가하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_35.png)

```java
@Test
public void testSort2() {
    Sort sort1 = Sort.by("mno").descending();
    Sort sort2 = Sort.by("memoText").ascending();
    Sort sortAll = sort1.and(sort2); // sort1과 sort2를 and 연산하여 하나의 정렬 조건으로 결합
    Pageable pageable = PageRequest.of(0, 10, sortAll);  // 페이지 번호 0, 페이지 크기 10, 결합된 정렬 조건으로 페이지 객체 생성
    Page<Memo> result = memoRepository.findAll(pageable); // 결합된 정렬 조건과 페이지 정보를 사용하여 Memo 엔티티를 조회하여 페이지 객체 생성

    List<Memo> memoList = result.getContent(); // 조회된 페이지의 Memo 엔티티 목록을 얻어옴
    for (Memo memo : memoList) {
        System.out.println(memo);
    }
}
```

```java
Sort 객체의 and() 를 이용해서 여러개의 정렬 조건을 추가할 수 있다

위의 예시는 mno 의 내림차순으로 정렬하고 그 안에서 memoText 를 오름차순으로 정렬된다
```

# 쿼리 메서드(Query Methods) 기능과 @Query

```java
예를들어 특정한 범위의 Memo 객체를 검색하거나 like 처리가 필요한 경우 Spring Data Jpa 는 다음과 같은 방법을 제공한다.

* 쿼리 메서드 : 메서드의 이름 자체가 쿼리의 구문으로 처리되는 기능

* @Query : SQL 과 유사하게 엔티티 클래스의 정보를 이용해서 쿼리를 작성하는 기능

* Querydsl 등의 동적 쿼리 처리 기능
```

## 쿼리 메서드 (Query Methods)

```java
쿼리 메서드는 메서드의 이름 자체가 Query 문이 되는 기능이다

쿼리 메서드는 주로 findBy 나 getBy로 시작하고 이후 필요한 필드 조건을 And, Or 같은 키워드를 이용해서 메서드의 이름 자체로 쿼리문을 만들어낸다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_36.png)

[Spring Data JPA - Reference Documentation](https://docs.spring.io/spring-data/jpa/docs/current-SNAPSHOT/reference/html/#jpa.query-methods)

```java
쿼리 메서드 키워드는 SQL 에서 사용되는 키워드와 동일하게 작성되어 있다

이에 관한 내용은 Spring Data JPA Reference 를 이용해서 찾을 수 있다. And, Or, Between 등이 있다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_37.png)

```java
쿼리 메서드는 사용하는 키워드에 따라서 파라미터 개수가 결정된다 예를들어 Between 의 경우 ?1, ?2 같은 두개의 변수가 필요하다는걸 알 수 있다
```

```java
쿼리 메서드의 리턴 타입도 상당히 자유롭다 가장 중요한 내용은

* select 를 하는 작업이라면 List 타입의 배열을 이용할 수 있다

* 파라미터에 Pageable 타입을 넣는 경우에는 무조건 Page<E> 타입
```

```java
예를들어 Memo 객체의 mno 값이 70부터 80사이 객체들을 구현하고 mno 역순으로 정렬하고 싶다면 다음과 같은 메서드를 MemoRepository 인터페이스에 추가하게 된다
```

**MemoRepository interface**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_38.png)

```java
public interface MemoRepository extends JpaRepository<Memo, Long> {
    List<Memo> findByMnoBetweenOrderByMnoDesc(Long from , Long to); // mno 값이 70부터 80사이 객체들을 구현하고 mno 역순으로 정렬
}
```

**Test**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_39.png)

```java
		@Test
    public void testQueryMethods() {
        List<Memo> list = memoRepository.findByMnoBetweenOrderByMnoDesc(70L,80L);

        for(Memo memo : list) {
            System.out.println(memo);
        }
    }
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_40.png)

## 쿼리 메서드와 Pageable 의 결합

```java
리스트(목록)를 원하는 쿼리를 실행해야 한다면 대부분 앞의 예제와 같이 OrderBy 같은 키워드를 사용해야 하기 때문에 메서드의 이름도 길어지고 혼동하기 쉽다

다행히도 쿼리 메서드는 Pageable 파라미터를 같이 결합해서 사용할 수 있기 때문에 정렬에 관련 부분은 Pageable 로 처리해서 간략하게 메서드를 생성할 수 있다
```

**MemoRepository interface**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_41.png)

```java
public interface MemoRepository extends JpaRepository<Memo, Long> {

    List<Memo> findByMnoBetweenOrderByMnoDesc(Long from , Long to);

    Page<Memo> findByMnoBetween(Long from, Long to, Pageable pageable);  //Pageable 처리
}
```

**Test**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381177/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_42.png)

람다식 사용

```java
		@Test
    public void testQueryMethodWithPageable() {
        Pageable pageable = PageRequest.of(0, 10, Sort.by("mno").descending());

        Page<Memo> result = memoRepository.findByMnoBetween(10L, 50L, pageable);  // pageable 결합

        result.get().forEach(memo -> System.out.println(memo)); // .get() 은 result 객체에서 조회된 Memo 엔티티 목록을 얻어온다. get() 메소드를 호출하여 실제로 Memo 엔티티들이 저장된 List를 얻을 수 있다.
    }                                                           // .forEach(...)는 Memo 엔티티 목록을 하나씩 순회하면서, 각 엔티티에 대해 정해진 작업을 하라는 뜻 이 경우에는 각 Memo 엔티티를 가져와서 그 내용을 출력하는 것이 해당 동작에 해당
```

기본

```java
@Test
public void testQueryMethodWithPageable() {
    Pageable pageable = PageRequest.of(0, 10, Sort.by("mno").descending());

    Page<Memo> result = memoRepository.findByMnoBetween(10L, 50L, pageable);

    List<Memo> memoList = result.getContent();
    for (Memo memo : memoList) {
        System.out.println(memo);
    }
}
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381179/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_43.png)

```java
실행 결과를 보면 Sort 를 이용하는 부분이 적용된 것을 확인할 수 있다.
```

# deleteBy로 시작하는 삭제 처리

```java
deleteBy 로 메서드이 이름을 시작하면 특정한 조건에 맞는 데이터를 삭제하는 것도 가능하다
```

```java
예를 들어 메모의 번호(mno) 가 10보다 작은 데이터를 삭제한다면 다음과 같이 작성할 수 있다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381179/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_44.png)

```java
public interface MemoRepository extends JpaRepository<Memo, Long> {

    List<Memo> findByMnoBetweenOrderByMnoDesc(Long from , Long to);

    Page<Memo> findByMnoBetween(Long from, Long to, Pageable pageable);

    void deleteByMnoLessThan(Long num);  //삭제 메서드
}
```

**Test**

```java
@Commit
@Transactional
@Test
public void testDeleteQueryMethod() {
		memoRepository.deleteByMnoLessThan(10L);
}
```

```java
테스트 코드에는 @Transactional 과 @Commit 을 사용한다

이는 deleteBy 의 경우 우선 select 문으로 해당 엔티티 객체를 가져오는 작업과 각 엔티티를 삭제하는 작업이 같이 이루어지기 때문이다

만약 @Transactional 가 없다면 아래와 같은 오류가 발생한다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381179/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_45.png)

```java
@Commit 은 최종 결과를 커밋하기 위해서 사용한다 이를 적용하지 않으면 테스트 코드의 deleteBy 는 기본적으로 Rollback 처리가 되어서 결과를 반영하지 않는다

(deleteBy 는 실제 개발에서 많이 사용되지 않는데 그 이유는 SQL을 이용하듯 한번에 삭제가 이루어 지는것이 아니라 각엔티티 객체를 하나씩 삭제하기 때문이다.

(실제 개발 시에는 deleteBy 를 사용하는 방식 보다는 @Query 를 이용해서 하나씩 삭제하는 비효율적인 부분을 개선한다.)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381179/notion-blog/_/_/_/02_Maria_Spring_Data_JPA/Untitled_46.png)

# @Query 어노테이션

```java
Spring Data JPA 가 제공하는 쿼리 메서드는 검색 같은 기능을 작성할 때 는 편리함을 제공하지만 조인이나 복잡한 조건을 처리해야 하는 경우 And, Or 등이 사용되면서 불편할 때가 많다

때문에 일반적으로 간단한 처리만 쿼리 메서드를 이용하고 @Query 를 이용하는 경우가 더 많다.
```

## @Query 의 처리

```java
@Query 의 경우는 메서드의 이름과 상관없이 메서드에 추가한 어노테이션을 통해서 처리한다

@Query 의 value 는 JPQL(Java Persitence Query Language) 로 작성하는데 흔히 객체 지향 쿼리 라고 불리는 구문들이다
```

## @Query 의 기능

```java
@Query 를 이용하면 다음과 같은 작업을 실행할 수 있다

* 필요한 데이터만 선별적으로 추출 가능

* 데이터베이스에 맞는 순수한SQL(NativeSQL)을 사용하는 기능

* insert, update, delete 와 같은 select 가 아닌 DML 등을 처리하는 기능(@Modifiying 과 함께 사용)
```

## @Query 의 사용법

```java
객체지향 쿼리는 테이블 대신에 엔티티 클래스를 이용하고 테이블 컬럼 대신에 클래스에 선언된 필드를 이용해서 작성한다

JPQL 은 SQL 과 상당히 유사하기 때문에 간단한 기능을 제작하는 경우에는 추가적인 학습 없이도 적용이 가능하다

예를 들어 mno 의 역순으로 정렬하라 는 기능을 @Query 를 이용하면 아래와 같은 형태가 된다

@Query("select m from Memo m order by m.mno desc")
List<Memo> getListDesc();
```

## @Query의 파라미터 바인딩

```java
@Query 의 경우 직접 SQL과 유사한 형태로 작성되기 때문에 where 구문과 그에 맞는 파라미터들을 처리할 때가 많다

이때는 아래와 같은 방식을 사용한다

* ?1 ?2 와 1부터 시작하는 파라미터의 순서를 이용하는 방식

* ':xxx' 와 같이 ':파라미터 이름' 을 활용하는 방식

* #{} 과 같이 자바 빈 스타일을 이용하는 방식
```

## : 파라미터 이용

```java
@Transactional
@Modifying
@Query("update Memo m set m.memoText = :memoText where m.mno = :mno")                     // 'Memo' 엔티티의 'mno' 값을 받아 일치하는 엔티티의 'memoText' 필드 값을 업데이트하는 쿼리
int updateMemoText(@Param("mno") Long mno, @Param("memoText") String memoText);       // 두 개의 파라미터를 받는다. 첫 번째 파라미터인 'mno'는 업데이트할 Memo 엔티티의 고유 번호를 나타내며, 두 번째 파라미터인 'memoText'는 새로운 메모 내용을 나타냄

@Param 을 사용해서  쿼리에 파라미터 이름 ex) :memoText 에 값을 바인딩 한다.

where m.mno = :mno (입력받은 mno @Param 와 같은 mno를 찾는다는거
```

## :#{} (객체사용)

```java
@Transactional
@Modifying
@Query("update Memo m set m.memoText = :#{#param.memoText} where m.mno = :#{param.mno}")
int updateMemoText(@Param("param") Memo memo );   // @Param 어노테이션을 사용하여 메소드 파라미터 이름을 'param'으로 지정 이 메소드는 Memo 엔티티 객체를 받음 Memo 객체에는 업데이트할 'mno'와 'memoText' 정보가 담겨있음.

: 를 이용하는 경우 여러 개의 파라미터를 전달할 때 복잡해 질 경우가 있다고 생각이 된다면 :# 를 이용해서 객체를 사용할 수 있다.
```

# @Query 와 페이징 처리

```java
쿼리 메서드와 마찬가지로 @Query 를 이용하는 경우에도 Pageable 타입의 파라미터를 적용하면 페이징 처리와 정렬에 관한 부분을 작성하지 않을 수 있다.

앞의 예제에서도 보았듯 리턴 타입을 Page<엔티티 타입> 으로 지정하는 경우에는 count 를 처리하는 쿼리를 적용할 수 있다.( 뒤에 나오는 nativeQurey 를 사용하는 경우에는 필수)

따라서 @Query 를 이용할 때는 별도의 countQuery 라는 속성을 적용해 주고 Pageable 타입의 파라미터를 전달하면 된다.
```

```java
@Query (value = "select m from Memo m where m.mno > : mno", countQuery = "select count(m) from Memo m where m.mno > : mno" ) // value 속성의 쿼리는 'Memo' 엔티티에서 'mno' 값이 주어진 값보다 큰 엔티티들을 선택
Page<Memo> getListWithQuery(Long mno, Pageable pageable);                                                                    // countQuery 속성의 쿼리는 'Memo' 엔티티에서 'mno' 값이 주어진 값보다 큰 엔티티들의 개수를 세어 반환
                                                                                                                             // Page<Memo> 반환 타입은 페이지 형태로 조회된 Memo 엔티티들을 담아 반환
																																																														 // 메소드의 파라미터로는 'mno' 값을 받고, 페이지 정보인 pageable도 함께 받음

즉, 이 메소드는 주어진 'mno' 값보다 큰 Memo 엔티티들을 데이터베이스에서 조회하고, 조회된 결과를 페이지 형태로 반환 이때 pageable에 포함된 페이지 번호와 크기, 그리고 정렬 조건에 따라 조회 결과를 페이지 단위로 반환
```

# Object[] 리턴

```java
@Query 의 장점 중 하나는 쿼리 메서드의 경우에는 엔티티 타입의 데이터만을 추출하지만 @Query 를 이용하는 경우에는 현재 필요한 데이터만 Object[] 의 형태로 선별적으로 추출할 수 있다

JPQL 을 이용할 때 경우에 따라서 Join 이나 GroupBy 등을 이용하는 경우가 종종 있는데 이럴 때 적당한 엔티티 타입이 존재하지 않는경우가 많기 때문에 이런 상황에서 유용하게 Object[] 타입을 리턴 타입으로 지정할 수 있다

예제) mno(pk) 와 memoText 그리고 현재 시간을 같이 얻어오고 싶다 그런데 Memo 엔티티 클래스 에서는 시간과 관련된 부분의 선언이 없기 때문에 추가적인 구문이 필요하다

JPQL 에서는 CURRENT_DATE, CURRENT_TIME, CURRENT_TIMESTAMP 와 같은 구문을 통해서 현재 데이터 베이스의 시간을 구할수 있는데 이를 이용하면 다음과 같은 형태가 된다

@Query(value  = "select m.mno, m.memoText, CURRENT_DATE from Memo m where m.mno > :mno", countQuery = "select count(m) from Memo m where m.mno > :mno")
Page<Object[] > getListWithQueryObject(Long mno, Pageable pageable);                  // Object[] 를 사용함으로서 Memo 클래스에 없는 날짜와 관련된 부분도 추가 가능해졌다


```

# Native SQL 처리

```java
@Query 의 기능중 데이터 베이스의 SQL 구문을 그대로 활용하는 기능이 존재한다

JPA 자체가 데이터베이스에 독립적으로 구현이 가능하다는 장점을 잃어버리는 방법이긴 하지만 복잡한 JOIN 구문 등을 처리하기 위해 어쩔 수 없이 선택하여 사용한다

@Query(value = "select * from memo where mno > 0", nativeQuery = true)
List<Object[]> getNativeResult();

@Query 의 nativeQuery 속성값을 true 로 설정하고 일반 sql 을 그대로 사용 가능하다.
```
