---
title: '화면 만들기'
date: '2026-09-02'
category: '프론트/넥사크로'
tags: ['프론트', '넥사크로']
draft: false
summary: ''
---

# 1. **프로젝트(Project) 생성**

```html
[Menu] File > New > Project 새로운 프로젝트를 생성한다.
```

```html
속성창에서 폼의 titletext 속성값을 수정합니다. titletext 속성값은 웹브라우저에 표시되는 제목으로
적용
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380945/notion-blog/_/_/_/Untitled.png)

# **데이터 셋(Dataset)**

```xml
넥사크로플랫폼 앱 내에서 데이터를 관리하는 기능을 제공

사용자가 입력하거나 서버에서 가져온 데이터를 보관

데이터의 변경(추가/수정/삭제) 상태를 관리
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380945/notion-blog/_/_/_/Untitled_1.png)

```xml
위의 원통 모양을 클릭하고 아무곳이나 클릭하면 데이터셋 추가
```

# **데이터셋 콘텐츠 에디터**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381451/notion-blog/_/_/_/Untitled_2.png)

```xml
일반적인 컴포넌트는 속성창에서 관련된 속성을 수정

하지만 데이터셋은 데이터를 다루기 위한 추가적인 도구로 데이터셋 콘텐츠 에디터가 제공됨

```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381451/notion-blog/_/_/_/Untitled_3.png)

```xml
데이터셋에서 데이터를 관리하기 위해서는 사용할 컬럼을 추가해주어야함

Columns 항목에서 [Add column to the dataset] 버튼을 클릭해 컬럼을 추가

Rows 항목에서 테스트를 위한 데이터를 추가할 수 있다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381451/notion-blog/_/_/_/Untitled_4.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381451/notion-blog/_/_/_/Untitled_5.png)

```xml
추가된 항목은 데이터셋 콘텐츠 에디터에서 소스 코드로 확인할 수 있다
```

# **컴포넌트 배치**

```xml
고객사 목록 조회 화면은 제목, 검색창, 검색버튼, 그리드로 구성
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381451/notion-blog/_/_/_/Untitled_6.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381528/notion-blog/_/_/_/Untitled_7.png)

```xml
Div 컴포넌트는 여러 개의 컴포넌트를 감싸주는 역할을 함

Div 컴포넌트를 먼저 배치하고 안에 들어갈 컴포넌트를 배치해야함
```

# **그리드**

```xml
데이터를 다루는 화면을 구성한다면 가장 많이 사용하는 컴포넌트가 그리드

그리드는 컴포넌트 혼자서 사용하기보다는 데이터를 관리하는 데이터셋과 연결해 사용
```

## **데이터 바인딩**

```xml
화면에 배치된 그리드에 데이터를 보여주기 위해서는 데이터셋을 연결해주어야 함

이런 연결 작업을 데이터 바인딩(Binding)

그리드뿐 아니라 데이터를 화면에 보여줄 수 있는 모든 컴포넌트는 데이터셋과 연결할 수 있음

연결하면 데이터를 보여주는 것뿐 아니라 그리드 내에서 직접 데이터를 수정하거나 입력할 수 있으며 변경된 데이터는 데이터셋에 바로 반영
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381528/notion-blog/_/_/_/Untitled_8.png)

```xml
그리드에 데이터셋을 연결하는 방법은 binddataset 속성값에 데이터셋 id값을 지정해주거나 넥사크로 스튜디오 화면 위에서 데이터셋 컴포넌트를 마우스로 클릭하고 그리드 컴포넌트 위로 드래그해서 옮겨줌
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381528/notion-blog/_/_/_/Untitled_9.png)

```xml
데이터셋을 드래그해서 그리드 컴포넌트 위에 가져다 놓으면 binddataset 속성값이 해당 데이터셋 id로 지정되면서 formats 속성에 데이터셋 컬럼을 기준으로 값이 만들어짐
```

## **그리드 콘텐츠 에디터(Grid Contents Editor)**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381522/notion-blog/_/_/_/Untitled_10.png)

```xml
다른 형식으로 데이터를 표기하기를 원한다면 그리드 콘텐츠 에디터에서 원하는 형식을 만들 수 있음

그리드를 더블클릭하거나 속성창에서 formats 속성과 연결된 버튼을 클릭하면 그리드 콘텐츠 에디터가 실행됨

바꾸고 싶다면 기존의 항목을 제거하고 새로운 항목을 작성하면됨
```

## 컬럼에 바인딩하는 항목 지정

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381522/notion-blog/_/_/_/Untitled_11.png)

```xml
Grid 컴포넌트와 바인딩된 Dataset이 있는 경우 Dataset에 설정된 컬럼 목록을 에디터에서 확인할 수 있음

text 속성에 직접 "bind:id"라는 값을 입력하거나 목록에서 "id" 항목을 선택할 수 있음
```

# **데이터 테스트**

```xml
테스트 단계에서는 기능 구현 여부만 확인할 때도 있다

테스트로 스크립트로 데이터를 데이터셋에 추가해주고 연결된 그리드에 데이터를 표시
```

## **버튼 클릭 이벤트**

```xml
btnSearch 버튼에 클릭 이벤트를 추가하고 데이터를 처리하는 스크립트를 작성

btnSearch 버튼 컴포넌트를 선택하고 속성창에서 onclick 이벤트를 추가
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381522/notion-blog/_/_/_/Untitled_12.png)

```jsx
예제 스크립트

this.divCommand_btnSearch_onclick = function(obj:nexacro.Button,e:nexacro.ClickEventInfo)
{
     var row = this.dsCustomers.addRow();
     this.dsCustomers.setColumn(row, "id", "TC-001");
     this.dsCustomers.setColumn(row, "name", "Tzuyu");
     this.dsCustomers.setColumn(row, "email", "ceo@twice.com");
     this.dsCustomers.setColumn(row, "phone", "6987-6543");
     this.dsCustomers.setColumn(row, "comp_name", "TWICE");
     this.dsCustomers.setColumn(row, "department", "0");
     this.dsCustomers.setColumn(row, "comp_phone", "6506-7000");
     this.dsCustomers.setColumn(row, "comp_addr", "Seoul");
};
```

## **Generate**

```jsx
만들어진 앱을 웹브라우저에서 실행

작성된 코드를 자바스크립트 파일로 변환

[Menu] Generate > Application
```

## **Quick View**

```jsx
웹서버를 실행하고 넥사크로 스튜디오에서 Quick View 를 실행
```

# **트랜잭션**

```jsx
넥사크로플랫폼 앱에서 코드 데이터를 어떻게 다루고 서버에 있는 데이터를 어떤 식으로 가져오는지 살펴보겠다
```

# **그리드 콤보**

```jsx
국가, 부서, 직급과 같은 데이터는 여러 번 반복해서 사용되는 항목

이런 데이터는 별도의 코드 테이블을 만들어 관리

(자바 enum 과 비슷한듯)

그리드 컴포넌트에는 데이터 형식에 따라 displaytype 속성을 선택할 수 있다
```

## 예제

```jsx
먼저 데이터셋을 새로 만들고 속성과 컬럼값을 아래와 같이 입력

코드를 [Dataset Editor > Source] 탭에 입력할 수도 있다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381523/notion-blog/_/_/_/Untitled_13.png)

```jsx
<ColumnInfo>
  <Column id="code" type="STRING" size="32"/>
  <Column id="value" type="STRING" size="32"/>
</ColumnInfo>
<Rows>
  <Row>
    <Col id="code">0</Col>
    <Col id="value">Boardroom</Col>
  </Row>
  <Row>
    <Col id="code">1</Col>
    <Col id="value">Accounting</Col>
  </Row>
  <Row>
    <Col id="code">2</Col>
    <Col id="value">Personal</Col>
  </Row>
  <Row>
    <Col id="code">3</Col>
    <Col id="value">Human resources</Col>
  </Row>
  <Row>
    <Col id="code">4</Col>
    <Col id="value">Sales</Col>
  </Row>
  <Row>
    <Col id="code">5</Col>
    <Col id="value">Marketing</Col>
  </Row>
  <Row>
    <Col id="code">6</Col>
    <Col id="value">Engineering</Col>
  </Row>
</Rows>
```

```jsx
그리드를 더블클릭해 그리드 콘텐츠 에디터를 실행

Department 항목의 데이터 셀을 선택하고 속성창에서 다음 항목을 수정

combodataset 항목은 직접 입력하거나 목록에서 선택할 수 있으며 combodataset 항목이 입력되면 combocodecol, combodatacol 항목도 직접 입력하거나 목록에서 선택할 수 있음
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381523/notion-blog/_/_/_/Untitled_14.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381523/notion-blog/_/_/_/Untitled_15.png)

```jsx
ADL이 아닌 폼을 수정한 경우에는 수정된 폼에 해당하는 파일만 다시 만들면 됨

메뉴에서 Generate Application이 아닌 Generate File을 선택
```

### 결과

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381523/notion-blog/_/_/_/Untitled_16.png)

# 실제 서버에서 데이터를 가져오기

```jsx
넥사크로플랫폼 앱에서 사용하는 데이터 형식에 따라 만들어진 XML 파일을 가져오는 방식으로 테스트를 진행
```

### 예제

```jsx
4개의 레코드를 가지고 있으며 조회가 성공된 것을 전제로 작성

<?xml version="1.0" encoding="utf-8"?>
<Root xmlns="http://www.nexacroplatform.com/platform/dataset" ver="5000" >
     <Parameters>
          <Parameter id="ErrorCode" type="int">0</Parameter>
          <Parameter id="ErrorMsg" type="string">SUCC</Parameter>
     </Parameters>
     <Dataset id="customers">
          <ColumnInfo>
               <Column id="id" type="STRING" size="4"/>
               <Column id="name" type="STRING" size="16"/>
               <Column id="email" type="STRING" size="32"/>
               <Column id="phone" type="STRING" size="16"/>
               <Column id="comp_name" type="STRING" size="32"/>
               <Column id="department" type="STRING" size="32"/>
               <Column id="comp_phone" type="STRING" size="16"/>
               <Column id="comp_addr" type="STRING" size="256"/>
          </ColumnInfo>
          <Rows>
               <Row>
                    <Col id="id">TC-001</Col>
                    <Col id="name">Tzuyu</Col>
                    <Col id="email">ceo@twice.com</Col>
                    <Col id="phone">6987-6543</Col>
                    <Col id="comp_name">TWICE</Col>
                    <Col id="department">0</Col>
                    <Col id="comp_phone">6506-7000</Col>
                    <Col id="comp_addr">Seoul</Col>
               </Row>
               <Row>
                    <Col id="id">TC-002</Col>
                    <Col id="name">Kei</Col>
                    <Col id="email">kei@lovelyz.ca</Col>
                    <Col id="phone">7357-3715</Col>
                    <Col id="comp_name">Lovelyz</Col>
                    <Col id="department">4</Col>
                    <Col id="comp_phone">7357-7000</Col>
                    <Col id="comp_addr">Lansing</Col>
               </Row>
               <Row>
                    <Col id="id">TC-003</Col>
                    <Col id="name">Choa</Col>
                    <Col id="email">choa@aoa.com</Col>
                    <Col id="phone">9025-0645</Col>
                    <Col id="comp_name">AOA</Col>
                    <Col id="department">2</Col>
                    <Col id="comp_phone">9025-7000</Col>
                    <Col id="comp_addr">Coral Springs</Col>
               </Row>
               <Row>
                    <Col id="id">TC-004</Col>
                    <Col id="name">Zhou Jieqiong</Col>
                    <Col id="email">zhou.jieqiong@ioi.ca</Col>
                    <Col id="phone">7026-3815</Col>
                    <Col id="comp_name">I.O.I</Col>
                    <Col id="department">3</Col>
                    <Col id="comp_phone">7026-7000</Col>
                    <Col id="comp_addr">Elmira</Col>
               </Row>
          </Rows>
     </Dataset>
</Root>
```

## **버튼 클릭 이벤트**

```jsx
앞에서 작성한 버튼 클릭 이벤트(divCommand_btnSearch_onclick)에 수작업으로 데이터를 추가하는 코드 대신에

sample.xml 파일에 접근해서 데이터를 가져오는 코드를 추가
```

```jsx
this.divCommand_btnSearch_onclick = function(obj:nexacro.Button,  e:nexacro.ClickEventInfo)
{

     var id = "search";
     var url = "http://localhost:8080/CustomerList/sample.xml";
     var reqDs = "";
     var respDs = "dsCustomers=customers";
     var args = "";
     var callback = "received";

     this.transaction(id, url, reqDs, respDs, args, callback);
}
```

```jsx
예제에서 transaction 메소드에 사용한 파라미터는 아래와 같다

transaction 메소드는 비동기통신, 바이너리통신, 압축통신을 지정하는 3개의 파라미터가 더 있으며 해당 파라미터는 생략하면 기본값이 적용
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381523/notion-blog/_/_/_/Untitled_17.png)

```jsx
transaction 메소드로 서비스 또는 파일에 접근한 이후에 지정된 콜백 함수를 실행

콜백함수는 아래와 같이 작성
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381524/notion-blog/_/_/_/Untitled_18.png)

```jsx
this.received = function (id, code, message) {
  if (code == 0) {
    this.alert(this.dsCustomers.rowcount + ' numbers of data have been found.')
    trace(this.dsCustomers.rowcount + ' numbers of data have been found.')
  } else {
    this.alert('Error[' + code + ']:' + message)
    trace('Error[' + code + ']:' + message)
  }
}
```

# **X-API**

```jsx
넥사크로플랫폼 앱은 화면(Form)에서 입력받은 데이터를 서버로 전송하고 데이터를 받아오기 위해 transaction() 메소드를 사용

transaction() 메소드는 입력 받은 변수와 데이터셋을 XML 형식으로 변형해 서버에 전달하고 콜백 함수를 통해 받아온 데이터를 처리

이런 과정에서 클라이언트가 전달한 변수와 데이터셋을 처리하고 데이터베이스에 있는 데이터를 처리하기 위해 서버단에서 동작하는 서비스가 필요

서버에 구현한 서비스에서는 요청받은 데이터를 다양한 데이터베이스에서 가져와 이를 적절한 형태로 가공하고 클라이언트에 전달

서비스는 JSP, Servlet, ASP, PHP 등 서버 환경에 따라 다양한 프로그래밍 언어로 작성할 수 있습니다. 이번 장에서는 간단한 JSP 서비스를 만들고 어떻게 동작하는지 살펴봅니다.
```

```jsx
간단한 테스트를 위해 데이터베이스에 연결하는 대신 파일 형태로 서버에 저장합니다.

아래 3가지 서비스를 설명

initdata.jsp: 서버에 기본 자료를 생성하고 파일로 저장합니다.

search.jsp: 저장된 파일에서 데이터를 읽어 Dataset을 만들고 클라이언트로 전송합니다.

save_list.jsp: 서버에 전송한 데이터로 기존 파일을 수정합니다.
```

## X-API 설치

### 1. X-API 설치

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381524/notion-blog/_/_/_/Untitled_19.png)

```jsx
X-API 배포 파일은 기술지원사이트에서 내려받을 수 있습니다.

http://support.tobesoft.co.kr > PRODUCT > nexacro 17> Download > Server [API, XENI]

개발용 라이선스 파일은 LICENSE 메뉴 아래 [개발용라이선스] 링크에서 신청할 수 있습니다.
```

### 2. 컨텍스트 파일 생성

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381524/notion-blog/_/_/_/Untitled_20.png)

```jsx
위에서 설정 했겠지만 한번더 언급
```

### 3. 내려받은 X-API 파일 압축 해제

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381524/notion-blog/_/_/_/Untitled_21.png)

```jsx
내려받은 X-API 파일의 압축을 해제합니다. 압축을 해제하면 docs, lib 폴더 2개가 보이는데 그중에서 lib 폴더 안에 있는 2개의 jar 파일을 사용할 겁니다.
```

### 4. jar 파일 넣어주기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381525/notion-blog/_/_/_/Untitled_22.png)

```jsx
docBase로 지정한 폴더를 생성하고(path 지정한경로)

 해당 폴더 안에 WEB-INF 폴더를 생성합니다. 그리고 WEB-INF 폴더 안에 lib 폴더를 생성하고 jar 파일과 라이선스 파일을 복사합니다.

요약하면 내가 만든 폴더에 WEB-INF 폴더를 만들고 lib 폴더를 만들고 넣어준다 (라이센스 키도 발급받아서 넣어줘야한다)
```

### 5.테스트를 위한 jsp 파일 생성

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381525/notion-blog/_/_/_/Untitled_23.png)

```jsx
docBase로 지정한 폴더 아래에 테스트를 위한 jsp 파일을 생성합니다.
```

```html
<%@ page contentType="text/html; charset=UTF-8" %>

<html>
  <head>
    <title>JarInfo</title>
    <style>
      * {
        font-family: Verdana;
      }
    </style>
  </head>
  <body>
    <pre>
<%
    new com.nexacro17.xapi.util.JarInfo().info(out);
%>
        </pre
    >
  </body>
</html>
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381525/notion-blog/_/_/_/Untitled_24.png)

```html
http://localhost:8080/CustomerListSearch/test.jsp 위의 경로로 접근했을 때 화면이 출력되는지
확인한다.
```

```html
WEB-INF는 Web Information의 약자 컨텍스트 루트 디렉토리 내에 있지만 사용자가 접근할 수 없으며
내부적으로 참조하기 위한 정보를 담고 있음 lib 디렉토리는 jar 파일을 넣어 둘 수 있는 공간 jar는 Java
Archive의 약자로 여러개의 자바 클래스 파일과 리소스 등을 하나의 파일로 묶어서 라이브러리처럼
관리하는 파일
```

```html
라이브러리 파일을 복사한 후에는 WAS 서비스를 재시작해주어야 합니다. WAS 설정에 따라 재시작이
필요없는 경우도 있습니다. 라이브러리 파일과 라이선스 파일은 같은 위치에 복사해야 인식할 수 있습니다.
```

## 전용객체

```html
연결된 라이브러리에서 사용되는 전용객체는 다음과 같습니다.
```

```html
* PlatformData : 데이터를 보관하는 기본객체 * PlatformRequest : JSP 요청 시에 XML Format Data를 읽고
객체화하는 Input 객체 * PlatformResponse : JSP 요청 시에 XML Format Data를 출력하는 output 객체 *
DatasetList & Dataset : 데이터를 2차원 Table 형태 또는 Table Array 타입으로 보관 * VariableList &
Variable : I/O 인자값으로 사용되는 단일 값을 보관 자세한 설명은 라이브러리에 포함된 X-API 매뉴얼을
참고해주세요.
```

## 화면 만들기 실습

```java
상세한 기능 설명은 아래로
```

[넥사크로플랫폼 17 초보자를 위한 자습서 17.1.3.100](http://docs.nexacro.com/getting_started_nexacro_17_ko/d82e8b8cd262cb1a#cfa5f5bf77602e72)

### 1. **화면에 Button 컴포넌트 추가**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381525/notion-blog/_/_/_/Untitled_25.png)

```html
기존 화면에 데이터 초기화, 저장을 위한 Button 컴포넌트를 추가합니다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381525/notion-blog/_/_/_/Untitled_26.png)

### 2. **initdata.jsp 생성**

```html
기본 자료를 생성하고 서버에 파일로 저장합니다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381526/notion-blog/_/_/_/Untitled_27.png)

```java
전체 코드 형태

// 1. 자바 라이브러리 지정 (nexacro platform X-API 포함)
// 2. MIME Type 정의
// 3. nexacro platform 기본객체(PlatformData) 생성
try {
    // 4. Data 처리
    // 5. ErrorCode, ErrorMsg 처리하기 (성공 메시지)
} catch (Error) {
    // 5. ErrorCode, ErrorMsg 처리하기 (실패 메시지)
}
// 6. 결과 Data Client에게 보내기
```

전체코드

```java
<!-- 1.Designating a Java library -->
<%@ page import="java.io.*" %>
<%@ page import="com.nexacro17.xapi.data.*" %>
<%@ page import="com.nexacro17.xapi.tx.*" %>

<!-- 2. Defining a MIME type -->
<%@ page contentType="text/xml; charset=UTF-8" %>

<%
/** 3. Creating a basic object of Nexacro Platform **/
PlatformData pdata = new PlatformData();

/** 5-1. Processing ErrorCode and ErrorMsg **/
int nErrorCode = 0;
String strErrorMsg = "START";

try {
    /** 4. Processing data: saving data as a file **/
    /** 4.1 Creating a dataset and inputting basic data to the dataset **/
    DataSet ds = new DataSet("customers");
    ds.addColumn("id",DataTypes.STRING, 4);
    ds.addColumn("name",DataTypes.STRING, 16);
    ds.addColumn("email", DataTypes.STRING, 32);
    ds.addColumn("phone", DataTypes.STRING, 16);
    ds.addColumn("comp_name", DataTypes.STRING, 32);
    ds.addColumn("department", DataTypes.STRING, 32);
    ds.addColumn("comp_phone", DataTypes.STRING, 16);
    ds.addColumn("comp_addr", DataTypes.STRING, 256);

    int row = 0;
    int i = 0;
    String[] customers = new String[8];

    customers[0] = "TC-001";
    customers[1] = "Tzuyu";
    customers[2] = "ceo@twice.com";
    customers[3] = "6987-6543";
    customers[4] = "TWICE";
    customers[5] = "0";
    customers[6] = "6506-7000";
    customers[7] = "Seoul";

    for (i = 0; i < 1; i++)
    {
        row = ds.newRow();
        ds.set(row,"id",customers[0]);
        ds.set(row,"name",customers[1]);
        ds.set(row,"email",customers[2]);
        ds.set(row,"phone",customers[3]);
        ds.set(row,"comp_name",customers[4]);
        ds.set(row,"department",customers[5]);
        ds.set(row,"comp_phone",customers[6]);
        ds.set(row,"comp_addr",customers[7]);
    }

    pdata.addDataSet(ds);

    /** 4.2 Saving a dataset to a file **/
    String targetFilename = "./saveFile.bin";
    OutputStream target = new FileOutputStream(targetFilename);
    PlatformResponse res = new PlatformResponse(target,
        PlatformType.CONTENT_TYPE_BINARY);
    res.setData(pdata);
    res.sendData();
    target.close();
    System.out.println("after file write..");

    /** 5.2 Setting ErrorCode and ErrorMsg for success**/
    nErrorCode = 0;
    strErrorMsg = "SUCC";

} catch (Throwable th) {
    /** 5.3 Setting ErrorCode and ErrorMsg for failure **/
    nErrorCode = -1;
    strErrorMsg = th.getMessage();
}

/** 5.4 Saving the ErrorCode and ErrorMsg to send them to the client **/
PlatformData senddata = new PlatformData();
VariableList varList = senddata.getVariableList();
varList.add("ErrorCode", nErrorCode);
varList.add("ErrorMsg", strErrorMsg);

/** 6. Sending result data to the client **/
HttpPlatformResponse res = new HttpPlatformResponse(response,
    PlatformType.CONTENT_TYPE_XML,"UTF-8");
res.setData(senddata);
res.sendData();
%>
```

### **데이터 초기화 이벤트 추가(initdata)**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381526/notion-blog/_/_/_/Untitled_28.png)

```java
onclick 이벤트를 추가합니다. 화면 내 버튼을 클릭하면 initdata.jsp 서비스가 호출되면서 서버에 데이터셋이 담긴 파일을 생성합니다.
```

```java
this.divCommand_btnInitdata_onclick = function(obj:nexacro.Button,  e:nexacro.ClickEventInfo)
{
     var id = "initdata";
     var url = "SvcList::initdata.jsp";
     var reqDs = "";
     var respDs = "";
     var args = "";
     var callback = "initdata_received";

     this.transaction(id, url, reqDs, respDs, args, callback);
}

this.initdata_received = function(id, code, message)
{
     if (code == 0) {
          this.alert(message);
          trace(message);
     } else {
          this.alert("Error["+code+"]:"+message);
          trace("Error["+code+"]:"+message);
     }
}
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381526/notion-blog/_/_/_/Untitled_29.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381526/notion-blog/_/_/_/Untitled_30.png)

```java
url 속성은 전체 URL을 지정할 수 있습니다. 매번 전체 URL을 지정하는 것이 번거롭고 자주 사용하는 도메인이라면 TypeDefinition에 서비스 URL을 추가하고 변경되는 파일명만 지정할 수 있습니다.

지정된 서비스는 아래와 같이 사용합니다.

위의 예제 url 부분을 보면  var url = "SvcList::initdata.jsp"; 이렇게 사용하고있다.
```

```java
var url = "[Service ID]::[file name]";
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381527/notion-blog/_/_/_/Untitled_31.png)

```java
버튼을 클릭해 initdata.jsp 파일을 호출하면 "saveFile.bin"이라는 이름의 파일이 생성됩니다. 생성된 파일은 WAS(예제에서는 아파치 톰캣)을 설치한 폴더 아래에 생성됩니다.

특정 경로에 파일을 저장하고자 하는 경우에는 아래와 같이 initdata.jsp 파일을 수정해 적용할 수 있습니다.

//String targetFilename = "./saveFile.bin";
File targetFile = new File("c:/saveFile.bin");
OutputStream target = new FileOutputStream(targetFile);
```

### **search.jsp 추가**

```java
저장된 파일에서 데이터를 읽어 Dataset을 만들고 클라이언트로 전송합니다.
```

```java
// 1. 자바 라이브러리 지정 (nexacro platform X-API 포함)
// 2. MIME Type 정의
// 3. nexacro platform 기본객체(PlatformData) 생성
try {
    // 4. Data 처리
    // 5. ErrorCode, ErrorMsg 처리하기 (성공 메시지)
} catch (Error) {
    // 5. ErrorCode, ErrorMsg 처리하기 (실패 메시지)
}
// 6. 결과 Data Client에게 보내기
```

```java
전체 코드

<!-- 1.Designating a Java library -->
<%@ page import="java.io.*" %>
<%@ page import="com.nexacro17.xapi.data.*" %>
<%@ page import="com.nexacro17.xapi.tx.*" %>

<!-- 2. Defining a MIME type -->
<%@ page contentType="text/xml; charset=UTF-8" %>
<%
/** 3. Creating a basic object of Nexacro Platform **/
PlatformData pdata = new PlatformData();

/** 5.1 Processing ErrorCode and ErrorMsg **/
int nErrorCode = 0;
String strErrorMsg = "START";

try {
    /** 4. Processing data : Loading data from the file: loading data from the file **/
    /** 4.1 Loading data from the file **/
    String sourceFilename = "./saveFile.bin";;
    InputStream source = new FileInputStream(sourceFilename);

    PlatformRequest req = new PlatformRequest(source,
        PlatformType.CONTENT_TYPE_BINARY);
    req.receiveData();
    source.close();

    /** 4.2 Copying the loaded data to the dataset **/
    pdata = req.getData();

    /** 5.2 Setting ErrorCode and ErrorMsg for success **/
    nErrorCode = 0;
    strErrorMsg = "SUCC";

} catch (Throwable th) {
    /** 5.3 Setting ErrorCode and ErrorMsg for failure **/
    nErrorCode = -1;
    strErrorMsg = th.getMessage();
}

/** 5.4 Saving ErrorCode and ErrorMsg to send them to the client **/
VariableList varList = pdata.getVariableList();
varList.add("ErrorCode", nErrorCode);
varList.add("ErrorMsg", strErrorMsg);

/** 6. Sending result data to the client **/
HttpPlatformResponse res = new HttpPlatformResponse(response,
    PlatformType.CONTENT_TYPE_XML,"UTF-8");
res.setData(pdata);
res.sendData();
%>
```

### 데이터 베이스 연결

```java
예제에서는 파일로 저장된 데이터셋을 가져와 그대로 클라이언트에게 전달합니다. 파일이 아닌 데이터베이스에 연결한 경우에는 아래와 같이 적용할 수 있습니다.

아래 예제에서 데이터베이스 설정은 예시를 위한 코드입니다. 실제 코드에서는 사용하는 환경에 맞게 수정해서 사용해야 합니다.
```

**search.jsp (JDBC Connect)**

```java
<%@ page import = "java.util.*" %>
<%@ page import = "java.sql.*" %>
<%@ page import = "java.io.*" %>
<%@ page import="com.nexacro17.xapi.data.*" %>
<%@ page import="com.nexacro17.xapi.tx.*" %>

<%@ page contentType="text/xml; charset=utf-8" %>

<%
/****** Service API initialization ******/
PlatformData pdata = new PlatformData();

int nErrorCode = 0;
String strErrorMsg = "START";

/******* JDBC Connection *******/
Connection conn = null;
Statement  stmt = null;
ResultSet  rs   = null;
Class.forName("com.microsoft.sqlserver.jdbc.SQLServerDriver");
conn = DriverManager.getConnection("jdbc:sqlserver://localhost:1433;;DatabaseName=Sample",
    "guest","guest");
stmt = conn.createStatement();

try {
    /******* SQL query *************/
    String SQL = "select * from sample_customers_list";
    rs = stmt.executeQuery(SQL);

    /********* Dataset Create ************/
    DataSet ds = new DataSet("customers");
    ds.addColumn("id",DataTypes.STRING, 4);
    ds.addColumn("name",DataTypes.STRING, 16);
    ds.addColumn("email", DataTypes.STRING, 32);
    ds.addColumn("phone", DataTypes.STRING, 16);
    ds.addColumn("comp_name", DataTypes.STRING, 32);
    ds.addColumn("department", DataTypes.STRING, 32);
    ds.addColumn("comp_phone", DataTypes.STRING, 16);
    ds.addColumn("comp_addr", DataTypes.STRING, 256);
    int row = 0;
    while(rs.next())
    {
        row = ds.newRow();
        ds.set(row, "id", rs.getString("id"));
        ds.set(row, "name", rs.getString("name"));
        ds.set(row, "email", rs.getString("email"));
        ds.set(row, "phone", rs.getString("phone"));
        ds.set(row, "comp_name", rs.getString("comp_name"));
        ds.set(row, "department", rs.getString("department"));
        ds.set(row, "comp_phone", rs.getString("comp_phone"));
        ds.set(row, "comp_addr", rs.getString("comp_addr"));
    }

    /********* Adding Dataset to PlatformData ************/
    pdata.addDataSet(ds);

    nErrorCode = 0;
    strErrorMsg = "SUCC";
}
catch(SQLException e) {
    nErrorCode = -1;
    strErrorMsg = e.getMessage();
}

/******** JDBC Close *******/
if ( stmt != null ) try { stmt.close(); } catch (Exception e) {}
if ( conn != null ) try { conn.close(); } catch (Exception e) {}

PlatformData senddata = new PlatformData();
VariableList varList = senddata.getVariableList();
varList.add("ErrorCode", nErrorCode);
varList.add("ErrorMsg", strErrorMsg);

/******** XML data Create ******/
HttpPlatformResponse res = new HttpPlatformResponse(response,
    PlatformType.CONTENT_TYPE_XML,"UTF-8");
res.setData(pdata);
res.sendData();
%>
```

### **save_list.jsp 추가**

```java
서버로 전송한 데이터를 파일로 저장합니다.
```

```java
// 1. Designating a Java library (including nexacro platform X-API)
// 2. Defining a MIME type
// 3. Creating a basic object of Nexacro Platform (PlatformData)
try {
    // 4. Receiving a request from the client
    // 5. Processing data
    // 6. Processing ErrorCode and ErrorMsg (success message)
} catch (Error) {
    // 6. Processing ErrorCode and ErrorMsg (failure message)
}
// 7. Sending result data to the client
```

전체코드

```java
<!-- 1. Designating a Java library -->
<%@ page import="java.io.*" %>
<%@ page import="com.nexacro17.xapi.data.*" %>
<%@ page import="com.nexacro17.xapi.tx.*" %>

<!-- 2. Defining a MIME type -->
<%@ page contentType="text/xml; charset=UTF-8" %>

<%
/** 3. Creating a basic object of Nexacro Platform **/
PlatformData pdata = new PlatformData();

/** 6.1 Processing ErrorCode and ErrorMsg **/
int nErrorCode = 0;
String strErrorMsg = "START";

try {
    /** 4. Receiving a request from the client **/
    // create HttpPlatformRequest for receive data from client
    HttpPlatformRequest req = new HttpPlatformRequest(request);
    req.receiveData();
    /** 5. Processing data: Loading data from the file **/
    /** 5.1 Loading data from the http object **/
    pdata = req.getData();

    /** Obtaining a dataset from the received data **/
    DataSet ds = pdata.getDataSet("customers");

    /** Saving data as a file with init data **/
    String targetFilename = "./saveFile.bin";
    OutputStream target = new FileOutputStream(targetFilename);
    PlatformResponse res = new PlatformResponse(target,
        PlatformType.CONTENT_TYPE_BINARY);
    res.setData(pdata);
    res.sendData();
    target.close();

    /** 6.2 Setting ErrorCode and ErrorMsg for success **/
    nErrorCode = 0;
    strErrorMsg = "person list saved complete : row count("+ds.getRowCount()+")";

} catch (Throwable th) {
    /** 6.3 Setting ErrorCode and ErrorMsg for failure **/
    nErrorCode = -1;
    strErrorMsg = th.getMessage();
    System.out.println("ERROR:"+strErrorMsg);
}

/** 6.4 Saving ErrorCode and ErrorMsg to send them to the client **/
PlatformData senddata = new PlatformData();
VariableList varList = senddata.getVariableList();
varList.add("ErrorCode", nErrorCode);
varList.add("ErrorMsg", strErrorMsg);

/** 7. Sending result data to the client **/
HttpPlatformResponse res = new HttpPlatformResponse(response,
    PlatformType.CONTENT_TYPE_XML,"UTF-8");
res.setData(senddata);
res.sendData();
%>
```

### **데이터 저장 이벤트 추가**

```java
onclick 이벤트를 추가합니다. 화면 내 버튼을 클릭하면 save_list.jsp 서비스가 호출되면서 화면에서 수정한 데이터셋을 서버로 전송하고 다시 파일로 저장합니다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381527/notion-blog/_/_/_/Untitled_32.png)

```java
데이터를 수정할 수 있는 기능을 간단하게 추가합니다. 화면 내 그리드를 더블클릭하고 Grid Contents Editor를 실행한 다음 Name 항목을 선택한 후 edittype 속성을 'text'로 수정합니다.
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788381527/notion-blog/_/_/_/Untitled_33.png)

```java
데이터 조회 후 해당 항목을 더블 클릭하면 데이터를 수정할 수 있습니다.

데이터 수정 후 저장(save) 버튼을 클릭해 데이터를 서버로 전송합니다.

화면을 새로 고침한 다음 조회(Search) 버튼을 클릭하면 수정한 데이터가 조회되는 것을 확인할 수 있습니다.
```

이벤트 코드

```java
this.divCommand_btnSaveList_onclick = function(obj:nexacro.Button,  e:nexacro.ClickEventInfo)
{
     var id = "save_list";
     var url = "SvcList::save_list.jsp";
     var reqDs = "customers=dsCustomers";
     var respDs = "";
     var args = "";
     var callback = "save_list_received";

     this.transaction(id, url, reqDs, respDs, args, callback);
}

this.save_list_received = function(id, code, message)
{
     if (code == 0) {
          this.alert(message);
          trace(message);
     } else {
          this.alert("Error["+code+"]:"+message);
          trace("Error["+code+"]:"+message);
     }
}
```
