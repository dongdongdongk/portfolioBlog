---
title: '서블릿7'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## HTML-Element(요소)의 구성 요소

```java
 Element(요소)의 구성 요소
	1)  <tagname attribute="속성값" attribute2="속성값2" ... > Contents </tagname>

	2) <tagname attribute="속성값" attribute2="속성값2" ... >

	- HTML는 Element(요소들)의 집합
	- Element는 중첩이 가능
	- tagName은 대소문자를 구별하지 않지만 소문자를 권장

	3) attribute
	- 해당 Element에 추가적인 정보를 제공 (이미지 경로, 파일의 경로, 크기...)
	- 모든 Element가 동일한 속성을 가지진 않음
	- 모든 Elelment가 동일한 속성이 있고 그것을 Global Attribute
		-- id, class, data-*, lang,...

	** 한페이지내에 같은 클래스명 사용가능
	** 한 Element 클래스명은 여러개 가능 구분은 스페이스바
	** 한페이지내에 같은 ID명은 사용 금지
```

### HTML-Text

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380895/notion-blog/_/_/JSP/_7/Untitled.png)

```html
1. Heading 태그 - display : block - 제목을 표현, 글자의 크기를 조정하고 볼드체

<h숫자> Contents </h숫자>
- 숫자 : 1 - 6 - 숫자가 높을 수록 크기가 작아짐 - 검색엔진에서 Heading 태그를 중요한 의미로 받아들일
가능성이 큼 2. hr - display : block - 수평선 3. p - display : block - 문단 태그 - 문단은 자동으로
한줄 간격이 생김 4. br - 엔터키 같은 역할
```

### HTML-Link

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380893/notion-blog/_/_/JSP/_7/Untitled_1.png)

```jsx
1. 태그명 : a

2. 주속성 : href
	- 가고자 하는 자원의 위치(경로, path)를 작성

~~3. 경로(path)
	- Root Directory : 최상위 디렉토리, 더 이상 올라 갈수 없는 곳
	- Root Directory 기준

	1) 절대경로
		- Root Directory 부터 시작
		- IP:PORT 까지를 Root 라 함 표시는 ' / '
		- Web에서 Root를 Context(Context Name이라 함)
		- Spring(Servlet) Leagcy 에서는 프로젝트명이 Context Name 임
		- /ContextName/경로명/....

	2) 상대경로
		- 현재 위치 기준 (현재 Directory 기준)
		- 한 Directory 위로 이동 : ../
		- 현재 Directory에서     : ./ 또는 생략
		- 현재 Directory에서 하위 Directory로 이동 : 이동할 Directory명

	3) 절대경로와 상대경로 구분~~
```

### HTML-Media

```jsx
1. Image
	display : inline
	a. 태그명 : img

	b. 속성
		1) src    : 이미지 파일의 경로
		2) alt    : 이미지가 없는경우 표시되는 문장, 접근성으로 사용
		3) width  : 이미지의 가로 크기 조정 (CSS에서 조정)
		4) height : 이미지의 세로 크기 조정 (CSS에서 조정)

2. Video

3. Audio
```

### HTML -image

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380893/notion-blog/_/_/JSP/_7/Untitled_2.png)

```html
input 태그의 type 속성으로 image
<input type="image" src="이미지경로" alt="이미지 이름" />

* alt="이미지 이름" : 이미지가 깨질시 나오는 텍스트 * 이미지 파일은 resources 에 저장해준다
```

### HTML-Table

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380893/notion-blog/_/_/JSP/_7/Untitled_3.png)

```jsx
1. 표를 만들 때 사용

<table>          : 표를 감싸는 태그
	<tr>           : 표의 행 (row)
			<td></td>  : 표의 열 (column)
	</tr>

</table>

2. 표를 생성시 제목과 내용을 분리
<table>
    <thead>
        <tr>
            <th>제목1</th><th>제목2</th>
        </tr>
    </thead>

    <tbody>
         <tr>
             <td>내용1</td><td>내용2</td>
         </tr>

    </tbody>

    <tfoot>
          <tr>
            <td>foot1</td><td>foot2</td>>
          </tr>
     </tfoot>
    </table>

3. 컬럼 확장(합치기)
	1) Column 확장 (가로로 확장, 옆으로)
			<td colspan="합치는 총 갯수">
	2) Row 확장 (셀로로 확장, 위아래)
			<td rowspan="합치는 총 갯수">

	* border 을 사용하면 선이 생긴다
```

### HTML-그룹 태그

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380894/notion-blog/_/_/JSP/_7/Untitled_4.png)

```jsx
- 여러개의 Element를 그룹으로 묶어주는 역할
- 태그 자체는 아무런 효과가 없음

1. div
	- display 속성이 block

2. span
	- display 속성이 inline
```

### HTML-List

```jsx
- 목록
- 주로 메뉴 생성시 사용
- block 태그
1. 순서가 있는 리스트 (Orderd List)
	<ol>
		<li></li>
		<li></li>
		...
	</ol>

2. 순서가 없는 리스트 (Unordered Liest)
	<ul>
		<li></li>
		<li></li>
		...
	</ul>

-- <li> 태그도 Block 태그
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380894/notion-blog/_/_/JSP/_7/Untitled_5.png)

### HTML-Form

```jsx
- 사용자로부터 입력 받거나 선택 할 때 사용
- 대부분이 서버로 전송 하는 데이터를 입력할 양식
- form 태그 영역 내의 것들을 서버로 전송
- form 태그 영역 내에는 input 태그들 사용

<form action="도착할 URL주소" method="도착할 메서드형식">

</form>

- method속성은 생략 가능 ( Default는 GET)
- GET 또는 POST로 변경 가능
```

### HTML-입력태그<input>

```jsx
- 주로 form 태그 내에서 사용
- form 태그 없이 사용도 가능 (JavaScript와 같이 사용)
- 사용자로 부터 데이터를 입력 받는 태그
- type이라는 속성값에 따라서 입력 양식이 달라짐

1. input
	<input type="값">

	1) type의 종류
		a. text     : 짧은 Text에 주로 사용
		b. password : 입력 되는 데이터를 * 로 표시
		c. file     : 파일 업로드시 파일을 선택
		d. date     : 날짜 입력 또는 선택
		e. number   : text와 비슷 , 숫자만 입력
		f. email    : 이메일 양식으로 입력
		g. tel      : 전화번호 양식으로 입력
		h. hidden   : 사용자에게는 보이지 않고 서버로 전송
		i. radio    : 여러개 중에서 하나를 선택 할 때
								: 그룹 별로 name 속성의 값을 일치시켜야 함
								: Server로 전송시 입력 할수 없으니 개발자가 미리 value에 속성에 값을 대입
		j. checkbox : 여러개중에서 다중 선택
								: 나머지는 Radio와 거의 같음

2. textarea
	- 비교적 긴글을 입력할 때 사용
	<textarea rows="" cols=""></textarea>

3. select
	- 여러개 중에서 하나를 선택
	<select name="">
		<option value="">값1</option>
		<option value="">값2</option>
		<option value="">값2</option>
	</select>

4. 공통속성
	1) value      : 입력 태그에 입력된 값이 저장되는 속성
								  파라미터의 값으로 서버로 전송
	2) **name       : 파라미터의 이름이 됨**

	3) readonly    : 입력(선택)불가, 읽기만 가능, 서버로 전송 가능

	4) disabled    : 입력(선택)불가, 읽기만 가능, 서버로 전송 불가

5. 입력 태그별 속성
	1) 사용자 타이핑하는 태그
			a. placeholder  : 입력양식에 hint 제공
```

### HTML-fieldset, legend, label

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380894/notion-blog/_/_/JSP/_7/Untitled_6.png)

\*개인정보, 로그인 정보 <legend>

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380894/notion-blog/_/_/JSP/_7/Untitled_7.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380894/notion-blog/_/_/JSP/_7/Untitled_8.png)

```jsx
1. fieldset
	- 폼 요소를 그룹으로 묶는 용도

2. legend
	- 그룹에  제목(title)을 입력하는 태그

3. label
	- 웹 접근성
	- radio, checkbox 선택시 이름을 선택해서 체크가 가능 (클릭되는 범위를 늘려주는듯)

fieldset 부모태그 legend, label 자식태그

fieldset 내부에 legend, label 이 있어야 함

<form class="frm" action="" method="post">

        <fieldset>
            <legend>ID</legend>
            <input type="text" name="id">
        </fieldset>

        <fieldset>
            <legend>비밀번호</legend>
            <input type="password" name="pw">
        </fieldset>

        <fieldset>
             <legend>성별</legend>
             <label for="female">여성</label>
             <input id="female" type="radio" name="check">
           </fieldset>

        <fieldset>
             <label for="male">남성</label>
             <input id="male" type="radio" name="check">

        </fieldset>

    </form>
```

### label for 예제

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380895/notion-blog/_/_/JSP/_7/Untitled_9.png)

```java
<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="UTF-8">
	<title>HTML label tag - for attribute</title>
</head>
<body>

    <form action="/examples/media/action_target.php" method="get">
        여러분의 나이대를 골라보세요.<br>
        <input type="radio" name="ages" id="teen" value="teenage">
        <label for="teen">10대</label><br>
        <input type="radio" name="ages" id="twenty" value="twenties">
        <label for="twenty">20대</label><br>
        <input type="radio" name="ages" id="thirty" value="thirties">
        <label for="thirty">30대</label><br>
        <input type="radio" name="ages" id="forty" value="forties">
        <label for="forty">40대 이상</label><br>
        <input type="submit">
	</form>

</body>
</html>

* id 와 for 을 같게 해서 연결해준다 (value = 선택시 파라미터 값)
```

### HTML-Button

```jsx
- Button을 생성하는 방법 크게 4(6) 가지

1. input 태그의 type속성으로 button
	- 아무런 기능이 없는 순수한 button
	- JavaScript와 같이 사용
	- value 속성의 값이 버튼의 이름

2. input 태그의 type속성으로 reset
	- 입력폼에 입력 된 내용을 초기화
	- value 속성의 값이 버튼의 이름

3. input 태그의 type속성으로 submit
	- 입력폼에 입력 된 내용을 form 태그의 action의 URL로 요청을 발생
	- submit event가 내장
	- value 속성의 값이 버튼의 이름

4. button 태그
	<button>버튼이름</button>
	type 속성의 값을 변경해서 1,2,3 기능을 전부 가질 수 있음
	type : button, reset, submit

5. a 또는 일반 태그
	- a 또는 일반 태그 css를 이용해서 버튼 처럼 보이게 함
	- 효과를 주기 위해 JavaScript를 같이 사용

6. input 태그의 type 속성으로 image
	<input type="image" src="이미지경로" value="버튼의이름">


-----------------------------------------------------------
form 태그 안에 button 들이 있으면 각각 고유의 기능(Event)이 실행

form 태그 바깥에 button들이 있으면 아무런 기능이 없는 button이 됨
JavaScript를 사용
```

### HTML-Semantic

```jsx
- HTML 5 에서 추가
- 의미가 있는 태그(검색엔진)
- Layout 구조 용

<header></header>
<nav></nav>
<section></section>
<article></article>
<footer></footer>
<aside></aside>

```
