---
title: '2023-04-17(기본설정)'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

# 프로젝트 생성

## 1.기본 경로지정 & 생성

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled.png>)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%201.png>)

```jsx

Spring Starter Project 로 새로운 프로젝트 생성
```

---

## 2.프로젝트 시작 플러그인 설정

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%202.png>)

```jsx
자동으로 MyBatis 같은 플러그인 추가해주는 기능 (MyBatis DB 연결전에 하면 오류)
```

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%203.png>)

```jsx
앞으로는 run server 가 아니라 이제 SpringBootApp 이걸로 실행
```

---

# 프로젝트 설정

## 1.views 폴더 생성

```jsx
src -> main -> webapp 폴더 생성
src -> main -> webapp -> WEB-INF 폴더 생성
src -> main -> webapp -> WEB-INF -> views 폴더 생성
```

---

## 2.Encoding 설정

```jsx
1. windows > preferences > general > workspace > Default Encoding 을 UTF-8로 변경

2. windows > preferences > general > web browser > External 선택, Chrome 선택

3. windows > preferences > > web -> css -> Encoding 을 UTF-8로 변경

4. windows > preferences > > web -> html -> Encoding 을 UTF-8로 변경

5. windows > preferences > > web -> jsp -> Encoding 을 UTF-8로 변경

6. windows > preferences > general > content type > text 확장 > Java properties files 선택 > Default Encoding 을 UTF-8로 변경 후 update
```

---

## 3.application 설정(xml 대신 사용)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%204.png>)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%205.png>)

```jsx
xml 을 더이상 사용안하고 application.properties 에서 설정한다

(예시는 server port 를 80으로 )
```

### 오류

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%206.png>)

```jsx
(설정시 주의)server port 를 80으로 했지만 여전히 8080 일수도 있다
```

---

## 4.jsp 사용 설정 & 설치

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%207.png>)

```jsx
jsp 를 지원하지 않기 때문에 Eclips Marketplace 에서 플러그인 설치가 필요하다.
```

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%208.png>)

```java
jSP 를 사용할 수 있게 해주는 플러그인
```

---

### jsp 사용설정<dependency>

```jsx

*아래 두 코드를 **pom.xml** 의 <dependencies> 에 붙여넣어준다

*스프링 부트 에서는 버전을 자동으로 최신으로 관리 해주지만 한번씩 오류가 날 떄 스스로 버전을

붙여 넣어주어야 한다.
```

```jsx
1. Tomcat을 사용 하는 것처럼 라이브러리 추가

위 코드는 Maven 프로젝트에서 Tomcat 서버를 내장(embedded)하여 JSP 파일을

처리할 수 있게 해주는 의존성(Dependency)을 추가하는 코드


  	<!-- https://mvnrepository.com/artifact/org.apache.tomcat.embed/tomcat-embed-jasper -->
		<dependency>
		    <groupId>org.apache.tomcat.embed</groupId>
		    <artifactId>tomcat-embed-jasper</artifactId>
		    <!-- 버전 삭제 -->
		</dependency>

2. JSTL 추가
		<!-- https://mvnrepository.com/artifact/javax.servlet/jstl -->
		<dependency>
		    <groupId>javax.servlet</groupId>
		    <artifactId>jstl</artifactId>
		     <!-- 버전 삭제 -->
		</dependency>

```

---

### jsp application.properties연결

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%209.png>)

```java
application.properties 에 아래 내용을 넣어준다.

# jsp
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

*
prefix 앞에 붙이는 경로
suffix 뒤에 붙이는 경로
```

---

### 오류

```jsx
https://mvnrepository.com/artifact/javax.servlet/jstl/1.2

오류시 위 링크 <version>1.2</version> 붙여넣기

주석으로 만들어서 저장 - 다시 주석풀고 저장
```

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2010.png>)

---

## 5.css,jsp 파일 경로

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2011.png>)

```java
1.css 와 javascript 파일들은(img도) static 폴더에 폴더를 만들어서 사용한다

2.templates 폴더는 jsp 를 사용하지 말기를 권장하기 때문에 타임리프 라는 html 파일을 넣어

사용한다.
```

```jsx
src>main>resources>static
src>main>resources>static>images
src>main>resources>static>css
src>main>resources>static>js

JSP에서 경로

src>main>resources>static => / 가 됨

/images/...
/css/...
/js/...

```

---

# Lombok 설치

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2012.png>)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2013.png>)

```jsx
lombok 설치

롬복의 기능은 내부적으로 DTO와 같은 모델에서 변수를 지정하면

자바 클래스로 만들 때 setter, getter, toString 등의 메소드를 자동으로 만들어주는 기능

@Data : getter, setter 전부생성

* 앞으로는 DTO 가 아닌 VO 로 이름을 변경해서 사용한다.
```

## Lombok 설치순서

[Download](https://projectlombok.org/download)

```jsx
1. Down Load

2. CMD 에서 명령어 실행 (설치파일 cmd로 경로 찾아가서 설치파일 실행시켜줌)
java -jar lombok...

3. location -> sts.exe 선택

4. install
```

## 오류

```java
설치시 sts 경로에 한글 경로가 섞여있으면 sts가 무반응으로 변함 (한글경로를 없에주자)
```

---

# DB연결

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%209.png>)

## 1. application.properties

```jsx
연결 정보 형식에 맞춰 application.properties 에 데이터를 기입해준다

연결 정보 형식
# username
spring.datasource.hikari.username=user01
# password
spring.datasource.hikari.password=user01
# url
spring.datasource.url=jdbc:mariadb://localhost:3306/iu
# driver
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
```

```jsx
**application.properties**

# port
server.port=81

# jsp
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

#DB

# username
spring.datasource.hikari.username=admin
# password
spring.datasource.hikari.password=kim14582367
# url  // 마지막에 db 는 DB 이름
spring.datasource.url=jdbc:mariadb://gdj60kim.cwbrgx5axvdi.ap-northeast-2.rds.amazonaws.com:3306/db
# driver
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
```

---

# 스프링 Boot test

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2014.png>)

```jsx
테스트

☞  통합테스트

@SpringBootTest



☞ 단위테스트

@WebMvcTest, @DataJpaTest, @RestClientTest, @JsonTest 등등
```

---

# 깃 업로드 개인정보 보안

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2015.png>)

```java
..1.application.properties 를 똑같이 복사해서 붙여넣어준다

2.이름을 -dev 를 붙여 구분해준다 ( - 를 붙이면 별칭을 붙이게 해주는것 )
```

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2016.png>)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2017.png>)

```java
1.Run Confiturations 에 들어간뒤

2.Spring Boot App 에 profile 에서 별칭을 준 파일을 선택하면 그걸로 실행이 된다.
```

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2018.png>)

![Untitled](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/Untitled%2019.png>)

```java
3.프로젝트 경로에 찾아 들어간 뒤 .gitignore 파일을 수정해서 git 에 올라가지 않을 파일

목록을 설정해준다.

4.### 은 주석을 의미하고 My Ignore 라 써주고 ###

(현재 application-dev.properties 가 설정되었다)
```

- [1. 설치 및 환경 설정](<2023-04-17(%EA%B8%B0%EB%B3%B8%EC%84%A4%EC%A0%95)/1%20%EC%84%A4%EC%B9%98%20%EB%B0%8F%20%ED%99%98%EA%B2%BD%20%EC%84%A4%EC%A0%95%207465f13174cd4e31863ae9776a00eda3.md>)
