---
title: '섹션 12. 셀레니움(selenium) find 문법'
date: '2026-09-02'
category: '기타, 사용 적음/크롤링 & 파이썬'
tags: ['기타, 사용 적음', '크롤링 & 파이썬']
draft: false
summary: ''
---

# XPATH, LINK TEXT, PARTIAL LINKTEXT 사용법

```python
추가적으로 XPATH,LINK TEXT, PARTIAL LINKTEXT 의 사용법을 익혀본다
```

```python
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import Keys
import time

options = Options()

options.add_argument("--start-maximized")
options.add_experimental_option("detach", True)

driver = webdriver.Chrome(options=options)

url = "https://naver.com"

driver.get(url)
time.sleep(2)

driver.find_element(By.XPATH, '//*[@title="검색어를 입력해 주세요."]').send_keys("블랙핑크", Keys.ENTER) # title 속성에 검색어를 입력해 주세요. 를 가진 요소 선택 바로 엔터하기
time.sleep(2)

# driver.find_element(By.XPATH, '//*[text()="뉴스"]').click() # 텍스트를 찾아서 뭔가를 할 정도는 알아야 한다 XPATH
driver.find_element(By.LINK_TEXT, "뉴스").click() # 텍스트가 "뉴스"인 하이퍼링크 요소를 찾는다. ( 주로 많이 사용 , 보통은 링크가 있는 텍스트를 찾아서 클릭을 많이 하기 때문 )
time.sleep(2)

driver.find_element(By.PARTIAL_LINK_TEXT, "블로").click() # 텍스트 중 일부만 포함해도 클릭하게 함
```

### XPATH

```python
XPATH 를 사용해서 원하는 요소를 찾는다
```

```python
driver.find_element(By.XPATH, '//*[@title="검색어를 입력해 주세요."]').send_keys("블랙핑크", Keys.ENTER)

# title 속성에 검색어를 입력해 주세요. 를 가진 요소 선택하고 바로 엔터키를 누르도록 설정해줬다 ( 뒤에 keys.ENTER )

driver.find_element(By.XPATH, '//*[text()="뉴스"]').click()

# 텍스트를 찾아서 뭔가를 할 정도는 알아야 한다 뉴스 라는 텍스트를 찾아서 클릭
```

### LINK_TEXT

```python
링크를 가진 텍스트를 찾는다
```

```python
driver.find_element(By.LINK_TEXT, "뉴스").click()

# 텍스트가 "뉴스"인 하이퍼링크 요소를 찾는다. ( 주로 많이 사용 , 보통은 링크가 있는 텍스트를 찾아서 클릭을 많이 하기 때문 )
```

### PARTIAL_LINK_TEXT

```python
텍스트를 부분적으로도 포함한 텍스트를 찾는다 ex) 블로그 에서 블로 만 있어도 찾음
```

```python
driver.find_element(By.PARTIAL_LINK_TEXT, "블로").click()

# 텍스트 중 일부만 포함해도 클릭하게 함
```

# find_elements 사용하여 같은 클래스 전부 가져와서 각 요소의 html과 text 출력하기

```python
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import Keys
import time

options = Options()

options.add_argument("--start-maximized")
options.add_experimental_option("detach", True)

driver = webdriver.Chrome(options=options)

url = "https://naver.com"

driver.get(url)
time.sleep(2)

navs = driver.find_elements(By.CSS_SELECTOR, ".link_service")

for num, i in enumerate(navs, 1):
    print(num)
    print(i.get_attribute("outerHTML"))
    print(i.text)
    print("")

```

### find_elements 사용해서 같은 클래스 전부 가져오기

```python
navs = driver.find_elements(By.CSS_SELECTOR, ".link_service")

find_elements 를 사용해서 같은 클래스를 가진 요소를 전부 가져왔다
```

```python
print(i.get_attribute("outerHTML")) 를 통해서

가져온 요소를 확인 가능하게 했다
```

![Untitled](<%EC%84%B9%EC%85%98%2012%20%EC%85%80%EB%A0%88%EB%8B%88%EC%9B%80(selenium)%20find%20%EB%AC%B8%EB%B2%95/Untitled.png>)

# 텍스트 기반으로 요소를 찾아서 클릭하는 또 다른 방법

```python
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import Keys
import time

options = Options()

options.add_argument("--start-maximized")
options.add_experimental_option("detach", True)

driver = webdriver.Chrome(options=options)

url = "https://naver.com"

driver.get(url)
time.sleep(2)

navs = driver.find_elements(By.CSS_SELECTOR, ".link_service")

for num, i in enumerate(navs, 1):
    print(num)
    print(i.get_attribute("outerHTML"))
    print(i.text)
    print("")

    if i.text == "블로그":
        i.click()
        break
time.sleep(2)

```

```python
이 방법은 기존 xpath 나 위의 방법들 보다 비 효율적이지만 크롤링을 하면서는 굉장히 다양한 상황이 있을 수 있기 때문에 방법을 많이 알아두면 좋다

for num, i in enumerate(navs, 1):
    print(num)
    print(i.get_attribute("outerHTML"))
    print(i.text)
    print("")

    if i.text == "블로그":
        i.click()
        break
time.sleep(2)

if 를 써서 text 의 내용이 같다면 클릭하고 brear 으로 반복을 종료하게 하는 방법이다
```
