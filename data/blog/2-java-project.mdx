---
title: '2. Java Project 기본'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## 1. Project 생성

```jsx
1. File > new > Java project
2. Project 명 입력 > finish
```

## 2. Class 생성

```jsx
1. 생성된 Project 내의 src 폴더 우클릭 > new > class 선택
2. 클래스명 입력(첫글자는 대문자) 후 finish
```

## 3. 주석문

```jsx
1. 단일 주석(한줄 주석)

// 단일 주석

2. 여러줄 주석

/*
		여러줄 주석
*/

3. 문서 주석
/**
		문서 주석
*/
```

## 4. 출력문

```java
1. System.out.println()
	- 출력후 줄바꿈(엔터)

2. System.out.print()
	 - 출력후 줄바꿈 없음
```

## 5. Java의 구성

- class 단위로 구성(기본 단위)
- class는 멤버변수와 멤버메서드 구성(세부 단위)

```java
//class 선언
접근지정자 class 클래스명 extends 부모클래스명 implements 인터페이스명1, 인터페이스명2,... {
		//멤버변수 선언
		접근지정자 [그외지정자] 데이터타입 변수명;

		//멤버메서드 선언
		접근지정자 [그외지정자] 리턴타입 메서드명([매개변수 선언들]){
			//메서드의 내용
		}

		//생성자
		접근지정자 클래스명과동일한메서드명([매개변수 선언들]){
			//생성자의 내용
		}


}
```

## 6. 프로그램 읽는 순서(실행 순서)

```java
1. 책읽는 순서와 같음
2. 위에서 아래로, 좌측에서 우측으로 실행
```

## 7. Java의 명명 규칙(이름 만드는 규칙)

```java
개발자는 클래스명, 변수명, 메서드명을 작명

1. 모든 이름은 이름만 보고 어떤 역할인지 추측가능
2. 클래스명의 첫글자는 대문자로 시작
3. 변수명의 첫글자는 소문자로 시작, 명사형식
4. 메서드명의 첫글자는 소문자로 시작, 동사형식
	- 끝에 () 가있으면 메서드, 없으면 변수

5. 두 단어 이상 합쳐서 이름을 만들 때
	ex) max + value

	1) Camel 표기법
		- 두번째 단어, 또는 뒤에 연결되는 단어에 첫글자를 대문자로 작성
		- ex) maxValueAge

	2) Snake 표기법
		- 단어와 단어 사이에 _(언더바)로 연결
		- ex) max_value

6. 모든 이름은 숫자로 시작 할 수 없음

7. 특수문자는 '_', '$' 만 허용

8. 띄어쓰기 불가

9. 한글 가능하지만 쓰지 말것을 권장합니다.
```

## 8. Package

```java
1. 관계있는 class들을 저장하는 폴더
2. 패키지명은 전세계에서 중복되지 않는 이름으로 작성
3. 도메인명의 역순으로 작성
  ex) com.google.****.***
```

## 9. Project Package Class 이름 수정

```java
1. Package Explorer에서 변경할 파일을 선택
2. F2 키 입력
3. 새로운 이름으로 변경 후 Finish
```
