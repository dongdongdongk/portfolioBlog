---
title: 'Maria DB'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

## 관리자(root) 접속

```jsx
1. mysql -u root -p [엔터]
2. 관리자 비번 입력 [엔터]
```

## 사용자 Database 생성

```jsx
CREATE DATABASE 데이터베이스명
create database user01;
```

## 사용자 생성

```jsx
1. User 생성
	create user 유저명@host identified by 비밀번호;
	create user 'user01'@'%' identified by 'user01';

	-- host
		 localhost 		    : localhost 에서만 접근 가능
		 %			          : 모든 외부 ip에서 접근 가능
		 211.211.211.211	: 211.211.211.211 에서만 접근 가능
		 211.211.211.%		: 211.211.211.xxx(/24) 대역대에서만 접근 가능

2. 권한 적용
	grant all privileges on database명.* to 유저명@host;
	grant all privileges on iu.* to 'user01'@'%';

3. 권한 적용 저장
	flush privileges;

4. 권한 삭제
REVOKE ALL PRIVILEGES GRANT FROM 'user_name'
```

## Limit

```jsx
SELECT 때 일정한 갯수 만큼 조회할 때 사용
Oracle의 ROWNUM 대신 사용

LIMIT 시작인덱스번호 , 조회할 갯수

SELECT * FROM QNA
ORDER BY NUM DESC
LIMIT 0 , 10

SELECT * FROM QNA
ORDER BY NUM DESC
LIMIT 10 , 10

SELECT * FROM QNA
ORDER BY NUM DESC
LIMIT 20 , 10
```
