---
title: '정적 페이지 크롤링(자바 스크립트가 없는 페이지)'
date: '2026-09-02'
category: '백엔드/스프링'
tags: ['백엔드', '스프링']
draft: false
summary: ''
---

# 크롤링 이란?

```jsx
스크랩핑(혹은 웹 스크랩핑)은 인터넷에서 존재하는 데이터를 컴퓨터 프로그램을 통하여 자동화된 방법으로 웹에서 데이터를 수집하는 모든 작업을 말한다.



크롤링은 많은 사람들이 스크랩핑과 혼용하여 사용하고 있지만, 사실은 그 의미가 상이한 용어다.



크롤링은 데이터를 수집하고 분류하는 것을 의미하며, 주로 인터넷 상의 웹페이지(html, 문서 등)를 수집해서 분류하고 저장하는 것을 뜻한다.
```

# 데이터를 가져와서 출력하기 (DB에 저장 x)

```java
jsoup 는 동적인(자바스크립트 사용) 하는 페이지는 불러오기가 불가능 하다 (네이버 맵 불러오려다 실패함)
```

## 1. jsoup 외부 라이브러리 추가

```jsx
크롤링을 하기 위해 라이브러리를 추가 해준다.
```

```jsx
<dependency>
  <groupId>org.jsoup</groupId>
  <artifactId>jsoup</artifactId>
  <version>1.12.2</version>
</dependency>
```

## 2. **크롤링 할 사이트 html 구조와 가져올 데이터 확인**

![Untitled](<%EC%A0%95%EC%A0%81%20%ED%8E%98%EC%9D%B4%EC%A7%80%20%ED%81%AC%EB%A1%A4%EB%A7%81(%EC%9E%90%EB%B0%94%20%EC%8A%A4%ED%81%AC%EB%A6%BD%ED%8A%B8%EA%B0%80%20%EC%97%86%EB%8A%94%20%ED%8E%98%EC%9D%B4%EC%A7%80)/Untitled.png>)

## 3. **크롤링한 데이터를 저장할 객체 만들기**

```java
@Getter
@Builder
@ToString
public class News {
    private String image;
    private String subject;
    private String url;
}
```

```jsx
@Getter :  Getter 메소드를 생성해 준다. (값을 가져옴)

@ToString : ToString 메소드 자동 생성 (객체가 가지고 있는 정보나 값들을 문자열로 만들어 리턴)

@Builder : 값을 주입하여 객체를 만드는 용도
```

## 4. **Service 생성**

```java
@Service
public class NewsService {
    private static String News_URL = "크롤링 할 URL";

    @PostConstruct
    public List<News> getNewsDatas() throws IOException {
        List<News> newsList = new ArrayList<>();
        Document document = Jsoup.connect(News_URL).get();

        Elements contents = document.select("section ul.type2 li");

        for (Element content : contents) {
            News news = News.builder()
                    .image(content.select("a img").attr("abs:src")) // 이미지
                    .subject(content.select("h4 a").text())		// 제목
                    .url(content.select("a").attr("abs:href"))		// 링크
                    .build();
            newsList.add(news);
        }

        return newsList;
    }
}
```

## 5. **Controller 생성**

```java
@Controller
public class NewsController {

    private final NewsService newsService;

    public NewsController(NewsService newsService) {
        this.newsService = newsService;
    }

    @GetMapping(실습
```

## 6. **출력화면 작성**

```java
		<div>
        <table class= "table">
            <tr>
                <th>이미지</th>
                <th>제목</th>
            </tr>
            <tr th:each="news : ${news} ">
                <td><a th:href="${news.url}"><img th:src="${news.image}"></a></td>
                <td><a th:href="${news.url}"><span th:text="${news.subject}"></span></a></td>
                </a>
            </tr>
        </table>
    </div>

*적당히 알아서 c:forEach 돌려라
```

# 실습

![Untitled](<%EC%A0%95%EC%A0%81%20%ED%8E%98%EC%9D%B4%EC%A7%80%20%ED%81%AC%EB%A1%A4%EB%A7%81(%EC%9E%90%EB%B0%94%20%EC%8A%A4%ED%81%AC%EB%A6%BD%ED%8A%B8%EA%B0%80%20%EC%97%86%EB%8A%94%20%ED%8E%98%EC%9D%B4%EC%A7%80)/Untitled%201.png>)

## 1. VO 만들기

```java
데이터를 넣을 VO 를 만들어 준다
```

```java
package com.delivery.store;

import lombok.Builder;
import lombok.Getter;
import lombok.ToString;

@Getter
@Builder
@ToString
public class StoreVO {

    private String title;
    private String subdivision;
    private String image;
    private String grade;
    private String review;
    private String url;
}
```

## 2.Controller 제작

/

![Untitled](<%EC%A0%95%EC%A0%81%20%ED%8E%98%EC%9D%B4%EC%A7%80%20%ED%81%AC%EB%A1%A4%EB%A7%81(%EC%9E%90%EB%B0%94%20%EC%8A%A4%ED%81%AC%EB%A6%BD%ED%8A%B8%EA%B0%80%20%EC%97%86%EB%8A%94%20%ED%8E%98%EC%9D%B4%EC%A7%80)/Untitled%202.png>)

```java
컨트롤러를 제작하고 Service 를 호출해준다
```

```java
@Controller
@RequestMapping("/store")
public class StoreController {

    @Autowired
    private StoreService storeService;

    @GetMapping("/list")
    public ModelAndView getStoreList() throws Exception {
        ModelAndView mv = new ModelAndView();
        List<StoreVO>storeVOList = storeService.getStoreDatas();
        mv.addObject("storeList", storeVOList);
        mv.setViewName("store/list");
        return mv;
    }

}
```

## 3.Service 제작

```java
.title(content.select("div.info a h2.title").text())  불러올 때 div.info 는 클래스 info 인 div 를 선택 하는것이고 띄우고 a 는 하위 태그를 선택 하는것이다 (h2.title 은 h2 의 title class 를 선택)

꼭 class 까지 적어 줄 필요없이 갈략하게 a div img 이런식으로 적는것도 가능하다
```

```java
@Slf4j
@Service
public class StoreService {
    private static String storeURL = "https://www.mangoplate.com/search/%EA%B0%95%EC%84%9C%EA%B5%AC%20%ED%99%94%EA%B3%A1%EB%8F%99%20%ED%94%BC%EC%9E%90%20";

    @PostConstruct
    public List<StoreVO> getStoreDatas() throws IOException {
        List<StoreVO> storeList = new ArrayList<>();
        Document document = Jsoup.connect(storeURL).get();
        System.out.println(document);
        Elements contents = document.select("figure.restaurant-item");                                             // 반복할 큰 컨테이너 전체
        System.out.println(contents);

        for (Element content : contents) {
            StoreVO storeVO = StoreVO.builder()
                    .title(content.select("div.info a h2.title").text()) //제목
                    .image(content.select("a div img").attr("abs:data-original")) // 이미지                         // 이미지를 넣을 때 abs:src 인지 original 등 외의 것인지 속성을 잘 구별해야한다.
//                    .subject(content.select("h4 a").text())
//                    .url(content.select("a").attr("abs:href"))		// 링크
                    .build();
            storeList.add(storeVO);
        }

        return storeList;
    }
```

## 4.JSP 제작

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
         pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<h1>List</h1>

<c:forEach var="storeList" items="${storeList}">
    <h3>${storeList.title}</h3>
    <img src="${storeList.image}">
</c:forEach>
```
