---
title: '서블릿'
date: '2026-09-02'
category: '기타, 사용 적음/국비'
tags: ['기타, 사용 적음', '국비']
draft: false
summary: ''
---

**Tomcat** : http 프로토콜을 이용해서 요청하는 url 주소가 오면 그 자원을 찾아서 응답으로 보내주는 역할

```sql
**Tomcat** : http 프로토콜을 이용해서 요청하는 url 주소가 오면 그 자원을 찾아서 응답으로

보내주는 역할

doget 이라는 멤버 메서드를 호출하기 위해서 객체를 만들어야함(Tomcat 에서 new 객체 생성)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380878/notion-blog/_/_/JSP/_/Untitled.png)

```sql
**Servlet : 자바와 Tomcat 사이에서 중간 역할을 하는것**

web.xml 에 있는 코드로 **배포 서술자** 라고 한다

우리가 만든 자바웹 프로그램을 servlet 을 이용하여 Tomcat이 읽으라고 명시해주는 것

<servlet> 코드의 뜻은
HomeController home = new HomeController() 과 같다

ex)
**<servlet-name>** 참조변수 명을 home 으로

**<servlet-class>** 패키지 com.iu.home.에 클래스 HomeController 을 찾아서 객체를 만들라

<객체 만들기 성공>

언제 만들지 명령

**<url-pattern>** / 이 들어오면

**<servlet-name>** home 을 실행하라 (위 servlet 의 home 실행)

* / 는 **루트** 라고하며 프로젝트명 까지의 주소다 http://localhost:8080/GDJ60_Servlet2/
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380875/notion-blog/_/_/JSP/_/Untitled_1.png)

```sql
HomeController 에서 응답을 내보내야 하는데 자바에서 TEST 만 찍고있다.

자바와 html 은 호환되지 않기때문에 자바의 소켓 프로그램과 비슷한 원리의 아웃풋 스트림을

만들어준다.

(매개변수로 받은 response 응답)

PrintWriter out = response.gerWritter();  //변수 out

out.println();   //print 메서드

out.close();    //자원 닫아주기
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380877/notion-blog/_/_/JSP/_/Untitled_2.png)

```sql
**JSP** :

서블릿은 자바 코드안에 html 을 쓰는것 (초창기 사용)

JSP는 반대로 html 안에 자바 코드를 넣는것

직접적으로 out.print(); 를 하는것이 아니라 서블릿으로 작성해 놓으면 자동으로

서블릿으로 변환(컴파일)(결국jsp도 서블릿)

*jsp 파일을 생성해서 만듬

*html 의 형태에 위의 코드가 추가
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380877/notion-blog/_/_/JSP/_/Untitled_3.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380877/notion-blog/_/_/JSP/_/Untitled_4.png)

```sql
uri : url 주소에서 ip포트를 제외한 나머지 주소

서버 내로 들어오면 ip주소가 필요없기 때문에 서버 내에서 움직이는 주소다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380877/notion-blog/_/_/JSP/_/Untitled_5.png)

```sql
위와 같이 주소를 입력하면 나중에 War 를 이용하여 익스포트시 프로젝트의 이름이 바뀌거나 하면

전체적인 주소가 달라지는 문제가 발생한다.

**상대경로 : 내가있는 현재 위치에 따라서 경로가 달라지는 것**

**절대경로 : 어디에 있던 동일한 경로로 움직이는 것**

* ./ : 같은경로

* ../ : 한단계 상위폴더
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380877/notion-blog/_/_/JSP/_/Untitled_6.png)

```sql
1.War Expot (아무경로에)

2.tomcat 폴더의 webapps 로 이동 (내 서버의 ip + 포트 하면 도착하는곳) war파일 넣기

3.bin 폴더에서 startup.bat 실행 (자바의 서버 꺼야함)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380878/notion-blog/_/_/JSP/_/Untitled_7.png)

```sql
위의 폴더는 개발때만 보이는 폴더로 실제 tomcat에서 서버를 만들고 나면 자바 소스 폴더와

webapp 내부의 폴더만 남는다
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380878/notion-blog/_/_/JSP/_/Untitled_8.png)

```sql
META-INF 와 WEB-INF 는 외부에서 접속할 수 없다 (외부 url 요청)

ex)http://localhost:8080/GDJ60_Servlet2/WEB-INF/views/test.jsp

이 폴더들은 서버 내부에서 접속 가능한 곳 이며(보안때문)
(주소만 알면 외부에서 멋대로 들어올 수 있기때문)
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380878/notion-blog/_/_/JSP/_/Untitled_9.png)

```sql

1.url 요청이 들어옴

2.Tomcat에 있는 매핑된 web.xml에 서블릿이 Tomcat에 객체 만들라 명령 실행

3.HomeController 에서 또다른 서블릿 JSP 를 호출해서

4.JSP 가 html 을 만들어준 뒤

5.Tomcat이 다시 받아 이를 응답으로 내보내준다 (응답은 html로 받는다)

```

## HomeController 에서 home JSP 로 호출하기

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380875/notion-blog/_/_/JSP/_/Untitled_10.png)

```sql
**문법**
**RequestDispatcher 변수 = request.getRequestDispatcher("JSP경로");
view.forward(request, response);**

RequestDispatcher view = request.getRequestDispatcher("/WEB-INF/views/home.jsp");
view.forward(request, response);

이 서블릿이 실행되면 위 경로로 보내시오

*서버 내부에서는 상대경로를 사용할 수 없다.

*서버 내부에서는 프로젝트 명이 무엇이든 간에 / 가 붙는다 (JSP에서는 루트가 /경로 였다면

 서버 내부에서는 / 가 루트다.)

```

**예제)**

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380875/notion-blog/_/_/JSP/_/Untitled_11.png)

```sql
서블릿에 /product url 이 들어옴

ProductController 클래스 객체 생성
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_12.png)

```sql
ProductController 에서 doGet메서드 productList.jsp 호출
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_13.png)

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_14.png)

```sql
productList.jsp 내부 <h1> 으로 출력
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_15.png)

```sql
home.jsp 에서 productList 로 들어갈떄

홈의 주소 : /GDJ60_Servlet2/

product 의 주소 : /GDJ60_Servlet2/product

./ 같은 위치에 product (익스플로러 폴더뷰를 믿을 수 없다)

*./product .jsp 가 붙지 않는이유 : jsp로 바로 가는것이 아닌 컨트롤러를 지나서 jsp에

도달하기 때문에

```

---

---

```sql
도메인 →ip :DNS
```

```sql
https://search.naver.com/search.naver?
where=nexearch&sm=top_hty&fbm=0&ie=utf8&query=%EB%89%B4%EC%A7%84%EC%8A%A4

파라미터 url 구분 ? 으로 표시

파라미터 구분 &

ex)http://localhost/product.do?name=test&price=20000

Parameter
	- Client에서 Server로 보내는 데이타
	- 이름과 값으로 구성
	- 이름 : String, 값 : String

```

```sql
요청을 보낼때 구성요소
필수
URL(ip:port/경로)
Method 형식중 하나  get or post 가 들어가야함

선택사항
파라미터(클라이언트가 서버로 보내는데이터)(이름=값)

1. GET
	- 단순 요청
	- 파라미터가 주소창에 노출(파라미터가 Header에 담김)
	- 작은 용량을 보낼 때

	A. Web Browser의 주소창에 직접 URL을 입력하는 경우
	B. A(link)태그 를 이용하는 경우
	C. Javascript의 location.href를 사용하는 경우
	D. Ajax 메서드에서 Get을 선택한 경우
	E. Form 태그 메서드에서 Get을 선택한 경우

2. POST
	- 보안에 민감한 요청
	- 파라미터가 주소창에 노출 X (파라미터가 Body에 담김)
	- 큰용량도 보낼 수 있음(이미지)

	A. Form 태그 메서드에서 Post를 선택한 경우
	B. Ajax 메서드에서 Post를 선택한 경우
```

![Untitled](https://res.cloudinary.com/du1acdhxq/image/upload/v1788380876/notion-blog/_/_/JSP/_/Untitled_16.png)
